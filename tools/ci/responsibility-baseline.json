{
  "_note": "Debt baseline for tools/ci/verify-responsibility-boundaries.mjs. Every field is a monotonic-non-increase ceiling: a real decrease must update the recorded number in the same change that caused it (never let a real reduction silently allow drifting back up); an increase fails the gate. Regenerated fields are computed, not hand-edited. 2026-09-06 RESOLVED exception: world_graph_live_dependency_count briefly rose 9 -> 10 as a disclosed side effect of the first real responsibility-tier decomposition executed under the correction program (crates/core/chronica-cap-world-admission split into chronica-adapter-postgres-world-admission + chronica-runtime-world-admission) -- an adversarial review of that same-day split found the adapter half's dependency was not actually load-bearing (its reconstruct_and_verify_world_history/history_read row-decode functions were fusing pure, I/O-free validation logic into what should have been a raw-SQL-only crate). A same-day follow-up commit relocated that pure validation into chronica_core_world_graph::d1 (reconstruct_and_verify_world_history_from_rows, decode_and_verify_entity_row, decode_and_verify_commit_row) and removed chronica-core-world-graph from the adapter crate's [dependencies] entirely, restoring the count to 9 -- see docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record of both the original split and this fix.",
  "misplaced_cap_package_count": 0,
  "misplaced_cap_package_count_by_root": {
    "core": 0,
    "runtime": 0,
    "adapter": 0
  },
  "_agent_skills_package_split_2026_09_16": "2026-09-16: CLASS-B SPLIT -- crates/core/chronica-cap-agent-skills-package split into crates/core/chronica-core-agent-skills-package (the pure, in-memory SkillsPackageRegistry, renamed IN PLACE, zero logic change) and crates/adapter/chronica-adapter-agent-skills-package (the real Postgres SkillsPackageRepo, moved byte-identical from that crate's former `#[cfg(feature = \"db\")] pub mod repo;`). Unlike the earlier same-day agent-{chat-persistence,instincts,questions} relocations (where the pure module was PRIVATE with no external callers, so a plain relocation sufficed), this crate's pure vocabulary was its PRIMARY PUBLIC API (lib.rs itself, no wrapping module) and its impure repo was a secondary, feature-gated PUBLIC module -- confirmed via full source read this was a genuine Class-B mix requiring physical decomposition. SkillsPackageRepo had ZERO real (non-dev) production callers anywhere in the repo at split time -- confirmed via exact fully-qualified-path grep, and the crate's own real caller (chronica-cap-company-agent-platform's agent_skills_package_route.rs) already explicitly documented it 'never touches the SEPARATE, untracked SkillsPackageRepo' -- so the adapter half ships with zero caller wiring, a pure physical relocation of already-dead-in-production code. Real caller repointed: chronica-cap-company-agent-platform's Cargo.toml dependency + agent_skills_package_route.rs's use path (renamed chronica-cap-agent-skills-package -> chronica-core-agent-skills-package). chronica-adapter-api-server carried a confirmed-DEAD Cargo.toml dependency on the pre-split crate (no db feature-array entry existed) -- removed outright. Two stale references corrected: a prose doc comment in chronica-adapter-api-server's own tests/agent_skills_package_route.rs, and a stale tenancy_filter_lint allow-list path (wrong crates/core/ prefix + old repo_tests.rs location, now crates/adapter/.../src/repo_tests.rs). Two pre-existing, unrelated copy-paste doc-comment errors in files this split directly touched were also fixed (not left to propagate into two locations): tests/money_guard.rs's doc comment wrongly described this crate as 'chronica-core-strategy-valuation (Profit-OS enterprise-value factor scoring)' -- a boilerplate artifact from an unrelated sibling crate's money_guard.rs -- corrected in both the retained pure crate's copy and the new adapter crate's adapted copy; repo_tests.rs's own doc comment named the wrong `cargo test -p chronica-strategy` invocation, corrected to the new crate name. Zero new forbidden edges: node tools/refoundation/validate-refoundation-layers.mjs passed with the same 153 known debt edges as before this change (the pure crate's rename only affects an already-allowed cap -> core edge; the new adapter crate has no real callers at all, hence no edge). M2 142 -> 141 (the rename drops the chronica-cap- prefix under crates/core/), M7 71 -> 70 (the impure half no longer lives under a chronica-cap--prefixed core-tier crate). Verified: cargo check --workspace clean (default + db); cargo test -p chronica-core-agent-skills-package and -p chronica-adapter-agent-skills-package (including --include-ignored live-DB tests) and -p chronica-cap-company-agent-platform --features db, all green (see commit for exact counts).",
  "_agent_questions_relocation_2026_09_16": "2026-09-16: relocated crates/core/chronica-cap-agent-questions -> crates/adapter/chronica-adapter-agent-questions (renamed). Same shape as the same-day chronica-adapter-agent-{chat-persistence,instincts} relocations: NOT a Class-B split, since question_service.rs is a PRIVATE module with no external caller anywhere in the repo. Its dependency on chronica-cap-agent-run-history (core-tier) for RunOwner stays a plain adapter -> core edge (always allowed). Its one real (non-dev) caller, chronica-cap-agent-session-routes, is cap-tier -- ONE new disclosed cap -> adapter edge, confirmed via node tools/refoundation/validate-refoundation-layers.mjs's own NEW FORBIDDEN EDGE output before recording it in tools/refoundation/refoundation-layer-debt.json (layer_debt_edge_count 152 -> 153). Real caller repointed: Cargo.toml dependency + agent_question_routes/handlers.rs's use path. chronica-adapter-api-server carried a confirmed-DEAD Cargo.toml dependency (only a prose doc-comment mention in router/mod.rs, never real code) -- removed outright, both the plain dep line and its own db feature-array entry. The crate's own .chronica descriptor and CAPABILITY.md had stale caller_entries pointing at the pre-refoundation crates/chronica-api/ path -- corrected to the real current caller, as did the sibling chronica-cap-agent-run-history's own descriptor's one prose mention. No stale tenancy_filter_lint entries existed. M2 143 -> 142, M7 72 -> 71. Verified: cargo check --workspace clean (default + db) -- disk filled mid-build during this cycle (target/debug/incremental hit ~8G again), cleared and re-verified clean; cargo test -p chronica-adapter-agent-questions and -p chronica-cap-agent-session-routes --features db, both green (see commit for exact counts).",
  "_agent_instincts_relocation_2026_09_16": "2026-09-16: relocated crates/core/chronica-cap-agent-instincts -> crates/adapter/chronica-adapter-agent-instincts (renamed). Same shape as the same-day chronica-adapter-agent-chat-persistence relocation: NOT a Class-B split, since instinct_cli.rs is a PRIVATE module with no external caller anywhere in the repo. Its one real (non-dev) caller, chronica-cap-agent-session-routes, is cap-tier -- ONE new disclosed cap -> adapter edge, confirmed via node tools/refoundation/validate-refoundation-layers.mjs's own NEW FORBIDDEN EDGE output before recording it in tools/refoundation/refoundation-layer-debt.json (layer_debt_edge_count 151 -> 152). Real caller repointed: Cargo.toml dependency + agent_instinct_routes/handlers.rs's use path. chronica-adapter-api-server carried a confirmed-DEAD Cargo.toml dependency (only a prose doc-comment mention in router/mod.rs, never real code) -- removed outright, both the plain dep line and its own db feature-array entry. The crate's own .chronica descriptor and CAPABILITY.md had stale caller_entries pointing at the pre-refoundation crates/chronica-api/ path -- corrected to the real current caller. No stale tenancy_filter_lint entries existed. M2 144 -> 143, M7 73 -> 72. Verified: cargo check --workspace clean (default + db); cargo test -p chronica-adapter-agent-instincts and -p chronica-cap-agent-session-routes --features db, both green (see commit for exact counts).",
  "_agent_chat_persistence_relocation_2026_09_16": "2026-09-16: relocated crates/core/chronica-cap-agent-chat-persistence -> crates/adapter/chronica-adapter-agent-chat-persistence (renamed). NOT a Class-B split: its private (non-pub) agent_persistence module is pure in-memory reference code with no external caller anywhere in the repo (its own module doc already says 'no runtime caller anywhere in the workspace'); the crate's public API is entirely real Postgres I/O. Its one real (non-dev) caller, chronica-cap-agent-session-routes, is cap-tier -- ONE new disclosed cap -> adapter edge, confirmed via node tools/refoundation/validate-refoundation-layers.mjs's own NEW FORBIDDEN EDGE output before recording it in tools/refoundation/refoundation-layer-debt.json (layer_debt_edge_count 150 -> 151). Real caller repointed: Cargo.toml dependency + agent_chat_routes/handlers.rs's use path. chronica-adapter-api-server carried a confirmed-DEAD Cargo.toml dependency (only a prose doc-comment mention in router/mod.rs, never real code) -- removed outright, both the plain dep line and its own db feature-array entry. The crate's own .chronica descriptor had stale caller_entries pointing at the pre-refoundation crates/chronica-api/ path -- corrected to the real current caller. No stale tenancy_filter_lint entries existed. M2 145 -> 144, M7 74 -> 73. Verified: cargo check --workspace clean (default + db); cargo test -p chronica-adapter-agent-chat-persistence and -p chronica-cap-agent-session-routes --features db, both green (see commit for exact counts).",
  "_workflows_durable_history_store_relocation_2026_09_16": "2026-09-16: relocated crates/core/chronica-cap-workflows-durable-history-store -> crates/adapter/chronica-adapter-workflows-durable-history-store (renamed). 100% real Postgres I/O (DurableHistoryRepo::save/load, unconditional sqlx, no pure vocabulary) -- a plain relocation. Its one real (non-dev) caller, chronica-core-workflows-agent-loop, is core-tier -- ONE new disclosed core -> adapter edge, confirmed via node tools/refoundation/validate-refoundation-layers.mjs's own NEW FORBIDDEN EDGE output before recording it in tools/refoundation/refoundation-layer-debt.json (layer_debt_edge_count 149 -> 150). Real caller repointed: Cargo.toml dependency + db feature array + durable_turn.rs's use path. One sibling crate (chronica-core-workflows-durable/src/store.rs) names this crate only in doc-comment prose, never a real Cargo dependency -- corrected for accuracy (a pre-existing, unrelated module-path inaccuracy in that same prose -- it says '::durable_history_repo' but DurableHistoryRepo lives directly in the crate root, no such module -- was left as-is, out of scope). No dead dependency existed in chronica-adapter-api-server. No stale tenancy_filter_lint entries existed. M2 146 -> 145, M7 75 -> 74. Verified: cargo check --workspace clean (default + db); cargo test -p chronica-adapter-workflows-durable-history-store and -p chronica-core-workflows-agent-loop --features db, both green (see commit for exact counts).",
  "_network_identity_privacy_budget_relocation_2026_09_16": "2026-09-16: relocated crates/core/chronica-cap-network-identity-privacy-budget -> crates/adapter/chronica-adapter-network-identity-privacy-budget (renamed). 100% real Postgres I/O (PersistentPrivacyBudget::spent/remaining/charge, unconditional sqlx, no pure vocabulary) -- a plain relocation. Its one real (non-dev) caller, chronica-cap-network-identity-knowledge, is core-tier -- ONE new disclosed core -> adapter edge, confirmed via node tools/refoundation/validate-refoundation-layers.mjs's own NEW FORBIDDEN EDGE output before recording it in tools/refoundation/refoundation-layer-debt.json (layer_debt_edge_count 148 -> 149). Real caller repointed: Cargo.toml dependency + db feature array + two src/ use-paths (lib.rs's learn_patterns_dp_gated_persistent + knowledge_privacy_budget_cross_test.rs). Two other sibling crates (chronica-core-network-identity-error, chronica-cap-network-identity-privacy-accountant) name this crate only in doc-comment prose, never a real Cargo dependency -- corrected for accuracy, no graph impact. No dead dependency existed in chronica-adapter-api-server. No stale tenancy_filter_lint entries existed. M2 147 -> 146, M7 76 -> 75. Verified: cargo check --workspace clean (default + db); cargo test -p chronica-adapter-network-identity-privacy-budget and -p chronica-cap-network-identity-knowledge --features db, both green (see commit for exact counts).",
  "_agent_knowledge_memory_repo_relocation_2026_09_16": "2026-09-16: relocated crates/core/chronica-cap-agent-knowledge-memory-repo -> crates/adapter/chronica-adapter-agent-knowledge-memory-repo (renamed). 100% real Postgres I/O (MemoryRepo: create_dataset/upsert_memory/insert_segment/rag_candidates/recall, unconditional sqlx dependency, no pure vocabulary, no db feature gate of its own -- confirmed via full source read) -- a plain relocation. This crate has ZERO real (non-dev) callers anywhere in the repo: its only prior Cargo.toml reference, chronica-cap-platform-admin-routes, never actually named MemoryRepo in its own src/ (confirmed via exact fully-qualified-path grep across the whole repo) -- that route crate's own knowledge_retrieve.rs doc comment already explicitly says it deliberately does NOT touch this repo, using chronica_cap_runtime_persistence::RuntimeMemoryRepo instead. The dead dependency line was removed outright from chronica-cap-platform-admin-routes/Cargo.toml (no db feature array entry existed for it). Four other crates (chronica-cap-platform-admin-routes/src/knowledge_retrieve.rs, chronica-adapter-api-server/src/router/social_and_helpdesk_routes.rs, chronica-core-context-compiler/src/context_memory/source.rs, its README.md, its .chronica descriptor) name this crate only in doc-comment prose explaining why they deliberately do NOT use it -- all corrected to the new name for accuracy, no Cargo.toml impact. No stale tenancy_filter_lint entries existed for this crate. node tools/refoundation/validate-refoundation-layers.mjs confirms 148 known debt edges remain and zero new edges (unchanged -- this relocation created no new dependency edge at all, real or disclosed). M2 148 -> 147, M7 77 -> 76. Verified: cargo check --workspace clean (default + db); cargo test -p chronica-adapter-agent-knowledge-memory-repo (3/3 money_guard pass; its two live-DB tests remain #[ignore]d as before, requiring DATABASE_URL -- unchanged by this move).",
  "_entity_trigger_dispatch_relocation_2026_09_16": "2026-09-16: relocated crates/core/chronica-cap-crm-support-entity-trigger-dispatch -> crates/adapter/chronica-adapter-crm-support-entity-trigger-dispatch (renamed). Entirely `#[cfg(all(feature = \"db\", feature = \"workflow-dispatch\"))] mod imp` real Postgres I/O (no pure vocabulary of its own) -- a plain relocation, not a Class-B split. Its one real (non-dev) caller, chronica-adapter-crm-support-sales-opportunity, was ALREADY adapter-tier (relocated earlier the same day), so this adds no new forbidden layer edge -- node tools/refoundation/validate-refoundation-layers.mjs confirms 148 known debt edges remain and zero new edges (unchanged from the prior scheduling-booking commit). Real caller repointed: its Cargo.toml dependency line + the workflow-dispatch feature array + src/store.rs's use path, plus CAPABILITY.md/.chronica/tests/money_guard.rs prose and assertion-string corrections (the money_guard assertion checks the literal `... = { workspace = true, optional = true }` dependency line text, so it had to match the renamed crate exactly, not just compile). No stale tenancy_filter_lint entries existed for this crate. M2 149 -> 148, M7 78 -> 77. Verified: cargo check --workspace clean (default + db). A PRE-EXISTING, unrelated test failure in this crate's own tests/money_guard.rs (crate_manifest_declares_no_money_adjacent_dependency) persists unchanged after the relocation -- its own overly-broad \"chronica-runtime\" substring check trips on this crate's own doc-comment prose about chronica-runtime-gdpr-erasure, confirmed via git stash against the prior commit that this predates the session entirely (same finding first surfaced in the decision-ledger relocation commit); not fixed, out of scope, documented in the crate's own .chronica known_gaps.",
  "_scheduling_booking_relocation_2026_09_16": "2026-09-16: relocated crates/core/chronica-cap-scheduling-booking -> crates/adapter/chronica-adapter-scheduling-booking (renamed). Entirely `#[cfg(feature = \"db\")]`-gated real Postgres I/O (no pure vocabulary of its own) -- a plain relocation, not a Class-B split. This crate has ZERO real (non-dev) external callers today (no HTTP route wires to it yet -- confirmed via exact fully-qualified-path grep across every .rs in the repo, and already documented as such in its own CAPABILITY.md before this change), so the relocation touches no other crate's production dependency graph -- only prose-comment crate-name corrections in sibling scheduling crates' Cargo.toml doc comments and three stale tenancy_filter_lint allow-list paths (wrong crates/cap/ prefix from before an older reorg). This relocation RESOLVED (not merely disclosed) the crate's own pre-existing core -> adapter layer-debt edge onto chronica-adapter-scheduling-host: that dependency is now adapter -> adapter, no longer a forbidden-target pair, so the edge entry `chronica-cap-scheduling-booking (core) -> chronica-adapter-scheduling-host (adapter)` was REMOVED from tools/refoundation/refoundation-layer-debt.json's forbidden_edges (149 -> 148 known debt edges) rather than renamed -- node tools/refoundation/validate-refoundation-layers.mjs confirms 148 known debt edges remain and zero new edges. M2 150 -> 149, M7 79 -> 78. Verified: cargo check --workspace clean (default + db); cargo test -p chronica-adapter-scheduling-booking --features db (live-DB booking_store_live.rs / round_robin_live.rs / money_guard.rs all pass).",
  "_crm_support_sales_opportunity_relocation_2026_09_16": "2026-09-16: relocated crates/core/chronica-cap-crm-support-sales-opportunity -> crates/adapter/chronica-adapter-crm-support-sales-opportunity (renamed). Both store.rs and document_notification_store.rs were entirely `#[cfg(feature = \"db\")]`-gated real Postgres I/O (no pure vocabulary left behind) -- a plain relocation, not a Class-B split. Real callers: chronica-adapter-crm-http (already adapter-tier, adds no new edge) and chronica-cap-crm-support-entity-trigger-dispatch (a dev-dependency only -- the layer gate never scans dev-dependencies, confirmed via `if (dependency.kind) continue` in validate-refoundation-layers.mjs). Removed a dead, unused direct dependency from chronica-adapter-api-server (both the plain dep line and its own `db` feature-array entry). Zero new forbidden edges disclosed -- `node tools/refoundation/validate-refoundation-layers.mjs` passed with the same 149 known debt edges as before this change. M2 151 -> 150, M7 80 -> 79.",
  "_posture_evidence_split_2026_09_16": "152 -> 151 (M2) and 81 -> 80 (M7): crates/core/chronica-cap-policy-posture-evidence (a genuine Class-B mix: pure PostureEvidenceCounts + a db-gated PostureEvidenceRepo, confirmed via full source read) SPLIT into crates/core/chronica-core-policy-posture-evidence (pure, no .chronica descriptor of its own) and crates/adapter/chronica-adapter-policy-posture-evidence (the real Postgres repo). Zero logic change to either half. FIRST core -> adapter edge disclosure this session (every prior one was cap -> adapter): chronica-core-api-state (core) -> the new adapter crate. This is NOT a newly-created edge -- chronica-core-api-state's own posture_floor.rs already constructed PostureEvidenceRepo directly, itself already gated behind that crate's own db feature; the dependency was core -> core (invisible to the gate) before this split, now visible. Precedented: chronica-core-api-state already carries 4 other disclosed core -> adapter edges predating this session. chronica-core-strategy-posture-gate needed only the pure sibling (confirmed via source read: uses PostureEvidenceCounts alone). chronica-adapter-api-server carried a confirmed-unused NON-optional dependency -- removed outright. A stale tenancy_filter_lint allow-list path (3 entries) corrected. Flagged but NOT addressed: chronica-core-api-state's own optional/db-gated Postgres I/O in posture_floor.rs is itself Class-B-shaped and a future decomposition candidate -- out of scope given that crate's much larger size. See tools/refoundation/refoundation-layer-debt.json's _note_2026_09_16_posture_evidence_split, layer_debt_edge_count 148 -> 149. Verified: cargo check --workspace clean (default + db), plus --tests for the split crates specifically. node tools/refoundation/validate-refoundation-layers.mjs: GREEN after disclosure (149 known edges) and M3 unchanged (5).",
  "_decision_ledger_relocation_2026_09_16": "153 -> 152 (M2) and 82 -> 81 (M7): crates/core/chronica-cap-policy-decision-ledger relocated + renamed to crates/adapter/chronica-adapter-policy-decision-ledger. 100% real Postgres mechanics (own doc says 'ROUTES AND RECORDS ONLY' over a single whole-module db-gate), no pure vocabulary to extract, zero logic change. Its chronica-cap-policy-approval-repo dep stays core-tier -- adapter -> core always allowed. ONE new disclosed edge: chronica-cap-org-workflow-routes (cap, already had 1 prior adapter-tier dep) -> the new adapter crate -- see tools/refoundation/refoundation-layer-debt.json's _note_2026_09_16_decision_ledger_relocation, layer_debt_edge_count 147 -> 148. No new edge from chronica-cap-policy-posture-evidence (core-tier): its use is [dev-dependencies]-only, already documented as 'a sanctioned test-only edge' before this relocation -- confirmed compiling clean via cargo check --workspace --features db --tests. chronica-adapter-api-server carried a confirmed-unused NON-optional dependency -- removed outright. A stale tenancy_filter_lint allow-list path (3 entries, wrong crates/cap/ prefix) corrected. Verified: cargo check --workspace clean (default + db). node tools/refoundation/validate-refoundation-layers.mjs: GREEN after disclosure (148 known edges) and M3 unchanged (5). (The --tests build also surfaced a pre-existing, unrelated compile error in chronica-cap-social-notes -- confirmed via git diff that this session never touched that crate; out of scope, not part of this session's established default verification routine which uses cargo check --workspace [--features db] without --tests.)",
  "_control_room_read_model_relocation_2026_09_16": "154 -> 153 (M2) and 83 -> 82 (M7): crates/core/chronica-cap-policy-control-room-read-model relocated + renamed to crates/adapter/chronica-adapter-policy-control-room-read-model. 100% real Postgres mechanics (the crate's own doc already said 'Reads only; NEVER money, NEVER mutation' over a single whole-module db-gate), no pure vocabulary to extract, zero logic change. Its two chronica-cap-policy-* deps (approval-repo, decision-ledger) stay core-tier -- adapter -> core is always allowed. TWO new disclosed edges (this crate had 2 real cap-tier callers): chronica-cap-deals-federation-routes (cap, already had 12 other adapter-tier deps) and chronica-cap-org-workflow-routes (cap, 1 prior adapter-tier dep) -> the new adapter crate -- see tools/refoundation/refoundation-layer-debt.json's _note_2026_09_16_control_room_read_model_relocation, layer_debt_edge_count 145 -> 147. chronica-adapter-api-server carried a confirmed-unused NON-optional dependency -- removed outright. A stale tenancy_filter_lint allow-list path (5 entries, wrong crates/cap/ prefix) corrected. Verified: cargo check --workspace clean (default + db). node tools/refoundation/validate-refoundation-layers.mjs: GREEN after disclosure (147 known edges) and M3 unchanged (5).",
  "_crm_support_campaign_appointment_relocation_2026_09_16": "155 -> 154 (M2) and 84 -> 83 (M7): crates/core/chronica-cap-crm-support-campaign-appointment relocated + renamed to crates/adapter/chronica-adapter-crm-support-campaign-appointment. Both submodules (channel_pacing_store, email_campaign_store) are whole-module #[cfg(feature = \"db\")] gated, 100% real Postgres mechanics, no pure vocabulary to extract, zero logic change (git mv, byte-identical src files). Only 2 real callers, both already adapter-tier, no new layer-debt edge: chronica-adapter-crm-http (real use-paths repointed) and chronica-adapter-api-server's own tests/crm_channel_pacing_route.rs (a real integration test, not the crate's own optional/unused-dep pattern seen in prior cohorts). Verified: cargo check --workspace clean (default + db). node tools/refoundation/validate-refoundation-layers.mjs reports IDENTICAL before/after (145 known edges, zero new/stale) and M3 unchanged (5).",
  "_osint_investigation_repo_split_2026_09_16": "156 -> 155 (M2) and 85 -> 84 (M7): crates/core/chronica-cap-osint-investigation-repo (a genuine Class-B mix: pure Investigation/Finding/ConfidenceTier vocabulary + a db-gated OsintRepo, confirmed via full source read) SPLIT into crates/core/chronica-core-osint-investigation (pure, no .chronica descriptor of its own, matching the chronica-core-helpdesk-kb precedent) and crates/adapter/chronica-adapter-osint-investigation-repo (the real Postgres repo, re-gated the same #[cfg(feature = \"db\")] mod imp discipline used throughout this session). Zero logic change to either half. Dropped seven genuinely-unused dependencies (rusqlite, quick-xml, serde_yaml, url, sha1, hmac, chrono) inherited from the crate's historical 0-change move out of the old flat chronica-osint crate, confirmed via a full-crate grep before the split. One new disclosed edge: chronica-cap-platform-misc-routes (cap) -> the new adapter crate (its own Cargo.toml already carried 14+ other real adapter-tier deps -- see tools/refoundation/refoundation-layer-debt.json's _note_2026_09_16_osint_investigation_split, layer_debt_edge_count 144 -> 145). chronica-adapter-api-server carried a confirmed-unused NON-optional dependency -- removed outright. Verified: cargo check --workspace clean (default + db). node tools/refoundation/validate-refoundation-layers.mjs: GREEN after disclosure (145 known edges) and M3 unchanged (5).",
  "_crm_support_issue_sla_relocation_2026_09_16": "157 -> 156 (M2) and 86 -> 85 (M7): crates/core/chronica-cap-crm-support-issue-sla relocated + renamed to crates/adapter/chronica-adapter-crm-support-issue-sla. Same shape as the helpdesk-family relocations before it: both submodules (issue_store, sla_store) are whole-module #[cfg(feature = \"db\")] gated, 100% real Postgres mechanics, no pure vocabulary to extract, zero logic change (git mv, byte-identical src/lib.rs). Only 2 real callers, both already adapter-tier: chronica-adapter-crm-http (2 files, real use-paths) -- adapter -> adapter, no disclosure needed. chronica-adapter-api-server carried a confirmed-unused NON-optional dependency (zero real src/ or tests/ usage, unconditionally compiled despite being dead weight) -- removed outright, along with its stale db-feature-forward entry. A stale tenancy_filter_lint allow-list path (already correctly crates/core/ before this move) updated to the new crates/adapter/ location. Verified: cargo check --workspace clean (default + db). node tools/refoundation/validate-refoundation-layers.mjs reports IDENTICAL before/after (144 known edges, zero new/stale) and M3 unchanged (5).",
  "_crm_support_pure_donor_rename_2026_09_15": "159 -> 157 (2 crates, ONE cohort commit, Class A PURE_BUT_STALE_CAP_NAME): chronica-cap-crm-support-{record-timeline-activity,warranty-claim-amc} each renamed IN PLACE (git mv, zero logic change) to chronica-core-*. First slice of a broader scan across the crm-support family (40 chronica-cap-* crates under crates/core/ still carry a real sqlx dependency; a name-pattern scan found only these 2 already fully pure with a proven sibling chronica-adapter-crm-support-* split). Mandatory DIRECTORY-rationale check run on both (zero blocking hits). Real dependents repointed: each crate's own sibling adapter crate (the only real external caller for both, confirmed via a full-repo grep before renaming); chronica-adapter-crm-http (warranty-claim-amc's caller, doc-comment-only mention). Verified: cargo check --workspace clean (default + db). node tools/refoundation/validate-refoundation-layers.mjs reports IDENTICAL before/after (144 known edges, zero new/stale) and M3/M7 unchanged (5, 86). Not gaming: both already had zero real I/O of their own before this commit.",
  "_helpdesk_conversations_inboxes_relocation_2026_09_15": "161 -> 159 (M2) and 88 -> 86 (M7): the last 2 chronica-cap-helpdesk-* crates under crates/core/, chronica-cap-helpdesk-inboxes and chronica-cap-helpdesk-conversations, relocated + renamed to crates/adapter/chronica-adapter-helpdesk-{inboxes,conversations} in ONE commit (conversations depends on inboxes, so both had to move together to avoid a transient core -> adapter edge). Both confirmed 100% real Postgres mechanics via their own module docs (both explicitly say 'Gated behind the db feature' for every item), no pure vocabulary to extract, zero logic change (git mv, byte-identical src/lib.rs). This relocation RESOLVED two previously-disclosed core -> adapter layer-debt edges (inboxes -> chronica-adapter-authorization-roles from _note_2026_09_08_loop68, conversations -> chronica-adapter-helpdesk-read-model from the 2026-09-07 subsystem-convergence pass) -- both now adapter -> adapter, needing no disclosure, removed from forbidden_edges. ADDED one new edge: chronica-cap-platform-misc-routes (cap) -> chronica-adapter-helpdesk-conversations (adapter), disclosed in tools/refoundation/refoundation-layer-debt.json's _note_2026_09_15_conversations_relocation -- that caller already carried 14+ other real adapter-tier deps, same precedented cap-routes -> adapter shape. Net layer_debt_edge_count for this commit: 145 -> 144. Real callers repointed with no new edge: chronica-runtime-helpdesk-automation (runtime -> adapter, both crates), chronica-adapter-helpdesk-http and chronica-adapter-postgres-helpdesk-automation (already adapter -> adapter). chronica-adapter-api-server carried confirmed-unused optional dependencies on both (zero real src/ or tests/ usage) -- removed outright. Verified: cargo check --workspace clean (default + db). cargo test on both relocated crates --features db --no-fail-fast -- --include-ignored (all green, incl. live-DB tests + money_guard), chronica-cap-platform-misc-routes, chronica-adapter-helpdesk-http, chronica-adapter-postgres-helpdesk-automation (all green). chronica-runtime-helpdesk-automation's 3 live-DB test files reproduce the SAME already-documented pre-existing failure counts as the thread-read/rename-batch cohorts before this one -- confirmed unrelated (that crate's own .chronica descriptor already documented the exact root cause: migration 0440's authority_state == 'inbox_bound' gate vs. create_thread's legacy_unassigned insert, predating this relocation). node tools/refoundation/validate-refoundation-layers.mjs: GREEN after disclosure (144 known edges, matching the net -1 change).",
  "_helpdesk_pure_donor_rename_batch_2026_09_15": "170 -> 161 (9 crates, ONE cohort commit, Class A PURE_BUT_STALE_CAP_NAME): chronica-cap-helpdesk-{agent-capacity,agents,dispatch,drafts,event-store,participants,read-model,teams,thread-comments} each renamed IN PLACE (git mv, zero logic change) to chronica-core-*. A systematic scan of the remaining 11 chronica-cap-helpdesk-* crates under crates/core/ found 9 of them already fully pure (zero sqlx dependency, confirmed via Cargo.toml grep) with an existing sibling chronica-adapter-helpdesk-* crate proving a real prior split had already happened -- this batch closes the M2 scoring gap left by those earlier splits never renaming the donor, matching the established pattern from earlier PURE_BUT_STALE_CAP_NAME cohorts this session. agent-capacity is the one exception: it never had an impure half at all (a deterministic in-memory policy calculator, no sibling adapter crate), confirmed by full source read -- also a valid pure-donor rename, just without the split narrative. The remaining 2 chronica-cap-helpdesk-* crates under crates/core/ (conversations, inboxes) still carry a real sqlx dependency and were deliberately left untouched -- queued for individual Class-B-or-relocation triage next. Mandatory DIRECTORY-rationale check run on all 9 (zero blocking hits). Real dependents repointed: chronica-runtime-helpdesk-automation (agent-capacity's only real external caller: use-path + Cargo.toml dep + CAPABILITY.md/.chronica descriptor), and each of the other 8's own sibling adapter crate (Cargo.toml dep line + real use-paths in src/ + tests/, since every one of those siblings is the crate's ONLY real external caller -- confirmed via a full-repo grep before renaming any of them). A stale tenancy_filter_lint allow-list path (dispatch's own SQL, plus participants'/thread-comments' live-test files) that had already drifted to a wrong `crates/cap/` prefix even before this rename was corrected to the real physical path in the same pass. ~20 doc-comment-only prose mentions across unrelated sibling crates (runtime-helpdesk-automation, workflows-assignment-defaults, helpdesk-automation/engine.rs, helpdesk-agent-capacity-policy, helpdesk-contacts, the two still-impure conversations/inboxes crates) were repointed for text consistency, none were real Cargo dependencies. Verified: cargo check --workspace clean (default + db). node tools/refoundation/validate-refoundation-layers.mjs reports IDENTICAL before/after (145 known edges, zero new/stale -- a rename changes no dependency direction) and M3/M7 unchanged (5, 88). Not gaming: every one of the 9 already had zero real I/O of its own before this commit; this is a pure label correction, not a relocation-in-disguise or a metric play.",
  "_thread_read_relocation_2026_09_15": "171 -> 170 (M2) and 89 -> 88 (M7): crates/core/chronica-cap-helpdesk-thread-read relocated + renamed to crates/adapter/chronica-adapter-helpdesk-thread-read. Same shape as the same-day chronica-cap-esign-contract-signature-store relocation immediately above: 100% real Postgres query mechanics (get_thread/list_threads/list_threads_for_contact/get_message/list_messages/report), confirmed via the crate's own module doc which already stated every item is gated behind the db feature since it needs sqlx + a live PgPersistence connection -- no pure vocabulary to extract. Zero logic change (git mv, byte-identical src/lib.rs). Three real callers repointed: chronica-runtime-helpdesk-automation (runtime -> adapter, no disclosure needed), chronica-adapter-helpdesk-http (adapter -> adapter, no disclosure needed), chronica-cap-platform-misc-routes (cap-tier, already carried 14 other real adapter-tier deps -- one new disclosed edge in tools/refoundation/refoundation-layer-debt.json's _note_2026_09_15_thread_read_relocation, layer_debt_edge_count 144 -> 145). chronica-adapter-api-server carried a confirmed-unused optional dependency (zero real src/ or tests/ usage) -- removed outright. Also repointed: the .chronica/capability.chronica.json + CAPABILITY.md descriptors (both the relocated crate's own and chronica-runtime-helpdesk-automation's sibling mentions), a stale tenancy_filter_lint allow-list file path (crates/core/chronica-core-persistence/tests/tenancy_filter_lint/allow_list_chunk_06.rs, matched against the real workspace-scanned path), and two doc-comment prose mentions (chronica-core-helpdesk-automation's engine.rs, chronica-runtime-helpdesk-automation's match_context.rs). Verified: cargo check --workspace clean (default + db) after a full cargo clean (target/debug/deps had grown to 25GB across 5 cohorts this session, exhausting disk -- reclaimed 28.6GiB). cargo test on the relocated crate (--features db --no-fail-fast -- --include-ignored, all live-DB tests + money_guard green), chronica-runtime-helpdesk-automation, chronica-cap-platform-misc-routes, and chronica-adapter-helpdesk-http -- all green. node tools/refoundation/validate-refoundation-layers.mjs: GREEN after disclosure (145 known edges).",
  "_esign_contract_signature_store_relocation_2026_09_15": "172 -> 171 (M2) and 90 -> 89 (M7): crates/core/chronica-cap-esign-contract-signature-store relocated + renamed to crates/adapter/chronica-adapter-esign-contract-signature-store. NOT a Class-B split: unlike the helpdesk-family cohorts, this crate is 100% real Postgres persistence mechanics (the ContractSignatureRow repo -- persist_signature/persist_computed_signature/list_signatures/load_report) with NO pure vocabulary of its own to extract -- confirmed by reading the whole crate: every pure type it touches (ContractSignature/EsignError/VerificationReport) already lived in the sibling chronica-core-esign-jades-signature before this move, and with the db feature off this crate compiled to an empty crate BEFORE this move too (its own module doc already said so). Zero logic change (byte-identical src/lib.rs, git mv). Real callers repointed: chronica-cap-deals-federation-routes (src/esign.rs, its own Cargo.toml already carried two other real adapter-tier deps -- chronica-adapter-esign-document-store, chronica-adapter-esign-envelope-audit-log -- establishing the same precedent as every sibling cap-routes -> adapter edge in this ledger), chronica-adapter-api-server (already adapter-tier, only its tests/esign_certificate_route.rs used the crate directly -- adapter -> adapter needs no disclosure). One new cap -> adapter edge disclosed in tools/refoundation/refoundation-layer-debt.json's _note_2026_09_15_esign_contract_signature_store_relocation (layer_debt_edge_count 143 -> 144). Also repointed: the .chronica/capability.chronica.json descriptor's implementation_home/owner_crate/covered_sources/witnesses/local_structural_commands fields, the money-path crate_graph_money_guard.rs's FORBIDDEN_DEPS list entry, and one sibling crate's (chronica-cap-esign-document-lifecycle) doc-comment prose mention + its own .chronica descriptor's composes_with entry. Verified: cargo check --workspace clean (default + db); node tools/refoundation/validate-refoundation-layers.mjs GREEN after disclosure.",
  "_class_b_helpdesk_custom_attributes_2026_09_15": "173 -> 172 (M2) and 91 -> 90 (M7): fourth Class-B split in the helpdesk family, same template as cohorts 1-3: chronica-cap-helpdesk-custom-attributes (Chatwoot custom_attribute_definitions parity, migration 0031; mixed a pure AttributeTarget/AttributeType/CustomAttributeDef/coercer/validator vocabulary with real Postgres CustomAttributeDefRepo mechanics -- definitions CRUD plus the per-entity value get/apply against support_contacts/support_threads -- behind a whole-file `#![cfg(feature = \"db\")]`) split into pure chronica-core-helpdesk-custom-attributes (unconditional, no sqlx, no db feature) plus a new custom_attributes module in the existing chronica-adapter-helpdesk-postgres crate (now thirteen modules). Simplest cohort yet: only ONE real caller outside the donor itself (chronica-adapter-helpdesk-http, already adapter-tier, already depended on chronica-adapter-helpdesk-postgres directly) -- no cap-tier route callers at all this time, so ZERO layer-debt edges needed (layer_debt_edge_count unchanged at 143). chronica-adapter-api-server carried a confirmed-unused optional dependency on the old crate (zero real src/ usage) -- removed outright, same as prior cohorts. Verified: cargo check --workspace clean (default + db); cargo test -p chronica-core-helpdesk-custom-attributes (8/8 pure unit tests) and -p chronica-adapter-helpdesk-postgres --features db --no-fail-fast -- --include-ignored --test-threads=1 (all suites green, incl. the new custom_attributes_module_moves_no_money test) plus chronica-adapter-helpdesk-http's full suite green. node tools/refoundation/validate-refoundation-layers.mjs: GREEN, zero new/stale edges.",
  "_class_b_helpdesk_kb_2026_09_15": "174 -> 173 (M2) and 92 -> 91 (M7): third Class-B split in the helpdesk family, same template as cohorts 1-2: chronica-cap-helpdesk-kb (Chatwoot help-center Article/Category parity, migration 0035; mixed a pure ArticleStatus/KbArticleRow/KbCategoryRow/slug-helper/validator vocabulary with real Postgres KbArticleRepo/KbCategoryRepo mechanics behind a whole-file `#![cfg(feature = \"db\")]`) split into pure chronica-core-helpdesk-kb (unconditional, no sqlx, no db feature -- both article and category concerns stay one physical crate, same as the donor) plus a new kb module in the existing chronica-adapter-helpdesk-postgres crate (now twelve modules). The two real cap-tier callers (chronica-cap-deals-federation-routes, chronica-cap-platform-admin-routes) reuse the SAME cap->adapter layer-debt edges already disclosed for the messenger split in this same crate pair (tools/refoundation/refoundation-layer-debt.json's _note_2026_09_15_messenger_split) -- zero NEW edges this cohort (layer_debt_edge_count unchanged at 143). A third cap-tier-adjacent caller, chronica-adapter-helpdesk-http (already adapter-tier, already depended on chronica-adapter-helpdesk-postgres directly), just repointed its use-paths -- no edge at all. A fourth caller, chronica-adapter-api-server, carried a confirmed-unused optional dependency on the old crate (only a doc-comment mention, zero real src/ usage) -- removed outright, same as prior cohorts. Verified: cargo check --workspace clean (default + db); cargo test -p chronica-core-helpdesk-kb (7/7 pure unit tests) and -p chronica-adapter-helpdesk-postgres --features db --no-fail-fast -- --include-ignored (all suites green, including the money_guard/mute_store_live/purge_live pre-existing breakage fixed as a prerequisite in the immediately prior commit). node tools/refoundation/validate-refoundation-layers.mjs: GREEN, zero new/stale edges.",
  "_class_b_helpdesk_messenger_2026_09_15": "175 -> 174 (M2) and 93 -> 92 (M7): second Class-B split in the helpdesk family, same template as cohort 1 (business-hours/csat/notifications/sla): chronica-cap-helpdesk-messenger (mixed a pure ConversationKind/MessageAuthorType/row-type vocabulary with a real Postgres MessengerRepo behind a whole-file `#[cfg(feature = \"db\")]`) split into pure chronica-core-helpdesk-messenger + a new messenger module in the existing chronica-adapter-helpdesk-postgres. Two real cap-tier callers (chronica-cap-deals-federation-routes, chronica-cap-platform-admin-routes -- both thin HTTP route surfaces, not orchestration/execution crates, so unlike the sla-escalation cohort's runtime relocation there is no ownership argument for moving them) needed to depend on the new adapter home directly; both already carried other real adapter-tier dependencies in their own Cargo.toml before this change, so this follows the SAME established disclosed cap-routes->adapter pattern used throughout this program (not a new debt category) -- two new edges disclosed in tools/refoundation/refoundation-layer-debt.json's _note_2026_09_15_messenger_split (layer_debt_edge_count 141 -> 143). A third real caller, chronica-adapter-api-server, carried a confirmed-unused optional dependency on the old crate (only feature-forwarding, zero src/ usage) -- removed outright rather than repointed, same as prior cohorts. Verified: cargo check --workspace clean (default + db); cargo test --features db across all touched crates green. Also fixed, as a required prerequisite, a pre-existing duplicate Cargo.toml dependency key in crates/adapter/chronica-adapter-social-data-export (a merge artifact on the pinned ancestry commit itself, confirmed via `git show` on that exact SHA before this change -- it broke `cargo metadata` for the WHOLE workspace, blocking all verification).",
  "_class_b_helpdesk_cohort1_2026_09_15": "179 -> 175 (M2) and 97 -> 93 (M7): FIRST genuine Class-B cohort (MIXED_PURE_PLUS_POSTGRES_CANDIDATE) -- 4 crates that mixed a pure classifier/vocabulary half with real Postgres mechanics behind a whole-file `#[cfg(feature = \"db\")]`: chronica-cap-helpdesk-{business-hours,csat,notifications,sla}. Each SPLIT (not renamed) into a new pure chronica-core-helpdesk-* crate (no sqlx, no db feature, unconditional) plus a new module in the existing consolidated chronica-adapter-helpdesk-postgres crate (business_hours.rs/csat.rs/notifications.rs/sla.rs), extending that crate's own established consolidation precedent (6 prior simple-CRUD modules from an earlier loop) rather than creating 4 new one-off adapter crates -- per-domain crate boundaries otherwise kept, matching this repo's existing one-adapter-per-feature convention elsewhere in the helpdesk family. Real dependent chronica-cap-helpdesk-sla-escalation needed to depend on the new adapter modules directly; since cap->adapter is a forbidden layer edge, that crate was relocated crates/cap/ -> crates/runtime/chronica-runtime-helpdesk-sla-escalation in the SAME commit (genuine runtime-tier periodic replay-safe sweep, exact precedent: chronica-runtime-helpdesk-automation/chronica-runtime-gdpr-erasure) -- this also drops cap_directory_count 34 -> 33 (tools/ci/topology-baseline.json updated separately). Real callers repointed: chronica-adapter-helpdesk-http (5 route files), chronica-adapter-api-server (2 test files + Cargo.toml, dropped 4 now-redundant unused optional deps), chronica-runtime-api-background-dispatch (use-path only). Fixed one self-inflicted money_guard false-positive in the relocated escalation crate's own test (its package name now legitimately contains the substring \"chronica-runtime\", tripping a whole-manifest scan -- rescoped the check to the [dependencies] table only, the same self-match-trap class as PR #5301). Verified: cargo check --workspace clean (default + db); full cargo test --features db --no-fail-fast across all touched crates -- 4 pure crates 45/45 unit tests pass, chronica-adapter-helpdesk-postgres unit+contact_note_store_live green, chronica-runtime-helpdesk-sla-escalation unit+money_guard green. Three live-DB integration-test failures (mute_store_live, purge_live, support_sla_escalation_live -- none in the 4 split modules' own tests) independently reproduced byte-identical via `git stash` on the pre-change base commit: a pre-existing `support_threads.authority_state NOT NULL` schema/fixture drift affecting shared seed helpers repo-wide, unrelated to this cohort, not fixed here (out of scope). validate-refoundation-layers.mjs: zero new/stale edges (141 unchanged) -- the cap->adapter hazard was avoided via relocation, not accepted as debt.",
  "_pure_stale_cap_name_cohort2_2026_09_15": "189 -> 179 (10 crates, ONE cohort commit, Class A PURE_BUT_STALE_CAP_NAME, second wave from the regenerated .chronica/convergence-classification.json): chronica-cap-agent-edit-history, chronica-cap-authorization-role-lifecycle, chronica-cap-authorization-roles, chronica-cap-crm-support-{analytics-action,analytics-alert-rule,analytics-dashboard,contacts,inbound-notification,metadata-view}, chronica-cap-social-pages -- each renamed IN PLACE (git mv, zero logic change) to chronica-core-*, all with an existing sibling chronica-adapter-* crate proving a real prior split. Mandatory DIRECTORY-rationale check re-run on all 10 (regex hit count 1-2 each, but every hit inspected individually and confirmed a false positive -- prose using \"deliberate/intentional\" to describe unrelated business-logic semantics, e.g. exact-match grant matching or API visibility choices from an earlier split, never a physical-placement rationale; none carried the DIRECTORY: tag). Real dependents (sibling adapter crates' Cargo.toml + source, plus chronica-core-authorization-roles<-chronica-core-authorization-role-lifecycle's own internal edge) repointed via exact-string substitution; a repo-wide sweep confirmed zero remaining live Cargo.toml dependency lines or Rust use:: paths on any old name (remaining hits are all historical prose in unrelated crates' doc comments, left untouched per the anti-over-documentation rule). cargo check --workspace clean on both default features and --features db; cargo test --features db across all 10 renamed crates + their 10 sibling adapters green (62 test-result blocks, 0 failures, exit 0). validate-refoundation-layers.mjs reports IDENTICAL before/after (141 known edges, zero new/stale) and M3/M7 unchanged (5, 97). Not gaming: every one of the 10 already had its real Postgres mechanics living in a sibling adapter crate before this commit.",
  "_pure_stale_cap_name_cohort_2026_09_15": "212 -> 189 (23 crates, ONE cohort commit, Class A PURE_BUT_STALE_CAP_NAME per .chronica/convergence-classification.json): chronica-cap-crm-support-{analytics-insight,apply-rating,contract-lifecycle,feature-flag,manage-skills,metadata-lifecycle,pipeline-funnel,productivity-notes-events-tags,survey-targeting-branching,surveys,thread-message-dispatch,thread-notes-followers}, chronica-cap-esign-envelope-audit-log, chronica-cap-helpdesk-{contacts,custom-filters,read-state,sentiment-model}, chronica-cap-policy-post-comment, chronica-cap-projection-read-model, chronica-cap-scheduling-{event-type,host}, chronica-cap-social-{conversations,data-export} each renamed IN PLACE (git mv, zero logic change) to chronica-core-*. All 23 were already genuinely pure (no sqlx dependency, no [features] table, confirmed by the classification scanner) with an existing sibling chronica-adapter-* crate proving a real prior split had already happened -- this batch closes the M2 scoring gap left by those earlier splits never renaming the donor. Mandatory DIRECTORY-rationale check run on all 23 (zero hits) plus a cross-cohort reference check (zero internal dependencies among the 23, so independent per-crate processing was safe) before any file was touched. Every real dependent across the whole repo (root Cargo.toml members+aliases, each sibling adapter crate's Cargo.toml+source, plus incidental real/doc references in ~30 unrelated crates) was repointed via exact-string substitution (both hyphenated and underscored forms) -- verified via a full repo-wide sweep showing zero remaining stale references. cargo metadata succeeded (Cargo.lock regenerated cleanly) and `cargo check --workspace --features db` compiled the ENTIRE workspace clean. Both debt gates re-run fresh: validate-refoundation-layers.mjs reports IDENTICAL before/after (141 known edges, zero new/stale -- a rename changes no dependency direction) and verify-responsibility-boundaries.mjs confirms M3/M7 unchanged (5, 97). Not gaming: every one of the 23 already had its real Postgres mechanics living in a sibling adapter crate before this commit; this is a pure label correction across a proven-homogeneous cohort, not a relocation-in-disguise or a metric play.",
  "_metadata_record_split_2026_09_15": "213 -> 212 (misplaced_cap_package_count) and 98 -> 97 (forbidden_core_io_count): crates/core/chronica-cap-crm-support-metadata-record (chronica-cap-* name under crates/core/, optional sqlx dependency) split into chronica-core-crm-support-metadata-record (pure: MetadataRecordRow+to_json, CreateRecordOutcome/UpdateRecordOutcome, is_uuid_shape; exactly one dependency, serde_json) and chronica-adapter-crm-support-metadata-record (crates/adapter/: create_record/list_records/get_record/update_record + field-validation logic, which needed the still-unsplit sibling chronica-cap-crm-support-metadata-registry's db-gated FieldType/FieldMetadataRow types and so could not live in the pure crate -- disclosed facade-purity judgment call). Real callers repointed: chronica-cap-platform-admin-routes, chronica-cap-social-antenna, chronica-adapter-api-server (its own dependency was mistakenly assumed dead at first -- tests/social_antennas_route.rs uses create_record directly -- corrected to a real repoint, not a removal). Two new layer-debt edges disclosed in tools/refoundation/refoundation-layer-debt.json's _note_2026_09_15_metadata_record_split (139 -> 141): chronica-cap-platform-admin-routes (cap) and chronica-cap-social-antenna (core, despite its cap-sounding name) both -> chronica-adapter-crm-support-metadata-record (adapter). Verified: cargo check/test on both new crates plus all three real dependents, all green (0 failures across 60+ tests); node tools/refoundation/validate-refoundation-layers.mjs GREEN.",
  "_esign_document_store_donor_rename_2026_09_15": "214 -> 213: crates/core/chronica-cap-esign-document-store renamed IN PLACE (git mv, zero logic change) to crates/core/chronica-core-esign-document-store. This is a NAMING CORRECTION, not a new split, and NOT the 'folders renamed, runtime behavior identical' anti-pattern this program's own hard anti-gaming rule warns against: the real architectural separation already happened for real in the 2026-09-07 responsibility-tier decomposition recorded below (_forbidden_core_io_count_2026_09_07) -- the real Postgres create_document_record/add_document_version/get_record/list_versions/get_latest_version/archive_record/record_stats/search_records mechanics already live in crates/adapter/chronica-adapter-esign-document-store, and this donor already declared zero sqlx and zero cargo features. It was simply left under its old chronica-cap- name, so tools/ci/verify-responsibility-boundaries.mjs's name-based countMisplacedCapPackages() check kept counting an already-pure crate as misplaced. Verified zero content/logic change: Cargo.toml [package] name + one added dated doc-comment note; src/money_guard.rs and the .chronica descriptor's implementation_home/owner_crate fields got only literal old-name -> new-name substitutions (the descriptor's caller_entries still cite a stale src/operations.rs/db_tests.rs layout from BEFORE this rename, a pre-existing staleness from the 2026-09-07 split, not introduced or worsened here). All real dependents repointed: root Cargo.toml (member + alias), chronica-adapter-esign-document-store's Cargo.toml dependency + its src/lib.rs pub use + src/operations.rs and src/rows.rs use lines. chronica-cap-deals-federation-routes (the one real cap-tier caller, via the adapter crate, unaffected by this rename) re-verified compiling clean with --features db. world_graph_live_dependency_count and forbidden_core_io_count are UNCHANGED (98) -- this rename changes no dependency direction and touches no I/O -- and node tools/refoundation/validate-refoundation-layers.mjs reports zero new/stale layer-debt edges (139, unchanged).",
  "_social_moderation_reports_split_2026_09_15": "215 -> 214 (misplaced_cap_package_count) and 99 -> 98 (forbidden_core_io_count): crates/core/chronica-cap-social-moderation-reports (a chronica-cap-* package physically under crates/core/, an M2 hit, that ALSO carried an `optional = true` sqlx dependency directly under crates/core/, an M7 hit -- the same crate scoring both metrics at once) was split into TWO new crates: chronica-core-social-moderation-reports (crates/core/ -- pure vocabulary: ReportRow/ModerationActionRow/RuleRow + to_json, the five outcome enums, every bound/column constant, and the field validators incl. the escalation-ladder state machine; declares exactly ONE dependency, serde_json, no sqlx, no [features] table) and chronica-runtime-social-moderation-reports (crates/runtime/ -- the real Postgres create_report/assign_report/resolve_report (incl. the escalation-ladder transaction + the account-moderation-state write into chronica-core-social-relationships)/list_reports_for_company/list_moderation_actions/list_moderation_actions_for_target, plus the rules-catalog create_rule/update_rule/delete_rule/list_rules/existing_rule_ids -- imitating chronica-runtime-gdpr-erasure's shape). This is a GENUINE responsibility-tier split, not a relocation-in-disguise: the pre-split crate's compatibility glob re-export (`pub use chronica_core_social_relationships::moderation::*;` -- a chronica-cap-* crate acting as a PERMANENT pass-through facade for chronica-core-social-relationships's real mute/block API, exactly the 'cap crate as permanent orchestration/compatibility-routing owner' anti-pattern this program's hard anti-gaming rule forbids) was DELETED OUTRIGHT, not carried into either new crate -- every real caller that reached chronica_core_social_relationships::moderation through it (chronica-cap-social-routes's social_moderation.rs/social_threads.rs/social_feed.rs, chronica-cap-social-notes's db_tests, chronica-cap-social-notifications, chronica-adapter-social-postgres-batch1's relationship_severance db_tests, chronica-core-social-relationships's own follow_store_tests.rs dev-dependency) now imports chronica_core_social_relationships::moderation DIRECTLY instead. Real dependents needing the runtime crate's own report/resolve/rules surface (chronica-cap-social-routes, chronica-adapter-social-postgres-batch3's moderation_appeals module, chronica-cap-social-notes's LANE-B db_tests, chronica-core-social-relationships's follow_store_tests.rs) were repointed to chronica-runtime-social-moderation-reports. chronica-adapter-api-server carried a CONFIRMED DEAD Cargo.toml dependency on the old crate (zero source references) -- REMOVED outright, not repointed. Both new chronica-cap-social-routes (cap) -> chronica-runtime-social-moderation-reports (runtime) and chronica-adapter-social-postgres-batch3 (core) -> chronica-runtime-social-moderation-reports (runtime) edges are disclosed in tools/refoundation/refoundation-layer-debt.json's _note_2026_09_15_moderation_reports_split (layer_debt_edge_count 137 -> 139). Verified: cargo check/test -p chronica-core-social-moderation-reports (5/5 money_guard + 1/1 pure unit test pass), -p chronica-runtime-social-moderation-reports --features db (19/19 db_tests live-DB pass via --ignored, 4/4 money_guard pass), and every real dependent (chronica-cap-social-routes, chronica-adapter-social-postgres-batch3, chronica-cap-social-notes, chronica-cap-social-notifications, chronica-adapter-social-postgres-batch1, chronica-core-social-relationships) still green with their own live-DB tests passing end to end through the new crate boundary.",
  "_analytics_cohort_donor_rename_2026_09_09_loop85": "216 -> 215: crates/core/chronica-cap-crm-support-analytics-cohort renamed IN PLACE (git mv, zero logic change) to crates/core/chronica-core-crm-support-analytics-cohort. This is a NAMING CORRECTION, not a new split: the 2026-09-08 LOOP 74 responsibility-tier decomposition had already made this crate fully pure and I/O-free (it deps on exactly TWO crates -- chronica-core-analytics-cohort, serde_json -- with no sqlx, no [features] table, and its own module doc already declared it performs no I/O since LOOP 74), but the donor was left under its old chronica-cap- name. countMisplacedCapPackages() in tools/ci/verify-responsibility-boundaries.mjs flags any crate under a PRODUCT_CRATE_ROOTS root (crates/core, crates/runtime, crates/adapter) whose [package] name starts with the literal string \"chronica-cap-\", REGARDLESS of whether the crate is actually pure -- so this genuinely-pure crate kept scoring an M2 hit it had already earned its way out of, and the LOOP 74 split never actually banked the M2 credit it was meant to produce. This rename closes that gap: verified zero content/logic change (Cargo.toml [package] name + one added dated doc-comment note; src/lib.rs got one added dated doc-comment note, no other line changed; tests/money_guard.rs, CAPABILITY.md and the .chronica descriptor got only literal old-name -> new-name substitutions). All real dependents repointed (chronica-adapter-crm-support-analytics-cohort's Cargo.toml dependency + its src/lib.rs pub use/use lines + one string marker in its tests/money_guard.rs; the root Cargo.toml workspace member + dependency alias) and re-verified green: cargo check/test -p chronica-core-crm-support-analytics-cohort (3/3 money_guard tests pass, same as before the rename) and -p chronica-adapter-crm-support-analytics-cohort --features db (4/4 money_guard tests pass). One sibling crate (chronica-cap-crm-support-commerce-kpi) that names this crate's old identifier only in a doc-comment prose mention (never a real Cargo dependency or call site) was repointed for text consistency only; chronica-cap-platform-misc-routes's Cargo.toml names the old identifier only in historical prose describing the LOOP 74 repoint (accurate at the time) and was deliberately left unchanged, same judgment as LOOP 84. world_graph_live_dependency_count and forbidden_core_io_count are UNCHANGED (this rename changes no dependency direction and touches no I/O) and node tools/refoundation/validate-refoundation-layers.mjs reports zero new/stale layer-debt edges (137, unchanged). See docs/_machine/graph-refoundation/subsystem-convergence.jsonl LOOP 85 for the full record.",
  "_analytics_funnel_donor_rename_2026_09_09_loop84": "217 -> 216: crates/core/chronica-cap-crm-support-analytics-funnel renamed IN PLACE (git mv, zero logic change) to crates/core/chronica-core-crm-support-analytics-funnel. This is a NAMING CORRECTION, not a new split: the 2026-09-08 LOOP 75 responsibility-tier decomposition had already made this crate fully pure and I/O-free (it deps on exactly THREE crates -- chronica-core-analytics-funnel, chronica-core-business-events, serde_json -- with no sqlx, no [features] table, and its own module doc already declared \"NO I/O\"), but the donor was left under its old chronica-cap- name. countMisplacedCapPackages() in tools/ci/verify-responsibility-boundaries.mjs flags any crate under a PRODUCT_CRATE_ROOTS root (crates/core, crates/runtime, crates/adapter) whose [package] name starts with the literal string \"chronica-cap-\", REGARDLESS of whether the crate is actually pure -- so this genuinely-pure crate kept scoring an M2 hit it had already earned its way out of, and the LOOP 75 split never actually banked the M2 credit it was meant to produce. This rename closes that gap: verified zero content/logic change (Cargo.toml [package] name + one added dated doc-comment note; src/lib.rs got one added dated doc-comment note, no other line changed; tests/money_guard.rs and CAPABILITY.md got only literal old-name -> new-name substitutions). All real dependents repointed (chronica-adapter-crm-support-analytics-funnel's Cargo.toml dependency + its src/lib.rs pub use/use lines; the root Cargo.toml workspace member + dependency alias) and re-verified green: cargo check/test -p chronica-core-crm-support-analytics-funnel (3/3 money_guard tests pass, same as before the rename) and -p chronica-adapter-crm-support-analytics-funnel --features db (4/4 money_guard tests pass). Three sibling crates (chronica-cap-crm-support-feedback-loop, chronica-cap-crm-support-analytics-retention, chronica-cap-crm-support-commerce-kpi) that name this crate's old identifier only in doc-comment prose (never a real Cargo dependency or call site) were repointed for text consistency only. world_graph_live_dependency_count and forbidden_core_io_count are UNCHANGED (this rename changes no dependency direction and touches no I/O) and node tools/refoundation/validate-refoundation-layers.mjs reports zero new/stale layer-debt edges. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl LOOP 84 for the full record.",
  "_misplaced_cap_package_count_2026_09_07z": "227 -> 226: crates/core/chronica-cap-crm-support-party-authorized (the M1.3 authorization pilot -- an orchestration wrapper that calls authorize() then delegates to create_party(), issuing ZERO raw SQL of its own; confirmed no sqlx dependency in its own Cargo.toml, so this crate never counted toward M7 forbidden_core_io_count at all) physically relocated + renamed to crates/runtime/chronica-runtime-crm-support-party-authorized -- the FIRST cap->runtime relocation in this program (every prior relocation, LOOP 42/46/47, was cap->adapter). This is a pure M2-only decrease: forbidden_core_io_count is UNCHANGED (132), unlike LOOP 42/46/47's combined M2+M7 wins, because this crate was never an M7 hit. create_party_authorized's body independently re-verified byte-identical against the pre-move original. The sole real caller, chronica-adapter-crm-support-lifecycle-events (already adapter-tier), repointed its use path + Cargo.toml dependency -- an adapter->runtime edge, NOT forbidden under FORBIDDEN_TARGETS.adapter (={infra}), confirmed zero new layer-debt edges via node tools/refoundation/validate-refoundation-layers.mjs. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "world_graph_live_dependency_count": 0,
  "forbidden_core_io_count": 0,
  "_post_comment_split_2026_09_08_loop70": "111 -> 110: crates/core/chronica-cap-policy-post-comment's responsibility-tier decomposition (LOOP 70). SPLIT, not relocated -- the determination was re-derived by reading the donor's actual current src/lib.rs (21 lines) and src/post_comment.rs (301 lines) in full, NOT inherited from the recon note. GATING SHAPE DIFFERED from every LOOP 59-69 sibling: this donor had NO `#[cfg(feature = \"db\")] mod imp` block at all. Its src/post_comment.rs carried a WHOLE-FILE `#![cfg(feature = \"db\")]` INNER attribute on line 15, with pure and impure items alike at top level, indentation 0 -- so the split could not be anchored on `grep -n '^#\\[cfg(feature = \"db\")\\]'` the way LOOP 69 was, and there was no `mod imp` wrapper whose indentation had to be reproduced. Spans were instead anchored on the item boundaries themselves (the `pub struct PostCommentRepo<'a>` declaration and the matching closing brace of its impl block), and the moved code needed NO re-indentation. The donor declares three OWN pure types (SubjectKind, AuthorType, PostCommentRow) plus a pure egress projection (PostCommentRow::to_redacted_json, over the shared chronica_core_security_redact::redact_text), a pure page bound (COMMENT_PAGE_MAX) and a pure clamp (clamp_comment_limit), none of which touches a PgPersistence -- so the donor was kept and SHRUNK to exactly that pure surface rather than renamed away, exactly as in LOOP 50/55/57/58/60/61/69. The real mechanics moved BYTE-IDENTICAL into the new crates/adapter/chronica-adapter-policy-post-comment: PostCommentRepo, its SELECT column-list const, its private map_row PgRow decoder, and its list_for_subject/get/add statements over the post_comments table (migration 0017_post_comments.sql). SHA-256 verified: the whole moved span (pre-split lines 132-241, 110 contiguous lines, doc comment included) hashes b6611bc9adfbc4fed112a16c1f4b5560db7f8d216f0f5071bfa6f4fe633fbebd BEFORE AND AFTER, with ZERO edits inside it; the two RETAINED spans were hashed the same way and are also unchanged (pure items, pre-split lines 21-130: d1576aa6b6d3528f542c961e81a5f6b12276328f3ef11371b114d2f60cff4f03; the four pure unit tests, pre-split lines 243-301: 155277563f35b667acdd16a662a5b20c9603342cfc9edbe037d3a5c933ccc0ee). No orphan-rule rename was required: map_row is an associated fn of PostCommentRepo, never an inherent impl on the pure PostCommentRow, so it moved with its own type and kept its name and privacy. The donor now declares NO sqlx and NO [features] table at all; its remaining edges (chronica-core with default features, chronica-core-security-redact, serde_json) reach no sqlx, since chronica-core forwards its own db feature only to chronica-core-persistence/chronica-core-version and neither is activated by a default-features dependency. misplaced_cap_package_count is deliberately UNCHANGED at 222: the donor legitimately stays under crates/core/ because what it now holds is genuinely pure.",
  "_social_data_export_split_2026_09_08_loop69": "112 -> 111: crates/core/chronica-cap-social-data-export's responsibility-tier decomposition (LOOP 69). SPLIT, not relocated -- the determination was re-derived by reading the donor's actual current 296-line src/lib.rs in full, NOT inherited from the recon note nor from the shape of its LOOP 59-68 siblings: its whole body was a single #[cfg(feature = \"db\")] mod imp, but it declares its OWN pure type (AccountExport -- one user's export bundle, whose eleven fields are String/bool/i64/Vec<String>/Vec<serde_json::Value>/serde_json::Value and name NO foreign type at all -- plus its pure camelCase to_json projection), so the donor was kept and SHRUNK to exactly that pure surface rather than renamed away, while the real mechanics moved BYTE-IDENTICAL into the new crates/adapter/chronica-adapter-social-data-export: the export_account_data aggregator (seven awaited sibling-store reads folded into one bundle), the private own_notes_any_visibility raw SELECT over social_notes (migration 0095, the ONE statement this capability owns), the EXPORT_NOTES_LIMIT bound that statement binds as its LIMIT, and the three #[ignore]d live-DB db_tests that exercise them. Span boundaries were anchored with grep -n '^#\\[cfg(feature = \"db\")\\]' (the ^ anchor excludes the pre-split module doc's own line-43 prose mention of the same string, and confirmed the only two real attributes were at lines 45 and 295). SHA-256 verified span by span; the four digests are recorded in crates/adapter/chronica-adapter-social-data-export/src/lib.rs's own module doc: the chronica_core/serde_json imports + five store aliases (lines 47-57) 8872d90a16b5aaed05c1c81fd4f1d78d150d61291f5fa0b59d44b935e2875823; EXPORT_NOTES_LIMIT + its doc (lines 59-64) c9f2b8110d12b31b61d205409ac69126153270a9e93c3005c9af1ff61d20522e; own_notes_any_visibility + export_account_data with their docs (lines 104-176, contiguous, 73 lines) 1600607b708b70d6f709d488346afe72dbbce3a47a523e09196ecc569a288e74; the whole db_tests module (lines 178-292, contiguous, 115 lines) 89806ce297d68d67dfafb2485ea849819b8ed6861a785943f8b8fa9997e48761. EVERY ONE OF THOSE FOUR DIGESTS IS UNCHANGED AFTER THE MOVE -- ZERO EDITS, the cleanest byte-identity result this batch has produced. Two things made that possible: the adapter also wraps the moved code in `mod imp`, preserving the original 4-space indentation exactly (LOOP 68's technique), and NO ORPHAN-RULE RENAME WAS NEEDED at all -- unlike LOOP 58/66/67/68 the only row decoder in play (NoteRow::map) belongs to chronica-cap-social-notes and was never this crate's to move, and the retained AccountExport keeps its own inherent impl in the donor because the adapter never needed to add one. The digests were asserted by a generating script (pre-move against `git show HEAD:...`, then RE-VERIFIED a second time from the FINAL on-disk adapter file after all hand-written doc edits), never eyeballed. NO ITEM NEEDED VISIBILITY WIDENING: every field of AccountExport was already pub, so export_account_data's struct literal crosses the new crate boundary unchanged, and the moved code needs nothing else private from the donor. The donor's sqlx dependency, its chronica-core dependency, its four sibling-store dependencies, its db feature and its whole [dev-dependencies] table were deleted OUTRIGHT -- it now has exactly ONE dependency (serde_json) and ZERO cargo features, which is what drops it out of M7; no facade-purity analysis was needed because the retained surface names no foreign type at all (only String/bool/i64/Vec<String>/serde_json::Value), the same cleanest-donor shape LOOP 68 hit. THE ONLY EDIT TO THE RETAINED PURE HALF is one doc-comment sentence: AccountExport::notes_truncated's doc linked [`EXPORT_NOTES_LIMIT`] as an intra-doc item, and since that const moved to the adapter (it is read ONLY by the moved SELECT -- as the LIMIT $3 bind and the truncation comparison), the doc now names the bound in prose instead. Proven to be the sole difference by diffing the pre-split lines 66-102 uniformly dedented 4 spaces against the retained source: that one sentence is the ONLY hunk, and no code line changed. ONE real caller, found by exact fully-qualified-path grep for chronica_cap_social_data_export:: across every .rs in the repo (never a bare function name): crates/cap/chronica-cap-social-routes/src/social_data_export.rs, 4 references. Being cap-tier it costs ONE new layer-debt edge (layer_debt_edge_count 126 -> 127, recorded BY HAND in tools/refoundation/refoundation-layer-debt.json's _note_2026_09_08_loop69, never via --write-baseline) -- the sixth of this exact, already-precedented class for that same route crate. It needed only ONE crate path afterwards, not two, because the adapter re-exports AccountExport unconditionally, so its old donor dependency was dropped rather than kept alongside; its bundle.to_json() call site is untouched (an inherent method needs no import). A confirmed-dead chronica-adapter-api-server dependency + db feature-array entry were removed (its src/ and tests/ were grepped for chronica_cap_social_data_export first: zero hits), and chronica-adapter-api-server --features db still checks clean afterwards. IN THE OTHER DIRECTION THIS SPLIT REMOVED A LATENT HAZARD: chronica-adapter-social-postgres-batch2 and -batch4 both documented chronica-cap-social-data-export (crates/core/) as a core-tier caller forbidding THEM from ever relocating to crates/adapter/; that caller is now adapter-tier, so that hazard is gone for both, and their stale prose (plus their now-wrong \"two hazards still stand\" counts) was corrected in this same change. misplaced_cap_package_count is unchanged at 222 by design -- the donor keeps its chronica-cap-* name under crates/core/, so this is a genuine M7-only win, not metric gaming. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_authorization_roles_split_2026_09_08_loop68": "113 -> 112: crates/core/chronica-cap-authorization-roles's responsibility-tier decomposition (LOOP 68). SPLIT, not relocated -- the determination was re-derived by reading the donor's actual current 336-line src/lib.rs in full, NOT inherited from the recon note nor from the shape of its LOOP 59-67 siblings: its whole body was a single #[cfg(feature = \"db\")] mod imp, but it declares its OWN pure types (RoleRow with its camelCase to_json view and its pure exact-token `grants` membership test; RoleAssignmentRow with its own to_json; CreateRoleOutcome; AssignRoleOutcome), so the donor was kept and SHRUNK to exactly that pure surface rather than renamed away, while the SEVEN real company-scoped Postgres ops (create_role/get_role/list_roles/assign_role/revoke_role/list_roles_for_principal and the SECURITY-CRITICAL shared authorize() decision function over authz_role + authz_role_assignment, migration 0302), the ROLE_COLS/ASSIGNMENT_COLS/ROLE_COLS_JOINED projection constants and the two private PgRow decoders moved BYTE-IDENTICAL (SHA-256 verified span by span; the five digests are recorded in crates/adapter/chronica-adapter-authorization-roles/src/lib.rs's own module doc) into the new crates/adapter/chronica-adapter-authorization-roles. Pre-split digests: ROLE_COLS + ASSIGNMENT_COLS (lines 38-42) ac13c349b925b4f69e8dec72c7b12991f01f6c3787e167b8f094707d9e08984f (UNCHANGED after the move -- the adapter also wraps the moved code in mod imp, so the original 4-space indentation is preserved exactly); the RoleRow decoder (lines 58-67) 52689402ba03f243ab1fa61d4d274eb532a10b891a3ae8d9a6575b3c29b9d56c; the RoleAssignmentRow decoder (lines 96-104) 34e3b17379bfbc62f7a177e22f44ce14ed2f6583271f05b61c42a9cb24a359bd; create_role + get_role + list_roles (lines 127-184, contiguous) 77cfb84ccb976ea4bb48a0caceaaefaf7759425a45cd9fe857211a30c86fc846; assign_role + revoke_role + ROLE_COLS_JOINED + list_roles_for_principal + authorize (lines 196-329, contiguous, 134 lines) fc161b21af6c13419f34f40ef2513eb5019e713a7f0370c21cabe07c16049a80. Span boundaries were anchored with grep -n '^#[cfg(feature = \"db\")]' (the ^ anchor excludes doc-comment prose; here it confirmed the only two real attributes are at lines 32 and 332). The ONLY edits across the moved spans are the two mechanical decoder renames forced by Rust's orphan rule (E0116) -- RoleRow::map -> free fn map_role_row (3 call sites) and RoleAssignmentRow::map -> free fn map_role_assignment_row (2 call sites) -- plus, for the two decoders themselves, a uniform 4-space dedent (they left impl blocks) and -> Self spelled out as -> RoleRow / -> RoleAssignmentRow. Both decoders were and remain crate-private, so no public API changed. Reversing each transform reproduces the pre-split digest EXACTLY, asserted in the generating script (with exact occurrence-count assertions of 3/2/1 before rewriting) rather than eyeballed, and every span was then RE-VERIFIED a second time from the FINAL on-disk adapter file after all hand doc edits. The rename lengths were chosen deliberately: map_role_row is the SAME LENGTH as RoleRow::map and map_role_assignment_row is within one character of RoleAssignmentRow::map, so rustfmt reproduces the pre-split line breaks and the adapter is fully `rustfmt --edition 2021 --check`-clean (as the pre-split donor was); an 18-character map_assignment_row was tried first and REJECTED because it made rustfmt re-wrap assign_role's Box::new(...) inside a span asserted byte-identical. NO ITEM NEEDED VISIBILITY WIDENING (unlike LOOP 66/67): every retained donor item was already pub, and the moved code needs nothing private from the donor. The donor's sqlx dependency, its chronica-core dependency, its db feature and its whole [dev-dependencies] table were deleted OUTRIGHT -- it now has exactly ONE dependency (serde_json) and ZERO cargo features, which is what drops it out of M7; no facade-purity analysis was needed because the retained surface names no foreign type at all (only String/Vec<String>/serde_json::Value), the cleanest donor shape this batch has produced. SIX real callers, all found by exact fully-qualified-path grep for chronica_cap_authorization_roles:: (never bare function names). Two are core-tier and call the genuinely-I/O functions, so they were repointed to the adapter and cost TWO new layer-debt edges (layer_debt_edge_count 124 -> 126, recorded BY HAND in tools/refoundation/refoundation-layer-debt.json's _note_2026_09_08_loop68, never via --write-baseline): chronica-cap-helpdesk-inboxes (authorize) and chronica-cap-crm-support-role-assignment-authorized (assign_role + authorize). chronica-runtime-crm-support-party-authorized was also repointed but is runtime-tier, which validate-refoundation-layers.mjs deliberately does not order, so it costs nothing (LOOP 50/55 precedent). chronica-adapter-authorization-role-lifecycle and chronica-adapter-crm-support-lifecycle-events name only the PURE RoleRow/RoleAssignmentRow in their src/ and so keep their donor dependency (their dev-only test fixtures moved to the adapter). The sixth, chronica-cap-authorization-role-lifecycle (LOOP 49's donor), also names only the pure RoleRow -- but its db feature FORWARDED to chronica-cap-authorization-roles/db, a feature this split deletes, so it had to change: LOOP 49's explicitly-disclosed judgment call (a db feature kept solely so UpdateRoleOutcome could name a RoleRow that then lived behind the sibling's db gate) DISSOLVED rather than moved -- that crate's db feature was deleted outright, its sibling dependency made unconditional, and UpdateRoleOutcome is now unconditionally available, with the matching chronica-cap-authorization-role-lifecycle/db and chronica-cap-authorization-roles/db entries dropped from chronica-adapter-authorization-role-lifecycle's db feature array. chronica-adapter-api-server was checked for a dead dependency and has NONE (it never declared or referenced this crate). misplaced_cap_package_count is unchanged at 222 by design -- the donor keeps its chronica-cap-* name under crates/core/, so this is a genuine M7-only win, not metric gaming. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_crm_contract_lifecycle_split_2026_09_08_loop67": "114 -> 113: crates/core/chronica-cap-crm-support-contract-lifecycle's responsibility-tier decomposition (LOOP 67). SPLIT, not relocated -- the determination was re-derived by reading the donor's actual current 445-line src/lib.rs in full, NOT by inheriting the outcome of its LOOP 59 / 60 / 61 / 62 / 63 / 64 / 65 / 66 siblings and not by trusting the recon note: its whole body was a single #[cfg(feature = \"db\")] mod imp, but it declares its OWN pure types (CrmContractRow with its to_domain rehydration into the ported chronica_core_erp_crm::contract::CrmContract, its fulfilment_status derivation through the ported fulfilment_status_as_of, and its camelCase to_json view; ContractView; CreateContractOutcome; UpdateContractOutcome; ContractPatch) and its OWN pure operators (parse_opt_date, date_ok), so the donor was kept and SHRUNK to exactly that pure surface rather than renamed away, while the SEVEN real company-scoped Postgres ops (create_contract/list_contracts/get_contract/update_contract/sign_contract/cancel_contract/update_fulfilment_terms over crm_contracts, migrations 0061 + 0411), the CON_COLS column-list constant, the `view` row-to-ContractView builder and the private CrmContractRow::map row decoder moved BYTE-IDENTICAL (SHA-256 verified span by span; the four digests are recorded in crates/adapter/chronica-adapter-crm-support-contract-lifecycle/src/lib.rs's own module doc) into the new crates/adapter/chronica-adapter-crm-support-contract-lifecycle. Pre-split digests: CON_COLS (lines 37-44) 9107dee2e80c2faa99e58b888b531c4df4dcc9f6ccaa19dd90bc0a3ce9363450 (unchanged after the move); the row decoder (lines 68-87) 879e2e7eb2f282184810d7fa3552a6fb7db1bfa1dffee33170a713091989f642; `view` (lines 169-185) 54e17a8f41c9026b2058cd810d5812317313524dfd60e4dc9626de729739522f; the seven async fns (lines 219-441, contiguous) 0292c087de9e49749f25444a940f0457bb97cc08c8f0d2e2289876e45586245f. Span boundaries were anchored with grep -n '^#[cfg(feature = \"db\")]' (the ^ anchor excludes the pre-split module doc's line-23 prose mention of the same string, proving the only real attributes are at lines 26 and 444). The ONLY edits across the moved spans are the mechanical CrmContractRow::map -> free fn map_row rename forced by Rust's orphan rule (2 call sites, in `view` and in update_contract) plus, for the decoder itself, a uniform 4-space dedent and spelling -> CoreResult<Self> out as -> CoreResult<CrmContractRow>; reversing each transform reproduces the pre-split digest EXACTLY, asserted in the generating script rather than eyeballed. FOUR donor items were widened private -> pub so the adapter can still reach them across the new crate boundary (parse_opt_date and date_ok, both called by create_contract and update_contract; CrmContractRow::to_domain and CrmContractRow::fulfilment_status, both called by `view`) -- all four keep byte-identical bodies, each carrying a one-line disclosure in its own doc. FACADE-PURITY VERIFIED TWO LEVELS DEEP FROM THE CURRENT TREE, re-checked for this loop rather than cited from LOOP 66 (a manifest could in principle have changed since): chronica-core-erp-crm declares NO [features] section at all and no sqlx (so it takes all nine of its deps at their DEFAULT feature set and never forwards chronica-core/db), and one level deeper none of its four chronica-* deps carries a sqlx line or a [features] section at all while chronica-core is default = [] with db opt-in (chronica-core-persistence likewise default = [] with sqlx optional behind ITS db -- its only unconditional sqlx line is a [dev-dependencies] entry). As in LOOP 66 (and unlike LOOP 65) the donor KEEPS its direct chronica-core edge, because its retained pure parse_opt_date / to_domain / fulfilment_status genuinely return CoreResult and map CoreError::Serialization -- taken at chronica-core's DEFAULT, sqlx-free feature set, with the donor's money guard pinning that that line never grows a features = [...]. The donor's db feature and its sqlx dependency were deleted OUTRIGHT: it declares no cargo features at all, which is what drops it out of M7. DISCLOSED DIFFERENCE FROM LOOP 66: this split DOES add one new layer-debt edge. CALLERS WERE FOUND BY EXACT FULLY-QUALIFIED-PATH GREP, not by bare function name -- crates/adapter/chronica-adapter-crm-http/src/crm/contract.rs (8 references) was already adapter-tier, but crates/cap/chronica-cap-deals-federation-routes/src/{search,legal}.rs (5 references between them) is cap-tier and calls the genuinely-I/O list_contracts from BOTH files, so its repoint is unavoidable: layer_debt_edge_count 123 -> 124, recorded BY HAND in tools/refoundation/refoundation-layer-debt.json's _note_2026_09_08_loop67, never via --write-baseline. search.rs additionally CONSTRUCTS a CrmContractRow literal directly (in its cross_subsystem_doc_tests unit-test module) and names ContractView; both still resolve, because the adapter re-exports the donor's whole pure surface unconditionally (pub use chronica_cap_crm_support_contract_lifecycle::*), so that caller needs exactly ONE crate path and its donor dependency was dropped rather than kept alongside. A confirmed-dead chronica-adapter-api-server dependency + db feature-array entry were removed (its src/ and tests/ were grepped for real usage first: zero hits). misplaced_cap_package_count is unchanged at 222 by design -- the donor keeps its chronica-cap-* name under crates/core/, so this is a genuine M7-only win, not metric gaming. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_crm_surveys_split_2026_09_08_loop66": "115 -> 114: crates/core/chronica-cap-crm-support-surveys's responsibility-tier decomposition (LOOP 66). SPLIT, not relocated -- the determination was re-derived by reading the donor's actual current 352-line src/lib.rs in full, NOT by inheriting the outcome of its LOOP 59 (survey_targeting_branching) / 60 / 61 / 62 / 63 / 64 / 65 siblings and not by trusting a prior scouting note: its whole body was a single #[cfg(feature = \\\"db\\\")] mod imp, but it declares its OWN pure types (CrmSurveyRow with its to_domain rehydration into the ported chronica_core_erp_crm::survey::Survey and its camelCase to_json view, SurveyView, CreateSurveyOutcome, UpdateSurveyOutcome, SurveyPatch) and its OWN pure operator (the fail-closed decode_questions jsonb decoder, with four unit tests pinning it), so the donor was kept and SHRUNK to exactly that pure surface rather than renamed away, while the four real company-scoped Postgres ops (create_survey/list_surveys/get_survey/update_survey over crm_surveys, migration 0063), the SUR_COLS column-list constant, the `view` row-to-SurveyView builder and the private CrmSurveyRow::map row decoder moved BYTE-IDENTICAL (SHA-256 verified span by span; the four digests are recorded in crates/adapter/chronica-adapter-crm-support-surveys/src/lib.rs's own module doc) into the new crates/adapter/chronica-adapter-crm-support-surveys. The ONLY edits across the moved spans are the mechanical CrmSurveyRow::map -> free fn map_row rename forced by Rust's orphan rule (2 call sites, in `view` and in update_survey); reversing them reproduces the pre-split digests exactly, asserted in the generating script rather than eyeballed. Two donor items were widened private -> pub so the adapter can still reach them across the new crate boundary (decode_questions, called by map_row; CrmSurveyRow::to_domain, called by `view`) -- both keep byte-identical bodies. FACADE-PURITY VERIFIED TWO LEVELS DEEP in the current tree, re-checked rather than cited from LOOP 61/62: chronica-core-erp-crm declares NO [features] section at all and no sqlx (so it takes all nine of its deps at their DEFAULT feature set and never forwards chronica-core/db), and one level deeper none of its four chronica-* deps carries a sqlx line at all while chronica-core is default = [] with db opt-in (chronica-core-persistence likewise default = [] with sqlx optional behind ITS db). DISCLOSED DIFFERENCE FROM LOOP 65: the donor KEEPS its direct chronica-core edge, because its retained pure decode_questions genuinely returns CoreResult and maps CoreError -- it is taken at chronica-core's DEFAULT, sqlx-free feature set, and the donor's money guard now pins that that line never grows a features = [...]. The donor's db feature and its sqlx dependency were deleted OUTRIGHT: it declares no cargo features at all, which is what drops it out of M7. BOTH real callers (crates/adapter/chronica-adapter-crm-http/src/crm/survey.rs, 5 references, and crates/adapter/chronica-adapter-crm-support-survey-targeting-branching/src/lib.rs, 3 references) were already adapter-tier, so the repoint is adapter -> adapter and introduces ZERO new layer-debt edges -- confirmed by node tools/refoundation/validate-refoundation-layers.mjs reporting 'no new edges (123 known debt edge(s) remain)', so refoundation-layer-debt.json was NOT touched. A confirmed-dead chronica-adapter-api-server dependency + db feature-array entry were removed (its src/ and tests/ were grepped for real usage first: none). misplaced_cap_package_count is unchanged at 222 by design -- the donor keeps its chronica-cap-* name under crates/core/, so this is a genuine M7-only win, not metric gaming. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_analytics_dashboard_split_2026_09_08_loop65": "116 -> 115: crates/core/chronica-cap-crm-support-analytics-dashboard's responsibility-tier decomposition (LOOP 65). SPLIT, not relocated -- the determination was re-derived by reading the donor's actual current 411-line src/lib.rs in full, NOT by inheriting the outcome of its LOOP 60 (analytics_action) / LOOP 62 (analytics_alert_rule) / LOOP 63 (analytics_insight) / LOOP 64 (feature_flag) siblings and not by trusting a prior scouting note: its whole body was a single #[cfg(feature = \"db\")] mod imp, but it declares its OWN pure types (DashboardRow with its to_dashboard rehydration into the ported chronica_core_analytics_dashboard::Dashboard and its camelCase to_json redact-out view, NewDashboard, CreateDashboardOutcome, AnnotationAdded), so the donor was kept and SHRUNK to exactly that pure surface rather than renamed away, while the eleven real company-scoped Postgres ops (create_dashboard/list_dashboards/get_dashboard/set_pinned/add_tag/remove_tag/add_annotation/remove_annotation/enable_sharing/disable_sharing/delete_dashboard over analytics_dashboard, migration 0076 -- the largest real-I/O surface of this split batch so far), the private mutate load-apply-persist helper, the private DashboardRow::map row decoder, the DASH_COLS column-list constant and the is_unique_violation duplicate detector moved BYTE-IDENTICAL (SHA-256 verified span by span; the four digests are recorded in crates/adapter/chronica-adapter-crm-support-analytics-dashboard/src/lib.rs's own module doc) into the new crates/adapter/chronica-adapter-crm-support-analytics-dashboard. The ONLY edit across the moved spans is the mechanical DashboardRow::map -> free fn map_row rename forced by Rust's orphan rule (6 call sites); reversing it reproduces the pre-split digest exactly, asserted rather than eyeballed. FACADE-PURITY VERIFIED TWO LEVELS DEEP, not assumed: the retained donor surface names foreign types, but chronica-core-analytics-dashboard declares NO [features] section at all and no sqlx (so it takes all six of its deps at their DEFAULT feature set), and one level deeper none of those six defaults on a db feature -- chronica-core-analytics-{metric-query,realtime-panel,insight-query} have no [features] section at all, chronica-core-business-events has a db feature but is default = [], and chronica-core is default = [] with db opt-in. So the donor's db feature, its sqlx dependency and its direct chronica-core dependency were deleted OUTRIGHT rather than narrowed: it now has 2 dependencies and ZERO cargo features, and its own money guard gained a donor_stays_pure_after_the_split test that fails if sqlx, chronica-core, a [features] table or a SQL statement ever reappears. misplaced_cap_package_count is deliberately UNCHANGED at 222: a SPLIT leaves the donor chronica-cap-*-named under crates/core/, and shipping genuinely pure types to the adapter tier purely to score M2 is exactly the metric-gaming this program forbids. The one real caller (crates/cap/chronica-cap-platform-misc-routes/src/analytics/dashboard.rs, found by exact fully-qualified-path grep -- 12 references) was repointed, adding one disclosed cap -> adapter layer-debt edge recorded by hand in tools/refoundation/refoundation-layer-debt.json under _note_2026_09_08_loop65 (layer_debt_edge_count 122 -> 123); a confirmed-dead chronica-adapter-api-server dependency was removed.",
  "_analytics_insight_split_2026_09_08n": "118 -> 117: crates/core/chronica-cap-crm-support-analytics-insight's responsibility-tier decomposition (LOOP 63). SPLIT, not relocated -- the determination was made by READING the actual current 329-line src/lib.rs in full, NOT by inheriting the outcome of its LOOP 60 (analytics_action) / LOOP 62 (analytics_alert_rule) siblings and not by trusting a prior loop's scouting note. Although the whole file sat inside a single #[cfg(feature = \"db\")] mod imp (the shape LOOP 52/53/54/56's relocations had), the crate declares THREE pub types of its own -- InsightRow (with its pure to_insight domain rehydration and its pure camelCase to_json operator view), NewInsight and CreateInsightOutcome. None of them touches a PgPersistence. That is real retained pure surface, so the donor was kept and shrunk rather than renamed away, exactly as in LOOP 50/55/57/58/60/61/62. THE FACADE-PURITY QUESTION WAS SETTLED RATHER THAN ASSUMED, and two levels deep: the retained surface DOES name foreign types -- Insight as to_insight's return (chronica-core-analytics-insight), and InsightQuery (to_insight's parse target) plus InsightError (CreateInsightOutcome::InvalidQuery's payload), both owned by chronica-core-analytics-insight-query. Both crates' Cargo.toml were read directly for this loop: chronica-core-analytics-insight-query deps only chronica-core-business-events + serde with NO [features] section at all and no sqlx; chronica-core-analytics-insight deps only that query crate, chronica-core-business-events, chronica-core and serde, again with NO [features] section and no sqlx. ONE LEVEL DEEPER (the rigor LOOP 60/61/62 established): chronica-core-business-events DOES declare an optional sqlx behind a db feature, and chronica-core has a db feature forwarding chronica-core-persistence/db + chronica-core-version/db -- but BOTH are default = [] and opt-in, and neither insight crate requests a feature on those edges, so both are taken at their DEFAULT (empty) feature set. No sqlx and no db feature reaches the donor through either edge, so the retained surface is pure, not pure-by-re-export-of-something-impure. The donor's sqlx and direct chronica-core dependencies were DROPPED OUTRIGHT along with its db feature, leaving chronica-core-analytics-insight + chronica-core-analytics-insight-query + serde_json as its only dependencies and zero cargo features; verified by `cargo check -p chronica-cap-crm-support-analytics-insight` compiling clean with no feature flags at all. The real mechanics moved BYTE-IDENTICAL into the new crates/adapter/chronica-adapter-crm-support-analytics-insight, verified by SHA-256 against `git show HEAD:crates/core/chronica-cap-crm-support-analytics-insight/src/lib.rs` in FOUR separately hashed non-contiguous spans, anchored by grep -n '^#[cfg(feature = \"db\")]' with the ^ anchor to exclude the module doc's line-15 prose mention of the same string (the real attributes are at pre-change lines 18 and 328). Span A, the INSIGHT_COLS projection constant with its doc comment, pre-change lines 26-28 -- sha256 fcf79472ba9b757cdcc84b454d004f1fa71ec008fd39484041340fd11fc0bfa9, MOVED WITH ZERO EDITS (it kept its original 4-space mod imp indentation), hashes identically on both sides. Span B, the is_unique_violation duplicate detector with its doc comment, pre-change lines 30-34 -- sha256 fbb17bd9c79d53dd0c8af7cc8c440ce8b68ac803d2c8b6f5673d5241ac15b562, likewise MOVED WITH ZERO EDITS and hashing identically on both sides. Span C, the row mapper, pre-change lines 53-67 -- sha256 6705f7ef02ea8c2b1062794cb0ccc1675282356f25486e2dc7b5310b56ea895d, e98af1f81ad391c673219c017d7820f102b446d25d7520917c6a9fe4132e21dc as it now stands in the adapter, and reversing this loop's three mechanical edits to it (re-indent 4, map_row -> map, InsightRow return type -> Self) reproduces the pre-change digest EXACTLY -- verified by actually running the reverse transform through sha256sum, not by inspection. Span D, the eight async function bodies (create_insight, list_insights, get_insight, rename, set_favorited, share, unshare, soft_delete) with their SQL strings, binds and CoreError::from mapping, pre-change lines 128-325 -- sha256 f23508d497f059bbe1b52ca56c2fb61e731e665198c84972e68d9df71faa4be0 pre-change and cdfefa2ed7a7b36c0867aecc4d0bd485d50dcc3bc120494bd37a7c908ac530c4 in the adapter; reversing the ONE mechanical rename below (7 occurrences on 7 lines) reproduces the pre-change digest EXACTLY, so that is provably the only difference across all 198 lines. No rustfmt reflow was applied to that span: map_row is SHORTER than InsightRow::map, so rustfmt would now join create_insight's three-line-wrapped Created(Box::new(map_row( call, and that call was deliberately LEFT at its original wrapping precisely so the span stays byte-identical modulo the rename alone -- the same deliberate non-rustfmt-clean choice as LOOP 60's and LOOP 62's adapters. ONE DISCLOSED MECHANICAL EDIT, not hidden: InsightRow::map -- a PRIVATE associated fn -- became the free fn map_row in the adapter, because Rust's orphan rule forbids an inherent impl InsightRow outside the crate that declares the type; its body is byte-identical modulo a uniform 4-space dedent (it lost the impl block's nesting level) plus spelling out the -> Self return type as -> InsightRow (a bare Self has no meaning outside an impl block), and its 7 call sites moved with the rename. The helper was, and stays, crate-private -- no public API changed. NOTABLY, and unlike LOOP 61 (three widened operators) and LOOP 62 (two), this split widened EXACTLY ZERO items: every donor item the moved functions consume (InsightRow, NewInsight, CreateInsightOutcome) was already pub, and the donor's two private helpers (INSIGHT_COLS, is_unique_violation) both moved wholesale because both of their consumer sets moved. Two import lines shrank, disclosed: json was dropped from the moved use serde_json::{json, Value} (only the retained InsightRow::to_json needed the macro; Value is still named by map_row's query-column read), and Insight was dropped from the moved use chronica_core_analytics_insight::{Insight, InsightRegistry, InsightStoreError} because its only naming use, to_insight, stayed in the donor. InsightRegistry, InsightStoreError and InsightQuery ARE still named in the adapter (by create_insight), so -- as in LOOP 61 and unlike LOOP 60/62 -- the adapter DOES declare real manifest edges to both domain crates. misplaced_cap_package_count is UNCHANGED at 222 by design: a SPLIT keeps the donor chronica-cap-*-named under crates/core/, unlike a RELOCATION; scoring M2 here would require relocating genuinely pure types into the adapter tier, which would be metric-gaming. world_graph_live_dependency_count unchanged at 5; cap_directory_count unchanged at 36. layer_debt_edge_count 120 -> 121: ONE new forbidden edge, chronica-cap-platform-misc-routes (cap) -> chronica-adapter-crm-support-analytics-insight (adapter), disclosed by hand in tools/refoundation/refoundation-layer-debt.json's _note_2026_09_08_loop63 (written by hand, NOT via --write-baseline). CALLERS WERE FOUND BY EXACT FULLY-QUALIFIED-PATH GREP, not by bare function name: a repo-wide grep for both the underscore and hyphen crate spellings found exactly ONE real (non-test) production caller, crates/cap/chronica-cap-platform-misc-routes/src/analytics/insight.rs (10 fully-qualified references -- the use line plus 9 call sites across the axum handlers behind POST|GET /api/companies/:company_id/analytics/insights, GET|PUT|DELETE .../insights/:short_id, GET .../insights/:short_id/run and POST .../insights/:short_id/share|unshare), which is cap-tier -- hence the one new edge. A second declared dependent, chronica-adapter-api-server, was CONFIRMED DEAD by grepping its own src/ and tests/ for chronica_cap_crm_support_analytics_insight, InsightRow, NewInsight and CreateInsightOutcome: zero hits of any kind, so its manifest dep + db feature-array entry were removed outright rather than repointed, per the standing LOOP-21 precedent -- `cargo check -p chronica-adapter-api-server --features db` was re-run after the removal and compiles clean. It still enables the new adapter's db feature transitively through chronica-cap-platform-misc-routes/db, and its analytics_insight_route.rs tests exercise the routes over HTTP, not the crate directly. STALE-CLAIM CORRECTIONS (long-gone flat crate names, fixed rather than carried on): the donor's .chronica caller_entries named crates/chronica-api/src/routes/analytics/insight.rs, crates/chronica-api/src/routes/analytics/meta_plane_gate.rs and crates/chronica-crm/src/store/mod.rs with chronica-api as the runtime owner; its covered_sources and all seven verification witnesses named crates/chronica-api/... paths; its relations named chronica_analytics::insight[_query]::* types; and its CAPABILITY.md claimed chronica-crm re-exports it as chronica_crm::store::analytics_insight_store, as did the root Cargo.toml workspace-dependency comment. Neither chronica-api nor chronica-crm exists in this repo (re-confirmed by repo-wide grep), so all were replaced with real current paths and the real chronica_core_analytics_insight[_query]::* type paths -- the seven witness test-fn names were each verified to exist in crates/adapter/chronica-adapter-api-server/tests/analytics_insight_route.rs before being written down. The donor .chronica's supersedes entry still names the flat chronica-crm file and was deliberately RETAINED as extraction provenance, annotated as historical. Donor maturity RUNTIME_BOUND -> PURE. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_feature_flag_split_2026_09_08_loop64": "117 -> 116: crates/core/chronica-cap-crm-support-feature-flag's responsibility-tier decomposition (LOOP 64). SPLIT, not relocated -- the determination was re-derived by reading the donor's actual current 395-line src/lib.rs in full: its whole body was a single #[cfg(feature = \"db\")] mod imp, but it declares its OWN pure types (FeatureFlagRow with its to_flag domain rehydration and its camelCase to_json view, NewFlag, FlagEvalResult, CreateFlagOutcome, UpdateFlagOutcome, DeleteFlagOutcome), so the donor was kept and shrunk rather than renamed away (same shape as LOOP 50/55/57/58/60/61/62/63, unlike the whole-crate relocations of LOOP 52/53/54/56). The six real async ops over feature_flag (migration 0073) -- create_flag, list_flags, get_flag, update_flag, soft_delete_flag, evaluate_flag -- plus the FF_COLS projection constant, the is_unique_violation duplicate detector, the parse_conditions/parse_variants serde helpers and the private FeatureFlagRow::map row decoder moved BYTE-IDENTICAL (SHA-256 verified span by span against git show HEAD:...; span boundaries anchored with grep -n '^#\\\\[cfg(feature = \"db\")\\\\]' so the module doc's prose mention of the same string could not be mistaken for a real attribute) into the new crates/adapter/chronica-adapter-crm-support-feature-flag. The ONLY edit inside a moved span was the mechanical FeatureFlagRow::map -> map_row rename forced by Rust's orphan rule (4 call sites; reversing it reproduces the pre-split digest exactly). M2 misplaced_cap_package_count is UNCHANGED at 222 by design: the donor keeps its chronica-cap-* package name and its crates/core/ home, since after the split it is a genuinely pure vocabulary crate (facade-purity verified two levels deep -- chronica-core-analytics-feature-flag deps only chronica-core + serde with NO [features] section and no sqlx, and chronica-core's db feature is default-off and unrequested), so its db feature, sqlx dependency and direct chronica-core dependency were deleted outright and it now has ZERO cargo features. One new, disclosed cap -> adapter layer-debt edge from the single real caller (crates/cap/chronica-cap-platform-misc-routes/src/analytics/feature_flag.rs, found by exact fully-qualified-path grep) -- recorded by hand in tools/refoundation/refoundation-layer-debt.json's _note_2026_09_08_loop64, layer_debt_edge_count 121 -> 122. A dead chronica-adapter-api-server dependency was confirmed dead (zero src/ or tests/ hits) and removed rather than repointed. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_inbound_notification_split_2026_09_08h": "124 -> 123: crates/core/chronica-cap-crm-support-inbound-notification's responsibility-tier decomposition (LOOP 57). SPLIT, not relocated -- the determination was made by READING the actual current 254-line src/lib.rs, not by assuming LOOP 52/53/54/56's relocation outcome for a differently shaped crate. Although the whole file sat inside a single #[cfg(feature = \"db\")] mod imp (the shape those four relocations had), it DOES declare local pure surface of its own: the InboundEventNotification projection struct (with a to_json view), the two-variant ProjectOutcome enum (Created/Duplicate), the bounded projection_title/projection_body text builders and their private `bound` helper, the TITLE_MAX/BODY_MAX/DISCLOSURE consts, and a real pure unit test over them (projection_text_is_receipt_metadata_not_payload). The one foreign type in that surface is chronica_core::CompanyId, VERIFIED ALREADY PURE by reading the manifests directly in the current tree: chronica-core is a thin re-export facade whose `db` feature forwards ONLY to chronica-core-persistence and chronica-core-version, and CompanyId comes from chronica-core-identifiers, whose only dependencies are serde, uuid and chronica-core-errors with no `db` feature at all. So the pure surface could be hoisted out of the gate UNCONDITIONALLY and the donor's `db` feature deleted outright rather than narrowed -- the LOOP 50/55 shape, explicitly NOT LOOP 49's UpdateRoleOutcome case. The real mechanics (project_from_receipt, persist_inbound_event_then_project, list_for_company, count_for_company, the private map_row helper, the NOTIF_COLS column list, the LIST_LIMIT bound and the store_moves_no_money guard test) moved BYTE-IDENTICAL into the new crates/adapter/chronica-adapter-crm-support-inbound-notification -- a 133-line region, sha256 be74307f83933fcadc1af8273c60973db3aa662b09ab4605ad3d120d770a0fe2, compared against git show HEAD:crates/core/chronica-cap-crm-support-inbound-notification/src/lib.rs. misplaced_cap_package_count is UNCHANGED at 222 by design: a SPLIT keeps the donor chronica-cap-*-named under crates/core/, unlike a RELOCATION; scoring M2 here would require relocating a genuinely pure value type into the adapter tier, which would be metric-gaming. The real production caller, chronica-adapter-inbound-webhooks-http (11 route files), and the integration-test caller chronica-adapter-api-server were repointed as a straight crate rename onto the adapter's re-export; BOTH are already adapter-tier, so this repoint is adapter -> adapter and introduces ZERO new layer-debt edges (layer_debt_edge_count stays 115, confirmed via node tools/refoundation/validate-refoundation-layers.mjs, which reported 'no new edges'). One disclosed API change: DISCLOSURE was a private const inside mod imp and had to become pub on the donor because the adapter's project_from_receipt INSERT binds it. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_scheduling_event_type_split_2026_09_08g": "126 -> 125: crates/core/chronica-cap-scheduling-event-type's responsibility-tier decomposition (LOOP 55). This crate was SPLIT, not relocated -- the determination was made by READING the actual current 215-line src/lib.rs, not by assuming LOOP 52/53/54's relocation outcome for a differently shaped crate. Unlike those three, this one DOES declare a local pure type of its own: the three-variant `CreateEventTypeOutcome` enum (Created(Box<EventType>) / Rejected(EventTypeError) / SlugConflict). Its payload types, EventType and EventTypeError, come from chronica-core-scheduling-domain, VERIFIED ALREADY PURE by reading that crate's Cargo.toml directly in the current tree: its only dependencies are serde, time and thiserror -- no sqlx, no chronica-core, no `db` feature at all. So the enum could be hoisted out of the `#[cfg(feature = \"db\")]` gate UNCONDITIONALLY and the donor's `db` feature deleted outright rather than narrowed -- exactly the shape of LOOP 50's sibling chronica-cap-scheduling-host split in this same scheduling family, and explicitly NOT LOOP 49's UpdateRoleOutcome case (which had to keep a db feature because its payload type lived inside an unsplit sibling's own db-gated module). The five real ops (create_event_type / set_round_robin_weighting / get_event_type / list_event_types / delete_event_type), plus the private map_row / is_unique_violation helpers and the SELECT column-list constant, moved to the new crates/adapter/chronica-adapter-scheduling-event-type, which re-exports CreateEventTypeOutcome unconditionally so a caller needs ONE path for both. BYTE-IDENTITY verified by SHA-256, not eyeballing, and anchored on the REAL `#[cfg(feature = \"db\")]` attribute lines (grep '^#\\\\[cfg' -- the module doc comment above them mentions the same string twice and would have mis-anchored a naive search): the moved real-I/O region is 168 lines, old src/lib.rs lines 43-210 vs new src/lib.rs lines 48-215, sha256 ba6365462abce5e4f5fb58ec94c19502f74cba1a7b70fb3e5c47737f086d6eaa on BOTH the git-show-HEAD original and the new adapter file. The enum region (old lines 33-41, inside mod imp) is likewise byte-identical to the donor's new top-level lines 38-46 after a 4-space dedent, sha256 532dcd88490d10b4ed62b1d340c083969851d450bce4b709f53ce7b03700ace6. The ONLY other edits are import lines: `use chronica_cap_scheduling_event_type::CreateEventTypeOutcome;` added, and the domain import narrowed by dropping EventTypeError (needed only by the enum that stayed behind). M2 misplaced_cap_package_count is DELIBERATELY UNCHANGED at 223: a split keeps the chronica-cap-*-named donor under crates/core/, so this is an M7-only win -- unlike LOOP 52/53/54's combined M2+M7 relocations. Donor maturity moves RUNTIME_BOUND -> PURE (it now has zero dependencies beyond the pure domain, zero cargo features, and zero I/O). CALLERS: identified by fully-qualified-path grep for `chronica_cap_scheduling_event_type::` (NOT a bare function-name grep). There is NO real (non-test) production caller anywhere in the repo. The only consumer is chronica-cap-scheduling-booking, and only from its [dev-dependencies], as a live-DB fixture chokepoint (tests/booking_store_live.rs and tests/round_robin_live.rs seed event types through create_event_type / set_round_robin_weighting). Both were repointed as a straight crate rename onto the adapter. Because that edge is DEV-ONLY, this split adds NO layer-debt edge at all -- validate-refoundation-layers.mjs checks `dependency.kind === null` edges only (see its own module doc) -- so layer_debt_edge_count stays 115, the one structural difference from LOOP 50's otherwise identical sibling split, which did add a disclosed core -> adapter edge. Independently confirmed by running the layer gate: 'no new edges'. chronica-adapter-api-server was checked per the standing rule and carries NO dependency or db-feature-array entry on this crate at all (grep of its Cargo.toml, src/ and tests/ returns nothing), so there was nothing dead to remove. The live-DB suite moved with the mechanics to crates/adapter/chronica-adapter-scheduling-event-type/tests/event_type_store_live.rs (5 tests, all re-run for real against local Postgres: 5 passed in 0.30s with DATABASE_URL set vs 5 skip-path lines printed and 0.00s with it unset -- a genuine live run, not a vacuous pass). The donor's money guard was rewritten to the LOOP 50 post-split shape (manifest check hardened to a line-prefix match so the manifest's own prose cannot false-positive; pure_type_source_is_actually_scanned pins the enum surface so the token scan cannot pass vacuously on an emptied-out src/) plus a new donor_stays_pure_after_the_split test asserting no I/O crate returns to the manifest and no SQL verb or cfg-gate returns to the source (comment-stripped first, since this crate's module doc legitimately narrates the gate it came out of). The adapter got the established 5-test adapter money-guard template including migration_schema_has_no_money_column over 0365_scheduling_event_types_bookings.sql, scanned from its first CREATE TABLE onward because that file's leading prose legitimately says 'NEVER money: no fee/deposit/payment column'. All 9 guard tests pass. Also corrected stale claims naming the long-gone flat crate chronica-scheduling: the donor descriptor's caller_entries/runtime_owners (crates/chronica-scheduling/src/store/mod.rs) and chronica-cap-scheduling-booking/CAPABILITY.md's 'chronica-scheduling re-exports this crate at store::booking_store' Callers section -- that crate is independently confirmed absent under crates/ (find -maxdepth 2 -iname 'chronica-scheduling' returns nothing) and absent from the workspace manifest.",
  "_event_definitions_relocation_2026_09_08f": "misplaced_cap_package_count 224 -> 223 (core 224 -> 223) AND forbidden_core_io_count 127 -> 126, BOTH in this one change: crates/core/chronica-cap-crm-support-event-definitions (LOOP 54) was NOT split -- it was PHYSICALLY RELOCATED, whole, to crates/adapter/chronica-adapter-crm-support-event-definitions (git mv, preserving history) and renamed in place. The determination was made by READING the actual current src/lib.rs, not by assuming the prior loops' outcome: the 176-line file is a module doc comment followed by a single #[cfg(feature = \"db\")] mod imp { ... } plus a #[cfg(feature = \"db\")] pub use imp::*; -- ZERO pub struct, pub enum or pub trait is declared anywhere in it, and with the `db` feature OFF the crate exports literally nothing. Its six public items (definition_json / derive_registry / list_definitions / get_definition / properties_for / set_description) are real sqlx mechanics (a SELECT over analytics_event_description, an INSERT ... ON CONFLICT UPSERT, and a company-scoped business_event derive-load) plus JSON rendering, over foreign types (EventDefinition, PropertyType, EventDefinitionRegistry, plus CompanyId/CoreError/CoreResult/PgPersistence from chronica-core). Those three domain types live in, and stay in, the separate chronica-core-analytics-event-definition crate -- VERIFIED pure by reading its Cargo.toml directly in the current tree: its ONLY dependencies are chronica-core and serde; it declares no sqlx and no `db` feature at all, and was NOT touched or moved by this change. The three non-pub items inside the gate (the EVENT_SCAN_LIMIT = 100_000 const, infer_type, parse_epoch) are private I/O tuning/parsing helpers reachable only from derive_registry, not a pure domain surface -- so a split would have left an empty, pointless donor stub, and the relocation template (LOOP 42 manage-email-blacklist / LOOP 46 projection-replay-state / LOOP 47 money-event-projector / LOOP 52 customer-master / LOOP 53 party-details) is its correct disposition. Maturity deliberately stays RUNTIME_BOUND, NOT PURE -- the crate still performs real I/O; relocation moves that I/O to the right tier, it does not make the crate pure. Byte-identity of the moved region independently verified by hash, not eyeballing: the region from the first `#[cfg(feature = \"db\")]` line to EOF is 157 lines with sha256 13b657bd53be709414aa0ad20796d70a5eb3ee3b5e0bcc2bc0fc5b6c9e8f4cce identical on both the git-show-HEAD original (crates/core/chronica-cap-crm-support-event-definitions/src/lib.rs lines 19-176) and the relocated file; only the module-doc header above it changed. CALLER: the one real production caller was identified by fully-qualified-path grep for `chronica_cap_crm_support_event_definitions::` (not a bare function-name grep) and is crates/cap/chronica-cap-platform-misc-routes/src/analytics/event_definition.rs -- its four axum handlers behind GET|PUT /api/companies/:company_id/analytics/event-definitions[...]. This CONFIRMS the dispatch brief's caller claim. That caller is physically under crates/cap/, so the repoint creates ONE new disclosed cap -> adapter layer-debt edge (layer_debt_edge_count 114 -> 115), hand-recorded in tools/refoundation/refoundation-layer-debt.json's forbidden_edges array plus its _note_2026_09_08_loop54 prose note (NOT via --write-baseline). It is that route crate's SIXTH edge of exactly this shape, not a novel debt class. chronica-adapter-api-server's dependency + db-feature-array entry on the old crate was confirmed dead-in-src (grep for crm_support_event_definitions / crm-support-event-definitions across its own src/ and tests/ returns nothing; its tests/analytics_event_definition_route.rs exercises the four routes over HTTP via common::router only, never importing the crate by name) and was removed outright rather than repointed, per the standing rule. CHRONICA-CORE-BUSINESS-EVENTS, disclosed and deliberately OUT OF SCOPE: the relocated crate depends on chronica-core-business-events for its company-scoped derive-load (chronica_core_business_events::list_events), and that sibling IS still a crates/core/ crate carrying an optional sqlx + its own `db` feature (verified by reading its Cargo.toml) -- i.e. not yet purified. This confirms the dispatch brief's 'KNOWN COMPLICATION' warning as accurate. The relationship is strictly ONE-WAY: this crate is a CONSUMER of that one (an adapter -> core edge, ALLOWED under FORBIDDEN_TARGETS.adapter = {infra}); that crate does not depend on this one. The relocation therefore required NO change to chronica-core-business-events and none was made -- its db feature array entry is simply carried across verbatim in the relocated crate's own `db` feature. Purifying it remains separate, unclaimed work. The crate's tests/money_guard.rs was widened from the donor's two include_str!-based checks to the established 4-test relocation template (crate_never_depends_on_a_money_path_crate / source_never_references_a_money_authorizing_primitive_by_name / every_op_source_is_actually_scanned pinning all six ops / migration_schema_has_no_money_column scanning 0077_analytics_event_descriptions.sql from its first CREATE TABLE onward, since that file's leading prose legitimately says \"Money-FREE\"); all 4 verified passing. Also corrected stale capability-descriptor + CAPABILITY.md caller claims referencing two long-gone flat crates (crates/chronica-api/src/routes/analytics/event_definition.rs and crates/chronica-crm/src/store/mod.rs, plus a claimed chronica_crm::store::event_definition_store re-export -- both directories independently confirmed absent under crates/), resolved to the real current caller and the real crates/adapter/chronica-adapter-api-server/tests/analytics_event_definition_route.rs witness paths, and the stale chronica_analytics::event_definition::* type paths resolved to the real chronica_core_analytics_event_definition::*.",
  "_party_details_relocation_2026_09_08e": "misplaced_cap_package_count 225 -> 224 (core 225 -> 224) AND forbidden_core_io_count 128 -> 127, BOTH in this one change: crates/core/chronica-cap-crm-support-party-details (LOOP 53) was NOT split -- it was PHYSICALLY RELOCATED, whole, to crates/adapter/chronica-adapter-crm-support-party-details (git mv, preserving history) and renamed in place. The determination was made by READING the actual current src/lib.rs, not by trusting the prior ledger note: the file is a module doc comment followed by a single #[cfg(feature = \"db\")] mod imp { ... } plus a #[cfg(feature = \"db\")] pub use imp::*; -- ZERO local struct, enum, const or trait is declared anywhere in it. All six ops (set_company_defaults / set_party_account / set_group_account / set_party_group / has_company_defaults / resolve_account -- the reported six-op list independently confirmed accurate against the file) are real sqlx mechanics whose only non-primitive types (CompanyDefaults, PartyAccountRegistry, PartyAccountError, PartyType, plus CompanyId/CoreError/CoreResult/PgPersistence from chronica-core) live in, and stay in, the separate chronica-core-erp-payments crate -- VERIFIED pure by reading its Cargo.toml directly: no sqlx dependency and no `db` feature at all (deps are chronica-core, chronica-core-erp-doctype, chronica-core-erp-ledger, chronica-core-erp-sales-pipeline, chronica-core-policy-money-gate, serde, serde_json, thiserror, time). NOTE the prior loop's guess was off on WHICH sibling: the task brief and the 2026-09-07 rejection note both suggested chronica-core-erp-selling; the actual foreign-type owner here is chronica-core-erp-payments (selling is LOOP 52's customer-master sibling). A split would therefore have left an empty, pointless donor stub, so the relocation template (LOOP 42 manage-email-blacklist / LOOP 46 projection-replay-state / LOOP 47 money-event-projector / LOOP 52 customer-master) is its correct disposition. Maturity deliberately stays RUNTIME_BOUND, NOT PURE -- the crate still performs real I/O; relocation moves that I/O to the right tier, it does not make the crate pure. Byte-identity of the moved region independently verified by hash, not eyeballing: the region from the first `#[cfg(feature = \"db\")]` line to EOF is 186 lines / 7192 bytes with sha256 a00ecc88961603d20ab69545b2229ebe33e3ebcb765945fb8b7c792162f3d058 identical on both the git-show HEAD original and the relocated file; only the module-doc header above it changed. The one real production caller, crates/adapter/chronica-adapter-crm-http/src/crm/party_account.rs, was already adapter-tier, so the repoint (use chronica_cap_crm_support_party_details as store -> use chronica_adapter_crm_support_party_details as store, same `store` alias) is adapter->adapter and adds NO new layer-debt edge (layer_debt_edge_count stays 114, confirmed via node tools/refoundation/validate-refoundation-layers.mjs). chronica-adapter-api-server's dependency + db-feature-array entry on the old crate was confirmed dead-in-src (grep for party_details/party_account across its own src/ returns nothing; its tests/crm_party_account_route.rs exercises the five routes over HTTP via common::router only, never importing the crate) and was removed outright rather than repointed, per the standing rule -- its db feature still reaches the crate transitively through chronica-adapter-crm-http/db. The crate's tests/money_guard.rs was widened from the donor's two include_str!-based checks to the established 4-test relocation template (crate_never_depends_on_a_money_path_crate / source_never_references_a_money_authorizing_primitive_by_name / every_op_source_is_actually_scanned pinning all six ops / migration_schema_has_no_money_column scanning 0418_crm_party_account_resolution.sql from its first CREATE TABLE onward, since that file's leading prose legitimately says \"Money-FREE\"); all 4 verified passing. Also corrected stale capability-descriptor + CAPABILITY.md claims referencing two long-gone flat crates (chronica-api, chronica-crm -- both independently confirmed absent under crates/), and the stale chronica_erp::accounting::* type paths, resolved to the real current chronica_core_erp_payments::accounting::* paths and the real crates/adapter/chronica-adapter-api-server/tests/crm_party_account_route.rs witness paths.",
  "_customer_master_relocation_2026_09_08d": "misplaced_cap_package_count 226 -> 225 (core 226 -> 225) AND forbidden_core_io_count 129 -> 128, BOTH in this one change: crates/core/chronica-cap-crm-support-customer-master (LOOP 52) was NOT split -- it was PHYSICALLY RELOCATED, whole, to crates/adapter/chronica-adapter-crm-support-customer-master (git mv, preserving history) and renamed in place. The usual shrink-donor-to-pure + new-adapter-sibling template does not apply: its entire #[cfg(feature = \"db\")]-gated mod imp carries ZERO local pure type surface -- all six ops (set_credit_limit / check_credit_limit / add_contact / add_address / primary_contact / primary_address) are real sqlx mechanics over foreign types (CustomerProfile, CustomerContact, CustomerAddress, CreditLimitError, CustomerContactError) that live in, and stay in, the separate ALREADY-PURE chronica-core-erp-selling crate (untouched by this change), so a split would have left an empty, pointless donor stub. This is the exact shape the 2026-09-07 productivity-notes-events-tags ledger entry recorded when it inspected this crate and rejected it as a SPLIT candidate; the relocation template (LOOP 42 manage-email-blacklist / LOOP 46 projection-replay-state / LOOP 47 money-event-projector) is its correct disposition. The one real caller, crates/adapter/chronica-adapter-crm-http/src/crm/customer_master.rs, was already adapter-tier, so the repoint is adapter->adapter and adds NO new layer-debt edge (layer_debt_edge_count stays 114). chronica-adapter-api-server's dependency + db-feature-array entry on the old crate was confirmed dead-in-src (its tests/crm_customer_master_route.rs exercises the routes over HTTP only, never importing the crate) and was removed outright rather than repointed, per the standing rule.",
  "_forbidden_core_io_count_2026_09_08c": "130 -> 129: crates/core/chronica-cap-projection-read-model's responsibility-tier decomposition (LOOP 51) removed its own optional sqlx (and its chronica-core + chronica-core-projection-engine edges, and its whole `db` feature) entirely from crates/core/. Its real Postgres mechanics -- get_read_model / upsert_read_model over projection_read_model (migration 0087), plus the private RM_COLS column-list constant and the inline CachedReadModel row decode -- moved BYTE-IDENTICAL (independently verified: the moved region is 95 lines / 3769 bytes with an identical sha256 on both sides) into the new crates/adapter/chronica-adapter-projection-read-model, which re-exports the pure CachedReadModel so a caller needs one crate path for both the row type and the real functions. The donor keeps ONLY that struct and is now genuinely pure: serde_json is its single dependency and it declares no cargo features at all. NO feature-gating judgment call was required (unlike LOOP 49's UpdateRoleOutcome): CachedReadModel's fields are plain String/Option<String>/i64/i32 plus a serde_json::Value, naming no type from any db-gated module; the sibling chronica-core-projection-engine, whose cursor::ProjectionCursor and projection::OutputModel the MOVED upsert_read_model takes as arguments, was independently re-verified pure by reading its Cargo.toml (serde/serde_json/chrono/sha2 only, no db feature). The one real production caller, chronica-core-api-state's get_projection_read_model route handler, was repointed onto the adapter under its existing read_model_store alias -- that caller is itself still under crates/core/, so this creates ONE new disclosed core -> adapter layer-debt edge (layer_debt_edge_count 113 -> 114), recorded in tools/refoundation/refoundation-layer-debt.json's _note_2026_09_08_loop51 rather than silently absorbed. The dev-only consumer (chronica-cap-projection-dependencies' live-DB invalidation tests) was repointed too but adds no edge; chronica-adapter-api-server's dead optional dependency on the donor was REMOVED rather than repointed. misplaced_cap_package_count (226), world_graph_live_dependency_count (5) and cap_directory_count (36) are all unchanged -- the donor stays a capability crate in place, and the new adapter carries no .chronica descriptor of its own (same shape as LOOP 50's chronica-adapter-scheduling-host).",
  "_forbidden_core_io_count_2026_09_08b": "131 -> 130: crates/core/chronica-cap-scheduling-host's responsibility-tier decomposition (LOOP 50) removed its own optional sqlx dependency -- and its chronica-core and time dependencies, and its `db` feature, entirely. The real add_host/list_hosts/remove_host Postgres mechanics over event_type_hosts (migration 0369) -- formerly a `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` in a single 140-line lib.rs, including add_host's validate -> confirm-owner -> INSERT transaction and its 23505 unique-violation -> AlreadyHost mapping, plus the private map_row/is_unique_violation helpers and the SELECT column-list constant -- moved BYTE-IDENTICAL into the new crates/adapter/chronica-adapter-scheduling-host. The donor now declares exactly one dependency, the already-pure chronica-core-scheduling-domain (serde/time/thiserror only, no db feature), and holds only the pure AddHostOutcome enum, unconditionally -- NO feature-gating judgment call was needed here, unlike LOOP 49's UpdateRoleOutcome. The one real production caller, chronica-cap-scheduling-booking's create_round_robin_booking, was repointed from chronica_cap_scheduling_host to chronica_adapter_scheduling_host under the same host_store alias; that repoint adds ONE disclosed core -> adapter layer-debt edge (layer_debt_edge_count 112 -> 113), recorded in tools/refoundation/refoundation-layer-debt.json. misplaced_cap_package_count is UNCHANGED at 226 -- the donor crate is still named chronica-cap-* and still lives under crates/core/, exactly as in LOOP 49's split-shaped (not relocation-shaped) loop. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_08a": "132 -> 131: crates/core/chronica-cap-authorization-role-lifecycle's responsibility-tier decomposition removed its own optional sqlx (and chronica-core) dependency entirely (the real update_role/delete_role SQL -- formerly a `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` in a single lib.rs, mixing real Postgres mechanics (including the unique-violation-to-DuplicateName error mapping) with the pure UpdateRoleOutcome/DeleteRoleOutcome enums under the SAME gate -- moved byte-identical to the new chronica-adapter-authorization-role-lifecycle, under crates/adapter/, along with a free-function `role_row` row decoder and the `ROLE_COLS`/`UNIQUE_VIOLATION` constants. DISCLOSED UP FRONT, an honest deviation from every prior loop's caller-repoint step: a full-repo grep for the exact crate path `chronica_cap_authorization_role_lifecycle::` (never the bare `update_role`/`delete_role` names, which false-positive-match the unrelated `chronica_cap_social_roles::update_role`/`delete_role` in `chronica-cap-social-routes/src/social_roles.rs`) found ZERO real (non-test) callers of the donor crate anywhere in the repo today -- only its own live-DB test. The donor's own doc comment claimed a `chronica-authorization` re-export path (`store::role_lifecycle`) that no longer exists; that flat crate was retired from this repo before this split, independently confirmed absent under crates/. This is still legitimate progress: the M7 gate counts a crate's own dependency, not caller reachability, and moving real I/O out of crates/core/ is correct architecture regardless of wiring status -- so there is NO caller to repoint and NO new layer-debt edge from this change (layer_debt_edge_count unchanged at 112). A JUDGMENT CALL, unlike every pure/adapter split precedent so far: `UpdateRoleOutcome` could NOT be made fully unconditional in the shrunk donor, because its `Updated` variant wraps `RoleRow`, a type owned by the sibling `chronica-cap-authorization-roles` crate -- and that sibling is NOT itself split (a separate, larger, out-of-scope M7 violation): `RoleRow`'s entire struct definition lives inside that sibling's own `#[cfg(feature = \"db\")]`-gated `mod imp`, so it simply does not exist as a type unless the sibling's `db` feature is on. The shrunk donor therefore keeps a `db` feature (renamed in meaning, not in name: it enables NO `sqlx`/`chronica-core` dependency of its own, only `chronica-cap-authorization-roles/db`, purely to unlock naming the sibling's row type) gating `UpdateRoleOutcome` alone; `DeleteRoleOutcome` (a plain unit-only enum, no sibling-type entanglement) is unconditionally available with no feature at all -- confirmed via `cargo check -p chronica-cap-authorization-role-lifecycle` (no features) succeeding and exposing only `DeleteRoleOutcome`, and via inspecting the actual rustc invocation (`cargo check -v`) confirming `sqlx` is not even compiled into the dependency graph for the adapter crate without its own `--features db`. chronica-adapter-api-server carries no dependency on the old crate at all (confirmed via grep across its own src/ and tests/) -- nothing to remove. This crate's tests/money_guard.rs pin test was renamed from an ops-scan (`fn update_role`/`fn delete_role`) to a pure-type-scan (`pure_type_source_is_actually_scanned`, checking for `enum UpdateRoleOutcome`/`enum DeleteRoleOutcome`), matching the established precedent; the new adapter crate carries forward a 3-test tests/money_guard.rs (crate_never_depends_on_a_money_path_crate / source_never_references_a_money_authorizing_primitive_by_name / a migration-schema-scan against `authz_role`'s/`authz_role_assignment`'s own 0302_authorization_roles.sql, scanning from the first `CREATE TABLE` onward since the file's own leading comment legitimately says \"Money-FREE\" in prose) matching the chronica-adapter-crm-support-warranty-claim-amc template. The donor's moved live-DB test (tests/role_lifecycle.rs, 5 tests exercising update_role/delete_role directly) moved byte-identical into the new adapter crate's own tests/ directory; all 5 verified genuinely executed (not skip-passed) against a live local Postgres 16 instance started this session (DATABASE_URL=postgres://chronica:chronica@127.0.0.1:5432/chronica, ~1.5s real runtime vs an instant skip-return) rather than merely asserted. The donor's .chronica/capability.chronica.json maturity field updated RUNTIME_BOUND -> PURE and CAPABILITY.md header updated to `Refoundation state: PURE`, with the stale `crates/chronica-authorization/src/store/mod.rs` caller_entries/runtime_owners path (that flat crate does not exist) corrected to an honestly empty caller list rather than left as a disclosed gap, per this program's standing rule -- the root workspace Cargo.toml's own adjacent stale comment (claiming the same nonexistent chronica-authorization re-export) was corrected in the same change. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_money_event_projector_relocation_2026_09_07y": "misplaced_cap_package_count 228 -> 227 (core 228 -> 227) AND forbidden_core_io_count 133 -> 132, BOTH in this one change: crates/core/chronica-cap-crm-support-money-event-projector physically relocated + renamed to crates/adapter/chronica-adapter-crm-support-money-event-projector. This is NOT the usual per-loop pattern (which only ever touched M7 forbidden_core_io_count, via a pure/adapter split leaving a shrunk donor behind) -- it is the SAME shape as the LOOP-42/46 relocations recorded at `_manage_email_blacklist_relocation_2026_09_07t` and `_projection_replay_state_relocation_2026_09_07x` above. This crate is UNLIKE every prior split candidate: it has ZERO local pure type surface of its own -- its one public function, `project_money_events`, is a single sqlx-based read-then-write operation (SELECT from finance_events, INSERT into business_event) returning a plain usize count, and its two private helpers (kind_str/placeholder_scope) are trivial and used only by that one function -- there is no struct/enum worth keeping in a pure donor. Applying the usual \"shrink donor to pure-only + new adapter crate\" template here would have left an empty, pointless donor stub, so the whole crate moved (git mv, preserving history) and was renamed in place instead -- no donor left behind, no re-export shim. Genuinely lands under crates/adapter/: the crate's real responsibility (Postgres mechanics reading finance_events and writing business_event) has always belonged there, and every line of source code was already 100% adapter-shaped. The db feature gate was KEPT (not ungated to always-on) after checking sibling crates/adapter/* sqlx-backed crates' Cargo.toml (chronica-adapter-crm-support-manage-email-blacklist, chronica-adapter-projection-replay-state, chronica-adapter-postgres-world-admission) -- every one of them keeps `default = []` / `db = [\"dep:sqlx\", ...]` wired into the workspace's global db feature, so this crate matches that established convention rather than diverging. MONEY-SAFETY: this is a byte-identical physical move only -- the function body, its SQL, and the money-boundary documentation were independently re-diffed against the pre-move original and confirmed byte-identical from the \"MONEY-ADJACENT but MONEY-FREE\" doc paragraph through the end of the file; only the crate's path, package name, and the module-doc header's framing (relocation vs split) changed. The one real (non-dev) caller, chronica-cap-platform-misc-routes's src/analytics/event.rs, repointed its `use` path (chronica_cap_crm_support_money_event_projector -> chronica_adapter_crm_support_money_event_projector) and Cargo.toml dependency name/db-feature-array entry -- UNLIKE the manage-email-blacklist precedent, this edge IS a new layer-debt edge: chronica-cap-platform-misc-routes is PHYSICALLY under crates/cap/, so repointing it onto the now-crates/adapter/ crate creates `chronica-cap-platform-misc-routes (cap) -> chronica-adapter-crm-support-money-event-projector (adapter)`, confirmed verbatim via `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output and disclosed in tools/refoundation/refoundation-layer-debt.json's `_note_2026_09_07y` (layer_debt_edge_count 111 -> 112, tracked in that file's own array). chronica-adapter-api-server carried an unused-in-src, optional Cargo.toml dependency + db-feature-array entry on the old crate (confirmed dead via grep across its own src/ and tests/ -- it never reached the real ops at all, not even transitively; its tests/money_event_projector_route.rs only exercises the route over HTTP, never imports the crate by name) -- REMOVED entirely (not repointed), per this program's standing rule. This crate's tests/money_guard.rs (already a top-level file, a DELIBERATE DEVIATION from the generic blanket-substring template since this crate's own doc comments legitimately NAME money-write primitives in prose to document the never-money boundary) carried forward unchanged in substance, extended with a fourth test (a migration-schema-scan against business_event's own 0071_business_events.sql -- the table this crate WRITES, deliberately NOT finance_events, which it only READS from by design) plus a fifth pin test (every_op_source_is_actually_scanned, confirming `fn project_money_events` is actually present in the scanned source, guarding against the write-surface check passing vacuously on an emptied-out src/). Also corrected this crate's own .chronica/capability.chronica.json and CAPABILITY.md, which still named the stale crates/chronica-api/... and crates/chronica-crm/... caller/owner/covered_sources/witness paths (both long-gone flat crates) -- resolved to the real current locations (crates/cap/chronica-cap-platform-misc-routes/src/analytics/event.rs, crates/adapter/chronica-adapter-api-server/tests/money_event_projector_route.rs) rather than left as a disclosed gap, per this program's standing rule. Maturity was left at RUNTIME_BOUND (matching the LOOP-42/46 precedent's reasoning: already not PURE, and no crates/adapter/* sibling from this program's prior relocation loops carries a different adapter-tier maturity-value precedent to align to). See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_projection_replay_state_relocation_2026_09_07x": "misplaced_cap_package_count 229 -> 228 (core 229 -> 228) AND forbidden_core_io_count 134 -> 133, BOTH in this one change: crates/core/chronica-cap-projection-replay-state physically relocated + renamed to crates/adapter/chronica-adapter-projection-replay-state. This is NOT the usual per-loop pattern (which only ever touched M7 forbidden_core_io_count, via a pure/adapter split leaving a shrunk donor behind) -- it is the SAME shape as the LOOP-42 relocation recorded at `_manage_email_blacklist_relocation_2026_09_07t` above. This crate is UNLIKE every prior split candidate: its entire `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` (get_cursor/upsert_cursor) carries ZERO local pure type surface of its own -- just thin async sqlx::query wrappers returning chronica_core_projection_engine::cursor::ProjectionCursor, a type that lives in, and was NOT touched or moved by this change, the separate ALREADY-PURE chronica-core-projection-engine crate. Applying the usual \"shrink donor to pure-only + new adapter crate\" template here would have left an empty, pointless donor stub, so the whole crate moved (git mv, preserving history) and was renamed in place instead -- no donor left behind, no re-export shim. Genuinely lands under crates/adapter/: the crate's real responsibility (Postgres mechanics against projection_state, migration 0086) has always belonged there, and every line of source code was already 100% adapter-shaped. The db feature gate was KEPT (not ungated to always-on) after checking sibling crates/adapter/* sqlx-backed crates' Cargo.toml (chronica-adapter-crm-support-manage-email-blacklist, chronica-adapter-postgres-world-admission, chronica-adapter-helpdesk-postgres) -- every one of them keeps `default = []` / `db = [\"dep:sqlx\", ...]` wired into the workspace's global db feature, so this crate matches that established convention rather than diverging. The one real (non-dev) caller, chronica-core-api-state's projections.rs, repointed its `use` path (chronica_cap_projection_replay_state -> chronica_adapter_projection_replay_state) and Cargo.toml dependency name/db-feature-array entry -- UNLIKE the LOOP-42 precedent, this edge IS a new layer-debt edge: chronica-core-api-state is PHYSICALLY under crates/core/, so repointing it onto the now-crates/adapter/ crate creates `chronica-core-api-state (core) -> chronica-adapter-projection-replay-state (adapter)`, confirmed verbatim via `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output and disclosed in tools/refoundation/refoundation-layer-debt.json's `_note_2026_09_07x` (layer_debt_edge_count 110 -> 111, tracked in that file's own array). chronica-adapter-api-server carried an unused-in-src, optional Cargo.toml dependency + db-feature-array entry on the old crate (confirmed dead via grep across its own src/ and tests/ -- it never reached the real ops at all, not even transitively) -- REMOVED entirely (not repointed), per this program's standing rule. This crate's tests/money_guard.rs (already a top-level file, unlike LOOP-42's donor which carried its guard inline inside `mod imp`) carried forward unchanged in substance, extended with the LOOP-42 template's third test (a migration-schema-scan against projection_state's own 0086_projection_state.sql); the redundant `db`-gated inline `#[cfg(test)] mod money_guard` inside `mod imp` (which never even compiled/ran without `--features db`) was removed from src/lib.rs, since the always-built top-level tests/money_guard.rs now covers the same invariant unconditionally. Also corrected this crate's own .chronica/capability.chronica.json and CAPABILITY.md, which still named the stale crates/chronica-projection/... caller/owner/covered_sources/witness paths (a long-gone flat crate) -- resolved to the real current locations (crates/core/chronica-core-api-state/src/projections.rs) rather than left as a disclosed gap, per this program's standing rule; also updated three sibling capability crates' docs (chronica-cap-projection-read-model, chronica-cap-projection-registry) and chronica-core-observability-kernel/README.md, which cross-referenced this crate by its old name in prose. Maturity was left at RUNTIME_BOUND (matching the LOOP-42 precedent's reasoning: already not PURE, and no crates/adapter/* sibling from this program's prior relocation/split loops carries a different adapter-tier maturity-value precedent to align to). See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_07w": "135 -> 134: crates/core/chronica-cap-social-clips's responsibility-tier decomposition removed its own optional sqlx (and chronica-core/chronica-core-social-domain/chronica-cap-social-notes) dependency entirely (the real create_clip/get_clip/list_clips_by_owner/add_note_to_clip/remove_note_from_clip/list_clip_notes/favorite_clip/unfavorite_clip/is_favorited/favorited_count SQL -- formerly a `#[cfg(feature = \"db\")]`-gated `mod imp { pub mod row; pub mod create; pub mod favorite; pub mod note; }` multi-file split, mixing real Postgres mechanics with the pure ClipRow/ClipNoteRow/ClipFavoriteRow structs (+ to_json) and the pure CreateClipOutcome/FavoriteOutcome/AddNoteOutcome enums under the SAME gate -- moved byte-identical to the new chronica-adapter-social-clips, under crates/adapter/, along with free-function `map_clip_row`/`map_clip_note_row`/`map_clip_favorite_row` row decoders (E0116: Rust forbids an inherent impl on a foreign type, since the row structs now live in the donor crate). The donor's original multi-file imp/{row,create,favorite,note}.rs layout was FLATTENED into the new adapter crate's single src/lib.rs (its own `mod imp { ... }`), matching the established convention already used by chronica-adapter-social-user-lists and chronica-adapter-postgres-social-handle for a multi-function split (not a behavior change, just where the functions live on disk). The pure types stayed in the shrunk, now-always-pure donor crate -- no `db` feature, no sqlx, no chronica-core, no chronica-core-social-domain, no chronica-cap-social-notes -- only serde_json (for to_json). `ClipTimelineEntry` (list_clip_notes's result item: a joined `chronica_cap_social_notes::NoteRow` plus the membership row's own `clip_seq`) did NOT move back to the donor, UNLIKE the sibling row/outcome types -- it wraps `NoteRow`, itself only defined behind `chronica-cap-social-notes`'s own `db` feature (that crate's entire `mod imp { pub mod row; ... }` is `#[cfg(feature = \"db\")]`-gated), so it cannot live in an always-pure, `db`-feature-free donor; it stays in the new adapter crate, `pub` (a `pub` fn's return type must be at least as visible) but never named directly by any caller outside the crate (confirmed via a full-repo grep before the split -- chronica-cap-social-routes's list_clip_notes handler only accesses `.note` per entry via `.iter()` field access). Three real (non-dev) callers repointed, spanning two edge shapes: (1) chronica-cap-social-routes's social_clips.rs (crates/cap/-tier) repointed its `use`/Cargo.toml/db-feature-array dependency to the new adapter crate -- a DISCLOSED cap -> adapter layer-debt edge (see tools/refoundation/refoundation-layer-debt.json's `_note_2026_09_07w`; layer_debt_edge_count unaffected here, tracked in that file's own array). (2) chronica-adapter-social-postgres-batch3's publish_targets.rs and chronica-cap-social-scheduled-posts (both PHYSICALLY under crates/core/ despite the first's `-adapter-`-shaped name -- an unrelated, earlier placement decision) each repointed their `use chronica_cap_social_clips as clip_store;` alias to `use chronica_adapter_social_clips as clip_store;` (clip_store::get_clip call sites unchanged) -- TWO more DISCLOSED core -> adapter layer-debt edges, same file, same note. chronica-adapter-api-server carried an unused-in-src, non-optional Cargo.toml dependency on the old crate (its own src/ and tests/ have zero real references -- confirmed via grep; it reached the real ops only transitively, via chronica-cap-social-routes) -- REMOVED entirely (not repointed), per this program's standing rule. `add_note_to_clip`/`list_clip_notes`'s own direct dependency on chronica-cap-social-notes (get_note/NoteRow reuse) moved unchanged into the new adapter crate: chronica-cap-social-notes was independently checked and found NOT YET pure (it still carries its own `db`-gated real Postgres mechanics, a much larger out-of-scope decomposition of its own), so there was no separate adapter-tier crate to repoint onto -- this is a plain adapter -> core edge, not forbidden under FORBIDDEN_TARGETS.adapter (={infra}), confirmed via the gate script's own output carrying no fourth NEW FORBIDDEN EDGE line for it. The donor's own moved live-DB tests (src/imp/db_tests.rs, 10 tests exercising every real function) moved byte-identical to the new adapter crate's own src/imp/db_tests.rs, since the functions they exercise moved there; all 10 pass against a live DATABASE_URL this loop. The donor's inline `mod imp { mod money_guard }` guard (no longer possible once the db-gated mod imp is gone) was relocated to a proper top-level tests/money_guard.rs, matching the chronica-cap-social-user-lists template (crate_manifest_declares_no_money_adjacent_dependency / source_imports_no_money_or_approvals_primitive / a new pure_type_source_is_actually_scanned pin test); the new adapter crate carries forward a 3-test tests/money_guard.rs (crate_never_depends_on_a_money_path_crate / source_never_references_a_money_authorizing_primitive_by_name / a migration-schema-scan test against social_clips's own 0099_social_clips.sql) matching the chronica-adapter-social-user-lists template. The donor's .chronica/capability.chronica.json maturity field updated RUNTIME_BOUND -> PURE and CAPABILITY.md header updated to `Refoundation state: PURE`, with stale pre-refoundation crates/chronica-api/... and crates/chronica-social/... caller/covered_sources/witness paths corrected to the real current locations in the same edit, per this program's standing rule. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_07v": "136 -> 135: crates/core/chronica-cap-social-user-lists's responsibility-tier decomposition removed its own optional sqlx (and chronica-core/chronica-core-social-domain/chronica-cap-social-notes) dependency entirely (the real create_user_list/get_user_list/list_user_lists_by_owner/add_member/list_members/remove_member/list_user_list_notes SQL -- formerly a `#[cfg(feature = \"db\")]`-gated `mod imp { pub mod row; pub mod create; pub mod membership; pub mod timeline; }` multi-file split, mixing real Postgres mechanics with the pure UserListRow/UserListMemberRow structs (+ to_json) and the pure CreateUserListOutcome/AddMemberOutcome enums under the SAME gate -- moved byte-identical to the new chronica-adapter-social-user-lists, under crates/adapter/, along with free-function `map_user_list_row`/`map_user_list_member_row` row decoders (E0116: Rust forbids an inherent impl on a foreign type, since the row structs now live in the donor crate). The donor's original multi-file imp/{row,create,membership,timeline}.rs layout was FLATTENED into the new adapter crate's single src/lib.rs (its own `mod imp { ... }`), matching the established convention already used by chronica-adapter-postgres-social-handle and chronica-adapter-helpdesk-read-model for a multi-function split (not a behavior change, just where the functions live on disk). The pure types stayed in the shrunk, now-always-pure donor crate -- no `db` feature, no sqlx, no chronica-core, no chronica-core-social-domain, no chronica-cap-social-notes -- only serde_json (for to_json). The one real (non-dev) caller of the impure ops, chronica-cap-social-routes's social_lists.rs, repointed its `use`/Cargo.toml/db-feature-array dependency to the new adapter crate -- that edge is a DISCLOSED cap -> adapter layer-debt edge (see tools/refoundation/refoundation-layer-debt.json's `_note_2026_09_07v`; layer_debt_edge_count unaffected here, tracked in that file's own array). chronica-adapter-api-server carried an unused-in-src, non-optional Cargo.toml dependency on the old crate (its own src/ and tests/ have zero real references -- confirmed via grep; it reached the real ops only transitively, via chronica-cap-social-routes) -- REMOVED entirely (not repointed), per this program's standing rule. `list_user_list_notes`'s own direct dependency on chronica-cap-social-notes (NoteRow/clamp_note_limit reuse) moved unchanged into the new adapter crate: chronica-cap-social-notes was independently checked and found NOT YET pure (it still carries its own `db`-gated real Postgres mechanics, a much larger out-of-scope decomposition of its own), so there was no separate adapter-tier crate to repoint onto -- this is a plain adapter -> core edge, not forbidden under FORBIDDEN_TARGETS.adapter (={infra}), confirmed via the gate script's own output carrying no second NEW FORBIDDEN EDGE line for it. The donor's own moved live-DB tests (src/imp/db_tests.rs, 11 tests exercising every real function) moved byte-identical to the new adapter crate's own src/imp/db_tests.rs, since the functions they exercise moved there; all 11 pass against a live DATABASE_URL this loop. The donor's inline `mod imp { mod money_guard }` guard (no longer possible once the db-gated mod imp is gone) was relocated to a proper top-level tests/money_guard.rs, matching the chronica-cap-crm-support-warranty-claim-amc template (crate_manifest_declares_no_money_adjacent_dependency / source_imports_no_money_or_approvals_primitive / a new pure_type_source_is_actually_scanned pin test); the new adapter crate carries forward a 3-test tests/money_guard.rs (crate_never_depends_on_a_money_path_crate / source_never_references_a_money_authorizing_primitive_by_name / a migration-schema-scan test against social_user_list's/social_user_list_member's own 0101_social_user_lists.sql) matching the chronica-adapter-crm-support-warranty-claim-amc template. The donor's .chronica/capability.chronica.json maturity field updated RUNTIME_BOUND -> PURE and CAPABILITY.md header updated to `Refoundation state: PURE`, with stale pre-refoundation crates/chronica-api/... and crates/chronica-social/... caller/covered_sources/witness paths corrected to the real current locations in the same edit, per this program's standing rule. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_07u": "139 -> 136 (3 crates, one change): the entangled helpdesk dispatch/event-store/read-model outbox cluster's responsibility-tier decomposition. All three crates/core/chronica-cap-helpdesk-{dispatch,event-store,read-model} carried their own real Postgres mechanics behind a `#[cfg(feature = \"db\")]`-gated `mod imp { ... }`, cross-referencing each other (read-model's project_pending calls dispatch's drain_pending and event-store's append_events WITHIN ITS OWN transaction for crash-safety). All three real (non-dev) callers of this cluster's real functions are ALSO crates/core/-tier or crates/adapter/-tier (chronica-cap-helpdesk-read-model called dispatch's drain_pending and event-store's append_events; chronica-adapter-helpdesk-contacts called event-store's append_events; chronica-cap-helpdesk-conversations called read-model's append_and_project), so all three were split TOGETHER in one change rather than one at a time -- splitting only crate #1 (dispatch) would have left crate #3 (read-model, still core-tier at that point) with a NEW forbidden core -> adapter edge with no compensating M7 decrease to show for it. Each donor's real mechanics moved BYTE-IDENTICAL into a new adapter-tier sibling (chronica-adapter-helpdesk-dispatch/-event-store/-read-model), including dispatch's `FOR UPDATE SKIP LOCKED` claim query and its executor-generic `impl sqlx::PgExecutor<'_>` signature, and read-model's one-transaction drain -> project crash-safety sequence in project_pending -- traced line-by-line against the pre-split source, transaction/executor handle threading unchanged (only the sibling `use` paths for drain_pending/append_events were re-pointed to their own new adapter siblings). Each donor kept only its pure type: dispatch kept PersistedEvent, event-store kept AppendedEvent, read-model kept InboxRow (+ to_json) -- each shrunk Cargo.toml drops sqlx/chronica-core/chronica-core-helpdesk-event/sibling-store dependencies and the db feature entirely. Real (non-dev) callers repointed: chronica-adapter-helpdesk-contacts's Cargo.toml/use-path repointed from chronica-cap-helpdesk-event-store to chronica-adapter-helpdesk-event-store (adapter -> adapter, not a new layer-debt edge). chronica-cap-helpdesk-read-model itself (now pure) repointed to chronica-adapter-helpdesk-dispatch and chronica-adapter-helpdesk-event-store internally (both now live inside the new chronica-adapter-helpdesk-read-model). chronica-cap-helpdesk-conversations (physically under crates/core/, not itself split by this pass -- its own future responsibility-tier decomposition candidate) repointed its append_and_project use-path/Cargo.toml dependency from chronica-cap-helpdesk-read-model to chronica-adapter-helpdesk-read-model -- a DISCLOSED core -> adapter debt edge, see tools/refoundation/refoundation-layer-debt.json's forbidden_edges array (layer_debt_edge_count unaffected: that file's own array grew by one entry to disclose it, tracked there rather than as an M7 concern). chronica-adapter-helpdesk-postgres's own module doc already documented that it deliberately excludes chronica-cap-helpdesk-dispatch (a genuine RUNTIME-tier orchestrator, not the batch's plain-CRUD shape) -- confirmed via grep this was doc-only, never a real Cargo dependency, so nothing to repoint there. chronica-adapter-api-server carries no dependency on any of the three old crate names at all (confirmed via grep across its own src/ and tests/) -- nothing to remove or repoint. Each new adapter crate carries forward a 3-test tests/money_guard.rs (crate_never_depends_on_a_money_path_crate / source_never_references_a_money_authorizing_primitive_by_name / a migration-schema-scan test against support_events's 0037_support_events.sql for dispatch and event-store, support_thread_read's 0038_support_thread_read.sql for read-model) matching the chronica-adapter-crm-support-warranty-claim-amc template; each shrunk donor's own money_guard.rs pin test was renamed from an ops-scan to a pure-type-scan, matching the same precedent. All three donors' .chronica/capability.chronica.json maturity fields updated RUNTIME_BOUND -> PURE and CAPABILITY.md headers updated to `Refoundation state: PURE`, with stale pre-refoundation crates/chronica-helpdesk/... caller/covered_sources/witness paths corrected to the real current locations in the same edit (found via a stale-path grep scan), per this program's standing rule. Verified genuinely (not silently skipped) against a live DATABASE_URL: all 6 dispatch_live tests, all 4 event_store_live tests, and all 5 read_model_live tests pass in the new adapter crates (the read-model live test's pre-existing insert_thread fixture also needed a one-line authority_state column fix for a migration-0440 NOT NULL constraint added long after that fixture was last touched -- confirmed via git stash to predate this change, unrelated to the split itself); chronica-adapter-helpdesk-contacts's own 6 contact_store_live tests + 3 money_guard tests still pass after its event-store repoint; chronica-cap-helpdesk-conversations's conversation_store_live suite is unchanged at 18 passed / 2 pre-existing failures both before and after this change (confirmed via git stash comparison against the unmodified tree -- both failures are pre-existing schema/fixture staleness unrelated to this split). See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_manage_email_blacklist_relocation_2026_09_07t": "misplaced_cap_package_count 230 -> 229 (core 230 -> 229) AND forbidden_core_io_count 140 -> 139, BOTH in this one change: crates/core/chronica-cap-crm-support-manage-email-blacklist physically relocated + renamed to crates/adapter/chronica-adapter-crm-support-manage-email-blacklist. This is NOT the usual per-loop pattern (which only ever touched M7 forbidden_core_io_count, via a pure/adapter split leaving a shrunk donor behind). This crate is UNLIKE every prior split candidate: its entire `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` (add/remove/is_blacklisted/list) carries ZERO local pure type surface of its own -- just thin async wrappers around sqlx::query/query_scalar returning primitives (bool/String/Vec<String>), calling EmailBlacklist::normalize, a pure function that lives in, and was NOT touched or moved by this change, the separate chronica-core-erp-crm crate. Applying the usual \"shrink donor to pure-only + new adapter crate\" template here would have left an empty, pointless donor stub, so the whole crate moved (git mv, preserving history) and was renamed in place instead -- no donor left behind, no re-export shim. Genuinely lands under crates/adapter/: the crate's real responsibility (Postgres mechanics against crm_email_blacklist, migration 0413) has always belonged there, and every line of source code was already 100% adapter-shaped. The db feature gate was KEPT (not ungated to always-on) after checking sibling crates/adapter/* sqlx-backed crates' Cargo.toml (chronica-adapter-postgres-world-admission, chronica-adapter-helpdesk-postgres, chronica-adapter-postgres-analytics-metric-lineage, chronica-adapter-crm-support-warranty-claim-amc, chronica-adapter-helpdesk-agents) -- every one of them keeps `default = []` / `db = [\"dep:sqlx\", ...]` wired into the workspace's global db feature, so this crate matches that established convention rather than diverging. The one real (non-dev) caller, chronica-adapter-crm-http's email_blacklist route module, repointed its `use` path (chronica_cap_crm_support_manage_email_blacklist -> chronica_adapter_crm_support_manage_email_blacklist) and Cargo.toml dependency name/db-feature-array entry -- that edge is adapter->adapter, not a new layer-debt edge. chronica-adapter-api-server carried an unused-in-src Cargo.toml dependency + db-feature-array entry on the old crate (confirmed dead via grep across its own src/ and tests/ -- it reaches the real ops only transitively, via chronica-adapter-crm-http) -- REMOVED entirely (not repointed), per this program's standing rule. tests/money_guard.rs (the crate's load-bearing money-free guard) carried forward unchanged in substance, rewritten to match the chronica-adapter-crm-support-warranty-claim-amc money_guard.rs template (crate_never_depends_on_a_money_path_crate / source_never_references_a_money_authorizing_primitive_by_name / a migration-schema-scan test against crm_email_blacklist's own 0413_crm_email_blacklist.sql). Also corrected the donor's .chronica/capability.chronica.json and CAPABILITY.md, which still named the stale crates/chronica-api/... and crates/chronica-crm/... caller/covered_sources/witness paths (both long-gone flat crates) -- resolved to the real current locations (crates/adapter/chronica-adapter-crm-http, crates/adapter/chronica-adapter-api-server/tests/crm_email_blacklist_route.rs, found via a stale-path find scan) rather than left as a disclosed gap, per this program's standing rule; maturity was left at RUNTIME_BOUND (already not PURE -- no crates/adapter/* sibling from this program's prior split loops carries a .chronica/capability.chronica.json at all, so there was no adapter-tier maturity-value precedent to align to; RUNTIME_BOUND is the dominant value used workspace-wide for a real-I/O-bound capability, 238 of 253 capability.chronica.json files). See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_07s": "141 -> 140: crates/core/chronica-cap-crm-support-warranty-claim-amc's responsibility-tier decomposition removed its own optional sqlx (and chronica-core) dependency entirely (the real create_claim/get_claim/close_claim/make_maintenance_visit SQL -- formerly a `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` in a single lib.rs, mixing real Postgres mechanics with the pure ClaimRow struct (+ to_domain/to_json) and the pure CloseOutcome enum under the SAME gate, plus pure helpers parse_status/amc_status_str/parse_date/format_date/status_db -- moved byte-identical to the new chronica-adapter-crm-support-warranty-claim-amc, under crates/adapter/, along with a free-function `map_claim_row` row decoder (E0116: Rust forbids an inherent impl on a foreign type, since ClaimRow now lives in the donor crate). The pure types stayed in the shrunk, now-always-pure donor crate (no `db` feature, no sqlx, no chronica-core -- only the dependency-light leaf chronica-core-errors for CoreError/CoreResult, chronica-core-erp-support for the WarrantyClaim domain type, serde_json, and time); parse_date (also needed by the adapter's close_claim to parse its caller-supplied `today`) and to_domain/to_json stayed with ClaimRow in the donor since they only ever needed CoreError/CoreResult (always available, never db-gated) and the always-pure chronica-core-erp-support domain crate -- format_date/status_db (write-direction only) moved to the adapter. The one real (non-dev) caller of the impure ops, chronica-adapter-crm-http's warranty_claim route module, repointed its `use`/Cargo.toml/db-feature-array dependency to the new adapter crate -- that edge is adapter->adapter, not a new layer-debt edge. chronica-adapter-api-server carried an unused-in-src, non-optional Cargo.toml dependency on the old crate (its one tests/crm_warranty_claim_route.rs mentions the crate only in a doc comment, never a `use` -- confirmed via grep) -- REMOVED entirely (not repointed), per this program's standing rule. Also corrected the donor's .chronica/capability.chronica.json and CAPABILITY.md, which still named the stale crates/chronica-api/... and crates/chronica-crm/... caller/covered_sources/witness paths (both long-gone flat crates) -- resolved to the real current locations (crates/adapter/chronica-adapter-crm-http, crates/adapter/chronica-adapter-api-server) found via a stale-path `find` scan, rather than left as a disclosed gap, per this program's standing rule. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_07r": "142 -> 141: crates/core/chronica-cap-helpdesk-agents's responsibility-tier decomposition removed its own optional sqlx (and chronica-core) dependency entirely (the real create_agent/list_agents/get_agent/update_agent/delete_agent SQL -- formerly a `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` in a single lib.rs, mixing real Postgres mechanics with the pure AgentRow struct (+ to_json/to_agent_json) and the pure CreateAgentOutcome/UpdateAgentOutcome enums under the SAME gate -- moved byte-identical to the new chronica-adapter-helpdesk-agents, under crates/adapter/, along with a free-function `map_agent_row` row decoder (E0116: Rust forbids an inherent impl on a foreign type, since AgentRow now lives in the donor crate). The pure types stayed in the shrunk, now-always-pure donor crate, no `db` feature at all, keeping only its chronica-core-helpdesk-domain dependency (for the pure AgentError type the Invalid variants carry) plus serde_json (and, dev-only, chronica-core-security-confidential for the inline mod cap18 least-disclosure test, which needs no database). Two real (non-dev) callers repointed, both already runtime/adapter tier (no new layer-debt edge): chronica-adapter-helpdesk-http's helpdesk_agents route module (adapter->adapter, `use`/Cargo.toml/db-feature-array entry) and chronica-runtime-helpdesk-automation's repo.rs load-balanced auto_assign orchestration (runtime->adapter, `use`/Cargo.toml/db-feature-array entry -- this crate already depended directly on other adapter-tier crates, e.g. chronica-adapter-helpdesk-teams, so this is the established shape). Three real dev-dependency callers repointed too: chronica-runtime-helpdesk-automation's own tests/auto_assign_live.rs, tests/fair_distribution_live.rs, tests/round_robin_live.rs (all genuinely seed agents via create_agent/AgentRow directly, confirmed by reading each file). Two dead-in-src Cargo.toml dependencies confirmed via grep and REMOVED entirely (not repointed), per this program's standing rule: chronica-adapter-api-server's optional dependency + db-feature-array entry (reached the real ops only transitively via chronica-adapter-helpdesk-http, never referenced directly in api-server's own src/ or tests/) and chronica-adapter-postgres-helpdesk-automation's dev-dependency (unreferenced anywhere in its own src/ or tests/ -- it reaches agent data only transitively via chronica-runtime-helpdesk-automation). The donor's own moved live-DB test (tests/agent_store_live.rs, exercising create_agent/list_agents/get_agent/update_agent/delete_agent directly) moved byte-identical to the new adapter crate's own tests/ directory, since the functions it exercises moved there. Also corrected a stale illustrative comment path in crates/infra/chronica-infra-build-fabric-postgres/src/lib.rs (pointed at the old crates/core/chronica-cap-helpdesk-agents/tests/agent_store_live.rs example) to the real new location, and corrected chronica-runtime-helpdesk-automation's CAPABILITY.md + .chronica/capability.chronica.json, which still named chronica-cap-helpdesk-agents as a direct dependency, to name chronica-adapter-helpdesk-agents. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_07q": "143 -> 142: crates/core/chronica-cap-helpdesk-custom-filters's responsibility-tier decomposition removed its own optional sqlx (and chronica-core) dependency entirely (the real create_filter/list_filters/get_filter/update_filter/delete_filter SQL -- formerly a `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` in a single lib.rs, mixing real Postgres mechanics with the pure CustomFilterRow struct (+ to_json) and the pure CreateFilterOutcome/UpdateFilterOutcome enums under the SAME gate -- moved byte-identical to the new chronica-adapter-helpdesk-custom-filters, under crates/adapter/, along with a free-function `map_custom_filter_row` row decoder (E0116: Rust forbids an inherent impl on a foreign type, since CustomFilterRow now lives in the donor crate). The pure types stayed in the shrunk, now-always-pure donor crate, no `db` feature at all, keeping only its chronica-core-helpdesk-domain dependency (for the pure FilterError type CreateFilterOutcome::Invalid/UpdateFilterOutcome::Invalid carry). The one real (non-dev) caller of the impure ops, chronica-adapter-helpdesk-http's helpdesk_custom_filters route module, repointed its `use`/Cargo.toml dependency to the new adapter crate -- that edge is adapter->adapter, not a new layer-debt edge (chronica-adapter-helpdesk-http was already adapter-tier). chronica-adapter-api-server carried an unused-in-src, optional Cargo.toml dependency on the old crate (reached the real ops only transitively via chronica-adapter-helpdesk-http, confirmed via grep across its own src/ and tests/) -- REMOVED entirely (not repointed), per this program's standing rule. The donor's own moved live-DB test (tests/custom_filter_store_live.rs, exercising create_filter/list_filters/get_filter/update_filter/delete_filter directly) moved byte-identical to the new adapter crate's own tests/ directory, since the functions it exercises moved there; 2/2 pass against a live DATABASE_URL this loop. Also corrected the donor's .chronica/capability.chronica.json and CAPABILITY.md, which still named the stale crates/chronica-api/src/routes and crates/chronica-helpdesk/src/store/mod.rs caller entries (chronica-helpdesk no longer exists as a crate) -- resolved to the real current locations rather than left as a disclosed gap, per this program's standing rule. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_07p": "144 -> 143: crates/core/chronica-cap-crm-support-thread-message-dispatch's responsibility-tier decomposition removed its own optional sqlx (and chronica-core) dependency entirely (the real post_message/messages_ordered/schedule_deferred/list_deferred_filtered/release_due/cancel_deferred SQL -- formerly a `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` in a single lib.rs, mixing real Postgres mechanics (including release_due's one-transaction `FOR UPDATE SKIP LOCKED` claim-and-post loop) with the pure ThreadMessageRow/DeferredMessageRow structs (+ to_json) and the pure CancelOutcome enum under the SAME gate -- moved byte-identical to the new chronica-adapter-crm-support-thread-message-dispatch, under crates/adapter/, along with free-function map_thread_message_row/map_deferred_message_row row decoders (E0116: Rust forbids an inherent impl on a foreign type, since the row types now live in the donor crate). The pure types stayed in the shrunk, now-always-pure donor crate, no `db` feature at all, only serde_json. The one real (non-dev) caller of the impure ops, chronica-adapter-crm-http's thread_dispatch route module, repointed its `use`/Cargo.toml dependency to the new adapter crate -- that edge is adapter->adapter, not a new layer-debt edge. chronica-adapter-api-server carried an unused-in-src, non-optional Cargo.toml dependency on the old crate -- REMOVED entirely (not repointed), per this program's standing rule. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "recorded_at": "2026-09-09",
  "_forbidden_core_io_count_2026_09_07o": "145 -> 144: crates/core/chronica-cap-crm-support-metadata-lifecycle's responsibility-tier decomposition removed its own optional sqlx (and chronica-core) dependency entirely (the real create_draft_version/list_versions/get_active_version/activate_version SQL -- formerly a `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` in a single lib.rs, mixing real Postgres mechanics (including the object-row `FOR UPDATE` lock and the one-transaction supersede/activate/cache-bump sequence) with the pure SchemaVersionRow struct (+ to_json)/CreateDraftOutcome/ActivateOutcome enums under the SAME gate -- moved byte-identical to the new chronica-adapter-crm-support-metadata-lifecycle, under crates/adapter/, along with a free-function `map_schema_version_row` row decoder (E0116: Rust forbids an inherent impl on a foreign type, since SchemaVersionRow now lives in the donor crate). The pure types stayed in the shrunk, now-always-pure donor crate, no `db` feature at all, only serde_json. The new adapter crate keeps activate_version's real dependency on chronica-cap-crm-support-metadata-registry (the SINGLE OWNER of bump_metadata_version_tx, control-plane corrections item D, and the object_metadata table this crate's own FOR UPDATE lock touches) -- that crate physically lives under crates/core/, so adapter -> core is not forbidden (FORBIDDEN_TARGETS.adapter = {infra}); not a new layer-debt edge. The one real (non-dev) caller of the impure ops, chronica-adapter-crm-support-lifecycle-events's activate_version_with_event, repointed its `use`/Cargo.toml dependency to the new adapter crate -- that edge is adapter->adapter, not a new layer-debt edge. chronica-adapter-api-server carried NO dependency on the old crate at all (confirmed via grep), so there was no dead dependency to remove this time. The donor's own crate-level live-DB integration test (tests/metadata_lifecycle.rs, exercising create_draft_version/list_versions/get_active_version/activate_version directly, not through an HTTP route) moved byte-identical to the new adapter crate's own tests/ directory, since the functions it exercises moved there; the caller crate's own tests/event_adapters.rs (relocated M1.5 event-adapter pilot tests) was repointed to import from the new adapter crate and continues to pass (7/7, including the two activate_version_with_event tests). Also corrected the donor's .chronica/capability.chronica.json and CAPABILITY.md, which still named the stale crates/chronica-crm/... caller/witness paths (chronica-crm no longer exists as a crate) -- resolved to the real current locations rather than left as a disclosed gap, per this program's standing rule. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_07n": "146 -> 145: crates/core/chronica-cap-crm-support-productivity-notes-events-tags's responsibility-tier decomposition removed its own optional sqlx (and chronica-core) dependency entirely (the real add_note/list_notes/delete_note SQL -- formerly a `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` in a single lib.rs, mixing real Postgres mechanics with the pure LeadNoteRow struct (+ to_json)/AddNoteOutcome enum under the SAME gate -- moved to the new chronica-adapter-crm-support-productivity-notes-events-tags, under crates/adapter/, along with a free-function `map_lead_note_row` row decoder (E0116: Rust forbids an inherent impl on a foreign type, since LeadNoteRow now lives in the donor crate). The pure types stayed in the shrunk, now-always-pure donor crate, no `db` feature at all, zero dependencies beyond serde_json. The one real (non-dev) caller of the impure ops, chronica-adapter-crm-http's note route module, repointed its `use`/Cargo.toml dependency to the new adapter crate -- that edge is adapter->adapter, not a new layer-debt edge. chronica-adapter-api-server carried an unused-in-src, non-optional Cargo.toml dependency on the old crate -- REMOVED entirely (not repointed), per this program's standing rule. Also corrected the donor's .chronica/capability.chronica.json and CAPABILITY.md, which still named the old crates/chronica-api/... and crates/chronica-crm/... witness/caller paths -- find . -name crm_lead_note_route.rs trivially resolves the real current test file at crates/adapter/chronica-adapter-api-server/tests/crm_lead_note_route.rs, and the real current route module at crates/adapter/chronica-adapter-crm-http/src/crm/note.rs; both were resolved rather than left as a disclosed gap, per this program's standing rule. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_07l": "148 -> 147: crates/core/chronica-cap-crm-support-record-timeline-activity's responsibility-tier decomposition removed its own optional sqlx (and chronica-core) dependency entirely (the real record_activity/list_timeline SQL -- formerly a `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` in a single lib.rs, mixing real Postgres mechanics with the pure CrmTimelineRow struct (+ to_activity/to_json) under the SAME gate -- moved to the new chronica-adapter-crm-support-record-timeline-activity, under crates/adapter/, along with a free-function `map_crm_timeline_row` row decoder (E0116: Rust forbids an inherent impl on a foreign type, since CrmTimelineRow now lives in the donor crate). The pure type stayed in the shrunk, now-always-pure donor crate, no `db` feature at all, keeping only its always-needed chronica-core-erp-crm dependency (for to_activity's TimelineActivity construction) + serde_json. The one real (non-dev) caller of the impure ops, chronica-adapter-crm-http's timeline route module, repointed its `use`/Cargo.toml dependency to the new adapter crate -- that edge is adapter->adapter, not a new layer-debt edge. chronica-adapter-api-server carried an unused-in-src, non-optional Cargo.toml dependency on the old crate -- REMOVED entirely (not repointed), per this program's standing rule. Also corrected the donor's .chronica/capability.chronica.json and CAPABILITY.md, which still named the old crates/chronica-api/... witness/caller paths -- find . -name crm_timeline_route.rs trivially resolves the real current test file at crates/adapter/chronica-adapter-api-server/tests/crm_timeline_route.rs, and the real current route module at crates/adapter/chronica-adapter-crm-http/src/crm/timeline.rs; both were resolved rather than left as a disclosed gap, per this program's standing rule. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_07m": "147 -> 146: crates/core/chronica-cap-crm-support-thread-notes-followers's responsibility-tier decomposition removed its own optional sqlx (and chronica-core) dependency entirely (the real log_internal_note/list_notes/subscribe/unsubscribe/is_follower/list_followers SQL -- formerly a `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` in a single lib.rs, mixing real Postgres mechanics with the pure ThreadNoteRow struct (+ to_json) under the SAME gate -- moved to the new chronica-adapter-crm-support-thread-notes-followers, under crates/adapter/, along with a free-function `map_thread_note_row` row decoder (E0116: Rust forbids an inherent impl on a foreign type, since ThreadNoteRow now lives in the donor crate). The pure type stayed in the shrunk, now-always-pure donor crate, no `db` feature at all, keeping only serde_json. The one real (non-dev) caller of the impure ops, chronica-adapter-crm-http's thread_messaging route module, repointed its `use`/Cargo.toml dependency to the new adapter crate -- that edge is adapter->adapter, not a new layer-debt edge. chronica-adapter-api-server carried an unused-in-src, non-optional Cargo.toml dependency on the old crate -- REMOVED entirely (not repointed), per this program's standing rule. Also corrected the donor's .chronica/capability.chronica.json and CAPABILITY.md, which still named the old crates/chronica-api/... witness/caller paths -- find . -name crm_thread_messaging_route.rs trivially resolves the real current test file at crates/adapter/chronica-adapter-api-server/tests/crm_thread_messaging_route.rs (containing log_internal_note_is_always_internal_and_lists_newest_first, blank_note_body_is_rejected, subscribe_is_idempotent, unsubscribe_deletes_then_404s_on_repeat, anonymous_mutation_is_denied, thread_state_is_tenant_scoped), and the real current route module at crates/adapter/chronica-adapter-crm-http/src/crm/thread_messaging.rs; both were resolved rather than left as a disclosed gap, per this program's standing rule. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_07k": "149 -> 148: crates/core/chronica-cap-crm-support-manage-skills's responsibility-tier decomposition removed its own optional sqlx (and chronica-core) dependency entirely (the real create_skill/set_active/find_all_flat_skills SQL -- formerly a `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` in a single lib.rs, mixing real Postgres mechanics with the pure SkillRow struct (+ to_json)/SetActiveOutcome enum under the SAME gate -- moved to the new chronica-adapter-crm-support-manage-skills, under crates/adapter/, along with a free-function `map_skill_row` row decoder (E0116: Rust forbids an inherent impl on a foreign type, since SkillRow now lives in the donor crate). The pure types stayed in the shrunk, now-always-pure donor crate, no `db` feature at all, zero dependencies. The one real (non-dev) caller of the impure ops, chronica-adapter-crm-http's skills route module, repointed its `use`/Cargo.toml dependency to the new adapter crate -- that edge is adapter->adapter, not a new layer-debt edge. chronica-adapter-api-server carried an unused-in-src, non-optional Cargo.toml dependency on the old crate -- REMOVED entirely (not repointed), per this program's standing rule. Also corrected the donor's .chronica/capability.chronica.json and CAPABILITY.md, which still named the old crates/chronica-api/... witness/caller paths -- find . -name crm_skills_route.rs trivially resolves the real current test file at crates/adapter/chronica-adapter-api-server/tests/crm_skills_route.rs, and the real current route module at crates/adapter/chronica-adapter-crm-http/src/crm/skills.rs; both were resolved rather than left as a disclosed gap, per this program's standing rule. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_07j": "150 -> 149: crates/core/chronica-cap-crm-support-pipeline-funnel's responsibility-tier decomposition removed its own optional sqlx (and chronica-core) dependency entirely (the real pipeline_summary group-by-stage SQL -- formerly a `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` in a single lib.rs, mixing real Postgres mechanics with the pure PipelineStage struct (+ to_json) under the SAME gate -- moved to the new chronica-adapter-crm-support-pipeline-funnel, under crates/adapter/, along with a free-function `map_pipeline_stage` row decoder (E0116: Rust forbids an inherent impl on a foreign type, since PipelineStage now lives in the donor crate). The pure type stayed in the shrunk, now-always-pure donor crate, no `db` feature at all, zero dependencies. The one real (non-dev) caller of the impure op, chronica-adapter-crm-http's pipeline route module, repointed its `use`/Cargo.toml dependency to the new adapter crate -- that edge is adapter->adapter, not a new layer-debt edge. chronica-adapter-api-server carried an unused-in-src, non-optional Cargo.toml dependency on the old crate -- REMOVED entirely (not repointed), per this program's standing rule. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_07i": "151 -> 150: crates/core/chronica-cap-crm-support-apply-rating's responsibility-tier decomposition removed its own optional sqlx (and chronica-core/chronica-core-erp-crm) dependency entirely (the real create_rating/list_ratings/get_rating SQL -- formerly a `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` in a single lib.rs, mixing real Postgres mechanics with the pure CrmRatingRow struct (+ to_json)/CreateRatingOutcome enum under the SAME gate -- moved to the new chronica-adapter-crm-support-apply-rating, under crates/adapter/, along with a free-function `map_rating_row` row decoder (E0116: Rust forbids an inherent impl on a foreign type, since CrmRatingRow now lives in the donor crate). The pure types stayed in the shrunk, now-always-pure donor crate, no `db` feature at all, zero dependencies. The one real (non-dev) caller of the impure ops, chronica-adapter-crm-http's rating route module, repointed its `use`/Cargo.toml dependency to the new adapter crate -- that edge is adapter->adapter, not a new layer-debt edge. chronica-adapter-api-server carried an unused-in-src Cargo.toml dependency on the old crate -- REMOVED entirely (not repointed), per this program's standing rule. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_07h": "152 -> 151: CORRECTS A MISTAKEN REJECTION -- commit 543bb71b rejected crates/core/chronica-cap-helpdesk-contacts as a pure/adapter split candidate, reasoning 'its entire mod imp is db-gated with no independently pure logic left in the crate'. A fresh adversarial review of that commit found the reasoning did not hold: CreateContactOutcome/SetBlockedOutcome/UpdateContactOutcome never touched sqlx/chronica_core directly, only the already-foreign, already-pure chronica_core_helpdesk_domain::contact::ContactError -- structurally identical to pure outcome enums this program had already extracted from 6+ sibling crates this session (chronica-cap-helpdesk-drafts's SaveDraftOutcome/GetDraftOutcome, chronica-cap-helpdesk-teams's CreateTeamOutcome/UpdateTeamOutcome/AddMemberOutcome, chronica-cap-helpdesk-participants's bare AddParticipantOutcome) without that disqualifying them. This loop applied the SAME split: the crate's own optional sqlx (and chronica-core/chronica-cap-helpdesk-event-store) dependency removed entirely (the real create_contact/set_blocked/update_contact/delete_contact SQL -- formerly a `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` in a single lib.rs -- moved to the new chronica-adapter-helpdesk-contacts, under crates/adapter/). The pure CreateContactOutcome/SetBlockedOutcome/UpdateContactOutcome enums stayed in the shrunk, now-always-pure donor crate (keeping only its chronica-core-helpdesk-domain dependency for ContactError), no `db` feature at all. FOUR real (non-dev/dev) callers repointed (all adapter/runtime/core-test-tier, none forbidden by FORBIDDEN_TARGETS, so zero new layer-debt edges): chronica-adapter-helpdesk-http's helpdesk_contacts route module (adapter->adapter, `use` line + Cargo.toml dep + db-feature-list entry); chronica-adapter-api-server's own tests/erasure_audit_route.rs, which genuinely calls create_contact/CreateContactOutcome directly to seed a GDPR-erasure-audit test subject (adapter->adapter, NOT dead-in-src -- confirmed by reading the full test file before repointing, unlike the LOOP-26/28/29/30-corrected dead-dependency cases) -- its Cargo.toml optional dependency + db-feature-list entry repointed, not removed; chronica-runtime-gdpr-erasure's own tests/purge_flow_live.rs + tests/audit_readback_live.rs dev-dependency, both genuinely seeding a contact via create_contact (runtime->adapter dev, repointed); chronica-cap-helpdesk-inboxes's own tests/inbox_store_live.rs dev-dependency, genuinely seeding a contact to prove find_by_inbox_and_source's identity lookup (core->adapter dev -- dev/build dependencies are exempt from FORBIDDEN_TARGETS per tools/refoundation/validate-refoundation-layers.mjs, confirmed no new edge). Also corrected a stale tenancy_filter_lint ALLOW_LIST file path (allow_list_chunk_06.rs, five entries) that pointed at 'crates/core/chronica-cap-helpdesk-contacts/tests/contact_store_live.rs' -- the test's real new home is crates/adapter/chronica-adapter-helpdesk-contacts/tests/contact_store_live.rs. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_07g": "153 -> 152: crates/core/chronica-cap-helpdesk-drafts's responsibility-tier decomposition removed its own optional sqlx (and chronica-core) dependency entirely (the real save_draft/get_draft/clear_draft/thread_belongs SQL -- formerly a `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` in a single lib.rs, mixing real Postgres mechanics with the pure DraftRow struct (+ to_redacted_json)/SaveDraftOutcome/GetDraftOutcome enums under the SAME gate -- moved to the new chronica-adapter-helpdesk-drafts, under crates/adapter/. The pure types stayed in the shrunk, now-always-pure donor crate (keeping its chronica-core-security-redact dependency, since DraftRow::to_redacted_json calls redact_text directly), no `db` feature at all. Two real (non-dev) callers repointed: chronica-adapter-helpdesk-http's helpdesk_drafts route module (adapter->adapter, one `use` line + one Cargo.toml dep) and chronica-adapter-helpdesk-postgres's tests/purge_live.rs dev-dependency (adapter->adapter dev, a cross-capability seeding test for save_draft/SaveDraftOutcome). One dead-in-src optional dep confirmed via grep and REMOVED entirely (not repointed), per this program's standing rule: chronica-adapter-api-server's Cargo.toml entry (reached the real ops only transitively via chronica-adapter-helpdesk-http, never referenced directly in api-server's own src/ or tests/). Also corrected a stale tenancy_filter_lint ALLOW_LIST file path (allow_list_chunk_06.rs, two entries) that still pointed at 'crates/cap/chronica-cap-helpdesk-drafts/tests/draft_store_live.rs' -- a path this crate never occupied even before this split -- now pointing at the real crates/adapter/chronica-adapter-helpdesk-drafts/tests/draft_store_live.rs destination. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_07f": "154 -> 153: crates/core/chronica-cap-helpdesk-teams's responsibility-tier decomposition removed its own optional sqlx (and chronica-core) dependency entirely (the real create_team/list_teams/get_team/update_team/delete_team/add_member/remove_member/list_members SQL -- formerly a `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` in a single lib.rs, mixing real Postgres mechanics with the pure TeamRow struct (+ to_json)/CreateTeamOutcome/UpdateTeamOutcome/AddMemberOutcome enums under the SAME gate -- moved to the new chronica-adapter-helpdesk-teams, under crates/adapter/, along with a rows.rs holding the column projection and a free-function `map_team_row` row decoder (a free function, not an inherent impl, since the row TYPE itself stays in the now-pure donor crate and Rust forbids adding an inherent impl to a foreign type -- rustc error E0116, not the orphan rule -- a terminology correction from the LOOP-28 precedent's own doc comment, flagged by a fresh reviewer). The pure types stayed in the shrunk, now-always-pure donor crate (keeping its chronica-core-helpdesk-domain dependency for the TeamError type carried by the Invalid variants), no `db` feature at all. Two real (non-dev) callers repointed: chronica-adapter-helpdesk-http's helpdesk_teams route module (adapter->adapter, one `use` line) and chronica-runtime-helpdesk-automation's repo.rs + tests/round_robin_live.rs (runtime->adapter, already the established shape for this crate -- not a new layer-debt edge). Two dead-in-src optional deps confirmed via grep and REMOVED entirely (not repointed), per this program's standing rule: chronica-adapter-api-server's Cargo.toml entry (reached the real ops only transitively via chronica-adapter-helpdesk-http) and chronica-adapter-postgres-helpdesk-automation's dev-dependency (unreferenced in its own tests/money_guard.rs, its only test file). See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_07e": "155 -> 154: crates/core/chronica-cap-helpdesk-thread-comments's responsibility-tier decomposition removed its own optional sqlx (and chronica-core) dependency entirely (the real thread_belongs/add_comment/list_comments/delete_comment SQL -- formerly a `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` in a single lib.rs, mixing real Postgres mechanics with the pure ThreadCommentRow struct (+ to_redacted_json)/AddCommentOutcome enum under the SAME gate -- moved to the new chronica-adapter-helpdesk-thread-comments, under crates/adapter/, along with a rows.rs holding the column projection and a free-function `map_thread_comment_row` row decoder (a free function, not an inherent impl, since the row TYPE itself stays in the now-pure donor crate and Rust's orphan rule forbids adding an inherent impl to a foreign type). The pure ThreadCommentRow/AddCommentOutcome types stayed in the shrunk, now-always-pure donor crate, no `db` feature at all, zero dependencies beyond chronica-core-security-redact + serde_json. Two real (non-dev) callers: chronica-adapter-helpdesk-http's helpdesk_comments route module repointed its `use`/Cargo.toml dependency to the new adapter crate (adapter->adapter, not a new layer-debt edge); chronica-adapter-helpdesk-postgres's own tests/purge_live.rs (a real, direct `use` of add_comment/AddCommentOutcome to seed a comment for its GDPR-purge-redaction proof) also repointed its dev-dependency (adapter->adapter). chronica-adapter-api-server carried an unused-in-src Cargo.toml dependency on the old crate (reached the real ops only transitively via chronica-adapter-helpdesk-http) -- REMOVED entirely (not repointed), per this program's standing rule and the LOOP-26-correction precedent for a demonstrably dead-in-src dependency. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_07d": "156 -> 155: crates/core/chronica-cap-helpdesk-participants's responsibility-tier decomposition removed its own optional sqlx (and chronica-core) dependency entirely (the real add_participant/remove_participant/list_participants/thread_belongs SQL -- formerly a `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` in a single lib.rs, alongside its own already-pure AddParticipantOutcome enum under the SAME gate -- moved to the new chronica-adapter-helpdesk-participants, under crates/adapter/; the pure AddParticipantOutcome enum stayed in the shrunk, now-always-pure donor crate, no `db` feature at all, zero dependencies). The one real (non-dev) caller of the impure ops, chronica-adapter-helpdesk-http's helpdesk_participants route module, repointed its `use`/Cargo.toml dependency to the new adapter crate -- that edge is adapter->adapter, not a new layer-debt edge (chronica-adapter-helpdesk-http was already adapter-tier). chronica-adapter-api-server carried an unused-in-src Cargo.toml dependency on the old crate (reached the real ops only transitively via chronica-adapter-helpdesk-http) -- REMOVED entirely (not repointed), per this program's standing rule and the LOOP-26-correction precedent for a demonstrably dead-in-src dependency. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_07c": "157 -> 156: crates/core/chronica-cap-helpdesk-read-state's responsibility-tier decomposition removed its own optional sqlx (and chronica-core) dependency entirely (the real mark_read/unread_counts/unread_for_thread SQL -- formerly a `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` in a single lib.rs, alongside its own already-pure MarkReadOutcome/ThreadUnread/UnreadSummary types under the SAME gate -- moved to the new chronica-adapter-helpdesk-read-state, under crates/adapter/; the pure types, including ThreadUnread::to_json/UnreadSummary::to_json, stayed in the shrunk, now-always-pure donor crate, no `db` feature at all). The one real (non-dev) caller of the impure ops, chronica-adapter-helpdesk-http's helpdesk_read_state route module, repointed its `use`/Cargo.toml dependency to the new adapter crate -- that edge is adapter->adapter, not a new layer-debt edge (chronica-adapter-helpdesk-http was already adapter-tier). chronica-adapter-api-server carried an unused-in-src Cargo.toml dependency on the old crate (reached the real ops only transitively via chronica-adapter-helpdesk-http) -- repointed to the new crate name rather than left dangling on a now-nonexistent `db` feature. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_07b": "158 -> 157: crates/core/chronica-cap-esign-envelope-audit-log's responsibility-tier decomposition removed its own optional sqlx (and chronica-core) dependency entirely (the real record_event/list_events/fold_prior_lifecycle_anchor SQL -- formerly a `#[cfg(feature = \"db\")]`-gated store.rs alongside its own already-pure verifier/type logic under the SAME umbrella gate -- moved to the new chronica-adapter-esign-envelope-audit-log, under crates/adapter/; the pure AuditLogRow/AuditLogRowVerification/AuditChainAnchor/AuditPageScope/NewAuditEvent/RecordAuditEventOutcome/AuditCursor/AuditHistoryPage types plus verify_audit_page/verify_paginated_audit_chain/validate_actor_type/audit_error_reason stayed in the shrunk, now-always-pure donor crate, no `db` feature at all). The one real (non-dev) cap-tier caller of the impure writers, chronica-cap-deals-federation-routes, repointed its `use`/Cargo.toml dependency to the new adapter crate (a disclosed cap->adapter layer-debt edge, tools/refoundation/refoundation-layer-debt.json's _note_2026_09_07l). chronica-adapter-api-server's own dependency was also repointed (its tests/esign_audit_route.rs and tests/esign_certificate_route.rs are real, non-dev callers of record_event/list_events) -- that edge is adapter->adapter, not a debt edge. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_07": "159 -> 158: crates/core/chronica-cap-esign-document-store's responsibility-tier decomposition removed its own optional sqlx dependency entirely (the real create_document_record/add_document_version/get_record/list_versions/get_latest_version/archive_record/record_stats/search_records SQL -- formerly a `#[cfg(feature = \"db\")]`-gated operations.rs alongside an already-100%-pure validators.rs under the SAME umbrella gate -- moved to the new chronica-adapter-esign-document-store, under crates/adapter/; the pure DocumentStoreError/validators/DocumentRecordRow/DocumentVersionRow/CreateRecordOutcome/AddVersionOutcome stayed in the shrunk, now-always-pure donor crate, no `db` feature at all). The one real (non-dev) caller of the impure writers, chronica-cap-deals-federation-routes (crates/cap/), repointed its `use`/Cargo.toml dependency to the new adapter crate (a disclosed cap->adapter layer-debt edge, tools/refoundation/refoundation-layer-debt.json's _note_2026_09_06k). chronica-adapter-api-server carried a dead (unused in src/ or tests/) Cargo.toml dependency on the old crate -- removed rather than repointed. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_06k": "160 -> 159: chronica-core-artifact-registry's responsibility-tier decomposition removed its own sqlx dependency entirely (the real ArtifactRepo -- register/get_content_scoped/review_scoped/get_scoped/find_by_sha256_scoped/list/list_for_account SQL -- formerly a `#[cfg(feature = \"db\")] pub mod repo` -- moved to the new chronica-adapter-postgres-artifact-registry, under crates/adapter/; the pure Artifact/ArtifactVersion/ReviewStatus domain, the sanitize_html/sanitize_svg/sanitize_mermaid sanitizers, and the in-memory ArtifactMetadataStore stayed in the shrunk, now-always-pure core crate, no `db` feature at all) -- see docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_06j": "161 -> 160: chronica-core-osint-text-analysis (2187 lines across string_analysis/stringstats/text/language/nameorigin/usernames/usearch/user_agent_pool/version/version_resolver/terminal/presentation submodules) declared an entirely UNUSED `rusqlite = { version = \"0.32\" }` Cargo dependency -- confirmed via `grep -rl rusqlite crates/core/chronica-core-osint-text-analysis/` returning zero hits anywhere in src/ (the only match was the Cargo.toml line itself), the same leftover-from-a-0-change-move pattern already fixed for 22 sibling chronica-core-osint-* crates in an earlier loop this program. The one `reqwest` string match in src/presentation.rs turned out to be inside that file's own money_guard-style forbidden-token scan (`concat!(\"reqwest\", \"::\")`), not a real dependency -- reqwest was never declared in this crate's Cargo.toml at all. Fixed by removing the dead rusqlite line entirely (dependency-hygiene, not a responsibility-tier decomposition -- nothing was mixed-responsibility here). Verified: cargo check clean, cargo test clean (2/2 money_guard + 0 unit tests), cargo check --workspace clean, all three gates green. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_06i": "162 -> 161: chronica-core-user-kv-feedback's responsibility-tier decomposition removed its own sqlx dependency entirely (the real UserKvRepo::{set,get,delete,list}/FeedbackRepo::{submit,get,list_for_user,list_for_company} SQL -- formerly two `#[cfg(feature = \"db\")] mod repo { ... }` blocks in kv.rs/feedback.rs -- moved to the new chronica-adapter-postgres-user-kv-feedback, under crates/adapter/; the pure KvKey/UserKvEntry/FeedbackRating/FeedbackRecord/UserId domain types and validate_kv_value/validate_comment/validate_context/validate_user_id stayed in the shrunk, now-always-pure core crate, no `db` feature at all) -- see docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_06h": "163 -> 162: chronica-core-social-profile-privacy's responsibility-tier decomposition removed its own sqlx dependency entirely (the real list_directory/set_hides_followers/get_hides_followers/set_discoverable/get_discoverable/set_locked/get_locked SQL -- formerly a single `#[cfg(feature = \"db\")] mod imp { ... }` -- moved to the new chronica-adapter-postgres-social-profile-privacy, under crates/adapter/; the pure DIRECTORY_PAGE_MAX/clamp_directory_limit, ProfilePrivacyRow/DirectoryEntry (struct + to_json only)/DirectoryCursor/SetProfilePrivacyOutcome/profile_privacy_error_reason stayed in the shrunk, now-always-pure core crate, no `db` feature at all) -- see docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_06g": "164 -> 163: chronica-core-analytics-metric-lineage's responsibility-tier decomposition removed its own sqlx dependency entirely (the real load_dependencies/add_dependency/record_revision/record_event SQL -- formerly a `#[cfg(feature = \"db\")] pub mod store` -- moved to the new chronica-adapter-postgres-analytics-metric-lineage, under crates/adapter/; the pure dependency-graph domain -- MetricDependencyGraph/MetricLineageStore/topological_order/the event & revision types -- stayed in the shrunk, now-always-pure core crate, no `db` feature at all) -- see docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_06f": "165 -> 164: chronica-core-social-handle's responsibility-tier decomposition removed its own sqlx dependency entirely (the real register_handle/get_own_handle/resolve_handle SQL, the raw-row decode, and is_unique_violation moved to the new chronica-adapter-postgres-social-handle, under crates/adapter/; the pure normalize_handle validation and HandleRow/RegisterHandleOutcome domain types stayed in the shrunk, now-always-pure core crate, no `db` feature at all) -- see docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_06e": "166 -> 165: chronica-core-social-user-profile's responsibility-tier decomposition removed its own sqlx dependency entirely (the real update_profile/get_profile SQL and the raw-row decode moved to the new chronica-adapter-postgres-social-user-profile, under crates/adapter/; the pure validation and domain types stayed in the shrunk, now-always-pure core crate, no `db` feature at all) -- see docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_world_graph_live_dependency_count_2026_09_06d": "6 -> 5: LOOP 11 removed chronica-adapter-machine-connector-daemon's direct dependency on chronica-core-world-graph. That dependency was a single duplicated struct-literal construction (a private now_provenance() helper building a chronica_core_world_graph::Provenance value) that already existed one crate away, in chronica-core-machine-semantics (which owns the design decision to reuse World-Graph's Provenance type verbatim -- see MachineResourceDescriptor.provenance's own doc comment). Added a public chronica_core_machine_semantics::provenance_now(source) helper and repointed the daemon's one call site to it, then deleted the daemon's now-dead chronica-core-world-graph Cargo dependency entirely. Zero functional change (byte-identical Provenance construction), zero new callers manufactured -- this is dependency-direction correction, not a semantic migration. A prior-loop reconnaissance pass independently confirmed this is the ONLY one of the 6 remaining world-graph dependents safely fixable in a bounded slice right now; the other 5 (chronica-core-machine-semantics itself, chronica-runtime-world-admission, chronica-adapter-machine-connector, chronica-cap-machine-connector-gateway) call real D0/D1/D2 domain functions as genuine production logic and require a much larger D0/D1/D2<->chronica-core-graph semantic migration not suited to one loop; chronica-runtime-world-graph-bridge remains a deliberately zero-caller, not-yet-wired migration scaffold that this program has twice now declined to delete or force-wire, since neither action would be a real fix. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_06d": "188 -> 166: a LOOP 10 dependency-hygiene audit (not a responsibility-tier decomposition -- there was no real I/O to decompose) found 22 crates/core/chronica-core-osint-* crates (bootstrap, collectors, confidence, config-runtime, correlation, data-catalog, dns, dns-domain, econ-worldmonitor, enrichment, event-model, export-graph, geo, index-pipeline, intel-api, match-scoring, reconcile, runtime-utils, search, spiderfoot-cli, text-extract, text-parser) all declaring an entirely UNUSED `rusqlite = { version = \"0.32\" }` Cargo dependency -- confirmed via `grep -rl rusqlite <crate>/**/*.rs` returning zero hits in every one of the 22, a leftover artifact of a prior '0-change move' that copied the former flat chronica-osint crate's full dependency list into each split-out module crate without pruning per-module. These are genuine M7 false positives (identical in kind to the existing chronica-core-errors/chronica-core-network-identity-error exemptions, but fixed by removing the actually-dead dependency line entirely rather than adding another exemption-list entry, since the dependency serves these crates no purpose at all). Verified: `cargo check` on all 22 individually + `cargo check --workspace` + `cargo test` on all 22 (0 failures) all clean after removal. Not a responsibility-tier fix (nothing was mixed-responsibility here -- these crates were already 100% pure), so this drop does not correspond to any new core/runtime/adapter split. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_forbidden_core_io_count_2026_09_06b": "191 -> 190: chronica-core-gdpr-erasure's responsibility-tier decomposition removed its own sqlx dependency entirely (the real SQL moved to the new chronica-adapter-postgres-gdpr-erasure, under crates/adapter/; the orchestration that used to dispatch to it moved to the new chronica-runtime-gdpr-erasure, under crates/runtime/) -- see docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_misplaced_cap_package_count_2026_09_06d": "231 -> 230: chronica-cap-helpdesk-automation's responsibility-tier decomposition into chronica-core-helpdesk-automation (pure core) + chronica-runtime-helpdesk-automation (orchestration) + chronica-adapter-postgres-helpdesk-automation (Postgres mechanics) retired its own misplaced chronica-cap-* package name -- the new crates are chronica-core-*/chronica-runtime-*/chronica-adapter-*, none chronica-cap-*.",
  "_forbidden_core_io_count_2026_09_06c": "190 -> 188: the same decomposition removed chronica-cap-helpdesk-automation's own sqlx dependency from crates/core/ entirely (-1), AND unblocked chronica-adapter-helpdesk-postgres's move from crates/core/ to crates/adapter/ (-1, that crate's own sqlx dependency no longer counts as a crates/core/ hit) -- see docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_world_graph_live_dependency_count_2026_09_06c": "9 -> 6: the FIRST real reduction in world_graph_live_dependency_count in this correction program (prior loops only ever affected M2/M7). chronica-core-world-graph's src/canonical.rs (generic canonical-JSON/hashing/newtype primitives -- Sha256Hash, SafeRevision, sha256_of, canonical_stringify, sort_by_key_then_content, JS_MAX_SAFE_INTEGER -- zero World/Admission/Capability domain coupling) was extracted verbatim into a new standalone crate, chronica-core-canonical-hash. chronica-digital-twin-model, chronica-digital-twin-evidence, and chronica-digital-twin-testkit -- the only 3 real (non-dev) dependents that used ONLY these generic primitives -- were repointed to depend on chronica-core-canonical-hash directly and had chronica-core-world-graph removed from their [dependencies] entirely. chronica-core-world-graph's own src/canonical.rs is now a thin `pub use chronica_core_canonical_hash::*;` re-export, so the remaining 6 live dependents (chronica-core-machine-semantics, chronica-runtime-world-admission, chronica-runtime-world-graph-bridge, chronica-adapter-machine-connector, chronica-adapter-machine-connector-daemon, chronica-cap-machine-connector-gateway -- none of which were in scope for this loop) and every other caller of chronica_core_world_graph::canonical::{Sha256Hash, SafeRevision, ...} keep working unchanged. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl's world_graph_dependency_extinction record for full verification detail.",
  "_contacts_split_2026_09_08i": "123 -> 122: crates/core/chronica-cap-crm-support-contacts's responsibility-tier decomposition (LOOP 58). SPLIT, not relocated -- the determination was made by READING the actual current 267-line src/lib.rs, not by assuming LOOP 52/53/54/56's relocation outcome for a differently shaped crate. Although the whole file sat inside a single #[cfg(feature = \"db\")] mod imp (the shape those four relocations had), it DOES declare local pure surface of its OWN: the CrmContactRow projection struct (with its camelCase to_json view), the three-variant CreateContactOutcome enum (Created/InvalidKind/LeadNotFound), the three-variant UpdateContactOutcome enum (Updated/NotFound/InvalidKind), the sparse ContactPatch struct, and the kind_ok closed-set predicate mirroring the crm_contacts table CHECK. UNLIKE LOOP 55 and LOOP 57, that retained surface contains NO FOREIGN TYPE AT ALL -- every CrmContactRow/ContactPatch field is a String, Option<String> or bool, and both outcome enums carry only Box<CrmContactRow> -- so there was not even a facade-purity question to settle: the donor's chronica-core dependency was DROPPED OUTRIGHT along with its `db` feature, leaving serde_json as its single remaining dependency and zero cargo features. The real mechanics (create_contact with its cross-tenant lead-ownership SELECT ... FOR UPDATE gate, list_contacts, get_contact, update_contact, the private CrmContactRow::map row mapper and the CONTACT_COLS column list) moved BYTE-IDENTICAL into the new crates/adapter/chronica-adapter-crm-support-contacts, verified by SHA-256 against git show HEAD:crates/core/chronica-cap-crm-support-contacts/src/lib.rs in three separately hashed spans (the moved region is non-contiguous, as in LOOP 57): CONTACT_COLS doc+const, pre-change lines 22-27, sha256 22589b8c5de33f3b8179284bd56441498007be38b22b54966d18e1b08700e8f8, identical with no edit at all; the row-mapper body, pre-change lines 49-61, sha256 (after the documented uniform 4-space dedent for the lost nesting level) ffa6cb01bb87a53115e7ebd7ab549c08d82596b2322996cebbd2c681ab84adbc; and the four async functions with their doc comments, pre-change lines 122-263, sha256 (after the ONE documented substitution below) ff0676b1e79e6a3ab56cbcc7b538c9872b3ed36b12307b2b85a6cb86e3329247. All 14 code-level SQL-bearing lines are byte-identical as a set, sha256 a9c7796398c108e1f16799f20a3676772fb9ba4c020f0634d48ff9246b90e20c on both sides. TWO MECHANICAL EDITS ARE DISCLOSED RATHER THAN HIDDEN, and a line-by-line diff proves they are the ONLY differences in the moved region: (1) the PRIVATE associated fn CrmContactRow::map became the free fn map_row in the adapter, because Rust's orphan rule forbids an inherent impl CrmContactRow outside the crate that declares the type; its body is unchanged and exactly 4 call sites (pre-change lines 171, 199, 217, 258) were renamed, with no signature, behavior or public-API change since the helper was and stays crate-private. (2) kind_ok was a private fn in the donor's mod imp; both of its consumers (create_contact, update_contact) moved to the adapter, so it is now pub on the donor and re-exported by the adapter -- the same class of disclosed visibility change as LOOP 57's DISCLOSURE const. misplaced_cap_package_count is UNCHANGED at 222 by design: a SPLIT keeps the donor chronica-cap-*-named under crates/core/, unlike a RELOCATION; scoring M2 here would require relocating genuinely pure value types into the adapter tier, which would be metric-gaming. Four real (non-test) callers were repointed onto the adapter's re-export as a straight crate rename: chronica-adapter-crm-http (adapter -> adapter, no edge) and chronica-runtime-crm-contact-execution (runtime-tier, which the layer gate deliberately does not order, no edge), plus chronica-cap-platform-misc-routes (cap -> adapter) and chronica-cap-crm-support-contact-csv-import (core -> adapter), which ARE two new forbidden layer edges -- layer_debt_edge_count 115 -> 117, hand-recorded in tools/refoundation/refoundation-layer-debt.json under _note_2026_09_08_loop58 (NOT via --write-baseline), with the self-liquidating rationale for each. A fifth declared dependent, chronica-adapter-api-server, was CONFIRMED DEAD by grepping its own src/ and tests/ for crm_support_contacts (zero hits) and its manifest dep + db feature-array entry were removed outright rather than repointed, per the standing LOOP-21 precedent -- it still enables the adapter's db feature transitively through chronica-adapter-crm-http/db, chronica-cap-platform-misc-routes/db and chronica-cap-crm-support-contact-csv-import/db. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_survey_targeting_branching_split_2026_09_08j": "122 -> 121: crates/core/chronica-cap-crm-support-survey-targeting-branching's responsibility-tier decomposition (LOOP 59). SPLIT, not relocated -- the determination was made by READING the actual current 286-line src/lib.rs, not by inheriting LOOP 52/53/54/56's relocation outcome for a differently shaped crate, and not by trusting a prior loop's scouting note. Although the whole file sat inside a single #[cfg(feature = \"db\")] mod imp (the shape those four relocations had), only ONE of its five functions was real I/O: survey_response_stats, the only item taking a &PgPersistence and causing a database round-trip. The other four are genuinely pure and are genuinely this crate's own work -- question_kind is a hand-written closed-set map from the persisted Odoo-ported QuestionType onto the ported analytics module's QuestionKind (SimpleChoice/MultipleChoice direct, Numerical -> Rating as the only numerically-averaged kind, TextBox and Date both -> Open), and resolve_question_metadata is the ordered survey-index-to-QuestionMeta builder; evaluate_survey_targeting and resolve_survey_next_step are pure boundary delegates. That is real retained pure surface, so the donor was kept and shrunk rather than renamed away. THE FACADE-PURITY QUESTION (LOOP 57's) WAS SETTLED RATHER THAN ASSUMED: unlike LOOP 58's donor, this one declares NO type of its own -- every type it names (Question, QuestionType, QuestionKind, QuestionMeta, SurveyConditions, VisitorContext, BranchAction, BranchTarget and the two error enums) is foreign, owned by chronica-core-erp-crm. That crate's Cargo.toml was read directly for this loop: its dependencies are chronica-core, chronica-core-automation-graph, chronica-core-erp-policy, chronica-core-erp-quota-limiting, chronica-core-erp-sales-pipeline, regex, serde, serde_json and time, and it declares NO [features] section at all, so it never forwards chronica-core/db and never reaches sqlx -- the retained surface is pure, not pure-by-re-export-of-something-impure. The donor's chronica-core, chronica-cap-crm-support-surveys and sqlx dependencies were DROPPED OUTRIGHT along with its `db` feature, leaving chronica-core-erp-crm as its single remaining dependency and zero cargo features. The real mechanics moved BYTE-IDENTICAL into the new crates/adapter/chronica-adapter-crm-support-survey-targeting-branching, verified by SHA-256 against git show HEAD:crates/core/chronica-cap-crm-support-survey-targeting-branching/src/lib.rs in three separately hashed spans (the moved region is non-contiguous, as in LOOP 57/58), each anchored by grep -n '^#\\[cfg(feature = \"db\")\\]' (the ^ anchor excludes the module doc's line-10 prose mention of the same string; the real attributes are at lines 13 and 285). Span A, the production I/O -- the survey_response_stats doc comment and async fn, pre-change lines 54-71 -- sha256 ead6b5bed78ef295594bc5816a5b2adf2975c7d26ef0a5743e866ecf3a39f572. Span B, the live-DB test module mod db_tests with its dev_pg/a_company harness, pre-change lines 210-281 -- sha256 9b78dfdcf9d472aefd8e17fe123b1ecd3fff63e33f682957853364a75ca205c9. Span C, the sample_questions test fixture the moved db test depends on, pre-change lines 101-125 -- sha256 bda061676b1373669532ad09149b15c1c245b4f8924c5b40d8414f3d23f8b981. ALL THREE HASH IDENTICALLY ON BOTH SIDES WITH ZERO EDITS -- no orphan-rule rename was forced (the moved surface is one free function, no inherent impl on a foreign type, unlike LOOP 58) and no dedent was needed (the adapter reproduces the donor's exact mod imp / mod tests / mod db_tests nesting depth, so every line keeps its original indentation). THE RETAINED PURE CODE WAS ALSO DRIFT-CHECKED, not merely assumed unchanged: question_kind + resolve_question_metadata (pre-change lines 25-52) are byte-identical after the documented uniform 4-space dedent for the lost mod imp nesting level, sha256 9d236c81758dd46d8602267317af3db93a642b673c027f106fec0d5c011056af on both sides. The two evaluators (pre-change lines 73-92) differ in EXACTLY 2 lines, both doc-comment-only and both disclosed: evaluate_survey_targeting's doc said CRM callers depend on `chronica-crm`, never on `chronica-erp` directly -- neither flat crate exists anywhere in this repo any more, so the sentence now names this capability and the ported ERP module instead. No code-level line of retained logic changed. SPAN C IS DUPLICATED, NOT MOVED: sample_questions is a test fixture still needed by two retained pure tests (unit_resolve_question_metadata_preserves_survey_order_and_ids and unit_stats_helper_builds_open_and_rating_answers), so it now exists byte-identically in BOTH crates' test modules. This is test scaffolding, not production code, and is disclosed rather than hidden. misplaced_cap_package_count is UNCHANGED at 222 by design: a SPLIT keeps the donor chronica-cap-*-named under crates/core/, unlike a RELOCATION; scoring M2 here would require relocating genuinely pure functions into the adapter tier, which would be metric-gaming. layer_debt_edge_count is UNCHANGED at 117 -- ZERO new forbidden edges, INDEPENDENTLY VERIFIED rather than inherited from a prior loop's scouting claim. An exact fully-qualified-path grep for both the underscore and hyphen crate spellings found exactly ONE real (non-test) caller in the whole repo: crates/adapter/chronica-adapter-crm-http/src/crm/survey_analytics.rs, which is adapter-tier, so the repoint is adapter -> adapter (allowed; FORBIDDEN_TARGETS.adapter = {infra}). The new adapter crate's own edges are adapter -> core (chronica-core, chronica-core-erp-crm, the donor and chronica-cap-crm-support-surveys, the last two being chronica-cap-*-named crates that physically live under crates/core/ and are therefore classified core by the path-based layer gate) -- all allowed. Because the adapter re-exports the donor's whole pure surface unconditionally, the caller needed a ONE-LINE `use ... as store;` swap plus its manifest dep and db feature-array entry, and nothing else. A second declared dependent, chronica-adapter-api-server, was CONFIRMED DEAD by grepping its own src/ and tests/ for both crate spellings: the only hit anywhere was a single doc-comment mention on line 3 of tests/crm_survey_analytics_route.rs, with zero code references, so its manifest dep + db feature-array entry were removed outright rather than repointed, per the standing LOOP-21 precedent -- it still enables the adapter's db feature transitively through chronica-adapter-crm-http/db, and its four live-DB route tests exercise the routes over HTTP, not the crate directly. STALE-CLAIM CORRECTIONS (long-gone flat crate names, fixed rather than carried on): the donor's .chronica caller_entries/runtime_owners named crates/chronica-api/src/routes/crm/{survey_analytics,mod}.rs and crates/chronica-crm/src/store/mod.rs; its CAPABILITY.md claimed chronica-crm re-exports it as chronica_crm::store::crm_survey_analytics and that it reuses chronica_erp::crm::*; the workspace Cargo.toml dependency comment repeated the chronica-crm re-export claim; and three route doc comments in survey_analytics.rs referenced a survey_targeting_branching::store:: module path. None of chronica-api, chronica-crm or chronica-erp exists in this repo (re-confirmed by listing crates/, which holds only adapter/, cap/, core/, infra/ and runtime/, and by a repo-wide grep), so all were replaced with real current paths. The donor .chronica's `supersedes` entries still name flat chronica-crm files and were deliberately RETAINED as extraction provenance, with an explicit known_gaps entry saying so -- they are a record of what this capability came FROM, not a live claim that those paths exist. LIVE-DB GENUINENESS PROVEN BY COMPARISON, not asserted: the moved db test was run twice against the same binary -- with DATABASE_URL unset it took the harness skip path and finished in 0.00s; with DATABASE_URL=postgres://chronica:chronica@127.0.0.1:5432/chronica it ran for real in 0.49s, and a follow-up psql query confirmed it had actually written 2 companies rows (test-co-surveystats, test-co-othertenant) and 1 crm_surveys row, including the cross-tenant negative assertion that the other company's lookup returns None. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_analytics_action_split_2026_09_08k": "121 -> 120: crates/core/chronica-cap-crm-support-analytics-action's responsibility-tier decomposition (LOOP 60). SPLIT, not relocated -- the determination was made by READING the actual current 270-line src/lib.rs, not by inheriting LOOP 52/53/54/56's relocation outcome for a differently shaped crate. Although the whole file sat inside a single #[cfg(feature = \"db\")] mod imp (the shape those four relocations had), the crate declares FOUR pub types of its own -- AnalyticsActionRow (with its pure to_action domain rehydration and its pure camelCase to_json operator view), NewAction, CreateActionOutcome and UpdateActionOutcome -- plus two pure serde operators, parse_steps (validate a raw Value into the ported Vec<ActionStep>) and steps_json (serialize it back for the jsonb column). Neither operator touches a PgPersistence. That is real retained pure surface, so the donor was kept and shrunk rather than renamed away, exactly as in LOOP 50/55/57/58. THE FACADE-PURITY QUESTION WAS SETTLED RATHER THAN ASSUMED: unlike LOOP 58's donor (which retained no foreign type at all), this one DOES name a foreign type -- AnalyticsActionRow holds a Vec<ActionStep> and to_action returns an Action, both owned by chronica-core-analytics-action. That crate's Cargo.toml was read directly for this loop: its only dependencies are serde and chronica-core, and it declares NO [features] section at all, so it takes chronica-core at that facade's DEFAULT feature set -- and crates/core/chronica-core/Cargo.toml shows default = [], its db feature (the only one it has) being opt-in and the only thing that forwards the sqlx-backed persistence crates. No sqlx and no db feature reaches the donor through that edge, so the retained surface is pure, not pure-by-re-export-of-something-impure. The donor's chronica-core and sqlx dependencies were DROPPED OUTRIGHT along with its db feature, leaving chronica-core-analytics-action + serde_json as its only dependencies and zero cargo features. The real mechanics moved BYTE-IDENTICAL into the new crates/adapter/chronica-adapter-crm-support-analytics-action, verified by SHA-256 against git show HEAD:crates/core/chronica-cap-crm-support-analytics-action/src/lib.rs in three separately hashed spans (the moved region is non-contiguous, as in LOOP 57/58/59), anchored by grep -n '^\\[cfg(feature = \"db\")\\]' with the ^ anchor to exclude the module doc's line-16 prose mention of the same string (the real attributes are at pre-change lines 19 and 269). Span A, the two constants MATCH_SCAN_LIMIT and ACT_COLS with their doc comments, pre-change lines 28-34 -- sha256 ff09afd7298176d054e72f454f9c2d170255ba9161ada712f155f1a4e5e42f41, MOVED WITH ZERO EDITS, hashes identically on both sides. Span B, the six async function bodies (create_action, list_actions, get_action, update_action, soft_delete_action, match_event) with their SQL strings, binds and CoreError::from mapping, pre-change lines 129-266 -- sha256 f8092940c059067cd646c7f07dd918387bfaa772d147b4a7cf1c63bf4d700dab pre-change and 1867a4f3e6ce18012adc6501cdd022c9c3421a0d3a131a9a1ff32f3e2bee7b13 in the adapter; reversing the ONE mechanical rename below reproduces the pre-change digest exactly, so those 4 lines are provably the only difference in that 138-line span. Span C, the row mapper, pre-change lines 50-61 -- sha256 a330af7874f95f78847a75a91378ee28bad759d9d84d7955604d1d4e0436dc41, reproduced EXACTLY by reversing this file's three mechanical edits to it (re-indent 4, map_row -> map, AnalyticsActionRow return type -> Self). TWO DISCLOSED MECHANICAL EDITS, not hidden: (1) AnalyticsActionRow::map -- a PRIVATE associated fn -- became the free fn map_row in the adapter, because Rust's orphan rule forbids an inherent impl AnalyticsActionRow outside the crate that declares the type; its body is byte-identical modulo a uniform 4-space dedent (it lost one nesting level) plus spelling out the -> Self return type as -> AnalyticsActionRow (a bare Self has no meaning outside an impl block), and its 4 call sites moved with the rename. The helper was, and stays, crate-private -- no public API changed. (2) parse_steps and steps_json were private fns in the donor's mod imp; both of their consumers (create_action, update_action) moved to the adapter, so they are now pub in the donor and re-exported there. The operators themselves are unchanged. Two import-list lines also shrank, disclosed: json was dropped from use serde_json::{json, Value} (only to_json needed it, and to_json stayed), and Action/ActionStep were dropped from the moved use chronica_core_analytics_action::{Action, ActionStep} because every remaining reference to them in the adapter is inferred or an inherent method call (row.to_action().matches_event(..)), never a named type -- so the adapter declares NO manifest edge to that domain crate at all. misplaced_cap_package_count is UNCHANGED at 222 by design: a SPLIT keeps the donor chronica-cap-*-named under crates/core/, unlike a RELOCATION; scoring M2 here would require relocating genuinely pure types into the adapter tier, which would be metric-gaming. layer_debt_edge_count 117 -> 118: ONE new forbidden edge, chronica-cap-platform-misc-routes (cap) -> chronica-adapter-crm-support-analytics-action (adapter), disclosed by hand in tools/refoundation/refoundation-layer-debt.json's _note_2026_09_08_loop60 (written by hand, NOT via --write-baseline). CALLERS WERE FOUND BY EXACT FULLY-QUALIFIED-PATH GREP, not by bare function name: a repo-wide grep for both the underscore and hyphen crate spellings found exactly ONE real (non-test) production caller, crates/cap/chronica-cap-platform-misc-routes/src/analytics/action.rs (the six axum handlers behind the /analytics/actions routes, 7 fully-qualified references), which is cap-tier -- hence the one new edge. A second declared dependent, chronica-adapter-api-server, was CONFIRMED DEAD by grepping its own src/ and tests/ for chronica_cap_crm_support_analytics_action: zero hits of any kind, so its manifest dep + db feature-array entry were removed outright rather than repointed, per the standing LOOP-21 precedent -- it still enables the new adapter's db feature transitively through chronica-cap-platform-misc-routes/db, and its five live-DB route tests exercise the routes over HTTP, not the crate directly. STALE-CLAIM CORRECTIONS (long-gone flat crate names, fixed rather than carried on): the donor's .chronica caller_entries/runtime_owners named crates/chronica-api/src/routes/analytics/{action,mod}.rs and crates/chronica-crm/src/store/mod.rs with chronica-api as the runtime owner; its covered_sources and all five verification witnesses named crates/chronica-api/... paths; its CAPABILITY.md claimed chronica-crm re-exports it as chronica_crm::store::analytics_action_store; and the workspace Cargo.toml dependency comment repeated that same re-export claim. None of chronica-api or chronica-crm exists in this repo (re-confirmed by a repo-wide grep), so all were replaced with real current paths -- the five witness test-fn names were each verified to exist in crates/adapter/chronica-adapter-api-server/tests/analytics_action_route.rs before being written down. The donor .chronica's supersedes entry still names the flat chronica-crm file and was deliberately RETAINED as extraction provenance, annotated as historical. LIVE-DB GENUINENESS PROVEN BY COMPARISON, not asserted: the five route tests were run twice against the SAME prebuilt binary -- with DATABASE_URL unset they took the harness skip path (dev_pg() -> None) and finished in 0.00s; with DATABASE_URL=postgres://chronica:chronica@127.0.0.1:5432/chronica they ran for real in 1.40s, and a follow-up psql query against analytics_action confirmed they had actually written rows at both run timestamps (06:38:26 and 06:39:26), including a 2-step action whose steps jsonb round-tripped (jsonb_array_length = 2) and a soft-deleted row (deleted = t) -- so the create/update/soft-delete/match paths all really touched Postgres. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_metadata_view_split_2026_09_08l": "120 -> 119: crates/core/chronica-cap-crm-support-metadata-view's responsibility-tier decomposition (LOOP 61). SPLIT, not relocated -- the determination was made by READING the actual current 275-line src/lib.rs, not by inheriting LOOP 52/53/54/56's relocation outcome for a differently shaped crate, and not by trusting a prior loop's scouting note. Although the whole file sat inside a single #[cfg(feature = \"db\")] mod imp (the shape those four relocations had), the crate declares THREE pub types of its own -- ViewMetadataRow (with its pure to_view_metadata domain rehydration and its pure camelCase to_json operator view), NewView and CreateViewOutcome -- plus three pure operators: parse_visibility (wire token -> ported ViewVisibility, unknown -> None), visibility_str (the canonical stored token) and to_json_array (the Vec<String> write half of the jsonb crossing). None of the three touches a PgPersistence. That is real retained pure surface, so the donor was kept and shrunk rather than renamed away, exactly as in LOOP 50/55/57/58/60. THE FACADE-PURITY QUESTION WAS SETTLED RATHER THAN ASSUMED: like LOOP 60's donor (and unlike LOOP 58's, which retained no foreign type at all), this one DOES name foreign types -- ViewVisibility in parse_visibility's return and visibility_str's argument, and ViewMetadata as to_view_metadata's return, all owned by chronica-core-erp-crm. That crate's Cargo.toml was read directly for this loop: it declares NO [features] section at all and no sqlx, so it takes every dependency at its DEFAULT feature set -- and crates/core/chronica-core/Cargo.toml shows default = [], its db feature (the only one it has) being opt-in and the only thing that forwards the sqlx-backed persistence crates. Its four sibling chronica-core-* dependencies (automation-graph, erp-policy, erp-quota-limiting, erp-sales-pipeline) were ALSO read one level deeper, per LOOP 60's rigor: none declares a [features] section and none declares sqlx. No sqlx and no db feature reaches the donor through that edge, so the retained surface is pure, not pure-by-re-export-of-something-impure. The donor's chronica-core, sqlx and chronica-cap-crm-support-metadata-registry dependencies were DROPPED OUTRIGHT along with its db feature, leaving chronica-core-erp-crm + serde_json as its only dependencies and zero cargo features (that registry edge existed solely for create_view's object-belongs-to-company gate, which moved with the I/O). The real mechanics moved BYTE-IDENTICAL into the new crates/adapter/chronica-adapter-crm-support-metadata-view, verified by SHA-256 against git show HEAD:crates/core/chronica-cap-crm-support-metadata-view/src/lib.rs in three separately hashed spans (the moved region is non-contiguous, as in LOOP 57/58/59/60), anchored by grep -n '^#[cfg(feature = \"db\")]' with the ^ anchor to exclude the module doc's line-15 prose mention of the same string (the real attributes are at pre-change lines 18 and 274). Span A, the VIEW_COLS projection constant with its doc comment, pre-change lines 28-32 -- sha256 01a3abf0561e16db2892d05c43d9cd60128edf23a98569d1ce1721d49e3e458a, MOVED WITH ZERO EDITS, hashes identically on both sides. Span B, the four async function bodies (create_view, list_views, get_view, soft_delete_view) with their SQL strings, binds and CoreError::from mapping, pre-change lines 151-271 -- sha256 d9122a53777f6414541ddb4584ce0e378316261b4237e8e7afa853a48a01a09d pre-change and 1ff2cc702ff09e7228d76c1b2ca37287c0dbd5ac8b7eb847e2baf89d1a9b20b2 in the adapter; reversing the ONE mechanical rename below (plus the single rustfmt reflow that shorter name forces at create_view's tail) reproduces the pre-change digest EXACTLY, so those are provably the only differences in that span (121 lines pre-change, 119 after the reflow). Span C, the row mapper, pre-change lines 74-89 -- sha256 96084b87751ce69e6db8526cd38681fa488e9abdeb7463f3a837954146bb87c4, reproduced EXACTLY by reversing this file's three mechanical edits to it (re-indent 4, map_row -> map, ViewMetadataRow return type -> Self). TWO DISCLOSED MECHANICAL EDITS, not hidden: (1) ViewMetadataRow::map -- a PRIVATE associated fn -- became the free fn map_row in the adapter, because Rust's orphan rule forbids an inherent impl ViewMetadataRow outside the crate that declares the type; its body is byte-identical modulo a uniform 4-space dedent (it lost one nesting level) plus spelling out the -> Self return type as -> ViewMetadataRow (a bare Self has no meaning outside an impl block), and its 3 call sites moved with the rename. The helper was, and stays, crate-private -- no public API changed. (2) parse_visibility, visibility_str and to_json_array were private fns in the donor's mod imp; to_json_array's only consumer and one consumer each of the other two moved to the adapter, so all three are now pub in the donor and re-exported there. The operators themselves are unchanged. Two import-list lines also shrank, disclosed: json was dropped from use serde_json::{json, Value} (only to_json needed it, and to_json stayed), and ViewMetadata/ViewVisibility were dropped from the moved use chronica_core_erp_crm::metadata_view::{ViewMetadata, ViewRegistry, ViewVisibility} because every remaining reference to them in the adapter is inferred, never a named type -- ViewRegistry IS still named there (ViewRegistry::default()), so unlike LOOP 60 the adapter DOES declare a real manifest edge to that domain crate. NOT rustfmt-clean by design in one place: the moved create_view carries a 110-char chronica_cap_crm_support_metadata_registry::get_object let-else line that rustfmt would rewrap. It was deliberately left alone to preserve byte identity, after confirming the PRE-CHANGE file had the identical rustfmt deviation on that exact line (rustfmt --check run against git show HEAD:... proves fmt is not a gate for this file); the genuinely NEW code in both crates is rustfmt-clean. misplaced_cap_package_count is UNCHANGED at 222 by design: a SPLIT keeps the donor chronica-cap-*-named under crates/core/, unlike a RELOCATION; scoring M2 here would require relocating genuinely pure types into the adapter tier, which would be metric-gaming. world_graph_live_dependency_count unchanged at 5; cap_directory_count unchanged at 36. layer_debt_edge_count 118 -> 119: ONE new forbidden edge, chronica-cap-platform-admin-routes (cap) -> chronica-adapter-crm-support-metadata-view (adapter), disclosed by hand in tools/refoundation/refoundation-layer-debt.json's _note_2026_09_08_loop61 (written by hand, NOT via --write-baseline). CALLERS WERE FOUND BY EXACT FULLY-QUALIFIED-PATH GREP, not by bare function name: a repo-wide grep for both the underscore and hyphen crate spellings found exactly ONE real (non-test) production caller, crates/cap/chronica-cap-platform-admin-routes/src/metadata.rs (9 fully-qualified references -- the four axum handlers behind POST|GET /api/companies/:company_id/metadata/objects/:object_id/views and GET|DELETE .../views/:view_id, the view_belongs_to_object route-scope helper's signature, one doc-comment cross-reference and the view_object_scope_tests module), which is cap-tier -- hence the one new edge. A second declared dependent, chronica-adapter-api-server, was CONFIRMED DEAD by grepping its own src/ and tests/ for chronica_cap_crm_support_metadata_view: zero hits of any kind, so its manifest dep + db feature-array entry were removed outright rather than repointed, per the standing LOOP-21 precedent -- it still enables the new adapter's db feature transitively through chronica-cap-platform-admin-routes/db, and its five live-DB route tests exercise the routes over HTTP, not the crate directly. STALE-CLAIM CORRECTIONS (long-gone flat crate names, fixed rather than carried on): the donor's .chronica caller_entries named crates/chronica-api/src/routes/metadata.rs and crates/chronica-crm/src/store/mod.rs with chronica-api as the runtime owner; its covered_sources and all five verification witnesses named crates/chronica-api/... paths; and its CAPABILITY.md claimed chronica-crm re-exports it as chronica_crm::store::view_metadata_store. Neither chronica-api nor chronica-crm exists in this repo (re-confirmed: crates/ holds only adapter/, cap/, core/, infra/ and runtime/), so all were replaced with real current paths -- the five witness test-fn names were each verified to exist in crates/adapter/chronica-adapter-api-server/tests/metadata_views_route.rs before being written down. The donor .chronica's supersedes entry still names the flat chronica-crm file and was deliberately RETAINED as extraction provenance, annotated as historical. LIVE-DB GENUINENESS PROVEN BY COMPARISON, not asserted: the five route tests were run twice against the SAME prebuilt binary (target/debug/deps/metadata_views_route-6dc177d2fa24706f) -- with DATABASE_URL unset they took the harness skip path and finished in 0.00s; with DATABASE_URL=postgres://chronica:chronica@127.0.0.1:5432/chronica they ran for real in 2.37s, and a follow-up psql query against view_metadata confirmed they had actually written rows (the table held 0 rows before the run and 3 after, one of them soft-deleted (deleted_at IS NOT NULL) and one carrying a fields/groups jsonb that round-tripped as jsonb_array_length 2 and 1) -- so the create/list/get/soft-delete paths all really touched Postgres. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_analytics_alert_rule_split_2026_09_08m": "119 -> 118: crates/core/chronica-cap-crm-support-analytics-alert-rule's responsibility-tier decomposition (LOOP 62). SPLIT, not relocated -- the determination was made by READING the actual current 279-line src/lib.rs in full, NOT by inheriting the outcome of the sibling crate its own module doc claims to mirror (\"Mirrors chronica-cap-crm-support-analytics-action\", LOOP 60) and not by trusting a prior loop's scouting note. That claim turned out to be true, but it was re-derived rather than assumed. Although the whole file sat inside a single #[cfg(feature = \"db\")] mod imp (the shape LOOP 52/53/54/56's relocations had), the crate declares FOUR pub types of its own -- AlertRuleRow (with its pure to_rule domain rehydration and its pure camelCase to_json operator view), NewRule, CreateRuleOutcome and UpdateRuleOutcome -- plus three pure operators: parse_comparison (wire token -> ported ComparisonOp, unknown -> None), comparison_str (the canonical stored token, matching the 0070 migration's CHECK) and validate (the whole name/metric/comparison body check, returning the canonical op). None of the three touches a PgPersistence. That is real retained pure surface, so the donor was kept and shrunk rather than renamed away, exactly as in LOOP 50/55/57/58/60/61. THE FACADE-PURITY QUESTION WAS SETTLED RATHER THAN ASSUMED: like LOOP 60's and LOOP 61's donors (and unlike LOOP 58's, which retained no foreign type at all), this one DOES name foreign types -- AlertRule as to_rule's return, and ComparisonOp taken/returned by parse_comparison, comparison_str and validate, all owned by chronica-core-analytics-alerts. That crate's Cargo.toml was read directly for this loop: its only dependencies are chronica-core and serde, it declares NO [features] section at all and no sqlx, so it takes chronica-core at its DEFAULT feature set -- and crates/core/chronica-core/Cargo.toml shows default = [], its db feature (the only one it has) being opt-in and forwarding only chronica-core-persistence/db + chronica-core-version/db. No sqlx and no db feature reaches the donor through that edge, so the retained surface is pure, not pure-by-re-export-of-something-impure. The donor's chronica-core and sqlx dependencies were DROPPED OUTRIGHT along with its db feature, leaving chronica-core-analytics-alerts + serde_json as its only dependencies and zero cargo features. The real mechanics moved BYTE-IDENTICAL into the new crates/adapter/chronica-adapter-crm-support-analytics-alert-rule, verified by SHA-256 against git show HEAD:crates/core/chronica-cap-crm-support-analytics-alert-rule/src/lib.rs in three separately hashed non-contiguous spans, anchored by grep -n '^#[cfg(feature = \"db\")]' with the ^ anchor to exclude the module doc's line-12 prose mention of the same string (the real attributes are at pre-change lines 15 and 278). Span A, the RULE_COLS projection constant with its doc comment, pre-change lines 24-27 -- sha256 7b8a2be6403765bda85b53c873b864dab810dab1fcb8b4bc4d695a5c6a71e184, MOVED WITH ZERO EDITS (it kept its original 4-space mod imp indentation), hashes identically on both sides. Span B, the row mapper, pre-change lines 67-79 -- sha256 64ff2d4cba54d48ca5387d24bd55b6f0bc9f1875577e93c3901747e8941926ee, 13382e36adee5468003df6b66d5be99fdd478b7e428292cc381184d3bfc8ebb1 as it now stands in the adapter, and reversing this loop's three mechanical edits to it (re-indent 4, map_row -> map, AlertRuleRow return type -> Self) reproduces the pre-change digest EXACTLY. Span C, the six async function bodies (create_rule, list_rules, get_rule, update_rule, delete_rule, evaluate_rule) with their SQL strings, binds and CoreError::from mapping, pre-change lines 149-275 -- sha256 8f6208e29c15260571b7dd88702b601cb65f5c7f8140c95a70dec4cba00861e2 pre-change and 02a93010f2d6ea67e9cc3a54bde205bd45ebe5b7a2cfc211c4f61343e7f882fd in the adapter; reversing the ONE mechanical rename below (4 occurrences on 4 lines) reproduces the pre-change digest EXACTLY, so that is provably the only difference across all 127 lines -- no rustfmt reflow was applied here (unlike LOOP 61, where one was unavoidable): map_row is SHORTER than AlertRuleRow::map, so rustfmt would now join the two call sites that the pre-change source had wrapped across lines, and those two were deliberately LEFT at their original wrapping precisely so the span stays byte-identical modulo the rename alone. Same deliberate non-rustfmt-clean choice as LOOP 60's adapter, which carries the identical wrapped map_row(&row), line. TWO DISCLOSED MECHANICAL EDITS, not hidden: (1) AlertRuleRow::map -- a PRIVATE associated fn -- became the free fn map_row in the adapter, because Rust's orphan rule forbids an inherent impl AlertRuleRow outside the crate that declares the type; its body is byte-identical modulo a uniform 4-space dedent (it lost one nesting level) plus spelling out the -> Self return type as -> AlertRuleRow (a bare Self has no meaning outside an impl block), and its 4 call sites moved with the rename. The helper was, and stays, crate-private -- no public API changed. (2) comparison_str and validate were private fns in the donor's mod imp; validate's only consumers (create_rule, update_rule) moved to the adapter, and comparison_str is consumed BOTH by those moved writers and by the donor's retained to_json, so both are now pub in the donor and re-exported there. parse_comparison deliberately did NOT change visibility -- its only consumers (to_rule and validate) both stayed -- and remains private, so the split widened exactly two items and no more. The operators themselves are unchanged, and all nine retained pure pieces were independently re-verified as present VERBATIM (modulo those two pub prefixes and the uniform dedent) in the new donor. Two import-list lines also shrank, disclosed: serde_json was dropped from the adapter ENTIRELY (manifest and imports -- the moved use serde_json::{json, Value} was only ever needed by to_json, which stayed, and no moved item reads or writes a json column), and AlertRule/ComparisonOp were dropped from the moved use chronica_core_analytics_alerts::{AlertRule, ComparisonOp} because every remaining reference to them in the adapter is inferred (validate's returned op) or an inherent method call (row.to_rule().would_fire(..)), never a named type -- so, as in LOOP 60 and unlike LOOP 61, the adapter declares NO manifest edge to that domain crate at all. misplaced_cap_package_count is UNCHANGED at 222 by design: a SPLIT keeps the donor chronica-cap-*-named under crates/core/, unlike a RELOCATION; scoring M2 here would require relocating genuinely pure types into the adapter tier, which would be metric-gaming. world_graph_live_dependency_count unchanged at 5; cap_directory_count unchanged at 36. layer_debt_edge_count 119 -> 120: ONE new forbidden edge, chronica-cap-platform-misc-routes (cap) -> chronica-adapter-crm-support-analytics-alert-rule (adapter), disclosed by hand in tools/refoundation/refoundation-layer-debt.json's _note_2026_09_08_loop62 (written by hand, NOT via --write-baseline). CALLERS WERE FOUND BY EXACT FULLY-QUALIFIED-PATH GREP, not by bare function name: a repo-wide grep for both the underscore and hyphen crate spellings found exactly ONE real (non-test) production caller, crates/cap/chronica-cap-platform-misc-routes/src/analytics/alert_rule.rs (7 fully-qualified references -- the use line plus the six axum handlers behind POST|GET /api/companies/:company_id/analytics/alert-rules, GET|PUT|DELETE .../alert-rules/:id and POST .../alert-rules/:id/evaluate), which is cap-tier -- hence the one new edge. A second declared dependent, chronica-adapter-api-server, was CONFIRMED DEAD by grepping its own src/ and tests/ for analytics_alert_rule and alert_rule: the single hit was a `cargo test -p chronica-api ...` line inside a doc comment, so its non-optional manifest dep + db feature-array entry were removed outright rather than repointed, per the standing LOOP-21 precedent -- it still enables the new adapter's db feature transitively through chronica-cap-platform-misc-routes/db, and its five live-DB route tests exercise the routes over HTTP, not the crate directly. STALE-CLAIM CORRECTIONS (long-gone flat crate names, fixed rather than carried on): the donor's .chronica caller_entries named crates/chronica-api/src/routes/analytics/alert_rule.rs, crates/chronica-api/src/routes/analytics/mod.rs and crates/chronica-crm/src/store/mod.rs with chronica-api as the runtime owner; its covered_sources and all six verification witnesses named crates/chronica-api/... paths; and its CAPABILITY.md claimed chronica-crm re-exports it as chronica_crm::store::analytics_alert_rule_store, as did the root Cargo.toml workspace-dependency comment. Neither chronica-api nor chronica-crm exists in this repo (re-confirmed by repo-wide grep), so all were replaced with real current paths -- the five witness test-fn names were each verified to exist in crates/adapter/chronica-adapter-api-server/tests/analytics_alert_rule_route.rs before being written down. The donor .chronica's supersedes entry still names the flat chronica-crm file and was deliberately RETAINED as extraction provenance, annotated as historical. LIVE-DB GENUINENESS PROVEN BY COMPARISON, not asserted: the five route tests were run twice against the SAME prebuilt binary (target/debug/deps/analytics_alert_rule_route-e887011172766be2) -- with DATABASE_URL unset all five printed the harness skip line and the suite finished in 0.00s; with DATABASE_URL=postgres://chronica:chronica@127.0.0.1:5432/chronica they ran for real in 1.39-1.92s, and a psql query against analytics_alert_rule confirmed they had actually written rows (the table was truncated to 0 rows before the run and held 4 after -- three gt/error_rate/100 rules, one of them with enabled = false, plus one lt/signups/10 rule, exercising the create/list/get/update/delete and disabled-rule would_fire paths against real Postgres). See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_social_pages_split_2026_09_08_loop71": "110 -> 109: crates/core/chronica-cap-social-pages's responsibility-tier decomposition (LOOP 71). SPLIT, not relocated -- the determination was re-derived by reading the donor's ACTUAL current src/lib.rs (323 lines) in full, NOT inherited from the recon note. GATING SHAPE: unlike LOOP 70 (a whole-file `#![cfg(feature = \"db\")]` inner attribute), this donor used the LOOP 59-69 shape -- a single `#[cfg(feature = \"db\")] mod imp { ... }` (pre-split line 19) wrapping EVERYTHING, pure and impure alike, at indentation 4, closed by `#[cfg(feature = \"db\")] pub use imp::*;` at the bottom. The brief's anchoring command `grep -n '^#\\[cfg(feature = \"db\")\\]'` was run FIRST against `git show HEAD:` output and returned exactly lines 18 and 322 -- the two real attribute sites, with no doc-comment false positive, because this donor's module doc says only \"Gated behind the `db` feature\" in prose and never quotes the attribute. WHY A SPLIT AND NOT A RELOCATION: the donor declares NINE public items of its own that name no PgPersistence, await nothing and issue no statement -- PageRow (the persisted row projection) with its camelCase to_json egress view, NewPage and UpdatePage (the request-body shapes), CreatePageOutcome and UpdatePageOutcome (the route verdicts, 201/409/422 and 200/404/422), and the four pure operators page_error_reason (PageError -> route message), validate_blocks (the CLOSED-SET PageBlock parse that is this capability's whole NO-EXECUTABLE-CONTENT security property, and the sole enforcement of the MAX_BLOCKS bound), looks_like_uuid (the id-vs-slug lookup decision) and visibility_token (the PageVisibility default). Two of the donor's own pre-existing unit tests exercise validate_blocks directly, so the split moved ZERO tests. The donor was therefore kept and SHRUNK to exactly that pure surface rather than renamed away, exactly as in LOOP 50/55/57/58/59/60/61/62/63/64/65/66/67/68/69/70. THE REAL MECHANICS moved BYTE-IDENTICAL into the new crates/adapter/chronica-adapter-social-pages: create_page, list_pages, get_page, update_page and delete_page over the social_pages table (migration 0082_social_pages.sql), the PAGE_COLS projection constant, the PAGE_LIST_LIMIT bound and the PgRow decoder. SHA-256 verified span by span against `git show HEAD:crates/core/chronica-cap-social-pages/src/lib.rs` (each digest is of the span's exact bytes plus one trailing newline), and re-verified off disk after every subsequent edit including the rustfmt pass: (A) PAGE_COLS + PAGE_LIST_LIMIT, pre-split lines 26-31, 2d0ff1c4d6d732e7ab99e82d454b741fa0b4b05f9338e1c5e8b5c2324cf55f68 BEFORE AND AFTER with ZERO edits (the adapter reproduces the donor's own `mod imp` nesting, so indentation was preserved exactly); (B) the PageRow decoder, pre-split lines 49-62, 2b42a8e7906f03dc611d5910981a1893ba64e4361919160a9e5dbbb511ac5c68 -> bf8f9cd8016b417db3a9793c5634d76034ba988860080d6d0922546f9ca4c324; (C) create_page + list_pages + get_page + update_page + delete_page, pre-split lines 170-297 CONTIGUOUS (128 lines), 283342338a69e8a001826cfcb672cc53def7be8973c2b94b25817c56f347d424 -> 8ea0a9e1bbf8ae8a10b24a34a2fc26d570dfd21c25576b7e44e0db15438c4c43 after the FOUR PageRow::map -> map_page_row renames, the only differences in that span -- no SQL string, no bind, no bind order, no tenant predicate and no control flow changed. ORPHAN-RULE RENAME, forced not chosen: PageRow::map was a PRIVATE associated fn on the row type, and Rust's orphan rule (E0116) forbids an inherent impl on a foreign type once PageRow lives in the donor crate, so it became the free crate-private map_page_row in the adapter -- byte-identical modulo a uniform 4-space dedent (it left an impl block) plus spelling out the `-> Self` return type as `-> PageRow`. map_page_row is deliberately EXACTLY as long as PageRow::map (12 characters each) so no rewritten line changed width and rustfmt reflowed nothing inside span C. The generating script asserted the reversal of every edit and the exact occurrence count (4) before rewriting. THE RETAINED spans were dedented by a uniform 4 spaces as they came out of `mod imp` to top level, with the reversal likewise asserted: R1 PageRow (lines 33-46) 58c6f4406047c75f7df7a0ac1d422c532d49d3ddfca87c18d5f3909fd3a67391 -> 039e86f8d035015f400b49c5a5f16b8727f9480f5bb8ed58576f290ccdf24686; R2 to_json (64-78) 0920e4efb3670b77b637ecf7dc0b29904c2285649606a2952034ffad02d64580 -> 4369726538f16471bfe0d3cbf0233a0f2f3b71034c385f02c19711ed79b2f22d; R3 NewPage/UpdatePage/both outcomes (81-119) 7fab58e3446915a056506805b619413e8722cf8eda228c975ea1799741848ab5 -> b6c0eb31ed941d11fbfa32491ebde62e95bb4fe480585d05cce1310fe5fa637a; R4 the four operators (121-168) 04d4e0f3f13616b639f7576818865ec869344197b38173a16a6fc8885e84d4c1 -> 88cb884dc1aa1b1a69ab12b77d0811fe35a0d115844d0773ecf99b0715578f51 (dedent PLUS the four private -> pub widenings, disclosed below); R5 the two pure unit tests (299-319) 20c35d02ce5b948800ea4fe07a990cde3ca91c223473ec47c2aabca7022541ec -> 70e2620a0da360cba526d79df5a7134ddd99cf90fc2caf20ebbf51bdd427c777. FOUR ITEMS WERE WIDENED from private to pub -- page_error_reason, validate_blocks, looks_like_uuid, visibility_token -- because their only consumers moved across the new crate boundary; all four keep byte-identical bodies and only their visibility changed, additively, matching LOOP 67's precedent. WHAT DROPS THIS CRATE OUT OF M7: its `db` feature, its whole [features] table and its sqlx dependency were deleted OUTRIGHT, and so were its now-unused chronica-core dependency (nothing retained names CompanyId/CoreError/CoreResult/PgPersistence) and its tokio dev-dependency (both retained tests are synchronous; tokio was dead even before this loop, verified by grepping the whole donor crate for `tokio` -- the only hit was the manifest line itself). Two dependencies remain, chronica-core-social-domain and serde_json, and there are no dev-dependencies. FACADE-PURITY VERIFIED TWO LEVELS DEEP IN THE CURRENT TREE, not assumed and not cited from an earlier loop, because the retained surface names foreign types (PageBlock, PageError, PageVisibility, MAX_BLOCKS): chronica-core-social-domain carries NO sqlx line and NO [features] section at all (its own manifest comment states the domain layer deliberately never had a `db` feature), so it takes chronica-core, chronica-core-projection-engine, chronica-core-projection-runtime, chronica-cap-identity-credentials, serde, serde_json, chrono, sha2, quick-xml and thiserror at their DEFAULT feature sets and never forwards chronica-core/db; one level deeper, chronica-core-projection-engine, chronica-core-projection-runtime and chronica-cap-identity-credentials each carry NO sqlx line and NO [features] section either (identity-credentials' only `sqlx` occurrence anywhere in its manifest is inside a prose comment asserting it has no sqlx dependency), and chronica-core is `default = []` with its db feature -- and therefore all sqlx -- opt-in. Confirmed empirically in Cargo.lock, whose chronica-cap-social-pages package entry lost its sqlx and chronica-core lines in this same change. misplaced_cap_package_count is deliberately UNCHANGED at 222 (core=222, runtime=0, adapter=0): the donor legitimately stays under crates/core/ because what it now holds is genuinely pure vocabulary, and the new crate is created under crates/adapter/ where it does not count toward the misplaced-cap metric. world_graph_live_dependency_count unchanged at 5. The paired layer-debt disclosure (three new cap -> adapter edges, 128 -> 131) is in tools/refoundation/refoundation-layer-debt.json's _note_2026_09_08_loop71.",
  "_social_author_timeline_relocation_2026_09_08_loop73": "misplaced_cap_package_count 222 -> 221 (by_root.core 222 -> 221) AND forbidden_core_io_count 108 -> 107, BOTH in this one change: crates/core/chronica-cap-social-author-timeline (LOOP 73) was NOT split -- it physically RELOCATED, whole, to crates/adapter/chronica-adapter-social-author-timeline, and was renamed to match its tier. TEMPLATE DETERMINATION, re-derived from the source and not inherited from any recon note: the brief for this loop offered BOTH templates and ordered that the outcome be derived by reading the crate's actual current source first. All 335 lines of its src/lib.rs were read in full. The crate declares NO type, enum, trait, const, static, trait or type alias of its own -- grep-verified, not eyeballed: `^\\s*(pub )?(struct|enum|trait|type|static|const) ` over the file at HEAD 3814754e returns ZERO matches. Its ENTIRE content is one `#[cfg(feature = \"db\")] mod imp { ... }` block (attribute on line 38) closed by `#[cfg(feature = \"db\")] pub use imp::*;` on lines 334-335, holding exactly ONE production item -- `pub async fn list_author_timeline(pg: &PgPersistence, company: &CompanyId, author_did: &str, limit: Option<i64>, cursor: Option<i64>) -> CoreResult<Vec<NoteRow>>`, which formats a SQL string, binds it through sqlx::query and awaits fetch_all -- plus six #[ignore]d live-DB tests. Every non-primitive name in it is FOREIGN: NoteRow / clamp_note_limit / NOTE_COLS are owned by the separate, ALREADY-EXTRACTED chronica-cap-social-notes crate (which was NOT touched and did NOT move), and CompanyId / CoreError / CoreResult / PgPersistence by chronica-core. With the db feature off the crate compiles to a library exporting literally nothing (independently confirmed: `cargo check -p chronica-adapter-social-author-timeline` with no features is clean). A SPLIT would therefore have left a donor with a literally empty public API, so the RELOCATION template (LOOP 42/46/47/48/52/53/54/56 shape) was applied instead of the LOOP 59-72 SPLIT template. WHY M7 REALLY DECREASES: the sqlx call did not disappear and was not hidden behind an indirection -- it is the SAME BYTES at a crates/adapter/ address. src/lib.rs's executable region, from the first `^#[cfg(feature = \"db\")]` line to EOF, hashes to SHA-256 e1d70adf87d2b8d0a6a003a69a6261ed848b076f548f4359d34b7f036ee5b72c both before (`git show HEAD:crates/core/chronica-cap-social-author-timeline/src/lib.rs | tail -n +38`) and after (`tail -n +54` of the relocated file). Only the module doc above that anchor was rewritten; the `^` anchor was used deliberately so that the new doc's own prose mentions of the attribute cannot shift it. WHY M2 REALLY DECREASES: M2 counts chronica-cap-*-named packages living OUTSIDE crates/cap/, and this package both STOPPED being chronica-cap-*-named AND moved to the directory matching its actual tier -- not a rename-to-dodge. Dependencies and feature wiring (chronica-core, optional chronica-cap-social-notes, optional sqlx, `default = []` / `db = [\"dep:sqlx\", \"dep:chronica-cap-social-notes\", \"chronica-core/db\", \"chronica-cap-social-notes/db\"]`) were carried across UNCHANGED, and maturity stays RUNTIME_BOUND, deliberately NOT PURE: the crate still performs real Postgres I/O, and calling it PURE would be false. CALLERS: all three real callers (social_author_timeline.rs, social_agent_profile.rs, social_rss.rs) live in crates/cap/chronica-cap-social-routes and were repointed from chronica_cap_social_author_timeline:: to chronica_adapter_social_author_timeline:: -- found by an EXACT fully-qualified-path grep, never a bare function name. Being cap-tier callers of an adapter-tier crate, this costs exactly ONE new layer-debt edge, `chronica-cap-social-routes (cap) -> chronica-adapter-social-author-timeline (adapter)`, copied verbatim from the gate's own NEW FORBIDDEN EDGE output and disclosed in tools/refoundation/refoundation-layer-debt.json's _note_2026_09_08_loop73 (edge count 133 -> 134); that same route crate already carries eight edges of this identical class. chronica-adapter-api-server's direct dependency on the old crate was DEAD (zero hits for the exact path across its src/ and tests/) and was removed OUTRIGHT rather than repointed to inflate a caller count, per the standing LOOP-21 precedent; its db-feature effect survives transitively via chronica-cap-social-routes/db. Both numbers were hand-edited here in the SAME change that caused them (the gate itself demands it); `--write-baseline` was NEVER run.",
  "_social_conversations_split_2026_09_08_loop72": "109 -> 108: crates/core/chronica-cap-social-conversations's responsibility-tier decomposition (LOOP 72). SPLIT, not relocated -- the determination was re-derived by reading the donor's ACTUAL current src/lib.rs (323 lines) in full, NOT inherited from any recon note. GATING SHAPE: like LOOP 71 and unlike LOOP 70 (a whole-file `#![cfg(feature = \"db\")]` inner attribute), this donor used the LOOP 59-69/71 shape -- a single `#[cfg(feature = \"db\")] mod imp { ... }` (pre-split line 29, attribute on line 28) wrapping EVERYTHING, pure and impure alike, at indentation 4, closed by `#[cfg(feature = \"db\")] pub use imp::*;` at the bottom. The anchoring command `grep -n '^#\\[cfg(feature = \"db\")\\]'` was run FIRST against `git show HEAD:` output and returned exactly lines 28 and 322 -- the two real attribute sites, with no doc-comment false positive, because this donor's module doc says only \"Gated behind the `db` feature\" in prose and never quotes the attribute. WHY A SPLIT AND NOT A RELOCATION: the donor declares a public type of its OWN that names no PgPersistence, awaits nothing and issues no statement -- ConversationRow, a struct it DEFINES (eight pub fields: id, company_id, recipient_user_did, participant_user_dids, last_note_id, unread, created_at, updated_at) rather than re-exports from a foreign crate, carrying the pure camelCase to_json egress projection the HTTP route calls on every returned row. That is genuine local pure vocabulary, so the donor was kept and SHRUNK to exactly that surface rather than renamed away, exactly as in LOOP 50/55/57/58/59/60/61/62/63/64/65/66/67/68/69/70/71. (Contrast LOOP 52/53/54/56, where the donor wrapped only FOREIGN types and the whole crate relocated.) THE REAL MECHANICS moved BYTE-IDENTICAL into the new crates/adapter/chronica-adapter-social-conversations: record_note_in_conversations (the extract-on-note-create hook the note substrate calls best-effort post-commit), list_conversations, mark_conversation_read and unread_conversation_count over the social_conversations table (migration 0401_social_conversations.sql), the CONVERSATION_COLS projection constant and the PgRow decoder. SHA-256 verified span by span against `git show HEAD:crates/core/chronica-cap-social-conversations/src/lib.rs` (each digest is of the span's exact bytes plus one trailing newline), and independently re-derived off disk after every subsequent edit: (A) CONVERSATION_COLS with its doc comment, pre-split lines 36-38, 3a341ba7ddf95332e39b882ba2e6fbf4beecabb519c80cd71eb519fa7693a4fd BEFORE AND AFTER with ZERO edits (the adapter reproduces the donor's own `mod imp` nesting, so the original 4-space indentation was preserved exactly); (B) the ConversationRow decoder, pre-split lines 54-65, ddff1037bb969cf46ed5c0031fe50310a520a02a7574aa12443f15e7326623d0 -> b86f7beb992b456af277499caced0b5649942ebfbbfe929a2cfcd9f864504203; (C) record_note_in_conversations + list_conversations + mark_conversation_read + unread_conversation_count, pre-split lines 80-188 CONTIGUOUS (109 lines, doc comments and blank separators included), d47e336cc45eb64df99222d4ce23b1e4cba929e9bc70a5d17352a35b354a82e6 -> 88f34265eb4ac3c34e6847527367e186a591b6de6af5a2e09e4926078b166752 after the ONE ConversationRow::map -> map_conversation_row rename, the only difference in that span -- no SQL string, no bind, no bind order, no tenant predicate, no ON CONFLICT target and no control flow changed; (D) the whole `mod db_tests` block (all five #[ignore]d live-DB tests), pre-split lines 190-319, 9d4ef11bc057091b8ae5cfacb1e70c53e825694aeb42649fa95ac9dfe480d7ca BEFORE AND AFTER with ZERO edits -- they reach the moved ops through `use super::*` exactly as before, and every ConversationRow field they read was already pub, so the new crate boundary is invisible to them. ORPHAN-RULE RENAME, forced not chosen: ConversationRow::map was a PRIVATE associated fn on the row type, and Rust's orphan rule (E0116) forbids an inherent impl on a foreign type once ConversationRow lives in the donor crate, so it became the free crate-private map_conversation_row in the adapter -- byte-identical modulo a uniform 4-space dedent (it left an impl block) plus spelling out the `-> Self` return type as `-> ConversationRow`. map_conversation_row is deliberately EXACTLY as long as ConversationRow::map (20 characters each) so the one rewritten call site kept its width and rustfmt reflowed nothing inside span C. The generating script asserted the reversal of every edit (undoing the rename/dedent reproduces the pre-split digest exactly) and the exact occurrence count (1 each) before rewriting. THE RETAINED spans were dedented by a uniform 4 spaces as they came out of `mod imp` to top level, with the reversal likewise asserted: R1 ConversationRow with its doc comment (pre-split lines 40-51) 704d7ae027cbbe311f0dfa1e2909cd472b7dc6b3e4c58bfbf8b61381a0bea8e1 -> 022e7d1675bc2b9f61a8ff214841bd2cb66d27aac2f78041485804125fde030d; R2 to_json with its doc comment (pre-split lines 67-77) c9e1c142351fd9a706bf0f39c41b043c7b16caa8e961b8d96a693274296c7136 -> 6afae27ad4b97246b51f5fe1c124f9ce6c3f368d472555121d37328a34aeef13. NOTHING was widened from private to pub by this split: ConversationRow and all eight of its fields, plus to_json, were ALREADY pub, so unlike LOOP 67/71 there is no visibility widening to disclose; the only visibility-shaped consequence ran the other way, with the private ConversationRow::map leaving the crate entirely. WHAT DROPS THIS CRATE OUT OF M7: its `db` feature, its whole [features] table and its sqlx dependency were deleted OUTRIGHT, and so were its now-unused chronica-core dependency (nothing retained names CompanyId/CoreError/CoreResult/PgPersistence) and BOTH its dev-dependencies -- the db-featured chronica-core and tokio -- because the only async tests left with the SQL. EXACTLY ONE dependency remains (serde_json) and there are no dev-dependencies. FACADE-PURITY VERIFIED IN THE CURRENT TREE, not assumed and not cited from an earlier loop: the single retained edge is serde_json, a leaf third-party crate that cannot reach chronica-core let alone chronica-core/db, and nothing retained here names a foreign chronica type at all, so there is no deeper facade to audit. Confirmed empirically in Cargo.lock, whose chronica-cap-social-conversations package entry is now exactly `dependencies = [ \"serde_json\" ]` -- its chronica-core, sqlx and tokio lines are gone in this same change. CALLERS: the exact fully-qualified-path grep `chronica_cap_social_conversations::` found TWO real (non-dev) call sites and no others -- crates/cap/chronica-cap-social-routes/src/social_conversations.rs (lines 51 and 73: list_conversations, mark_conversation_read) and crates/core/chronica-cap-social-notes/src/imp/create.rs (line 212: record_note_in_conversations). Both were repointed onto the adapter, which re-exports ConversationRow UNCONDITIONALLY (outside its own db gate) so each caller needs exactly one crate path and its former direct donor dependency was DROPPED rather than kept alongside. A third manifest, crates/adapter/chronica-adapter-api-server/Cargo.toml, declared the donor plus chronica-cap-social-conversations/db in its db feature array but never named it anywhere in its own src/ or tests/ (its only social-conversation reference goes through chronica-cap-social-routes' re-export), so that genuinely DEAD dependency was removed outright per the standing LOOP-21 precedent. misplaced_cap_package_count is deliberately UNCHANGED at 222 (core=222, runtime=0, adapter=0): the donor legitimately stays under crates/core/ because what it now holds is genuinely pure vocabulary, and the new crate is created under crates/adapter/ where it does not count toward the misplaced-cap metric. world_graph_live_dependency_count unchanged at 5. The paired layer-debt disclosure (two new edges -- one cap -> adapter, one core -> adapter -- 131 -> 133) is in tools/refoundation/refoundation-layer-debt.json's _note_2026_09_08_loop72.",
  "_crm_support_analytics_cohort_split_2026_09_08_loop74": "107 -> 106: crates/core/chronica-cap-crm-support-analytics-cohort's responsibility-tier decomposition (LOOP 74). SPLIT, not relocated -- the determination was re-derived by reading the donor's ACTUAL current src/lib.rs (317 lines) in full with the Read tool, NOT inherited from any recon note (and this crate had in fact been flagged 'deferred' by an earlier recon pass, a flag that was re-checked rather than trusted: its one real caller, chronica-cap-platform-misc-routes, is an ordinary cap-tier HTTP route crate of exactly the kind LOOP 54/58/60/62/63/69/71/72 already repointed, so the crate is attemptable today and the deferral no longer applies). GATING SHAPE: like LOOP 71/72/73 and unlike LOOP 70 (a whole-file `#![cfg(feature = \"db\")]` inner attribute), this donor used the LOOP 59-69 shape -- a single `#[cfg(feature = \"db\")] mod imp { ... }` (attribute on pre-split line 19, `mod imp {` on line 20) wrapping EVERYTHING, pure and impure alike, at indentation 4, closed by `#[cfg(feature = \"db\")] pub use imp::*;` on pre-split lines 316-317. The anchoring command `grep -n '^#\\[cfg(feature = \"db\")\\]'` was run FIRST against `git show HEAD:` output and returned exactly lines 19 and 316 -- the two real attribute sites, with no doc-comment false positive, because this donor's module doc quotes only `#[cfg(feature = \"db\")] pub mod store;` (chronica-crm's pre-extraction gate) in prose, which the `^` anchor cannot match; `grep -n '^#!\\[cfg(feature = \"db\")\\]'` returned nothing, confirming there is no whole-file gate. WHY A SPLIT AND NOT A RELOCATION: `grep -nE '^\\s*(pub )?(struct|enum|trait|type|static|const) '` over the ORIGINAL file returns SIX matches, and four of them are genuine local vocabulary this crate DEFINES rather than re-exports, none of which names a PgPersistence, awaits anything or issues a statement -- CohortRow (nine pub fields: id, company_id, name, description, deleted, definition, version, created_at, updated_at) carrying BOTH the pure to_cohort rehydration into the ported chronica_core_analytics_cohort::ManagedCohort domain AND the pure camelCase to_json egress projection the HTTP route calls on every returned row; NewCohort, the validated request body the route's own cohort_body parser constructs; and the two route-verdict enums CreateCohortOutcome (Created/Invalid -> 201/422) and UpdateCohortOutcome (Updated/NotFound/Invalid -> 200/404/422) that the handlers match on to choose status codes. That is genuine local pure vocabulary, so the donor was kept and SHRUNK to exactly that surface rather than renamed away, exactly as in LOOP 50/55/57/58/59/60/61/62/63/64/65/66/67/68/69/70/71/72. (Contrast LOOP 52/53/54/56/73, where the donor wrapped only FOREIGN types and the whole crate relocated; the other two grep matches, the EVENT_SCAN_LIMIT and COH_COLS consts, are I/O parameters and moved with the statements.) THE REAL MECHANICS moved BYTE-IDENTICAL into the new crates/adapter/chronica-adapter-crm-support-analytics-cohort: create_cohort, list_cohorts, get_cohort, update_cohort, soft_delete_cohort and compute_members over the analytics_cohort table (migration 0072_analytics_cohorts.sql), plus the COH_COLS projection constant, the EVENT_SCAN_LIMIT = 100_000 behavioral-scan bound, the parse_event_kind / parse_scope persisted-text decoders, the validate_definition write-path canonicalizer, the company-scoped load_company_stream event loader and the PgRow decoder. SHA-256 verified span by span against `git show HEAD:crates/core/chronica-cap-crm-support-analytics-cohort/src/lib.rs` (each digest is of the span's exact bytes plus one trailing newline), and independently re-derived off disk after the files were written: (A) the leading REUSE comment + EVENT_SCAN_LIMIT + COH_COLS + parse_event_kind + parse_scope, pre-split lines 30-56 CONTIGUOUS, 1546360f8514b4c1aa07f959c9e1adfa6db8bcaaf337effa4ef695fcd73130d5 BEFORE AND AFTER with ZERO edits (the adapter reproduces the donor's own `mod imp` nesting, so the original 4-space indentation was preserved exactly); (B) the CohortRow PgRow decoder, pre-split lines 73-85, 6cc08189df60d0b33b6b4d4c8b60ccbf6098cb47343222cece5fcd0f3b2c86b8 -> 38369f71cbe6671a13f82f14cb4709b7e257a10301e1c42cafc2564b4a4e4903 after a uniform 4-space dedent (it left an impl block) plus the forced `map` -> `map_cohort_row` / `-> Self` -> `-> CohortRow` signature change; (C) validate_definition with its two doc-comment lines, pre-split lines 143-149, 54bf9384c940ae1e6273f19a33e850bac43d5957609fe6926afa57ec9160fc4b BEFORE AND AFTER with ZERO edits; (D) create_cohort + list_cohorts + get_cohort + update_cohort + soft_delete_cohort + load_company_stream + compute_members, pre-split lines 151-313 CONTIGUOUS (163 lines, doc comments and blank separators included), d0f671893847c65454b3a5304d30c39a327be0d58447c593a6b0b1f05c986647 -> 6979d207fce996f5a9020c9215d64907eca75d05bb61e3f8c48e2acaf934d204 after the FOUR CohortRow::map -> map_cohort_row call-site renames, the only difference in that span -- no SQL string, no bind, no bind order, no tenant predicate, no RETURNING list and no control flow changed, and the generating script additionally asserted that all 163 lines kept their EXACT character length. FORCED RENAME, DISCLOSED: CohortRow::map was a PRIVATE associated fn; Rust's orphan rule (E0116) forbids an inherent impl on a now-foreign type, so it became the free crate-private map_cohort_row in the adapter. map_cohort_row is deliberately EXACTLY as long as CohortRow::map (14 characters each), so no call site reflows. It was and remains crate-private; no public API changed. THE RETAINED HALF was dedented by a uniform 4 spaces as it came out of `mod imp` to top level -- CohortRow's declaration (pre-split lines 58-70, 38503158e79fdd9779688c3edb34c36bc16e382e20014e64bc4520e825f12f00 -> b2b327fbe6ef56ccf1d2523eb90db4dc9959580ad07893d1dc7b2a43db3c724d), to_cohort + to_json (pre-split lines 87-115, 387c73b397e11ad7cebc9650b33b9f0f70959a1846ca875057b9f8712e38a148 -> 475a8166f5762003e6438a8e5bb52169c9020a89f268e8aeff5cac483d3b0e6a) and NewCohort + CreateCohortOutcome + UpdateCohortOutcome (pre-split lines 118-141, b95daba375b59f7838b7d0075091ee24b1ab32232e65a25cdbe8be4b661fe7f8 -> 328098560a1d4a3c5bcb8b34a1103025c3b8d5f34291de0ab9f946ca92636632). Every dedent and every rename was REVERSAL-ASSERTED by the generating script before it wrote anything (re-indenting / un-renaming reproduces the pre-split digest exactly), and the script asserted the exact occurrence counts (1 decoder signature, 4 call sites) and that all 317 pre-split lines are accounted for -- 276 inside a hashed span, 41 structural (module doc, use block, `mod imp` wrapper, blank separators) -- so a mismatch would have aborted the split. NOTHING was widened from private to pub: CohortRow, its nine fields, to_cohort, to_json, NewCohort, CreateCohortOutcome and UpdateCohortOutcome were already pub; the movement ran the other way, with the PRIVATE CohortRow::map, parse_event_kind, parse_scope, validate_definition and load_company_stream all leaving the donor entirely. PURITY OF THE RETAINED HALF verified three ways from the current tree, not cited from an earlier loop: (1) MANIFEST -- the donor's `sqlx = { workspace = true, optional = true }` line, its whole [features] table (default = [] / db = [\"dep:sqlx\", \"chronica-core/db\", \"chronica-core-business-events/db\"]), its chronica-core dependency and its chronica-core-business-events dependency were all DELETED OUTRIGHT, leaving exactly TWO dependencies (chronica-core-analytics-cohort, serde_json) and zero dev-dependencies; (2) FACADE-PURITY of the one non-leaf dependency that could have leaked I/O -- chronica-core-analytics-cohort deps chronica-core, chronica-core-business-events and serde all with DEFAULT features, and both of those crates declare `default = []` with sqlx strictly optional, so no sqlx can reach the donor through that edge; (3) EMPIRICALLY IN Cargo.lock, whose chronica-cap-crm-support-analytics-cohort package entry now lists exactly chronica-core-analytics-cohort and serde_json and carries no sqlx line at all (the adapter's entry carries it instead). A new pin test, crates/core/chronica-cap-crm-support-analytics-cohort/tests/money_guard.rs#retained_pure_surface_is_actually_scanned, asserts the twelve retained pure markers are all present in the scanned source (so the pre-existing money scan cannot pass vacuously on a gutted module) AND that sqlx / PgPersistence / PgRow / CoreResult / CompanyId and the three SQL clause forms naming the table (INTO/FROM/UPDATE analytics_cohort) never reappear in the file's CODE (comment-stripping applied first, because the module doc legitimately names all of them in prose to record where the mechanics went). The table is pinned by those clause forms rather than by the bare table name because the bare name is a SUBSTRING of the donor's one remaining real dependency path, chronica_core_analytics_cohort -- a blanket match on it was tried first and was RED for a reason that has nothing to do with I/O, which is exactly the pre-existing-red blanket-prose-match trap LOOP 73 warned about, caught here by running the test rather than by assuming it was sound. The pre-existing donor guard was checked for pre-existing-red blanket prose matches before being kept, per the LOOP 73 precedent: its manifest scan uses a raw `contains` over Cargo.toml for chronica-policy / chronica-runtime / chronica-approvals and its source scan a raw `contains` over src/lib.rs for thirteen money tokens, so the new manifest and module-doc prose were written to avoid every one of those strings; both tests pass green after the split. THE ONE REAL CALLER, crates/cap/chronica-cap-platform-misc-routes/src/analytics/cohort.rs (found by EXACT fully-qualified-path grep for `chronica_cap_crm_support_analytics_cohort::` across the whole repo -- seven hits, all in that one file), was repointed onto the adapter, which re-exports the donor's whole pure surface UNCONDITIONALLY outside its own db gate, so the caller's now-redundant DIRECT dependency on the donor was dropped: one crate path, not two. chronica-adapter-api-server's Cargo.toml dependency on the donor plus its `chronica-cap-crm-support-analytics-cohort/db` feature entry were confirmed DEAD (exact-path grep across its src/ AND tests/ for the crate path and for CohortRow / NewCohort / CreateCohortOutcome / UpdateCohortOutcome / compute_members returned zero hits; its tests/analytics_cohort_route.rs drives the capability purely over HTTP) and REMOVED OUTRIGHT per the standing LOOP-21 precedent. NOT AN M2 CHANGE, and the asymmetry with LOOP 73 is deliberate: misplaced_cap_package_count stays EXACTLY at 221 (by_root.core 221, runtime 0, adapter 0) because the donor package keeps both its chronica-cap-* name and its crates/core/ home -- only a whole-crate relocation moves M2, and this was a split. world_graph_live_dependency_count is likewise unchanged at 5. The repoint creates exactly ONE new forbidden cap -> adapter edge, `chronica-cap-platform-misc-routes (cap) -> chronica-adapter-crm-support-analytics-cohort (adapter)`, copied verbatim from validate-refoundation-layers.mjs's own NEW FORBIDDEN EDGE output and hand-added to tools/refoundation/refoundation-layer-debt.json (134 -> 135) with the dated _note_2026_09_08_loop74 disclosure, in this SAME change. `--write-baseline` was NEVER run.",
  "_crm_support_analytics_funnel_split_2026_09_08_loop75": "LOOP 75 (2026-09-08) responsibility-tier decomposition. forbidden_core_io_count 106 -> 105: crates/core/chronica-cap-crm-support-analytics-funnel -- at 456 lines the largest crate this program has attempted -- stopped declaring real Postgres I/O. It used the same gating shape as LOOP 59-69/71/72/73/74 (CHECKED, NOT ASSUMED, against `git show HEAD:`): a single OUTER `#[cfg(feature = \"db\")]` attribute on line 1 introducing `mod imp {` on line 2 and wrapping EVERYTHING -- pure and impure alike -- at indentation 4, closing on line 453, with a second outer attribute over `pub use imp::*;` on lines 455-456; `grep -n '^#!\\[cfg(feature = \"db\")\\]'` returned NOTHING, so it is NOT a whole-file INNER attribute. Because everything was inside that gate, the crate declared sqlx directly under crates/core/ even though seven of its declarations are genuinely pure. SPLIT, not a relocation: the donor DECLARES FunnelRow, NewFunnel, FunnelStepCount, StepTiming, FunnelComputeResult, CreateFunnelOutcome and UpdateFunnelOutcome -- structs and enums it defines rather than re-exports -- and they are load-bearing for a real caller, so the donor was kept and SHRUNK (456 lines -> 153) rather than renamed away. It retains parse_order, FunnelRow with its step_kinds/order/to_json views, NewFunnel, the three readout shapes and the two route-verdict enums, and now declares exactly THREE dependencies (chronica-core-analytics-funnel, chronica-core-business-events, serde_json), ZERO cargo features and ZERO dev-dependencies -- its sqlx dependency, its whole [features] table, its chronica-core dependency and its time dependency were deleted OUTRIGHT, and Cargo.lock (regenerated by cargo itself, never hand-edited) now lists exactly those three for the package with no sqlx line. The real mechanics -- create_funnel, list_funnels, get_funnel, update_funnel, soft_delete_funnel and compute_funnel over analytics_funnel (migration 0074_analytics_funnels.sql), plus the FUN_COLS projection constant, the EVENT_SCAN_LIMIT = 100_000 scan bound, the order_str / parse_steps / parse_event_kind / kind_str / parse_scope / parse_ts persisted-text codecs, the validate write-path canonicalizer and the private FunnelRow::map PgRow decoder (now the free map_funnel_row there, Rust's orphan rule -- deliberately the SAME 14 characters, so all four rewritten call sites keep their original width and rustfmt reproduces the pre-split line breaks) -- moved BYTE-IDENTICAL into the new crates/adapter/chronica-adapter-crm-support-analytics-funnel (420 lines, of which 305 are the six moved spans). Four of the six moved spans hash IDENTICALLY before and after; the other two differ only by the disclosed orphan-rule rename plus, for the decoder, a uniform 4-space dedent, and undoing exactly those edits reproduces the BEFORE digests byte-for-byte. The exact digests are recorded in the adapter's own src/lib.rs module doc and in the subsystem-convergence ledger entry for LOOP 75. misplaced_cap_package_count is UNCHANGED at 221 and that is correct rather than an omission: this was a SPLIT, so the donor package keeps both its chronica-cap-* name and its crates/core/ home; only a whole-crate relocation (LOOP 73's shape) moves M2, and claiming an M2 win here would have been false. world_graph_live_dependency_count is likewise unchanged at 5. The M7 decrease is real, not definitional: the six statements still exist, still run against the same analytics_funnel table with the same binds and the same company_id predicates, and are still exercised end-to-end against live Postgres -- they simply live at an adapter-tier address now. --write-baseline was NEVER run.",
  "_agent_edit_history_split_2026_09_08_loop76": "LOOP 76 (2026-09-08) responsibility-tier decomposition. forbidden_core_io_count 105 -> 104: crates/core/chronica-cap-agent-edit-history stopped declaring real Postgres I/O. ITS GATING SHAPE IS UNLIKE EVERY PRIOR LOOP IN THIS PROGRAM AND WAS CHECKED, NOT ASSUMED, against `git show HEAD:`: this crate had NO cargo feature gate at all. `grep -n 'cfg(feature'` over BOTH pre-split source files returned NOTHING and `grep -n '\\[features\\]'` over the pre-split Cargo.toml returned NOTHING -- there was no `#[cfg(feature = \"db\")] mod imp` wrapper (LOOP 59-75's shape) and no whole-file inner attribute either. sqlx was a plain, ALWAYS-ON dependency, and the pre-split manifest recorded that as a deliberate choice: \"every item here needs a live sqlx PgPool connection -- sqlx is therefore a plain, always-on dependency: this crate is itself only ever pulled in ... behind ITS OWN db feature gate (one level up)\". That one-level-up reasoning is exactly what put unconditional sqlx under crates/core/. A SECOND IDIOM DIFFERENCE, likewise verified rather than assumed: this capability never went through chronica-core's PgPersistence wrapper -- AgentEditHistoryStore<'a> borrows a RAW sqlx::PgPool and calls sqlx::query_as directly. That idiom was carried across UNCHANGED rather than normalised, so the new adapter crate deps NO chronica-core crate at all. SPLIT, not a relocation: the donor's SECOND FILE, src/edit_prediction_engine.rs, was already a complete, self-contained pure domain module -- EditHistory (an in-memory BTreeMap<String, usize> last-edited-line-per-file tracker), EditHistoryError, and validate_file, the single validation authority BOTH halves call -- with four unit tests of its own, zero sqlx and no caller outside this crate. It is load-bearing (the moved record_edit still calls validate_file, so Rust stays the sole validation authority and SQL stays storage only), so the donor was kept and SHRUNK rather than renamed away. THE CRATE'S OWN DOCUMENTATION WAS READ IN FULL FIRST, per the standing lesson from the abandoned first LOOP 76 attempt against chronica-adapter-social-postgres-batch1: this crate's Cargo.toml header, both module docs and its .chronica/capability.chronica.json contain NO 'DIRECTORY:' section and no deliberate crates/core/-placement rationale -- the only occurrence of the word 'deliberately' is an anti-claim about AgentEditHistoryView carrying no companyId field. The donor now declares exactly ONE dependency (thiserror), ZERO cargo features and ZERO dev-dependencies; its sqlx, serde, uuid and chrono dependencies and its tokio/serde_json dev-dependencies were deleted OUTRIGHT, and Cargo.lock (regenerated by cargo itself, never hand-edited) now lists exactly `thiserror 2.0.20` for the package and no sqlx line. The real mechanics -- AgentEditHistoryStore::new / record_edit / predict_next_location over agent_edit_history (migration 0392_agent_edit_history.sql), plus the StoredEdit sqlx::FromRow row shape, the MAX_FILE_PATH = 4096 bound, the wire-facing AgentEditHistoryView projection with its From<StoredEdit> conversion, the StoreError taxonomy and its From<EditHistoryError> bridge -- moved into the new crates/adapter/chronica-adapter-agent-edit-history as src/store.rs. That is the pre-split src/lib.rs's lines 31-199 CONTIGUOUS (169 lines, the entire remainder of the file after its module doc and the `mod edit_prediction_engine;` line) at the SAME zero indentation -- no `mod imp` wrapper was introduced, precisely so no re-indent was needed. EXACTLY TWO of those 169 lines differ, both forced by the crate boundary rather than chosen: the `use crate::edit_prediction_engine::{...}` line now names the donor crate, and the moved inline money guard's include_str!(\"lib.rs\") now reads include_str!(\"store.rs\"). The two sub-spans between and after those lines -- pre-split lines 32-152 (121 lines, the whole store including both SQL strings, every bind and all error mapping) and 154-199 (46 lines, the rest of the money guard plus the whole #[cfg(test)] mod tests) -- hash IDENTICALLY before and after. NO ORPHAN-RULE FIX WAS NEEDED, and that was checked rather than assumed: the only impl naming a now-foreign type is `impl From<EditHistoryError> for StoreError`, which is legal because the target type is local. The retained pure half is byte-identical AS A WHOLE FILE (f012ed3e1dc473c2589932a999927b1e8dc3285e2ba5ac9650a82c902810c688 before and after, 114 lines, zero edits of any kind). The exact digests are recorded in the adapter's own src/lib.rs module doc and in the subsystem-convergence ledger entry for LOOP 76. misplaced_cap_package_count is UNCHANGED at 221 and that is correct rather than an omission: this was a SPLIT, so the donor package keeps both its chronica-cap-* name and its crates/core/ home; only a whole-crate relocation (LOOP 73's shape) moves M2, and claiming an M2 win here would have been false. world_graph_live_dependency_count is likewise unchanged at 5. The M7 decrease is real, not definitional: both statements still exist, still run against the same agent_edit_history table with the same binds, the same ON CONFLICT (company_id,file_path) upsert key and the same company_id predicate, and are still exercised end-to-end against live Postgres through chronica-adapter-api-server's tests/agent_edit_history_route_db.rs -- they simply live at an adapter-tier address now. --write-baseline was NEVER run.",
  "_helpdesk_sentiment_model_split_2026_09_08_loop77": "LOOP 77 (2026-09-08) responsibility-tier decomposition. forbidden_core_io_count 104 -> 103: crates/core/chronica-cap-helpdesk-sentiment-model stopped declaring real Postgres I/O. ITS GATING SHAPE WAS CHECKED, NOT ASSUMED, against `git show HEAD:`: this crate used a crate-level WHOLE-FILE `#![cfg(feature = \"db\")]` inner attribute on line 1 -- LOOP 70's shape, NOT the LOOP 59-75 `#[cfg(feature = \"db\")] mod imp { ... }` wrapper and NOT LOOP 76's no-gate-at-all shape -- backed by a real `[features]` table, `db = [\"dep:sqlx\", \"chronica-core/db\"]`, and `sqlx = { workspace = true, optional = true }`. That whole-file attribute is why the crate's pure half was invisible without `db`: SentimentModelKind, which depends on nothing, could not be named unless a caller turned the database feature on. Because every item sat at ZERO indentation under an attribute rather than inside a wrapper module, the moved mechanics needed no de-indentation at all, which is why the moved spans reproduce with ZERO edits. THE CRATE'S OWN DOCUMENTATION WAS READ IN FULL FIRST, per the standing lesson from the abandoned relocation attempt against chronica-adapter-social-postgres-batch1 earlier in this session: src/lib.rs's module doc, the Cargo.toml header comment, CAPABILITY.md and .chronica/capability.chronica.json were read end to end and grepped for 'DIRECTORY:', 'deliberate' and 'intentional'. There is NO 'DIRECTORY:' section and NO considered-placement rationale anywhere -- the single hit for 'deliberate' is the module doc's phrase 'This store is deliberately model-shape-agnostic', a statement about model_json being an opaque serde_json::Value, not about the crate's directory. SPLIT, not a relocation: the donor declares a real, load-bearing local pure vocabulary of its own -- `pub enum SentimentModelKind` with as_str/parse, the single authority on the \"bayes\"/\"svm\" spelling that the HTTP route parses off the wire (parse -> None is what produces its 422) and that both SQL statements bind into model_kind -- plus its own round-trip unit test. So the donor was kept and SHRUNK rather than renamed away. The donor now declares NO [dependencies] table at all, NO [features] table and NO dev-dependencies: its chronica-core, serde_json and sqlx dependencies, its `db` feature and its whole-file `#![cfg(feature = \"db\")]` attribute were all deleted OUTRIGHT, and Cargo.lock (regenerated by cargo itself, never hand-edited) now lists the package with no dependency list. The real mechanics -- SentimentModelRepo<'a>::new / upsert / get over support_sentiment_models (migration 0393_support_sentiment_models.sql, renumbered from 0392 on top of main), plus the SentimentModelRow row shape and the private `map` PgRow decoder -- moved into the new crates/adapter/chronica-adapter-helpdesk-sentiment-model as src/store.rs. Because the pure enum sits BETWEEN the two impure regions of the pre-split file, the moved material is TWO contiguous spans rather than one, and EVERY moved line is unedited: pre-split lines 15-17 (the whole import block, 5b6e32d67b08e2281f57b866cc2614ea594b4d6ef897c906607e6efa22fb7ce6) and pre-split lines 44-121 (78 lines, SentimentModelRow through the closing brace of impl SentimentModelRepo, f363b5139620ea90bccf1f10612fc1a2b2b13d8bbf4309dead61ca815dbaef0d) hash IDENTICALLY before and after. Exactly ONE line was ADDED above them -- `use chronica_cap_helpdesk_sentiment_model::SentimentModelKind;` -- forced by the crate boundary, since both ops take a SentimentModelKind and the enum stayed behind. NO ORPHAN-RULE FIX WAS NEEDED, and that was checked rather than assumed: the private row decoder `fn map(r: &sqlx::postgres::PgRow) -> SentimentModelRow` is an associated fn on SentimentModelRepo, and BOTH SentimentModelRepo and SentimentModelRow moved together, so nothing it names became foreign and it stayed an associated fn -- no free-fn rewrite, no rename, no reflow. The retained pure half is likewise unedited: pre-split lines 19-42 (0f097470bbbb032432261934a6477146d4f9f0c1e0ce90c19d2a1557f490a2fe) and pre-split lines 123-135 (e7f5f60ecfafd8cfc17abd575b1c534a9f0072438c09f851fde84ede217f05c8), both before and after. The exact digests are recorded in the adapter's own src/lib.rs module doc and in the subsystem-convergence ledger entry for LOOP 77. misplaced_cap_package_count is UNCHANGED at 221 and that is correct rather than an omission: this was a SPLIT, so the donor package keeps both its chronica-cap-* name and its crates/core/ home; only a whole-crate relocation (LOOP 73's shape) moves M2, and claiming an M2 win here would have been false. world_graph_live_dependency_count is likewise unchanged at 5, and layer_debt_edge_count is UNCHANGED at 137 -- the layer gate reported NO new forbidden edge, because this capability's only real caller (chronica-adapter-helpdesk-http) is already adapter-tier, so the repoint creates an adapter -> adapter edge that FORBIDDEN_TARGETS does not bar; tools/refoundation/refoundation-layer-debt.json was therefore left COMPLETELY untouched. The M7 decrease is real, not definitional: both statements still exist, still run against the same support_sentiment_models table with the same binds, the same ON CONFLICT (company_id, model_kind) upsert key, the same DO UPDATE SET clause and the same company_id read predicate, and are still exercised end-to-end against live Postgres through chronica-adapter-api-server's tests/helpdesk_sentiment_model_route.rs -- they simply live at an adapter-tier address now. --write-baseline was NEVER run.",
  "_helpdesk_macros_relocation_2026_09_08_loop78": "LOOP 78 (2026-09-08) responsibility-tier decomposition. misplaced_cap_package_count 221 -> 220 (by_root.core 221 -> 220) AND forbidden_core_io_count 103 -> 102, BOTH in this one change: crates/core/chronica-cap-helpdesk-macros was RELOCATED WHOLE to crates/adapter/chronica-adapter-helpdesk-macros and renamed to match its tier, so a chronica-cap-* package no longer sits under crates/core/ (M2) and real Postgres I/O no longer sits under crates/core/ at that address (M7). THE CRATE'S OWN DOCUMENTATION WAS READ IN FULL FIRST, per the standing lesson from the abandoned relocation attempt against chronica-adapter-social-postgres-batch1 earlier in this session: src/lib.rs's module doc, the Cargo.toml header comment, CAPABILITY.md and .chronica/capability.chronica.json were read end to end and grepped case-insensitively for 'DIRECTORY:', 'deliberate', 'intentional', 'on purpose', 'kept under' and 'stays in'. There is NO 'DIRECTORY:' section and NO considered-placement rationale anywhere in any of the four files -- the grep returned ZERO hits of any kind, not merely zero relevant ones -- so nothing here reverses an earlier deliberate decision. ITS GATING SHAPE WAS CHECKED, NOT ASSUMED, against `git show HEAD:`: this crate used a crate-level WHOLE-FILE `#![cfg(feature = \"db\")]` inner attribute on line 1 -- LOOP 70/77's shape, NOT the LOOP 59-76 `#[cfg(feature = \"db\")] mod imp { ... }` wrapper and NOT LOOP 76's no-gate-at-all shape -- backed by a real [features] table, `db = [\"dep:sqlx\", \"chronica-core/db\"]`, and `sqlx = { workspace = true, optional = true }`. That whole-file attribute is decisive here for a reason beyond formatting: with `db` off this crate exported LITERALLY NOTHING, which is itself the proof that it had no pure surface to keep behind. Confirmed empirically, not by reading: `cargo test -p chronica-adapter-helpdesk-macros` (no features) runs 0 unit tests where the same command with --features db runs 1. RELOCATION, not a split, and the classification was made mechanically rather than by eye. The prior recon note flagged this crate as 'RELOCATION-leaning' but was re-derived from scratch rather than trusted: `grep -nE '^\\s*(pub )?(struct|enum|trait|type|static|const) '` over the 175-line pre-move src/lib.rs returns exactly THREE hits, and none of them is a pure domain item. `pub struct MacroRow` is the persisted support_macros row (id, company_id, name, actions jsonb, created_at, updated_at) -- a database row shape whose only method, to_json, is an egress projection of that row; `pub struct MacroRepo<'a>` holds nothing but `pg: &'a PgPersistence`, a borrow of the shared connection pool; and `const SELECT: &'static str` is a raw SQL projection string, which is I/O vocabulary rather than a domain type. Every one of the crate's five production ops (create/list/get/update/delete) issues raw sqlx statements. The pure macro vocabulary this store validates against was NEVER local to this crate: the closed AutomationAction set and validate_actions live in chronica-core-helpdesk-automation, which this crate merely depends on. With zero genuine local pure surface there was nothing to keep behind, so the whole crate moved and NO donor, stub or re-export shim remains under crates/core/. BYTE IDENTITY: all five files hashed IDENTICALLY immediately after `git mv` (whole-file SHA-256, before and after: Cargo.toml 8ef39e4b7c6074bcb09b8c0dc123b5d802146795eb6e1a98f89d44e38a2f7175; src/lib.rs 0dba9dacb61ea86d7b195ea420968c2f975689b6f39de3e66b99eb0d4bacde31; tests/money_guard.rs 4a400b3fdaa2fe9935c9bc2f57dfcade2d7dddfad2909ef0f19bce9577375481; CAPABILITY.md 3d7edadfb97919caf696840393ea287a3359299af6d4dd0fc448192e5d7b26d0; .chronica/capability.chronica.json 161cd11d9ef00f10abf1499c79eea704cc6a08c4f8ce67c52f9024c99e383257). The tier-facing prose was then rewritten in four of them, but the EXECUTABLE BODY of src/lib.rs was left bit-for-bit alone and re-verified: the 162-line span from the `use chronica_core::` import line to EOF (pre-move lines 14-175, post-move lines 30-191, shifted only because the module doc grew) hashes 6637aff0c2482a874808cbd906e4803e02b61ca5ad5836d639a077ba60edee3f both before (from `git show HEAD:`) and after, and `cmp` reports the two spans identical. Line 1, the `#![cfg(feature = \"db\")]` attribute, is also unchanged. The dependency set, the [features] table and the db wiring are UNCHANGED by the relocation -- only the [package] name field moved. CALLER REPOINT: an EXACT fully-qualified-path grep for `chronica_cap_helpdesk_macros::` across the workspace returns exactly ONE production site, crates/adapter/chronica-adapter-helpdesk-http/src/helpdesk_macros.rs:32, confirming the recon's sole-real-caller claim. Repointed to `chronica_adapter_helpdesk_macros::MacroRepo`; nothing about the API changed, only the package name, so the seven MacroRepo call sites in that file were untouched. chronica-adapter-api-server was checked INDEPENDENTLY per the standing LOOP-21 precedent: its optional Cargo dependency on this crate is DEAD (zero occurrences of chronica_cap_helpdesk_macros anywhere in its own src/ or tests/), so it was removed outright along with its two db-feature entries. That crate still exercises this capability end-to-end -- its tests/helpdesk_macro_route.rs drives the routes through chronica-adapter-helpdesk-http, whose db feature already propagates chronica-adapter-helpdesk-macros/db -- so the removal cost no coverage. Both dependents were ALREADY adapter-tier, which is why the layer gate reports zero new forbidden edges. LAYER DEBT: `node tools/refoundation/validate-refoundation-layers.mjs` printed NO `NEW FORBIDDEN EDGE:` line and reports 'no new edges (137 known debt edge(s) remain)' -- unchanged from before the change, so layer_debt_edge_count stays 137 and tools/refoundation/refoundation-layer-debt.json was left COMPLETELY untouched (`git diff --stat` shows no entry for it). --write-baseline was NEVER run. THE DECREASES ARE REAL, NOT DEFINITIONAL: all five statements still exist, still run against the same support_macros table (migration 0024_support_macros.sql) with the same binds, the same company_id tenant predicate on every op, and the same jsonb actions column, and are still exercised end-to-end against live Postgres by chronica-adapter-api-server's 8 tests/helpdesk_macro_route.rs tests -- they simply live at an adapter-tier address now.",
  "_commerce_order_intake_relocation_2026_09_08_loop79": "LOOP 79 (2026-09-08). WHAT MOVED: the whole crate crates/core/chronica-cap-commerce-order-intake (172 lines, single-file) was PHYSICALLY RELOCATED to crates/adapter/chronica-adapter-commerce-order-intake and its package renamed to match its tier. This is a RELOCATION, not a SPLIT -- nothing was left behind under crates/core/ because there was nothing pure to leave. THE CAPABILITY: the commerce ORDER-INTAKE DECISION/IDEMPOTENCY LEDGER (table commerce_order_intake_decisions, migration 0152). It records the Clear/Block verdict that chronica_core_commerce_order_domain::prefilter_order_intake ALREADY COMPUTED, keyed by (company_id, idempotency_key), with a write-once payload_fingerprint (SHA-256 over stable_json({action, companyId, request})) bound at INSERT time. It mirrors DecisionLedgerRepo::claim()'s tri-state contract from migration 0014: INSERT ... ON CONFLICT DO NOTHING RETURNING makes exactly one concurrent submission win Fresh; a loser re-reads the winning row and compares fingerprints -- MATCH is Existing (idempotent replay), MISMATCH is Conflict, and the stored row is NEVER surfaced beyond that comparison and NEVER updated. WHY RELOCATION AND NOT SPLIT: verified independently by structural grep plus a read of what each declared item actually does, not taken from recon. The crate declares exactly four items and every one is a database artifact. DECISION_COLS is a SQL projection string. OrderIntakeDecisionRow exists solely to carry what a PgRow decoded -- its eleven fields are the eleven projected columns, its private map(&sqlx::postgres::PgRow) IS the row decoder, and its to_json is the camelCase egress view of exactly those same columns. ClaimOutcome wraps that same row in two of its three variants, so it cannot exist without it. get_decision and record_decision are async sqlx statements against a live PgPersistence pool. The genuinely PURE vocabulary this capability rests on -- OrderIntakeRequest, OrderPrefilterDecision, and the prefilter_order_intake verdict function itself -- is OWNED BY chronica-core-commerce-order-domain and already lived there before this loop; this crate only records what that pure function decided. Keeping a donor behind would have meant either a crate containing only a row struct that cannot be produced without a database, or duplicating chronica-core-commerce-order-domain's vocabulary -- which is precisely the caller-farming this program exists to remove. This matches the LOOP 73 and LOOP 78 relocation findings for structurally identical donors, and contrasts with LOOP 77, which SPLIT a sibling crate only because that one did declare a load-bearing local pure enum. GATING SHAPE, CHECKED RATHER THAN ASSUMED: this crate uses a THIRD shape, distinct from both the LOOP 59-75 `#[cfg(feature = \"db\")] mod imp { ... }` wrapper and the LOOP 70/77/78 whole-file `#![cfg(feature = \"db\")]` inner attribute. It has NO feature gate of any kind and NO [features] table at all -- sqlx and chronica-core/db are plain, always-on dependencies, the LOOP 76 shape. That was proven empirically, not read off the manifest: `cargo check -p chronica-adapter-commerce-order-intake --features db` FAILS with \"the package 'chronica-adapter-commerce-order-intake' does not contain this feature: db\". The absence of a gate is itself independent structural proof that the crate had no pure surface: a crate with any db-free code would have needed a gate to expose it. Reachability is gated one level up instead -- the sole caller puts its entire commerce_order_intake module behind its own `#![cfg(feature = \"db\")]`. Because there was no gate and no wrapper module, every item sat at zero indentation, so nothing needed de-indenting and no reflow of any kind was possible. M7 forbidden_core_io_count 102 -> 101: real Postgres I/O (a SELECT and an INSERT ... ON CONFLICT ... RETURNING, both binding company_id, both through the shared pool) no longer sits under crates/core/ at that address. M2 misplaced_cap_package_count 220 -> 219 (by_root.core 220 -> 219): a chronica-cap-*-named package no longer sits under crates/core/, because the package itself was renamed to chronica-adapter-commerce-order-intake as it moved. TWO metrics moving together is the signature of a relocation; the SPLIT loops (69-72, 74-77) moved M7 alone and were M2-neutral by design because their donors kept chronica-cap-* names under crates/core/. M3 world_graph_live_dependency_count is untouched at 5 -- this change adds no world-graph live dependency. LAYER DEBT WENT UP, AND THAT WAS EXPECTED: exactly one NEW FORBIDDEN EDGE, 'chronica-cap-erp-commerce-ops-routes (cap) -> chronica-adapter-commerce-order-intake (adapter)', copied verbatim from the gate's own output. layer_debt_edge_count 137 -> 138. Unlike LOOP 77/78, whose callers were already adapter-tier, THIS caller is a cap-tier route crate, so a cap -> core dependency became a cap -> adapter one. The edge represents NO new coupling whatsoever: not one call site, import, type or SQL statement changed direction, and no dependency was added. The edge was already real and merely INVISIBLE to the gate while adapter code sat under crates/core/ wearing a chronica-cap-* name; recording it converts concealed debt into disclosed debt. Full disclosure in tools/refoundation/refoundation-layer-debt.json's _note_2026_09_08_loop79. --write-baseline was NEVER run for either file. NOT METRIC GAMING: nothing was deleted, weakened, stubbed or hidden behind a feature to make a counter move. Both SQL statements still exist character for character -- SHA-256-proven over the moved 152-line executable span, which hashes 4cb00b8ecd1f66ae852d3204d482d4895bba42845259b0ad89a61f4374adc5c0 both before (extracted from `git show HEAD:crates/core/chronica-cap-commerce-order-intake/src/lib.rs`) and after, with `cmp` reporting the two extracted spans byte-for-byte identical. Zero lines of executable code were edited, added or removed; no orphan-rule fix was needed because nothing crossed a crate boundary -- the whole crate moved as a unit. The same table is still the only one touched, and the same caller still calls the same two ops through the same API at the same five source positions. This loop did NOT reduce the total amount of I/O in the repo by one line and does not claim to; what changed is the DIRECTORY the code sits in and the tier its package name declares, which is exactly what M2 and M7 are defined to measure. SAFETY CHECK PERFORMED FIRST: all four documentation surfaces (src/lib.rs module doc, the Cargo.toml header comment, CAPABILITY.md and .chronica/capability.chronica.json) were read end to end and grepped case-insensitively for 'DIRECTORY:', 'deliberate', 'intentional', 'on purpose', 'kept under', 'stays under', 'remains under' and 'physically'. ZERO hits of any kind. The crate's own former manifest header said the opposite of a considered core placement -- it described itself as a store 'lifted verbatim out of its former location under crates/chronica-api/src/routes/', i.e. it landed under crates/core/ by a mechanical lift, never by an argued decision.",
  "_company_model_template_relocation_2026_09_08_loop80": "LOOP 80 (2026-09-08). WHAT MOVED: the whole crate crates/core/chronica-cap-company-model-template (184 lines, single-file) -> crates/adapter/chronica-adapter-company-model-template, renamed chronica-cap-company-model-template -> chronica-adapter-company-model-template so the package name matches its tier. TWO METRICS FELL IN THIS ONE CHANGE: misplaced_cap_package_count 219 -> 218 (by_root.core 219 -> 218), because a package literally named chronica-cap-* no longer lives outside crates/cap/; and forbidden_core_io_count 101 -> 100, because a crates/core/ crate with a real, always-on sqlx dependency no longer sits under crates/core/. Both decreases come from the same relocation and are recorded here in the same change that caused them. WHY A RELOCATION AND NOT A SPLIT: the crate declared ZERO genuine local pure surface. A structural grep (`grep -nE '^\\s*(pub )?(struct|enum|trait|type|static|const) '`) returned exactly three hits, and each was judged on what it actually DOES rather than on its name. (1) EXPORT_COLS is a raw SQL projection string, literally a fragment of a SELECT list, interpolated into both statements. (2) TemplateExportRow exists solely to carry what a PgRow decoded: its ten fields ARE the ten projected columns (including the id::text and created_at::text Postgres casts leaking into the Rust type), and its only private method, `fn map(r: &sqlx::postgres::PgRow) -> Self`, IS the row decoder. Its one public method, `reverify`, is the decisive item and it owns NO LOGIC AT ALL -- its entire body is `import_package(&self.package_bytes)`, a delegation into chronica-cap-template-store. A one-line delegation is not pure surface worth keeping; it is a call. (3) PersistExportOutcome is an outcome enum, but ALL THREE of its variants are Box<TemplateExportRow>, so it cannot be lifted anywhere the row does not also go. The two free functions (persist_export, get_export) are async sqlx statements against a live PgPersistence pool. THE DECISIVE POINT: the genuinely pure vocabulary this capability rests on -- TemplatePackage, export_package, import_package, verify_package and StoreError -- is OWNED BY chronica-cap-template-store and already lived there before this loop. A split would have had to leave behind either a crate containing nothing but a row struct that cannot be constructed without a database, or a duplicate of vocabulary another crate already owns. Neither is progress, so the whole crate moved. GATING SHAPE: this crate has NO cfg-feature gate of any kind -- not a `#[cfg(feature = \"db\")] mod imp` wrapper, not a whole-file `#![cfg(feature = \"db\")]` inner attribute, and no [features] table in its manifest at all. sqlx and chronica-core/db are unconditional. This was proven empirically, not assumed: `cargo check -p chronica-adapter-company-model-template --features db` fails with \"the package ... does not contain this feature: db\". It is the same third gating shape LOOP 79 recorded, and it is itself the structural proof that there was no db-free code to leave behind. NOT METRIC GAMING: no code was deleted, no dependency was hidden and no responsibility was reassigned to make these two numbers fall. The 163-line executable body of src/lib.rs is BYTE-IDENTICAL before and after (SHA-256 b7958ed6fa72c18a2b9e1bebb88cee5dd781adda6e91a6705df98c72639f64f0, re-derived from the git HEAD blob and from the post-move working tree independently), and the manifest's [dependencies] block is byte-identical too (SHA-256 c1245c735a2d49d2b4cdd86a5618e8f4c38543141589bc611162e2feca060c1a) -- the only executable line of the manifest that changed is the [package] name field. The same relocation ADDED one forbidden layer-debt edge (chronica-cap-org-workflow-routes (cap) -> chronica-adapter-company-model-template (adapter), layer_debt_edge_count 138 -> 139), which is the honest cost of these two decreases and is disclosed in tools/refoundation/refoundation-layer-debt.json's _note_2026_09_08_loop80: the cap-tier caller's dependency on this Postgres I/O was ALREADY REAL, it was merely invisible to the layer gate while the adapter code sat under crates/core/ wearing a chronica-cap-* name.",
  "_crm_support_helpdesk_migration_relocation_2026_09_09_loop81": "LOOP 81 (2026-09-09) responsibility-tier decomposition, and the FIRST loop in this program's recent run whose metric answer is 'BOTH GATED NUMBERS CORRECTLY STAY PUT'. WHAT MOVED: the whole crate crates/cap/chronica-cap-crm-support-helpdesk-migration (325 lines, single-file; capability crm-support.helpdesk_platform_migration; the crm_helpdesk_migration_runs table from migration 0423_crm_helpdesk_migration_runs.sql) -> crates/adapter/chronica-adapter-crm-support-helpdesk-migration, renamed chronica-cap-crm-support-helpdesk-migration -> chronica-adapter-crm-support-helpdesk-migration so the package name matches its tier. A RELOCATION, not a split. misplaced_cap_package_count UNCHANGED at 218 (by_root core=218, runtime=0, adapter=0), and that is the CORRECT outcome, not a missed win -- it was verified by reading tools/ci/verify-responsibility-boundaries.mjs itself rather than assumed. M2 is computed by countMisplacedCapPackages(), which iterates only PRODUCT_CRATE_ROOTS = ['core', 'runtime', 'adapter']; crates/cap/ is deliberately NOT in that list, because a chronica-cap-* package sitting under crates/cap/ is where such a package is SUPPOSED to be -- M2 counts packages that kept the cap NAME while leaving the cap DIRECTORY. This crate was under crates/cap/, so it never counted toward M2 at all. The rename to chronica-adapter-* was therefore not merely cosmetic: without it, moving the directory to crates/adapter/ would have INCREASED M2 from 218 to 219 and FAILED the gate outright (that is exactly the 'path relocation, not migration' failure the gate's own violation message describes). Renaming in the same change is what holds it at 218. forbidden_core_io_count UNCHANGED at 100, likewise correctly and likewise verified from the script: countForbiddenCoreIo() iterates listCrateDirs('core') ONLY, so M7 is defined purely over crates/core/. This crate has never lived under crates/core/, so its optional sqlx dependency was never one of the 100 hits and could not have been removed from that count by any move. world_graph_live_dependency_count UNCHANGED at 5 (this crate never named chronica-core-world-graph). WHERE THE REAL WINS LANDED INSTEAD, both disclosed in the same change: (1) cap_count 36 -> 35 in tools/ci/topology-baseline.json -- one fewer crate under the transitional crates/cap/ root, which is the metric this particular relocation actually moves; and (2) layer_debt_edge_count 139 -> 138 in tools/refoundation/refoundation-layer-debt.json -- a genuine REPAIR, not a relabelling. The edge 'chronica-cap-crm-support-helpdesk-migration (cap) -> chronica-adapter-crm-support-lms-helpdesk (adapter)' was forbidden under FORBIDDEN_TARGETS.cap and is now adapter -> adapter, which is not forbidden; validate-refoundation-layers.mjs printed zero NEW FORBIDDEN EDGE lines and exactly one STALE DEBT EDGE line, whose string was copied verbatim into the removal. No new edge appeared on the caller side because the sole real caller, chronica-adapter-crm-http, is already adapter-tier (the LOOP 77/78 shape, not the LOOP 79/80 shape). WHY RELOCATION AND NOT SPLIT: verified independently by reading all 325 lines and by the prescribed structural grep, not taken from the recon. Every item the crate declares lives inside ONE `#[cfg(feature = \"db\")] mod imp { ... }` wrapper re-exported by a matching `#[cfg(feature = \"db\")] pub use imp::*;` -- so with the db feature off the crate's public surface is literally empty and there is nothing a pure-tier consumer could name. The declared items are RUN_COLS (a raw SQL projection string interpolated into all six statements), RunRow (the decoded crm_helpdesk_migration_runs row: its seven fields ARE the seven projected columns, including the id::text Postgres cast leaking into the Rust type, and its methods are a `fn map(r: &sqlx::postgres::PgRow) -> Self` row decoder, three jsonb/TEXT rehydrators and a camelCase egress to_json), parse_status (the private decoder for the status TEXT column, mapping the six DDL CHECK literals onto an IMPORTED enum -- not a domain rule of its own), and four write-outcome enums (CreateRunOutcome, UpdateCursorOutcome, AbandonOutcome, RestartOutcome) whose success variants all Box<Value> that same row. The genuine pure vocabulary -- MigrationRunStatus, MigrationCursor, MigrationStats, CursorUpdateOutcome and all eight decision functions (validate_import_types, validate_integration_provider, resolve_cursor_update, compute_restart_stats, is_restartable, is_abandonable, is_stalled, is_integration_import) -- is OWNED by chronica-adapter-crm-support-lms-helpdesk and was never redefined here, so a split would have had nothing to leave behind but a duplicate. NOT METRIC GAMING: no code was deleted, no dependency was hidden behind a feature, and no responsibility was reassigned. The 306-line executable body is byte-identical, SHA-256 38754bb49435e04562c34a61a1a2d4266e6f931b282a0afda0d68adf3471a3f2 computed both from the git HEAD blob and from the post-move working tree; the manifest's [dependencies]+[features] blocks are byte-identical too, SHA-256 9d8890391f8aaefe652759e9fab286a2acbd85e6edb5b49e23abb19a18793433, so the optional-sqlx wiring and the `db = [\"dep:sqlx\", \"chronica-core/db\"]` gate survive unchanged. The safety check for a 'DIRECTORY:'/deliberate-placement rationale was performed FIRST across all four documentation surfaces and returned zero hits of any kind. --write-baseline was never run on any baseline in this change.",
  "_crm_support_email_group_newsletter_relocation_2026_09_09_loop82": "LOOP 82 (2026-09-09) responsibility-tier decomposition, and -- like LOOP 81 immediately before it -- a loop whose metric answer is 'BOTH GATED NUMBERS CORRECTLY STAY PUT'. ALL FOUR NUMERIC FIELDS IN THIS FILE ARE DELIBERATELY UNCHANGED (misplaced_cap_package_count 218, its by_root breakdown core=218 / runtime=0 / adapter=0, world_graph_live_dependency_count 5, forbidden_core_io_count 100), and this note exists to explain WHY that is the correct result rather than a missed win. WHAT MOVED: the whole crate crates/cap/chronica-cap-crm-support-email-group-newsletter (338 lines, single-file; capability crm-support.email_group_newsletter; the crm_email_groups and crm_email_group_members tables from migration 0422_crm_email_groups.sql) -> crates/adapter/chronica-adapter-crm-support-email-group-newsletter, renamed chronica-cap-crm-support-email-group-newsletter -> chronica-adapter-crm-support-email-group-newsletter so the package name matches its tier. A RELOCATION, not a split. WHY M7 CANNOT MOVE, READ OUT OF THE SCRIPT RATHER THAN ASSUMED: tools/ci/verify-responsibility-boundaries.mjs defines `const PRODUCT_CRATE_ROOTS = ['core', 'runtime', 'adapter']`, and M7 is computed by countForbiddenCoreIo(), which iterates listCrateDirs('core') ONLY. M7 is therefore defined purely over crates/core/. This crate lived under crates/cap/, so its optional sqlx dependency was NEVER one of the 100 hits and could not have been removed from them. 100 -> 100 is arithmetically forced, not a shortfall. WHY M2 CANNOT MOVE EITHER, WHICH IS THE LESS OBVIOUS HALF: M2 is computed by countMisplacedCapPackages(), which also iterates PRODUCT_CRATE_ROOTS -- crates/cap/ is deliberately EXCLUDED, because M2 counts packages that kept the chronica-cap-* NAME while leaving the cap DIRECTORY. This crate was under crates/cap/ with a chronica-cap-* name, i.e. exactly where such a package is supposed to be, so it never counted toward M2 either. 218 -> 218 is likewise correct. BUT THE RENAME WAS STILL MANDATORY, AND THIS IS THE LOAD-BEARING POINT: had the directory moved to crates/adapter/ WITHOUT the package being renamed, the package would have become a chronica-cap-*-named package sitting under an enumerated product root, M2 would have INCREASED 218 -> 219, and the gate would have FAILED outright. The rename is what keeps this file's numbers flat; it is not cosmetic. M3 (world_graph_live_dependency_count = 5) is unrelated to crate placement and was not touched. WHERE THE REAL WINS LANDED, both recorded in their own baselines in this same change: cap_count 35 -> 34 in tools/ci/topology-baseline.json (the crates/cap/ directory count, which the topology gate warns about and enforces as a floor), and layer_debt_edge_count 138 -> 137 in tools/refoundation/refoundation-layer-debt.json -- ZERO new forbidden edges and exactly ONE REPAIRED. The layer gate printed one line and nothing else, `STALE DEBT EDGE (remove from baseline): chronica-cap-crm-support-email-group-newsletter (cap) -> chronica-adapter-crm-support-channel-connectors (adapter)`, and that entry was hand-removed (never --write-baseline). See _note_2026_09_09_loop82 in that file for the full disclosure. NOT METRIC GAMING: the 316-line executable body is byte-identical (SHA-256 5c570e81b1b037a35f3e1d6e9d11e21a7d8536b9e97ae4b59afe1660fb8f513b, re-derived independently from the git HEAD blob and from the post-move working tree), the manifest's [dependencies] and [features] blocks are byte-identical (SHA-256 ac66dc920cb2eb2a7ffae52a90949c379b5125f9a38eae3f6cac2d7888951d70), and all six public ops, the private recount helper and all nine tenant-scoped statements are now PINNED by a rebuilt 4-test money guard that would fail if any of them were quietly removed. The only dependency deleted anywhere in this change is chronica-adapter-api-server's, proven dead by grep across that crate's entire src/ and tests/ before removal.",
  "_helpdesk_email_layout_store_relocation_2026_09_09_loop83": "LOOP 83 (2026-09-09) responsibility-tier decomposition. UNLIKE LOOP 81 and LOOP 82 immediately before it -- both of which relocated crates that sat under crates/cap/ and therefore moved NEITHER of these two counters -- this donor sat under crates/core/ and so genuinely counted toward BOTH. misplaced_cap_package_count 218 -> 217 (by_root.core 218 -> 217) AND forbidden_core_io_count 100 -> 99, BOTH from the same single change: the WHOLE crate crates/core/chronica-cap-helpdesk-email-layout-store (367 lines, single file; ~200 lines of production body plus a ~165-line `#[cfg(test)] mod tests` block whose inner `db_tests` module exercises a real dev Postgres) was PHYSICALLY RELOCATED to crates/adapter/chronica-adapter-helpdesk-email-layout-store and the package renamed to match its tier. M2 falls because a `chronica-cap-*`-named package no longer sits under crates/core/; M7 falls because a crates/core/ crate with a real, always-on `sqlx` dependency no longer sits under crates/core/ either. Both were confirmed against the actual script logic (countMisplacedCapPackages / countForbiddenCoreIo in verify-responsibility-boundaries.mjs) rather than assumed, and the crate is NOT in CONFIRMED_IO_SEAM_EXEMPTIONS. WHY A RELOCATION AND NOT A SPLIT: the crate declares zero genuine pure surface. EmailLayoutRepo holds a &PgPersistence and its three ops are async raw-sqlx statements; EmailTemplateRow's own doc says it 'adds the DB identity + scope' to the IMPORTED, tenant-agnostic EmailTemplate and its fields ARE the SELECT's projected columns (id/created_at/updated_at are String only because the projection casts them ::text), constructed nowhere but inside the private `fn map(r: &sqlx::postgres::PgRow)`; CreateTemplateOutcome is a write-outcome type whose content is decided by the database (ScopeConflict is read off a Postgres unique-index constraint NAME) and whose one pure-looking variant, Invalid(TemplateValidationError), carries a type ALREADY OWNED BY THE PURE TIER (chronica-core-helpdesk-email-layout), so a pure donor stub would only have re-wrapped another crate's pure type. THE `is_scope_conflict(&sqlx::Error)` QUESTION, ANSWERED EXPLICITLY because it is the one item that could plausibly have made this a SPLIT: its LOGIC genuinely IS pure -- it performs no I/O, only `as_database_error().and_then(|db| db.constraint())` and two `==` comparisons -- but it is not pure SURFACE, for three independent reasons. (1) It is PRIVATE; nothing outside the crate can name it. (2) Its two comparison values are literal Postgres UNIQUE INDEX names owned by migration 0387's DDL (support_email_templates_company_scope_uniq / support_email_templates_inbox_scope_uniq) -- schema facts, not domain vocabulary. (3) DECISIVELY: its argument is `&sqlx::Error`, the driver's own type, so a core-tier crate retaining it would have had to retain the sqlx dependency under crates/core/ -- i.e. it would have kept counting toward forbidden_core_io_count and this line of the baseline would not have moved at all. A crate whose entire API is `fn(&sqlx::Error) -> bool` is pure in name only. The private template_type_str/parse_template_type pair was ruled the same way: pure, but the codec for the `template_type text` COLUMN, whose two literals are exactly what 0387's support_email_templates_type_chk CHECK constraint permits (and parse_template_type is lossy by design, `_ => Content`, correct ONLY because that constraint restricts the column). GATING SHAPE, verified rather than assumed -- this is the THIRD of the three shapes this program has now seen: NO gate at all. No [features] section, no `#[cfg(feature = \"db\")] mod imp { ... }` wrapper, no whole-file `#![cfg(feature = \"db\")]` inner attribute; sqlx and chronica-core/db are plain, always-on dependencies because every item needs a live PgPersistence. Unchanged by the relocation. The executable body (363 lines, everything from `use chronica_core::{` to EOF) moved BYTE-IDENTICALLY: SHA-256 b21750fa89732109b6bf8655d421dddea960a0babea097d70d9e8f786411bbcd both before (from git show HEAD:) and after. Only the module doc, the manifest header, tests/money_guard.rs, CAPABILITY.md and the .chronica descriptor changed. layer_debt_edge_count is genuinely UNCHANGED at 137: the sole real caller, chronica-adapter-helpdesk-http, was ALREADY adapter-tier, so the edge onto this crate went from adapter -> core (allowed) to adapter -> adapter (allowed) and the layer gate reported neither a NEW FORBIDDEN EDGE nor a STALE DEBT EDGE -- tools/refoundation/refoundation-layer-debt.json was therefore left completely untouched. cap_directory_count is likewise unchanged at 34: this donor never lived under crates/cap/, so tools/ci/topology-baseline.json was not touched either. chronica-adapter-api-server carried an optional dependency on the old name plus a `dep:` entry in its db feature but names the crate nowhere in its own src/ or tests/ (verified by fully-qualified-path grep); that dead dependency was removed outright per the standing LOOP-21 precedent and LOOP 82's identical disposition, and api-server still compiles with --features db.",
  "_note_2026_09_16_analytics_session_recording_split": "CLASS-B SPLIT of chronica-cap-analytics-session-recording: pure rrweb core renamed in place to chronica-core-analytics-session-recording-rrweb (drops misplaced_cap_package_count by 1, since chronica-cap-* is no longer its name), and the real Postgres I/O (store.rs, http.rs's consent-gated write path) moved to the new chronica-adapter-analytics-session-recording crate under crates/adapter/ (drops forbidden_core_io_count by 1, since that I/O is no longer misclassified as core-tier). See tools/refoundation/refoundation-layer-debt.json's matching note for the newly-disclosed cap->adapter edge this split's real caller (chronica-cap-platform-admin-routes) now has on the new adapter crate.",
  "_note_2026_09_16_browser_commerce_relocation": "Wholesale relocation of chronica-cap-browser-commerce (100% impure BrowserCommerceRun jsonb persistence store; module's own pre-existing doc comment already confirmed no db-free code exists in it) from crates/core/ to crates/adapter/, renamed to chronica-adapter-browser-commerce. Drops misplaced_cap_package_count by 1 (no longer chronica-cap-* named) and forbidden_core_io_count by 1 (real Postgres I/O no longer misclassified as core-tier). See tools/refoundation/refoundation-layer-debt.json's matching note for the newly-disclosed cap->adapter edge the real caller (chronica-cap-company-money-actions) now has.",
  "_note_2026_09_16_browser_webapp_relocation": "Wholesale relocation of chronica-cap-browser-webapp (100% impure WebAppDefinition/invocation-receipt jsonb persistence store; module's own pre-existing doc comment already confirmed no db-free code exists in it) from crates/core/ to crates/adapter/, renamed to chronica-adapter-browser-webapp. Drops misplaced_cap_package_count by 1 (no longer chronica-cap-* named) and forbidden_core_io_count by 1 (real Postgres I/O no longer misclassified as core-tier). See tools/refoundation/refoundation-layer-debt.json's matching note for the newly-disclosed cap->adapter edge the real caller (chronica-cap-company-platform-ops) now has.",
  "_note_2026_09_16_commerce_amazon_observation_relocation": "Wholesale relocation of chronica-cap-commerce-amazon-observation (100% impure append-only Amazon observation-history persistence store; module's own pre-existing doc comment already confirmed no db-free code exists in it) from crates/core/ to crates/adapter/, renamed to chronica-adapter-commerce-amazon-observation. Drops misplaced_cap_package_count by 1 (no longer chronica-cap-* named) and forbidden_core_io_count by 1 (real Postgres I/O no longer misclassified as core-tier). See tools/refoundation/refoundation-layer-debt.json's matching note for the newly-disclosed cap->adapter edge the real caller (chronica-cap-company-commerce-storefront) now has.",
  "_note_2026_09_16_commerce_cart_relocation": "Wholesale relocation of chronica-cap-commerce-cart (100% impure cart CRUD/compare-and-swap persistence store; module's own pre-existing doc comment already confirmed no db-free code exists in it) from crates/core/ to crates/adapter/, renamed to chronica-adapter-commerce-cart. Drops misplaced_cap_package_count by 1 (no longer chronica-cap-* named) and forbidden_core_io_count by 1 (real Postgres I/O no longer misclassified as core-tier). See tools/refoundation/refoundation-layer-debt.json's matching note for the newly-disclosed cap->adapter edge the real caller (chronica-cap-company-commerce-storefront) now has.",
  "_note_2026_09_16_commerce_kiosk_relocation": "Wholesale relocation of chronica-cap-commerce-kiosk (100% impure kiosk session + atomic claim-before-commit order lifecycle persistence store; module's own pre-existing doc comment already confirmed no db-free code exists in it) from crates/core/ to crates/adapter/, renamed to chronica-adapter-commerce-kiosk. Drops misplaced_cap_package_count by 1 (no longer chronica-cap-* named) and forbidden_core_io_count by 1 (real Postgres I/O no longer misclassified as core-tier). See tools/refoundation/refoundation-layer-debt.json's matching note for the newly-disclosed cap->adapter edge the real caller (chronica-cap-company-commerce-storefront) now has.",
  "_note_2026_09_16_company_business_map_relocation": "Wholesale relocation of chronica-cap-company-business-map (100% impure business-map persistence adapter; every item lives behind #[cfg(feature = \"db\")], public surface empty with db off) from crates/core/ to crates/adapter/, renamed to chronica-adapter-company-business-map. Drops misplaced_cap_package_count by 1 (no longer chronica-cap-* named) and forbidden_core_io_count by 1 (real Postgres I/O no longer misclassified as core-tier). See tools/refoundation/refoundation-layer-debt.json's matching note for the newly-disclosed cap->adapter edge the real caller (chronica-cap-org-workflow-routes) now has.",
  "_note_2026_09_16_company_invitations_split": "CLASS-B SPLIT of chronica-cap-company-invitations: pure invitations lifecycle (InvitationLedger) renamed in place to chronica-core-company-invitations (drops misplaced_cap_package_count by 1, since chronica-cap-* is no longer its name), and the real Postgres I/O (store/membership/db_accept) moved to the new chronica-adapter-company-invitations crate under crates/adapter/ (drops forbidden_core_io_count by 1, since that I/O is no longer misclassified as core-tier). See tools/refoundation/refoundation-layer-debt.json's matching note for the newly-disclosed cap->adapter edge this split's real caller (chronica-cap-org-workflow-routes) now has on the new adapter crate.",
  "_note_2026_09_16_commerce_site_intelligence_relocation": "Wholesale relocation of chronica-cap-crm-support-commerce-site-intelligence (100% impure site-intelligence persistence adapter; every item lives behind #[cfg(feature = \"db\")], public surface empty with db off) from crates/core/ to crates/adapter/, renamed to chronica-adapter-crm-support-commerce-site-intelligence. Drops misplaced_cap_package_count by 1 (no longer chronica-cap-* named) and forbidden_core_io_count by 1 (real Postgres I/O no longer misclassified as core-tier). See tools/refoundation/refoundation-layer-debt.json's matching note for the newly-disclosed cap->adapter edge the real caller (chronica-cap-platform-misc-routes) now has.",
  "_note_2026_09_16_contact_csv_import_relocation": "Wholesale relocation of chronica-cap-crm-support-contact-csv-import (100% impure CSV-import persistence orchestrator; every item lives behind #[cfg(feature = \"db\")], public surface empty with db off) from crates/core/ to crates/adapter/, renamed to chronica-adapter-crm-support-contact-csv-import. Drops misplaced_cap_package_count by 1 (no longer chronica-cap-* named) and forbidden_core_io_count by 1 (real Postgres orchestration no longer misclassified as core-tier). This relocation ALSO self-liquidates the LOOP 58 disclosed core->adapter debt edge onto chronica-adapter-crm-support-contacts (removed from tools/refoundation/refoundation-layer-debt.json in this same change, layer_debt_edge_count 162->161) -- see that file's matching note for the full resolution record.",
  "_note_2026_09_16_cross_holding_store_relocation": "131 -> 130 (misplaced_cap_package_count) and 60 -> 59 (forbidden_core_io_count): chronica-cap-cross-holding-store wholesale relocation crates/core/ -> crates/adapter/chronica-adapter-cross-holding-store (100% impure DB-backed persistence adapter, migration 0165, over chronica-core-cross-holding-domain's pure types -- no db feature gating anywhere, sqlx a plain unconditional dependency, no [features] section at all). Real caller chronica-cap-deals-federation-routes's src/cross_holding.rs repointed; one new disclosed cap->adapter layer-debt edge (see tools/refoundation/refoundation-layer-debt.json's _note_2026_09_16_cross_holding_store_relocation).",
  "_note_2026_09_16_erp_fulfillment_relocation": "130 -> 129 (misplaced_cap_package_count) and 59 -> 58 (forbidden_core_io_count): chronica-cap-erp-fulfillment wholesale relocation crates/core/ -> crates/adapter/chronica-adapter-erp-fulfillment (100% impure ERP fulfillment lifecycle persistence adapter, migration 0316 -- no db feature gating anywhere, sqlx a plain unconditional dependency, no [features] section at all). Real caller chronica-cap-erp-commerce-ops-routes's src/erp_fulfillment.rs repointed; one new disclosed cap->adapter layer-debt edge (see tools/refoundation/refoundation-layer-debt.json's _note_2026_09_16_erp_fulfillment_relocation).",
  "_note_2026_09_16_inventory_ledger_relocation": "129 -> 128 (misplaced_cap_package_count) and 58 -> 57 (forbidden_core_io_count): chronica-cap-inventory-ledger wholesale relocation crates/core/ -> crates/adapter/chronica-adapter-inventory-ledger (100% impure inventory stock-ledger persistence adapter -- no db feature gating anywhere, sqlx a plain unconditional dependency, no [features] section at all). Real caller chronica-cap-erp-commerce-ops-routes's src/inventory.rs repointed; one new disclosed cap->adapter layer-debt edge (see tools/refoundation/refoundation-layer-debt.json's _note_2026_09_16_inventory_ledger_relocation).",
  "_note_2026_09_16_hard_refoundation_backend_reset": "misplaced_cap_package_count 128 -> 0, world_graph_live_dependency_count 5 -> 0, forbidden_core_io_count 57 -> 0: the HARD REFOUNDATION backend reset deleted the entire crates/ tree (crates/cap, crates/core, crates/runtime, crates/adapter, crates/infra -- 1,315 crates total), replacing it with a minimal core/runtime/adapter workspace at the repository root. All three metrics are defined over the retired crates/ layout and legitimately go to 0, not because the underlying debt was individually resolved crate by crate (the 128 remaining misplaced-cap-package sweep this session was mid-way through, and the pre-existing forbidden-core-io/world-graph-live-dependency debt), but because that layout no longer exists. See docs/decisions/0016-legacy-backend-retired-to-git-history.md for recovery instructions and the full pre-reset inventory. This is the enforced floor going forward."
}
