# Atlas bootstrap and migration tools

This directory is not a production semantic owner.

Long-lived native behavior belongs under `core/`, `runtime/`, `adapter/`, or `apps/`. Existing tool families remain here only until their behavior has been censused, absorbed into a native capability, verified, and then retired. Do not add a parallel semantic universe under `tools/`.
