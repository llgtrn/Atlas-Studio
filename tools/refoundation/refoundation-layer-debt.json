{
  "schema": "chronica.refoundation.layer-debt.v1",
  "_note_2026_09_15_messenger_split": "TWO new edges, verbatim from validate-refoundation-layers.mjs: `chronica-cap-deals-federation-routes (cap) -> chronica-adapter-helpdesk-postgres (adapter)` and `chronica-cap-platform-admin-routes (cap) -> chronica-adapter-helpdesk-postgres (adapter)`. Both are the direct consequence of splitting crates/core/chronica-cap-helpdesk-messenger (M2 hit) into a pure chronica-core-helpdesk-messenger + a new messenger module in the existing chronica-adapter-helpdesk-postgres; both real callers are thin route/-routes crates that cannot themselves move without an out-of-scope decomposition of their own. Same established cap-routes -> adapter shape as every sibling edge in this file (e.g. the metadata-record split note immediately below). layer_debt_edge_count 141 -> 143. See tools/ci/responsibility-baseline.json's paired note.",
  "_note_2026_09_15_metadata_record_split": "TWO new edges, verbatim from validate-refoundation-layers.mjs: `chronica-cap-platform-admin-routes (cap) -> chronica-adapter-crm-support-metadata-record (adapter)` and `chronica-cap-social-antenna (core) -> chronica-adapter-crm-support-metadata-record (adapter)` (chronica-cap-social-antenna is physically under crates/core/ despite its name). Both are the direct consequence of splitting crates/core/chronica-cap-crm-support-metadata-record (M2+M7 hit) into a pure chronica-core-crm-support-metadata-record + chronica-adapter-crm-support-metadata-record; both real callers cannot themselves move without an out-of-scope decomposition of their own. Same established cap/core -> adapter shape as every sibling edge in this file. layer_debt_edge_count 139 -> 141. See tools/ci/responsibility-baseline.json's paired note.",
  "_note_2026_09_15_moderation_reports_split": "TWO new edges added this pass, disclosed rather than silently absorbed (exact strings taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output, not hand-guessed): `chronica-adapter-social-postgres-batch3 (core) -> chronica-runtime-social-moderation-reports (runtime)` and `chronica-cap-social-routes (cap) -> chronica-runtime-social-moderation-reports (runtime)`. Both are the direct, unavoidable consequence of a genuine responsibility-tier DECOMPOSITION (not a relocation): the former `crates/core/chronica-cap-social-moderation-reports` (a `chronica-cap-*` package physically under `crates/core/`, an M2 `misplaced_cap_package_count` hit, that also carried an `optional = true` `sqlx` dependency directly under `crates/core/`, an M7 `forbidden_core_io_count` hit) split into a pure vocabulary crate, `chronica-core-social-moderation-reports` (crates/core/, no sqlx, no [features] table), and a new runtime-tier orchestration crate, `chronica-runtime-social-moderation-reports` (crates/runtime/, imitating `chronica-runtime-gdpr-erasure`'s shape), which now owns the real Postgres create_report/assign_report/resolve_report/list_reports_for_company/list_moderation_actions/list_moderation_actions_for_target + the rules-catalog CRUD. `chronica-adapter-social-postgres-batch3` (PHYSICALLY under crates/core/, an unrelated, earlier placement decision) needs `list_moderation_actions_for_target` for `moderation_appeals::create_appeal`'s self-service discovery path; `chronica-cap-social-routes` (crates/cap/) needs the full report/resolve/assign/rules-CRUD surface for its HTTP routes. Both are real, non-dev [dependencies] edges onto the genuinely-decomposed-out runtime crate that neither caller can avoid without a much larger, out-of-scope crate decomposition of its own -- the SAME already-established `cap -> runtime` debt shape as this file's pre-existing `chronica-cap-deals-federation-routes (cap) -> chronica-runtime-gdpr-erasure (runtime)` and `chronica-cap-platform-misc-routes (cap) -> chronica-runtime-event-dispatch (runtime)` entries (both re-verified as still accurate in the current tree before citing as precedent), extended here to a `core -> runtime` edge for the same reason a `core -> adapter` edge already exists elsewhere in this file when a core-tier crate's real caller cannot itself move. `chronica-adapter-api-server` carried an unused-in-src Cargo.toml dependency + db-feature-array entry on the old crate (confirmed dead via grep across its own `src/`/`tests/` -- zero real references) -- REMOVED entirely (not repointed), per this program's standing rule. The pre-split crate's compatibility glob re-export (`pub use chronica_core_social_relationships::moderation::*;`) was DELETED outright, not carried into either half -- every real caller that reached `chronica_core_social_relationships::moderation` through it (mute/block/domain-block in `chronica-cap-social-routes`, `chronica-cap-social-notes`, `chronica-cap-social-notifications`, `chronica-adapter-social-postgres-batch1`, `chronica-core-social-relationships`'s own dev-dependency test) now imports `chronica_core_social_relationships::moderation` DIRECTLY instead, creating no new edge (each of those crates already had a `chronica-core-social-relationships` dependency edge, real or dev-only). layer_debt_edge_count: 137 -> 139 (2 new, 0 repaired). See `tools/ci/responsibility-baseline.json`'s `_social_moderation_reports_split_2026_09_15` for the paired M2/M7 metric disclosure.",
  "_note_2026_09_07y": "One new edge added this pass, disclosed rather than silently absorbed: `chronica-cap-platform-misc-routes (cap) -> chronica-adapter-crm-support-money-event-projector (adapter)` (exact string taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller change, not hand-guessed). It is the direct, unavoidable consequence of `chronica-cap-crm-support-money-event-projector`'s WHOLE-CRATE PHYSICAL RELOCATION (not a split -- see docs/_machine/graph-refoundation/subsystem-convergence.jsonl and the crate's own src/lib.rs module doc, same LOOP 42/46 RELOCATE shape): `crates/core/chronica-cap-crm-support-money-event-projector` moved verbatim to `crates/adapter/chronica-adapter-crm-support-money-event-projector`, since it has ZERO local pure type surface of its own -- its one public function, `project_money_events`, is a single sqlx-based read-then-write operation (its two private helpers, `kind_str`/`placeholder_scope`, are trivial and used only by that one function) -- so the usual 'shrink donor to pure-only + new adapter crate' template would have left an empty, pointless donor stub. The one real (non-dev) caller, `chronica-cap-platform-misc-routes`'s `src/analytics/event.rs` (the `project_money_events` call behind `POST /api/companies/:company_id/analytics/events/project-money`), is PHYSICALLY under `crates/cap/`, so repointing its `use`/Cargo.toml/db-feature-array dependency onto the now-`crates/adapter/`-tier crate creates this edge -- a cap-tier route crate reaching a genuinely-decomposed-out adapter crate it cannot avoid without a much larger, out-of-scope route-crate decomposition. Same, already-established shape as every sibling `chronica-cap-platform-misc-routes (cap) -> chronica-adapter-* (adapter)` edge already in this file (`chronica-adapter-crm-support-ad-spend`, `chronica-adapter-osint-http-core`, `chronica-adapter-outbound-fetch`, `chronica-adapter-postgres-artifact-registry`, all re-verified as still accurate in the current tree before citing as precedent) -- `chronica-cap-platform-misc-routes` already carries four edges of this exact shape, so this is its fifth, not a novel debt class. `chronica-adapter-api-server` carried an unused-in-src, optional Cargo.toml dependency + db-feature-array entry on the old crate (confirmed dead via grep across its own `src/`/`tests/` -- zero real references, not even transitive; its `tests/money_event_projector_route.rs` only exercises the route over HTTP, never imports the crate by name) -- REMOVED entirely (not repointed), per this program's standing rule. layer_debt_edge_count: 111 -> 112 (1 new, 0 repaired). See `tools/ci/responsibility-baseline.json`'s `_money_event_projector_relocation_2026_09_07y` for the paired M2/M7 metric disclosure, and `docs/_machine/graph-refoundation/subsystem-convergence.jsonl` for the full ledger entry.",
  "_note_2026_09_07x": "One new edge added this pass, disclosed rather than silently absorbed: `chronica-core-api-state (core) -> chronica-adapter-projection-replay-state (adapter)` (exact string taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller change, not hand-guessed). It is the direct, unavoidable consequence of `chronica-cap-projection-replay-state`'s WHOLE-CRATE PHYSICAL RELOCATION (not a split -- see docs/_machine/graph-refoundation/subsystem-convergence.jsonl and the crate's own src/lib.rs module doc): `crates/core/chronica-cap-projection-replay-state` moved verbatim to `crates/adapter/chronica-adapter-projection-replay-state`, since its entire `#[cfg(feature = \"db\")]`-gated `mod imp { ... }` (get_cursor/upsert_cursor) carries ZERO local pure type surface of its own -- just thin async sqlx wrappers returning `chronica_core_projection_engine::cursor::ProjectionCursor`, a type that lives in, and stays in, the separate ALREADY-PURE `chronica-core-projection-engine` crate -- so the usual 'shrink donor to pure-only + new adapter crate' template would have left an empty, pointless donor stub. The one real (non-dev) caller, `chronica-core-api-state`'s `projections.rs` (`get_cursor`/`upsert_cursor` calls in the projection-catalog routes), is PHYSICALLY under `crates/core/`, so repointing its `use`/Cargo.toml/db-feature-array dependency onto the now-`crates/adapter/`-tier crate creates this edge -- a core-tier crate reaching a genuinely-decomposed-out adapter crate it cannot avoid without a much larger, out-of-scope route/state-crate decomposition of `chronica-core-api-state` itself. Same, already-established shape as `chronica-core-api-state (core) -> chronica-adapter-api-gateway-server (adapter)` and `chronica-core-api-state (core) -> chronica-adapter-outbound-fetch (adapter)` (this same `forbidden_edges` list, both re-verified as still accurate in the current tree before citing as precedent) -- `chronica-core-api-state` already carries two edges of this exact shape, so this is its third, not a novel debt class. `chronica-adapter-api-server` carried an unused-in-src, optional Cargo.toml dependency + db-feature-array entry on the old crate (confirmed dead via grep across its own `src/`/`tests/` -- zero real references, not even transitive) -- REMOVED entirely (not repointed), per this program's standing rule. layer_debt_edge_count: 110 -> 111 (1 new, 0 repaired). See `tools/ci/responsibility-baseline.json`'s `_projection_replay_state_relocation_2026_09_07x` for the paired M2/M7 metric disclosure, and `docs/_machine/graph-refoundation/subsystem-convergence.jsonl` for the full ledger entry.",
  "_note_2026_09_07_loop24": "TWO forbidden_edges entries REPAIRED (removed, not repointed): `chronica-cap-api-background-dispatch (cap) -> chronica-runtime-event-dispatch (runtime)` and `chronica-cap-api-background-dispatch (cap) -> chronica-runtime-social-retention-gc (runtime)`. This pass physically relocated chronica-cap-api-background-dispatch itself (0-change move, logic byte-identical) from crates/cap/ to crates/runtime/chronica-runtime-api-background-dispatch -- a fresh full read of its src/background.rs found it is orchestration/lifecycle WIRING (env-gated spawn, connect-retry backoff, shutdown, health-marker transitions) that never issues a raw SQL statement itself (its only direct DB touch is a PgPersistence::connect_from_env pool handshake) and owns no domain logic of its own, calling only into already-decomposed runner crates -- genuine runtime-tier shape, not the adapter-tier mechanical-I/O shape its own pre-existing 'RUNTIME_BOUND (real I/O)' self-label suggested at a glance (that label is this codebase's idiom for 'has a real I/O dependency', not a tier claim; confirmed by re-reading the whole file, not just the doc comment). Both edges become runtime -> runtime (same-tier) after the move, so they are simply removed, not repointed to a new string. Its remaining 3 real deps (chronica-cap-helpdesk-sla-escalation, chronica-cap-network-privacy-scheduler, chronica-cap-network-privacy-rollup) are now runtime -> cap edges, which FORBIDDEN_TARGETS.runtime (={infra}) does not forbid, so no new edge is created there. Its one real caller, chronica-adapter-api-server, is now adapter -> runtime, also not forbidden (FORBIDDEN_TARGETS.adapter = {infra}). Net layer_debt_edge_count: 106 -> 104 (2 repaired, 0 new). Renamed away from the chronica-cap- prefix in the same change to avoid a new M2 (misplaced_cap_package_count) hit -- confirmed M2 unchanged at 230. cap_directory_count: 37 -> 36 (tools/ci/topology-baseline.json updated in the same change). Exact edge removal confirmed via `node tools/refoundation/validate-refoundation-layers.mjs`'s own output. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full ledger entry.",
  "_note_2026_09_06h": "cap-retirement correction, NOT a new violation: two `forbidden_edges` entries repointed, net flat (102 -> 102). `chronica-core-cli-media (core) -> chronica-cap-media-bgm-library (cap)` and `chronica-core-cli-media (core) -> chronica-cap-media-parser-writers (cap)` are REMOVED (stale -- their targets no longer exist under `crates/cap/`) and replaced by `chronica-core-cli-media (core) -> chronica-adapter-media-bgm-library (adapter)` and `chronica-core-cli-media (core) -> chronica-adapter-media-parser-writers (adapter)` (exact strings confirmed via `node tools/refoundation/validate-refoundation-layers.mjs`'s own output: `no new edges (102 known debt edge(s) remain)`, i.e. both new strings already matched the tool's own forbidden-edge detection against the real post-migration dependency graph). This is a genuine TIER-NAME CORRECTION, not a new architectural compromise: both target crates always performed real, credential-free, network-free filesystem I/O with no orchestration -- exactly `crates/adapter/`'s own definition -- they were simply misplaced under the transitional `crates/cap/` root from their original extraction out of the former flat `chronica-media` crate. `chronica-cap-media-bgm-library` relocated wholesale (unchanged logic) to `chronica-adapter-media-bgm-library`. `chronica-cap-media-parser-writers` genuinely mixed a pure half (sample-timescale rescaling: `fix_float`/`convert_sample_timing_to_webcodecs_timescale`) with an impure half (the real file-backed `NodeWriter`/`WavWriter`); the pure half was extracted to a NEW pure crate, `chronica-core-media-parser-writers` (`crates/core/` -- a `core -> core` edge, not a debt edge, matching this program's `chronica-core-social-user-profile` / `chronica-adapter-postgres-social-user-profile` split precedent), and the impure half relocated to `chronica-adapter-media-parser-writers`, which depends on the new pure crate and re-exports its items so `chronica-core-cli-media` needs only one import path per original crate. Both relocated/new packages were renamed to drop the `chronica-cap-` prefix in the same change specifically to avoid a new M2 (`misplaced_cap_package_count`) hit under `tools/ci/verify-responsibility-boundaries.mjs` -- confirmed M2 unchanged at 230 after this move (`node tools/ci/verify-responsibility-boundaries.mjs --report`). See `docs/_machine/graph-refoundation/subsystem-convergence.jsonl` for the full ledger entry, and `tools/ci/verify-repository-topology.mjs` / `tools/ci/topology-baseline.json` for the `cap_directory_count` metric (41 -> 39, baseline updated in the same change).",
  "rationale": "Exact task-start debt; entries are not architectural exceptions. Remove an entry when its edge is repaired.",
  "_note_2026_09_06": "One new edge added this pass, disclosed rather than silently absorbed: `chronica-cap-deals-federation-routes (cap) -> chronica-runtime-gdpr-erasure (runtime)`. It is a direct consequence of chronica-core-gdpr-erasure's responsibility-tier decomposition (see docs/_machine/graph-refoundation/subsystem-convergence.jsonl) -- execute_purge/verify_purge_completed/etc. moved from the pure core-tier domain crate to a new runtime-tier orchestration facade, and this cap-tier HTTP-route crate is a genuine, unavoidable caller of that orchestration (a cap-tier crate cannot depend on adapter or runtime under FORBIDDEN_TARGETS, but routing HTTP requests to real orchestration has to live somewhere). CORRECTED 2026-09-06 (adversarial review of this same pass): an earlier version of this note additionally cited `chronica-cap-api-background-dispatch -> chronica-runtime-event-dispatch` as a second matching precedent -- independently checked and that one is NOT the same shape (that crate's own module doc states it adds no HTTP route at all; it is boot-time task-spawning wiring that starts a background relay at process startup, not a route crate forwarding an HTTP request to orchestration). The genuinely matching precedent is `chronica-cap-platform-misc-routes -> chronica-runtime-event-dispatch` (real axum route handlers calling into the same runtime-tier facade), which is the established pattern this new edge follows. The crate this pass actually targeted for a genuinely-new-and-clean placement is the ADAPTER half (chronica-adapter-postgres-gdpr-erasure), which lands under crates/adapter/ with ZERO new edges of its own -- only the new runtime crate depends on it.",
  "_note_2026_09_06b": "Two new edges added this pass, disclosed rather than silently absorbed: `chronica-cap-social-routes (cap) -> chronica-adapter-postgres-social-user-profile (adapter)` and `chronica-cap-platform-admin-routes (cap) -> chronica-adapter-postgres-social-user-profile (adapter)` (exact strings taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller changes, not hand-guessed). Both are the direct, unavoidable consequence of `chronica-core-social-user-profile`'s responsibility-tier decomposition (see docs/_machine/graph-refoundation/subsystem-convergence.jsonl): the real `update_profile`/`get_profile` Postgres mechanics moved out of that core-tier crate entirely, into the new `chronica-adapter-postgres-social-user-profile` (crates/adapter/); the pure validation (`validate_profile_update`) and domain types (`ProfileField`/`ProfileRow`/`UpdateProfileOutcome`) stayed in the shrunk, now-always-pure core crate. Both real (non-dev) callers of the relocated `update_profile`/`get_profile` are HTTP route crates under `crates/cap/` (`chronica-cap-social-routes`'s `social_user_profile.rs`, `chronica-cap-platform-admin-routes`'s `self_api.rs`), which cannot depend on `crates/runtime/` or `crates/adapter/` under `FORBIDDEN_TARGETS.cap` -- yet a route handler forwarding a validated write/read to real persistence has to reach it somehow, and a route-crate cannot itself live below `crates/cap/` without a much larger, out-of-scope route-crate decomposition. Independently re-verified before citing as precedent (both still accurate in the current tree): `chronica-cap-deals-federation-routes -> chronica-runtime-gdpr-erasure` (this same forbidden_edges list, `_note_2026_09_06` above) and `chronica-cap-platform-misc-routes -> chronica-runtime-event-dispatch` (grepped `chronica-cap-platform-misc-routes/src/` for real `chronica_runtime_event_dispatch` usage -- confirmed present in `src/analytics/{insight,olap_rollup,mod,meta_plane_gate}.rs`, real axum route handlers calling into that runtime-tier facade, not boot-time wiring) -- both are the SAME shape, a cap-tier HTTP-route crate calling a genuinely-decomposed-out orchestration/persistence tier it cannot otherwise reach, so this is the established pattern this new pair of edges follows, not a novel debt class.",
  "_note_2026_09_06c": "Two new edges added this pass, disclosed rather than silently absorbed: `chronica-cap-social-notes (core) -> chronica-adapter-postgres-social-handle (adapter)` and `chronica-cap-social-routes (cap) -> chronica-adapter-postgres-social-handle (adapter)` (exact strings taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller changes, not hand-guessed). Both are the direct, unavoidable consequence of `chronica-core-social-handle`'s responsibility-tier decomposition (see docs/_machine/graph-refoundation/subsystem-convergence.jsonl): the real `register_handle`/`get_own_handle`/`resolve_handle` Postgres mechanics moved out of that core-tier crate entirely, into the new `chronica-adapter-postgres-social-handle` (crates/adapter/); the pure validation (`normalize_handle`) and domain types (`HandleRow`/`RegisterHandleOutcome`) stayed in the shrunk, now-always-pure core crate. These two edges are of TWO DIFFERENT SHAPES, each independently precedented: (1) `chronica-cap-social-routes -> chronica-adapter-postgres-social-handle` is a `crates/cap/`-tier HTTP route crate (`social_handles.rs`, the self-service handle registration routes) reaching the relocated `register_handle`/`get_own_handle` -- re-verified as still accurate the SAME shape already established by `chronica-cap-social-routes -> chronica-adapter-postgres-social-user-profile` (this same forbidden_edges list, `_note_2026_09_06b` above), which this pass's own edge is essentially a sibling of (same cap-tier caller, same adapter-tier target crate family). (2) `chronica-cap-social-notes -> chronica-adapter-postgres-social-handle` is DIFFERENT: `chronica-cap-social-notes` is a `chronica-cap-*`-named capability crate that is PHYSICALLY misplaced under `crates/core/` from an earlier extraction (confirmed by directory: `crates/core/chronica-cap-social-notes/`) -- its `imp/create.rs`'s `emit_note_notifications` resolves a raw MFM `@handle` mention through `resolve_handle` before the mute check / notification insert (issue #1580 fix), a real, unavoidable dependency this pass does not attempt to fix by relocating the caller crate itself (out of scope, a much larger route/capability-crate decomposition). Re-verified before citing as precedent (still accurate in the current tree, grepped both source trees for real, non-doc-comment usage): `chronica-core-api-state (core) -> chronica-adapter-api-gateway-server (adapter)` and `chronica-core-cli-analytics (core) -> chronica-adapter-analytics-server (adapter)` (this same forbidden_edges list; `chronica-core-cli-analytics/src/{prometheus_interserver_cli.rs,acme_protocol_eval.rs}` both contain real `chronica_adapter_analytics_server` usage) -- both are the SAME shape as edge (2): a misplaced-under-core capability/CLI crate needing to reach a genuinely-decomposed-out adapter it cannot avoid, an already-established, tolerated debt class in this program, not a novel one.",
  "_note_2026_09_06d": "One new edge added this pass, disclosed rather than silently absorbed: `chronica-cap-agent-session-routes (cap) -> chronica-adapter-postgres-analytics-metric-lineage (adapter)` (exact string taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller changes, not hand-guessed). It is the direct, unavoidable consequence of `chronica-core-analytics-metric-lineage`'s responsibility-tier decomposition (see docs/_machine/graph-refoundation/subsystem-convergence.jsonl): the real `load_dependencies`/`add_dependency`/`record_revision`/`record_event` Postgres mechanics (formerly a `#[cfg(feature = \"db\")] pub mod store` directly under `crates/core/`) moved out of that core-tier crate entirely, into the new `chronica-adapter-postgres-analytics-metric-lineage` (`crates/adapter/`); the pure dependency-graph domain (`MetricDependencyGraph`/`MetricLineageStore`/`topological_order`/the event & revision types) stayed in the shrunk, now-always-pure core crate. The real (non-dev) caller of the relocated functions is `chronica-cap-agent-session-routes`'s `metric_lineage_routes` (`dto.rs`/`handlers.rs`), a `crates/cap/`-tier HTTP route crate, which cannot depend on `crates/adapter/` under `FORBIDDEN_TARGETS.cap` -- yet a route handler forwarding a validated write/read to real persistence has to reach it somehow, and a route crate cannot itself live below `crates/cap/` without a much larger, out-of-scope route-crate decomposition. Independently re-verified before citing as precedent (both still accurate in the current tree, re-read in full): `chronica-cap-social-routes -> chronica-adapter-postgres-social-user-profile` (this same `forbidden_edges` list, `_note_2026_09_06b`) and `chronica-cap-social-routes -> chronica-adapter-postgres-social-handle` (`_note_2026_09_06c`) -- both are the exact same shape: a `crates/cap/`-tier HTTP route crate reaching a genuinely-decomposed-out `crates/adapter/` crate it cannot avoid, an already-established, tolerated debt class in this program, not a novel one. This pass's second (adapter-tier) real caller, `chronica-adapter-api-server`, needed no disclosure at all (adapter -> adapter is allowed under `FORBIDDEN_TARGETS.adapter`).",
  "_note_2026_09_06e": "Four new edges added this pass, disclosed rather than silently absorbed: `chronica-cap-social-routes (cap) -> chronica-adapter-postgres-social-profile-privacy (adapter)`, `chronica-cap-platform-admin-routes (cap) -> chronica-adapter-postgres-social-profile-privacy (adapter)`, `chronica-adapter-social-postgres-batch3 (core) -> chronica-adapter-postgres-social-profile-privacy (adapter)`, and `chronica-core-social-relationships (core) -> chronica-adapter-postgres-social-profile-privacy (adapter)` (exact strings taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller changes, not hand-guessed). All four are the direct, unavoidable consequence of `chronica-core-social-profile-privacy`'s responsibility-tier decomposition (see docs/_machine/graph-refoundation/subsystem-convergence.jsonl): the real `list_directory`/`set_hides_followers`/`get_hides_followers`/`set_discoverable`/`get_discoverable`/`set_locked`/`get_locked` Postgres mechanics moved out of that core-tier crate entirely, into the new `chronica-adapter-postgres-social-profile-privacy` (`crates/adapter/`); the pure types (`ProfilePrivacyRow`/`DirectoryEntry`/`DirectoryCursor`/`SetProfilePrivacyOutcome`) and helpers (`DIRECTORY_PAGE_MAX`/`clamp_directory_limit`/`profile_privacy_error_reason`) stayed in the shrunk, now-always-pure core crate. These four edges are of TWO DIFFERENT SHAPES, each independently precedented: (1) `chronica-cap-social-routes -> chronica-adapter-postgres-social-profile-privacy` (the opt-in profile-directory route, `social_directory.rs`) and `chronica-cap-platform-admin-routes -> chronica-adapter-postgres-social-profile-privacy` (the hides_followers/discoverable toggle routes, `profile_privacy.rs`) are both `crates/cap/`-tier HTTP route crates reaching the relocated functions -- re-verified as the SAME shape already established by `chronica-cap-social-routes -> chronica-adapter-postgres-social-user-profile` and `chronica-cap-platform-admin-routes -> chronica-adapter-postgres-social-user-profile` (this same forbidden_edges list, `_note_2026_09_06b`). (2) `chronica-adapter-social-postgres-batch3 -> chronica-adapter-postgres-social-profile-privacy` and `chronica-core-social-relationships -> chronica-adapter-postgres-social-profile-privacy` are DIFFERENT: both callers are PHYSICALLY under `crates/core/` despite the first carrying an `-adapter-`-shaped name (confirmed by directory: `crates/core/chronica-adapter-social-postgres-batch3/`; that crate's own module doc explains it stays under `crates/core/` because ITS OWN real callers span `crates/core/` and `crates/cap/` -- an unrelated, independent placement decision from this pass). `chronica-adapter-social-postgres-batch3`'s `follow_requests::request_follow`/`authorize_follow_request` call the relocated `get_locked`/`set_locked` for the locked-account branch (donor: `FollowService`'s own gating), a real, load-bearing production dependency unchanged by this pass. `chronica-core-social-relationships`'s `follow::row::hides_list_from` calls the relocated `get_hides_followers` to gate `list_followers`/`list_following` (A-lane aN23), also unchanged by this pass. Re-verified before citing as precedent (still accurate in the current tree, grepped both source trees for real, non-doc-comment usage): `chronica-cap-social-notes (core) -> chronica-adapter-postgres-social-handle (adapter)` (this same forbidden_edges list, `_note_2026_09_06c`) -- the same shape as edge (2): a capability/consolidation crate PHYSICALLY misplaced under `crates/core/` needing to reach a genuinely-decomposed-out adapter it cannot avoid, an already-established, tolerated debt class in this program, not a novel one.",
  "_note_2026_09_06g": "Five new edges added this pass, disclosed rather than silently absorbed: `chronica-cap-company-money-actions (cap) -> chronica-adapter-postgres-artifact-registry (adapter)`, `chronica-cap-company-platform-ops (cap) -> chronica-adapter-postgres-artifact-registry (adapter)`, `chronica-cap-deals-federation-routes (cap) -> chronica-adapter-postgres-artifact-registry (adapter)`, `chronica-cap-platform-misc-routes (cap) -> chronica-adapter-postgres-artifact-registry (adapter)`, and `chronica-cap-media-code-artifacts (core) -> chronica-adapter-postgres-artifact-registry (adapter)` (exact strings taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller changes, not hand-guessed). All five are the direct, unavoidable consequence of `chronica-core-artifact-registry`'s responsibility-tier decomposition (see docs/_machine/graph-refoundation/subsystem-convergence.jsonl): the real `ArtifactRepo` Postgres mechanics (`register`/`get_content_scoped`/`review_scoped`/`get_scoped`/`find_by_sha256_scoped`/`list`/`list_for_account`, formerly a `#[cfg(feature = \"db\")] pub mod repo` directly under `crates/core/`) moved out of that core-tier crate entirely, into the new `chronica-adapter-postgres-artifact-registry` (`crates/adapter/`); the pure `Artifact`/`ArtifactVersion`/`ReviewStatus` domain, the `sanitize_html`/`sanitize_svg`/`sanitize_mermaid` sanitizers, and the in-memory `ArtifactMetadataStore` stayed in the shrunk, now-always-pure core crate. These five edges are of TWO DIFFERENT SHAPES, each independently precedented: (1) the four `chronica-cap-*-routes`/`chronica-cap-company-*` edges are `crates/cap/`-tier HTTP route/action crates (`chronica-cap-company-money-actions`'s `companies.rs` board-review action, `chronica-cap-company-platform-ops`'s `artifacts_content.rs`/`genesis.rs`, `chronica-cap-deals-federation-routes`'s `holding.rs` holding-feed read, `chronica-cap-platform-misc-routes`'s `store_platform/artifact.rs`) reaching the relocated `ArtifactRepo` -- re-verified as the SAME shape already established by `chronica-cap-social-routes -> chronica-adapter-postgres-social-user-profile` (this same forbidden_edges list, `_note_2026_09_06b`) and every sibling cap -> adapter edge in this ledger. (2) `chronica-cap-media-code-artifacts (core) -> chronica-adapter-postgres-artifact-registry (adapter)` is DIFFERENT: `chronica-cap-media-code-artifacts` is a `chronica-cap-*`-named capability crate PHYSICALLY misplaced under `crates/core/` from an earlier extraction (confirmed by directory: `crates/core/chronica-cap-media-code-artifacts/`) -- its own `persistence.rs` uses only the PURE `MAX_ARTIFACT_CONTENT_BYTES` constant plus the pure `sanitize_html`/`sanitize_svg`/`sanitize_mermaid` sanitizers (never `ArtifactRepo` itself), but `MAX_ARTIFACT_CONTENT_BYTES` moved out of the pure crate alongside the impure repo it exclusively bounds, so this misplaced crate now reaches it through the adapter crate's re-export. Re-verified before citing as precedent (still accurate in the current tree, grepped both source trees for real, non-doc-comment usage): `chronica-cap-social-notes (core) -> chronica-adapter-postgres-social-handle (adapter)` (this same forbidden_edges list, `_note_2026_09_06c`) -- the same shape as edge (2): a capability crate PHYSICALLY misplaced under `crates/core/` needing to reach a genuinely-decomposed-out adapter it cannot avoid, an already-established, tolerated debt class in this program, not a novel one. Two callers named in the task's own caller list needed NO edge and NO change beyond confirming this: `chronica-core-engine-tool-adapters` (physically core-tier) uses only the pure `Artifact`/`ArtifactMetadataStore` types via its `artifact_adapter.rs`/`integration.rs`, never `ArtifactRepo`, and carries no `db` feature at all -- confirmed by a full-crate grep finding zero `ArtifactRepo`/`repo::` references; its own `tests/money_guard.rs` (`source_performs_no_live_io`) independently proves this by scanning for forbidden I/O tokens including `sqlx`. `chronica-cap-runtime-ops-routes` was found, on inspection, to depend on the old crate WITHOUT USING IT AT ALL (its own `runtime_artifact_meta.rs` doc comment explicitly states \"Does not wrap `chronica_core_artifact_registry::ArtifactRepo`\" -- it wraps the unrelated `chronica_cap_runtime_persistence::ArtifactRepo` instead) -- the dead Cargo.toml dependency and its `/db` feature-list entry were removed rather than repointed, since repointing an unused dependency would manufacture a phantom edge with no real caller behind it. `chronica-adapter-api-server` (adapter-tier) needed no disclosure at all (adapter -> adapter is allowed under `FORBIDDEN_TARGETS.adapter`) -- its own production `src/` carries no direct usage either (only its `tests/{store_platform_route,artifacts_route/*}.rs` integration tests exercise `ArtifactRepo` directly, repointed to the new adapter crate alongside the Cargo.toml dependency swap).",
  "_note_2026_09_06f": "One new edge added this pass, disclosed rather than silently absorbed: `chronica-cap-runtime-ops-routes (cap) -> chronica-adapter-postgres-user-kv-feedback (adapter)` (exact string taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller changes, not hand-guessed). It is the direct, unavoidable consequence of `chronica-core-user-kv-feedback`'s responsibility-tier decomposition (see docs/_machine/graph-refoundation/subsystem-convergence.jsonl): the real `UserKvRepo`/`FeedbackRepo` Postgres mechanics (formerly `#[cfg(feature = \"db\")] mod repo` blocks in that crate's own `kv.rs`/`feedback.rs`, directly under `crates/core/`) moved out entirely, into the new `chronica-adapter-postgres-user-kv-feedback` (`crates/adapter/`); the pure validation/domain types (`KvKey`/`UserKvEntry`/`FeedbackRating`/`FeedbackRecord`/`UserId`/the size-bound constants/`validate_*`) stayed in the shrunk, now-always-pure core crate. THIS PASS DIFFERS FROM EVERY PRIOR ENTRY IN THIS LEDGER in one important respect, investigated in full before deciding a placement: the impure repos were previously re-exported not from a `crates/cap/`- or `crates/core/`-tier capability crate, but from `chronica-core` ITSELF -- the foundational facade crate (`crates/core/chronica-core`) that nearly the entire workspace depends on (`pub use chronica_core_user_kv_feedback::{FeedbackRepo, UserKvRepo};` behind its own `db` feature, plus an unconditional `pub mod user_kv_feedback { pub use chronica_core_user_kv_feedback::user_kv_feedback::*; }` that would also have carried them). Repointing that re-export at the new adapter crate, as every prior entry in this ledger does for its own donor crate's facade, would have created `chronica-core`'s FIRST EVER `core -> adapter` edge -- categorically more sensitive than a route-crate or misplaced-capability-crate edge, since `chronica-core` sits at the base of the dependency graph rather than at a leaf. Before choosing a placement, every real (non-dev) consumer of the impure `chronica_core::{FeedbackRepo, UserKvRepo}` re-export was enumerated workspace-wide (`grep -rl 'FeedbackRepo\\|UserKvRepo'` across `crates/`, then individually reading each hit to separate real usage from doc-comment lineage citations): exactly ONE real consumer exists, `chronica-cap-runtime-ops-routes`'s `runtime_user_kv.rs` (a `crates/cap/`-tier HTTP route module, itself `#![cfg(feature = \"db\")]`-gated end to end). A second candidate, `chronica-adapter-cli-runtime`'s `user_kv_feedback_eval.rs`, was read in full and confirmed by its OWN module doc to deliberately skip `UserKvRepo`/`FeedbackRepo` as out of scope, using only the pure validators (`UserId`/`validate_user_id`/`KvKey`/`validate_kv_value`/`FeedbackRating`/`validate_comment`/`validate_context`) -- it needed no change. Three more crates matched a broad `chronica_core::user_kv_feedback` grep (`chronica-core-erp-db-schema`, `chronica-core-erp-doctype`, `chronica-core-erp-quota-limiting`) but were individually verified to be FALSE POSITIVES: all three matches are inside `///` doc comments citing `MAX_KV_KEY_BYTES`/`MAX_KV_VALUE_BYTES` as a size-bound lineage precedent in prose, never a real `use`/call; two of the three (`chronica-core-erp-db-schema`, `chronica-core-erp-doctype`) do not even carry a Cargo.toml dependency on `chronica-core` or `chronica-core-user-kv-feedback` at all, so they could not compile against these symbols even if the prose were code. Because the real consumer set was exactly one crate and that crate already sits in `crates/cap/` (a tier already permitted to carry adapter-edge debt under this program's established pattern), this pass took OPTION (b) from its own task directive rather than option (a): `chronica-core`'s re-export of `FeedbackRepo`/`UserKvRepo` was DELETED entirely (both the explicit `#[cfg(feature = \"db\")] pub use` and, structurally, from the unconditional `user_kv_feedback::*` glob, since the pure crate no longer defines those symbols under any feature at all), and `chronica-cap-runtime-ops-routes` was repointed to depend on `chronica-adapter-postgres-user-kv-feedback` directly. This avoids `chronica-core` ever acquiring a `core -> adapter` edge, replacing it with an ordinary `cap -> adapter` edge of the SAME shape already established by `chronica-cap-social-routes -> chronica-adapter-postgres-social-user-profile` (`_note_2026_09_06b`) and its siblings in this ledger -- re-verified as still accurate before citing as precedent. `chronica-core`'s own PURE re-exports of this crate's items (`KvKey`/`UserKvEntry`/`FeedbackRating`/`FeedbackRecord`/`UserId`/`MAX_COMMENT_BYTES`/`MAX_CONTEXT_BYTES`/`MAX_KV_VALUE_BYTES`, plus the `chronica_core::user_kv_feedback` module path's `validate_user_id`/`validate_kv_value`/`validate_comment`/`validate_context`) are UNCHANGED and continue to work through the facade unmodified, since those items never left `chronica-core-user-kv-feedback` -- `chronica-cap-runtime-ops-routes` itself still imports them from `chronica_core::user_kv_feedback` exactly as before, only its two impure `UserKvRepo`/`FeedbackRepo` imports moved to the new adapter crate.",
  "_note_2026_09_07v": "One new edge added this pass, disclosed rather than silently absorbed: `chronica-cap-social-routes (cap) -> chronica-adapter-social-user-lists (adapter)` (exact string taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller change, not hand-guessed). It is the direct, unavoidable consequence of `chronica-cap-social-user-lists`'s responsibility-tier decomposition (see docs/_machine/graph-refoundation/subsystem-convergence.jsonl): the real `create_user_list`/`get_user_list`/`list_user_lists_by_owner`/`add_member`/`list_members`/`remove_member`/`list_user_list_notes` Postgres mechanics (formerly a `#[cfg(feature = \"db\")] mod imp` directly under `crates/core/`) moved out of that crate entirely, into the new `chronica-adapter-social-user-lists` (`crates/adapter/`); the pure `UserListRow`/`UserListMemberRow` types (+ `to_json`) and the pure `CreateUserListOutcome`/`AddMemberOutcome` outcome enums stayed in the shrunk, now-always-pure core crate. The one real (non-dev) caller of the relocated functions, `chronica-cap-social-routes`'s `social_lists.rs`, is a `crates/cap/`-tier HTTP route crate, which cannot depend on `crates/adapter/` under `FORBIDDEN_TARGETS.cap` -- yet a route handler forwarding a validated read/write to real persistence has to reach it somehow, and a route crate cannot itself live below `crates/cap/` without a much larger, out-of-scope route-crate decomposition. Same, already-established shape as `chronica-cap-social-routes -> chronica-adapter-postgres-social-handle` and every sibling `chronica-cap-*-routes -> chronica-adapter-*` edge already in this file (re-verified as still accurate before citing as precedent). `list_user_list_notes`'s own direct dependency on `chronica-cap-social-notes` (for `NoteRow`/`clamp_note_limit` reuse) moved unchanged into the new adapter crate: `chronica-cap-social-notes` was independently checked and found NOT YET pure (it still carries its own `#[cfg(feature = \"db\")]`-gated real Postgres mechanics behind its own `db` feature -- a much larger, out-of-scope decomposition of its own), so there is no separate adapter-tier crate to repoint onto; this is a plain `adapter -> core` edge (the physical layer `chronica-cap-social-notes` lives under is `crates/core/`), which `FORBIDDEN_TARGETS.adapter` (= {infra}) does not forbid -- confirmed via the gate script's own output carrying no second `NEW FORBIDDEN EDGE:` line for this dependency. The pre-existing `chronica-adapter-api-server` Cargo.toml dependency on the pre-split crate was confirmed dead in its own `src/`/`tests/` (reaches the real ops only transitively, via `chronica-cap-social-routes`) and removed outright rather than repointed, per this program's standing LOOP-21 precedent.",
  "_note_2026_09_08_loop51": "One new edge added this pass, disclosed rather than silently absorbed: `chronica-core-api-state (core) -> chronica-adapter-projection-read-model (adapter)` (exact string taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` line after the caller change, not hand-guessed). layer_debt_edge_count: 113 -> 114 (1 new, 0 repaired). LOOP 51 decomposed crates/core/chronica-cap-projection-read-model: its real Postgres mechanics (get_read_model/upsert_read_model over projection_read_model, migration 0087, plus the private RM_COLS column-list constant and the inline row decode) moved BYTE-IDENTICAL into the new crates/adapter/chronica-adapter-projection-read-model, closing an M7 forbidden_core_io_count hit (130 -> 129, baseline updated in this same change). The donor kept ONLY the pure CachedReadModel row struct and is now genuinely pure -- zero cargo features, serde_json its single dependency; no feature-gating judgment call was needed, since that struct's fields are plain String/Option<String>/i64/i32 plus a serde_json::Value and name no type from any db-gated module. The one real (non-dev) production caller, `chronica-core-api-state`'s projections.rs (`get_read_model`/`upsert_read_model` in the get_projection_read_model route handler), was repointed onto the adapter under its existing `read_model_store` alias; that caller is itself PHYSICALLY under crates/core/ despite owning real sqlx I/O (its own, larger, out-of-scope M7 violation that a route/state-crate decomposition would be needed to fix), so the repoint creates this edge. Same caller and same already-established shape as `chronica-core-api-state (core) -> chronica-adapter-projection-replay-state (adapter)` from the 2026-09-07 replay-state relocation (_note_2026_09_07x), plus its pre-existing `-> chronica-adapter-api-gateway-server` and `-> chronica-adapter-outbound-fetch` edges (all three re-verified as still present in this same forbidden_edges list before citing as precedent) -- this is chronica-core-api-state's FOURTH edge of this exact class, not a novel debt class. The second repointed consumer, chronica-cap-projection-dependencies' live-DB invalidation tests, is a DEV-dependency and therefore adds NO edge (cargo strips dev-dependencies from the production dependency graph this validator walks) -- independently confirmed by the validator reporting exactly ONE new edge. `chronica-adapter-api-server` carried an unused-in-src, optional Cargo.toml dependency + db-feature-array entry on the donor crate (confirmed dead via grep across its own src/ and tests/ -- the only `projection_read_model` hits there are raw SQL table names and the route-handler function name, zero crate references) -- REMOVED entirely (not repointed), per this program's standing rule and exactly as _note_2026_09_07x did for the same manifest. Recorded by hand, NOT via --write-baseline (which would destroy the notes above). See tools/ci/responsibility-baseline.json's _forbidden_core_io_count_2026_09_08c for the paired M7 metric disclosure.",
  "_note_2026_09_08_loop54": "One new edge added this pass, disclosed rather than silently absorbed: `chronica-cap-platform-misc-routes (cap) -> chronica-adapter-crm-support-event-definitions (adapter)` (exact string taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller change, not hand-guessed). It is the direct, unavoidable consequence of `chronica-cap-crm-support-event-definitions`'s WHOLE-CRATE PHYSICAL RELOCATION (not a split -- see docs/_machine/graph-refoundation/subsystem-convergence.jsonl and the crate's own src/lib.rs module doc, same LOOP 42/46/47/48/52/53 RELOCATE shape): `crates/core/chronica-cap-crm-support-event-definitions` moved verbatim to `crates/adapter/chronica-adapter-crm-support-event-definitions`, since it has ZERO local pure type surface of its own -- it declares no `pub struct`/`pub enum` at all, its entire body is one `#[cfg(feature = \"db\")] mod imp { ... }`, and with the `db` feature off it exports literally nothing. Every non-primitive type it names (`EventDefinition`, `PropertyType`, `EventDefinitionRegistry`) lives in, and stays in, the separate ALREADY-PURE `chronica-core-analytics-event-definition` crate (verified from its own Cargo.toml in the current tree: dependencies are only chronica-core + serde; no sqlx, no `db` feature) -- NOT touched or moved by this change. Its three non-`pub` in-gate items (`EVENT_SCAN_LIMIT`, `infer_type`, `parse_epoch`) are private I/O tuning/parsing helpers used only by `derive_registry`, not a pure domain surface, so the usual 'shrink donor to pure-only + new adapter crate' template would have left an empty, pointless donor stub. The one real (non-dev) caller, `chronica-cap-platform-misc-routes`'s `src/analytics/event_definition.rs` (the four axum handlers behind `GET|PUT /api/companies/:company_id/analytics/event-definitions[...]`, verified by a fully-qualified-path grep for `chronica_cap_crm_support_event_definitions::`, not a bare function-name grep), is PHYSICALLY under `crates/cap/`, so repointing its `use`/Cargo.toml/db-feature-array dependency onto the now-`crates/adapter/`-tier crate creates this edge -- a cap-tier route crate reaching a genuinely-decomposed-out adapter crate it cannot avoid without a much larger, out-of-scope route-crate decomposition. Same, already-established shape as every sibling `chronica-cap-platform-misc-routes (cap) -> chronica-adapter-* (adapter)` edge already in this file (`chronica-adapter-crm-support-ad-spend`, `chronica-adapter-crm-support-money-event-projector`, `chronica-adapter-osint-http-core`, `chronica-adapter-outbound-fetch`, `chronica-adapter-postgres-artifact-registry`, all re-verified as still present in this same forbidden_edges array before citing as precedent) -- `chronica-cap-platform-misc-routes` already carries FIVE edges of this exact shape, so this is its sixth, not a novel debt class. `chronica-adapter-api-server` carried an unused-in-src, non-optional Cargo.toml dependency + db-feature-array entry on the old crate (confirmed dead via grep across its own `src/`/`tests/` -- zero real references; its `tests/analytics_event_definition_route.rs` only exercises the four routes over HTTP and never imports the crate by name) -- REMOVED entirely (not repointed), per this program's standing rule. DISCLOSED AND DELIBERATELY OUT OF SCOPE: the relocated crate depends on `chronica-core-business-events`, which is ITSELF a `crates/core/` crate that still carries an optional sqlx + its own `db` feature (not yet purified). That is a separate, pre-existing condition this relocation neither creates nor repairs; the relationship is strictly one-way (this crate CONSUMES `chronica_core_business_events::list_events`, an adapter -> core edge which is ALLOWED under FORBIDDEN_TARGETS.adapter; that crate does not depend on this one), so no change to that sibling was needed or made. layer_debt_edge_count: 114 -> 115 (1 new, 0 repaired). See `tools/ci/responsibility-baseline.json` for the paired M2/M7 metric disclosure, and `docs/_machine/graph-refoundation/subsystem-convergence.jsonl` for the full ledger entry.",
  "_note_2026_09_08_loop50": "One new edge added this pass, disclosed rather than silently absorbed: `chronica-cap-scheduling-booking (core) -> chronica-adapter-scheduling-host (adapter)` (exact string taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller change, not hand-guessed). It is the direct, unavoidable consequence of LOOP 50's RESPONSIBILITY-TIER DECOMPOSITION of `crates/core/chronica-cap-scheduling-host` (an M7 forbidden_core_io_count hit: a 140-line single-file crate whose `#[cfg(feature = \"db\")]`-gated `mod imp` carried real sqlx add_host/list_hosts/remove_host mechanics over `event_type_hosts`, migration 0369, directly under crates/core/). Those mechanics moved BYTE-IDENTICAL into the new `crates/adapter/chronica-adapter-scheduling-host`; the donor keeps only its pure `AddHostOutcome` enum, unconditionally (its payload types `Host`/`HostError` come from the already-pure, already-split `chronica-core-scheduling-domain`, which declares only serde/time/thiserror and has no `db` feature -- so, unlike LOOP 49's `UpdateRoleOutcome`, no feature-gating judgment call was needed). This is a SPLIT, not a LOOP 42/46/47-shaped whole-crate relocation: the crate had genuine local pure type surface worth keeping behind, so misplaced_cap_package_count is unchanged at 226. The one real (non-dev) production caller, `chronica-cap-scheduling-booking`'s `create_round_robin_booking` (`host_store::list_hosts`, plus its `round_robin_live.rs` live-DB suite, which imports all four names from the single adapter path the adapter's `AddHostOutcome` re-export provides), is PHYSICALLY under `crates/core/` -- itself a larger, separate, out-of-scope M7 violation (it owns real sqlx booking/attendee writes of its own) -- so repointing its `use`/Cargo.toml/db-feature-array dependency onto the now-`crates/adapter/`-tier crate creates this edge. It could only be avoided by ALSO decomposing or relocating the booking crate in the same pass, which is exactly the kind of scope creep this program's one-crate-per-loop discipline forbids; the edge becomes stale (and must be removed from this baseline) the moment the booking crate is itself converged. The repoint is a straight crate rename under the same `host_store` alias -- `create_round_robin_booking` calls only the I/O function `list_hosts` and never names `AddHostOutcome`, so no second edge back to the pure donor was created either. Same, already-established shape as every sibling `chronica-cap-* (core) -> chronica-adapter-* (adapter)` edge already recorded in this baseline by LOOPs 43/44/45/46/47.",
  "_note_2026_09_08_loop80": "LOOP 80 (2026-09-08) added exactly ONE forbidden edge, and it was PREDICTED before the gate was run rather than discovered by it: 'chronica-cap-org-workflow-routes (cap) -> chronica-adapter-company-model-template (adapter)'. The string was COPIED VERBATIM from the gate's own `NEW FORBIDDEN EDGE:` line (the gate exited 1 and printed exactly that one line and nothing else), never hand-typed or reconstructed from the crate names. WHY IT APPEARED: LOOP 80 relocated the whole crate chronica-cap-company-model-template from crates/core/ to crates/adapter/chronica-adapter-company-model-template and renamed it to match its tier. Its sole real caller, chronica-cap-org-workflow-routes, is a CAP-TIER route crate, so the dependency that previously read cap -> core (permitted) now reads cap -> adapter (forbidden by physical directory). This repeats LOOP 79's shape exactly, for the same structural reason: the caller is a route crate that has not itself been relocated yet. layer_debt_edge_count 138 -> 139. WHAT THE EDGE DOES AND DOES NOT MEAN: it does NOT represent any new coupling. Not one call site, import, type or SQL statement changed direction, and no dependency was added anywhere -- chronica-cap-org-workflow-routes called persist_export/get_export through exactly the same API before this loop as after, at the same three source positions in src/company_model.rs (lines 125, 224 and 269), all inside its own `#[cfg(feature = \"db\")]` gate; only the crate NAME in those paths changed, chronica_cap_company_model_template:: -> chronica_adapter_company_model_template::. What changed is that the Postgres I/O it depends on is now HONESTLY LABELLED as adapter-tier instead of being mislabelled as core-tier. The edge was therefore ALREADY REAL and was simply INVISIBLE to the gate while the adapter code sat under crates/core/ wearing a chronica-cap-* name; recording it here converts concealed debt into disclosed debt. That is a net accuracy gain even though the counter goes up, and it is why M7 forbidden_core_io_count fell 101 -> 100 and M2 misplaced_cap_package_count fell 219 -> 218 in the same change: the same relocation that revealed this edge removed a real instance of core-tier I/O and a real misplaced cap package. AN EDGE THAT DID *NOT* APPEAR, AND WHY: the relocated crate depends on chronica-cap-template-store, which REMAINS under crates/core/. That is adapter -> core, and FORBIDDEN_TARGETS in tools/refoundation/validate-refoundation-layers.mjs gives the adapter tier the forbidden set {\"infra\"} only -- core is not in it -- so the dependency is legal where it stands. This was read out of the gate's own source before the move rather than assumed. chronica-cap-template-store was also checked for a dependency BACK on this crate (grep over its manifest and every src/*.rs): there is none, so the relocation introduced no cycle risk either. A SECOND EDGE THAT DID NOT APPEAR: chronica-adapter-api-server also declared this crate, but that dependency was confirmed DEAD (no file under its src/ or tests/ names the package; the route handlers reach the adapter only through chronica-cap-org-workflow-routes via `use crate::routes::*`) and was REMOVED rather than renamed, per the LOOP-21 precedent and the LOOP 79 precedent. Even had it been kept it would have been adapter -> adapter and produced no edge. THE REAL REMAINING DEBT this edge names is that chronica-cap-org-workflow-routes is itself still cap-tier; when that route crate eventually moves to crates/adapter/ (it is an HTTP route surface, i.e. mechanical I/O), this edge becomes adapter -> adapter and can be retired from this array. Nothing about the company.model-template capability needs to change for that to happen. PROCESS: --write-baseline was NEVER run. This single string was hand-added to forbidden_edges, inserted at index 38, its correct alphabetical position inside the array's sorted prefix (between the chronica-cap-media-code-artifacts entry and the chronica-cap-platform-admin-routes block), so that the sorted prefix grew from 43 to 44 entries and no pre-existing entry was reordered, moved or removed.",
  "_note_2026_09_09_loop81": "LOOP 81 (2026-09-09) added ZERO new forbidden edges and REPAIRED exactly ONE. The gate printed no `NEW FORBIDDEN EDGE:` line at all; it printed exactly one line and nothing else: `STALE DEBT EDGE (remove from baseline): chronica-cap-crm-support-helpdesk-migration (cap) -> chronica-adapter-crm-support-lms-helpdesk (adapter)`, and exited 1. That entry is REMOVED here (never repointed to a new string), and the removal string was COPIED VERBATIM from the gate's own output rather than hand-typed or reconstructed from the crate names. layer_debt_edge_count 139 -> 138. WHY THE EDGE DISAPPEARED: LOOP 81 relocated the whole crate chronica-cap-crm-support-helpdesk-migration from crates/cap/ to crates/adapter/chronica-adapter-crm-support-helpdesk-migration and renamed it to match its tier. The relocated crate's dependency on chronica-adapter-crm-support-lms-helpdesk -- from which it imports the ENTIRE domain vocabulary it persists (MigrationRunStatus, MigrationCursor, MigrationStats, CursorUpdateOutcome) and all eight decision functions (validate_import_types, validate_integration_provider, resolve_cursor_update, compute_restart_stats, is_restartable, is_abandonable, is_stalled, is_integration_import) -- previously read cap -> adapter, forbidden under FORBIDDEN_TARGETS.cap = {adapter, runtime, infra}. It now reads adapter -> adapter, which FORBIDDEN_TARGETS.adapter (= {infra} only) does not forbid, so the debt is genuinely gone rather than relabelled. WHAT THE REPAIR DOES AND DOES NOT MEAN: not one call site, import, type or SQL statement changed direction, and no dependency was added or removed anywhere in the relocated crate -- its [dependencies] and [features] blocks are byte-identical to their pre-move form (SHA-256 9d8890391f8aaefe652759e9fab286a2acbd85e6edb5b49e23abb19a18793433 over the block from `[dependencies]` to EOF, both before and after). What changed is that the DEPENDING crate is now on the correct side of the tier boundary, so the coupling that was always adapter-shaped is now labelled adapter-shaped. This is the same shape as _note_2026_09_07_loop24, the other crates/cap/-rooted whole-crate relocation in this file, which likewise repaired its edges by moving the SOURCE crate rather than by moving its target. NO NEW EDGE APPEARED ON THE CALLER SIDE, and that was predicted before the gate ran: the sole real caller, chronica-adapter-crm-http (src/crm/helpdesk_migration.rs), is ALREADY adapter-tier, so its dependency went from adapter -> cap (permitted) to adapter -> adapter (permitted) -- the LOOP 77/78 shape, not the LOOP 79/80 shape where a still-cap-tier route crate caller manufactured a new edge. chronica-adapter-api-server carried a non-optional Cargo.toml dependency plus a `chronica-cap-crm-support-helpdesk-migration/db` feature-array entry on the old name, confirmed DEAD via grep across its own src/ and tests/ (zero real references; its tests/crm_helpdesk_migration_route.rs exercises the routes over HTTP and names the crate only inside a doc comment) -- REMOVED entirely rather than repointed, per this program's standing LOOP-21 rule; the routes it mounts still reach the capability through chronica-adapter-crm-http/db. --write-baseline was NEVER run; this entry was removed by hand. See tools/ci/responsibility-baseline.json's _crm_support_helpdesk_migration_relocation_2026_09_09_loop81 for the paired metric disclosure (M2 and M7 both UNCHANGED, and why), tools/ci/topology-baseline.json for cap_count 36 -> 35, and docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full ledger entry.",
  "_note_2026_09_09_loop82": "LOOP 82 (2026-09-09) added ZERO new forbidden edges and REPAIRED exactly ONE -- the same outcome as LOOP 81 and, like it, PREDICTED before the gate was run rather than discovered by it. The gate printed no `NEW FORBIDDEN EDGE:` line at all; it printed exactly one line and nothing else: `STALE DEBT EDGE (remove from baseline): chronica-cap-crm-support-email-group-newsletter (cap) -> chronica-adapter-crm-support-channel-connectors (adapter)`, and exited 1. That entry is REMOVED here (never repointed to a new string), and the removal string was COPIED VERBATIM from the gate's own output rather than hand-typed or reconstructed from the crate names. layer_debt_edge_count 138 -> 137. WHY THE EDGE DISAPPEARED: LOOP 82 relocated the whole crate chronica-cap-crm-support-email-group-newsletter from crates/cap/ to crates/adapter/chronica-adapter-crm-support-email-group-newsletter and renamed it to match its tier. The relocated crate's dependency on chronica-adapter-crm-support-channel-connectors -- from which it imports the ENTIRE domain vocabulary it persists (EmailGroup, RenderedWelcomeEmail) and all three decision functions (is_valid_email_address, send_welcome_email, get_welcome_url) -- previously read cap -> adapter, forbidden under FORBIDDEN_TARGETS.cap = {adapter, runtime, infra}. It now reads adapter -> adapter, which FORBIDDEN_TARGETS.adapter (= {infra} only) does not forbid, so the debt is genuinely gone rather than relabelled. THE SPLIT-VS-RELOCATION DECISION IS WHAT MADE THE REPAIR POSSIBLE, and it is worth recording here because the opposite choice would have PRESERVED this exact edge. The one candidate for a genuine pure surface was AddSubscribersReport (with its SubscriberOutcome enum) -- checked explicitly rather than assumed DB-shaped. Its welcome_emails field is a Vec<(String, RenderedWelcomeEmail)>, and RenderedWelcomeEmail is OWNED by chronica-adapter-crm-support-channel-connectors, an adapter-tier crate. Splitting that type out into a pure cap-tier donor stub would therefore have had to keep the dependency on the adapter crate, i.e. keep this very forbidden edge, while adding a new crate for the privilege. The type is in any case not pure in substance: it has no methods and no constructor, its Added-vs-SkippedDuplicate content is read off inserted.rows_affected() (i.e. off the table's own UNIQUE (company_id, email_group_id, email) constraint), and its total_subscribers is the raw bigint that SELECT COUNT(*) returned. A whole-crate relocation was therefore both the correct classification and the only mechanic that repairs the edge. WHAT THE REPAIR DOES AND DOES NOT MEAN: not one call site, import, type or SQL statement changed direction, and no dependency was added or removed anywhere in the relocated crate -- its [dependencies] and [features] blocks are byte-identical to their pre-move form (SHA-256 ac66dc920cb2eb2a7ffae52a90949c379b5125f9a38eae3f6cac2d7888951d70 over the block from `[dependencies]` to EOF, both before and after), as is the 316-line executable body of src/lib.rs (SHA-256 5c570e81b1b037a35f3e1d6e9d11e21a7d8536b9e97ae4b59afe1660fb8f513b). What changed is that the DEPENDING crate is now on the correct side of the tier boundary, so coupling that was always adapter-shaped is now labelled adapter-shaped. This is the same shape as _note_2026_09_09_loop81 and _note_2026_09_07_loop24, the other crates/cap/-rooted whole-crate relocations in this file, which likewise repaired their edges by moving the SOURCE crate rather than its target. NO NEW EDGE APPEARED ON THE CALLER SIDE, also predicted before the gate ran: the sole real caller, chronica-adapter-crm-http (src/crm/email_group.rs, found by exact fully-qualified-path grep for chronica_cap_crm_support_email_group_newsletter, which returns exactly two hits both in that one file), is ALREADY adapter-tier, so its dependency went from adapter -> cap (permitted) to adapter -> adapter (permitted) -- the LOOP 77/78/81 shape, not the LOOP 79/80 shape where a still-cap-tier route crate caller manufactured a new edge. chronica-adapter-api-server carried a non-optional Cargo.toml dependency plus a `chronica-cap-crm-support-email-group-newsletter/db` feature-array entry on the old name, confirmed DEAD via grep across its own src/ and tests/ (zero references to the crate's module path anywhere; its tests/crm_email_group_route.rs and tests/crm_subscriber_deferred_q_route.rs exercise the routes over HTTP and never import the store crate) -- REMOVED entirely rather than repointed, per this program's standing LOOP-21 rule; the routes it mounts still reach the capability through chronica-adapter-crm-http/db, which remains in that same feature array. --write-baseline was NEVER run; this entry was removed by hand. See tools/ci/responsibility-baseline.json's _crm_support_email_group_newsletter_relocation_2026_09_09_loop82 for the paired metric disclosure (M2 and M7 both UNCHANGED, and why), tools/ci/topology-baseline.json for cap_count 35 -> 34, and docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full ledger entry.",
  "forbidden_edges": [],
  "_note_2026_09_16_agent_questions_relocation": "2026-09-16: ONE new disclosed edge -- chronica-cap-agent-questions relocated + renamed to crates/adapter/chronica-adapter-agent-questions. Same shape as the same-day chronica-adapter-agent-{chat-persistence,instincts} relocations: NOT a Class-B split, since question_service.rs is a PRIVATE module with no external caller anywhere in the repo. The crate's public API (AgentQuestionStore) is entirely real Postgres I/O; its dependency on chronica-cap-agent-run-history (core-tier) for RunOwner stays a plain adapter -> core edge (always allowed, not layer debt). Its one real (non-dev) caller, chronica-cap-agent-session-routes (cap-tier), constructs AgentQuestionStore directly in agent_question_routes/handlers.rs -- confirmed via exact fully-qualified-path grep this is the crate's only real dependent (a chronica-adapter-api-server mention was a prose-only doc comment, its Cargo.toml dependency was dead and removed). Edge string confirmed via `node tools/refoundation/validate-refoundation-layers.mjs`'s own NEW FORBIDDEN EDGE output before adding it here. layer_debt_edge_count 152 -> 153. No stale tenancy_filter_lint entries existed.",
  "_note_2026_09_16_agent_instincts_relocation": "2026-09-16: ONE new disclosed edge -- chronica-cap-agent-instincts relocated + renamed to crates/adapter/chronica-adapter-agent-instincts. Same shape as the same-day chronica-adapter-agent-chat-persistence relocation: NOT a Class-B split, since instinct_cli.rs is a PRIVATE module with no external caller anywhere in the repo (its own module doc says so). The crate's public API (AgentInstinctStore) is entirely real Postgres I/O. Its one real (non-dev) caller, chronica-cap-agent-session-routes (cap-tier), constructs AgentInstinctStore directly in agent_instinct_routes/handlers.rs -- confirmed via exact fully-qualified-path grep this is the crate's only real dependent (a chronica-adapter-api-server mention was a prose-only doc comment, its Cargo.toml dependency was dead and removed). Edge string confirmed via `node tools/refoundation/validate-refoundation-layers.mjs`'s own NEW FORBIDDEN EDGE output before adding it here. layer_debt_edge_count 151 -> 152. No stale tenancy_filter_lint entries existed.",
  "_note_2026_09_16_agent_chat_persistence_relocation": "2026-09-16: ONE new disclosed edge -- chronica-cap-agent-chat-persistence relocated + renamed to crates/adapter/chronica-adapter-agent-chat-persistence. NOT a Class-B split: its private (non-pub) agent_persistence module is pure in-memory reference code with no external caller anywhere in the repo (confirmed via its own module doc, 'no runtime caller anywhere in the workspace', and via grep -- it exists solely so this crate's own tests can prove the impure store's error variants match). The crate's public API (ChatPersistenceStore/ChatOwner/ChatMessageView/SessionSummaryView) is entirely real Postgres I/O or a wire-only projection of it. Its one real (non-dev) caller, chronica-cap-agent-session-routes (cap-tier), constructs ChatPersistenceStore directly in agent_chat_routes/handlers.rs -- confirmed via exact fully-qualified-path grep this is the crate's only real dependent (a chronica-adapter-api-server mention is a prose-only doc comment, the Cargo.toml dependency there was dead and removed). Edge string confirmed via `node tools/refoundation/validate-refoundation-layers.mjs`'s own NEW FORBIDDEN EDGE output before adding it here. layer_debt_edge_count 150 -> 151. No stale tenancy_filter_lint entries existed.",
  "_note_2026_09_16_workflows_durable_history_store_relocation": "2026-09-16: ONE new disclosed edge -- chronica-cap-workflows-durable-history-store relocated + renamed to crates/adapter/chronica-adapter-workflows-durable-history-store (100% real Postgres I/O: DurableHistoryRepo::save/load, unconditional sqlx, no pure vocabulary). Its one real (non-dev) caller, chronica-core-workflows-agent-loop (core-tier), constructs DurableHistoryRepo directly in durable_turn.rs (its own db feature) -- confirmed via exact fully-qualified-path grep this is the crate's only real dependent anywhere in the repo (the only other mention, in chronica-core-workflows-durable/src/store.rs, is prose-only doc comments, never a real Cargo dependency). Edge string confirmed via `node tools/refoundation/validate-refoundation-layers.mjs`'s own NEW FORBIDDEN EDGE output before adding it here. layer_debt_edge_count 149 -> 150. No dead dependency existed in chronica-adapter-api-server for this crate. No stale tenancy_filter_lint entries existed.",
  "_note_2026_09_16_network_identity_privacy_budget_relocation": "2026-09-16: ONE new disclosed edge -- chronica-cap-network-identity-privacy-budget relocated + renamed to crates/adapter/chronica-adapter-network-identity-privacy-budget (100% real Postgres I/O: PersistentPrivacyBudget::spent/remaining/charge, unconditional sqlx, no pure vocabulary). Its one real (non-dev) caller, chronica-cap-network-identity-knowledge (core-tier), constructs PersistentPrivacyBudget directly in learn_patterns_dp_gated_persistent (its own db feature) -- confirmed via exact fully-qualified-path grep this is the crate's only real dependent anywhere in the repo (two other mentions, in chronica-core-network-identity-error and chronica-cap-network-identity-privacy-accountant, are prose-only doc comments, not real Cargo dependencies). Edge string confirmed via `node tools/refoundation/validate-refoundation-layers.mjs`'s own NEW FORBIDDEN EDGE output before adding it here. layer_debt_edge_count 148 -> 149. No dead dependency existed in chronica-adapter-api-server for this crate. No stale tenancy_filter_lint entries existed.",
  "_note_2026_09_16_scheduling_booking_relocation": "2026-09-16: RESOLVED (not merely disclosed) -- chronica-cap-scheduling-booking relocated + renamed to crates/adapter/chronica-adapter-scheduling-booking. Its dependency onto chronica-adapter-scheduling-host is now adapter -> adapter (no longer a forbidden-target pair), so the edge `chronica-cap-scheduling-booking (core) -> chronica-adapter-scheduling-host (adapter)` is removed from forbidden_edges rather than renamed. layer_debt_edge_count 149 -> 148. Zero real (non-dev) external callers existed for this crate, so this relocation touches no other crate's dependency graph -- the only Cargo.toml changes elsewhere are prose-comment crate-name corrections and three stale tenancy_filter_lint allow-list paths (wrong crates/cap/ prefix from before an older reorg).",
  "_note_2026_09_06i": "cap-retirement correction, 2 new edges disclosed (102 -> 104): `chronica-cap-company-platform-ops (cap) -> chronica-adapter-openbao-vault (adapter)` and `chronica-cap-deals-federation-routes (cap) -> chronica-adapter-openbao-vault (adapter)`. chronica-cap-policy-secrets-encryption (the V2 OpenBao VaultBackend implementation, a real HTTP client wrapping the audited vaultrs crate for OpenBao/Vault secrets-manager calls, with zero orchestration logic) was relocated wholesale from crates/cap/ to crates/adapter/chronica-adapter-openbao-vault -- the same adapter-shaped classification batch 8's own topology-baseline note already gave this exact crate when it was rejected from that batch's cap->core migration (\"clean dependency graph but adapter-shaped -- a live VaultBackend making real reqwest/mTLS calls to an external OpenBao server via the vaultrs crate, not domain logic despite its 'policy' prefix\"). Both real (non-dev) callers of OpenBaoBackend are cap-tier route/action crates that construct the concrete backend directly: chronica-cap-company-platform-ops/src/vault_secret_verify.rs and chronica-cap-deals-federation-routes/src/esign.rs. Under FORBIDDEN_TARGETS a cap-tier crate may not depend on adapter, but a route/action crate wiring an HTTP request to a real secrets-backend call has to live somewhere -- this is the SAME, already-established cap->adapter shape as the many chronica-cap-*-routes -> chronica-adapter-postgres-* edges already in this file (re-verified precedent: `chronica-cap-social-routes (cap) -> chronica-adapter-postgres-social-user-profile (adapter)`, present in forbidden_edges before this change). The package was also renamed away from the chronica-cap- prefix in the same change (to chronica-adapter-openbao-vault) specifically to avoid a new M2 (misplaced_cap_package_count) hit under tools/ci/verify-responsibility-boundaries.mjs -- confirmed M2 unchanged after this move. chronica-adapter-api-server's own Cargo.toml dependency on this crate was also repointed, but that is an adapter->adapter edge (not debt): confirmed no real src/ usage of OpenBaoBackend anywhere in that crate (only a pass-through Cargo.toml dependency plus prose test-doc-comment mentions), so no third edge is created. Exact new-edge strings confirmed via `node tools/refoundation/validate-refoundation-layers.mjs`'s own \"NEW FORBIDDEN EDGE\" output BEFORE adding them here, not hand-guessed. See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full ledger entry and tools/ci/topology-baseline.json for the cap_directory_count metric (39 -> 38).",
  "_note_2026_09_06j": "104 -> 105: crates/cap/chronica-cap-api-outbound-fetch relocated wholesale (0-change move, logic byte-identical) to crates/adapter/chronica-adapter-outbound-fetch and renamed away from the chronica-cap- prefix in the same change (same precedent as the 2026-09-06 chronica-cap-policy-secrets-encryption -> chronica-adapter-openbao-vault move recorded above: a chronica-cap-*-named crate with zero orchestration/gating logic of its own -- every module is a real network sink or a pure parser feeding one, gating happens at the caller's invoke_tool door, confirmed via tests/money_guard.rs's own money-free assertion -- is adapter-shaped, not migration debt). This REPAIRED 4 stale cap->adapter edges (the crate's own outbound deps to chronica-adapter-{browser-integration-connectors,crm-support-channel-connectors,infra-connector-toolkit,osint-http-core} are now adapter->adapter, not forbidden) and renamed 1 existing core->cap edge (chronica-core-api-state -> chronica-cap-api-outbound-fetch) into an equally-forbidden core->adapter edge under the new name -- but introduced 6 NEW disclosed edges: the crate's 5 real cap-tier callers (chronica-cap-{platform-misc-routes,platform-admin-routes,company-platform-ops,deals-federation-routes,runtime-engine-preview}) each now hold a cap->adapter edge to the renamed crate, plus the renamed core->adapter edge itself. Net +1 edge (104 -> 105), disclosed honestly rather than hidden -- these 5 new cap->adapter edges are the same tolerated shape as the many pre-existing chronica-cap-*-routes -> chronica-adapter-postgres-* edges already in this file (e.g. chronica-cap-social-routes -> chronica-adapter-postgres-social-user-profile), and chronica-adapter-api-server's own dependency on the crate is adapter->adapter (not forbidden, no edge). See docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full record.",
  "_note_2026_09_06k": "2026-09-07 responsibility-tier decomposition (subsystem-convergence ledger): crates/core/chronica-cap-esign-document-store split into a pure domain crate (kept, same name/location -- types, validators, idempotency fingerprints, no db feature, no sqlx) and a new adapter-tier crate, crates/adapter/chronica-adapter-esign-document-store (the real create_document_record/add_document_version/get_record/list_versions/get_latest_version/archive_record/record_stats/search_records sqlx mechanics). The crate's only real (non-dev) caller of the impure writers, chronica-cap-deals-federation-routes (crates/cap/, an HTTP route crate), cannot depend on crates/adapter/ under FORBIDDEN_TARGETS.cap -- yet a route handler forwarding a validated write/read to real persistence has to reach it somehow. Same shape as chronica-cap-company-money-actions (cap) -> chronica-adapter-postgres-artifact-registry (adapter) and every sibling cap->adapter edge already in this file. chronica-adapter-api-server carried a dead (unused in src/ or tests/) Cargo.toml dependency on the old crate -- removed rather than repointed, avoiding a manufactured phantom edge (same LOOP-21 chronica-cap-runtime-ops-routes precedent).",
  "_note_2026_09_07l": "104 -> 105 (+1 new disclosed edge; LOOP 24's runtime-api-background-dispatch move brought the count to 104 immediately before this loop): crates/core/chronica-cap-esign-envelope-audit-log split into a pure domain crate (kept, same name/location -- AuditLogRow/AuditLogRowVerification/AuditChainAnchor/AuditPageScope/NewAuditEvent/RecordAuditEventOutcome types, verify_audit_page/verify_paginated_audit_chain/the lifecycle-sequence verifiers, validate_actor_type/audit_error_reason validation helpers -- no db feature, no sqlx/chronica-core dependency at all) and a new adapter-tier crate, crates/adapter/chronica-adapter-esign-envelope-audit-log (the real record_event/list_events/fold_prior_lifecycle_anchor sqlx mechanics, exact same shape as the chronica-cap-esign-document-store split immediately above). The crate's one real (non-dev) cap-tier caller of the impure writers, chronica-cap-deals-federation-routes (esign_audit.rs's record_event/list_events, esign.rs's list_events alias), cannot depend on crates/adapter/ under FORBIDDEN_TARGETS.cap -- same unavoidable shape as every sibling cap->adapter edge already in this file, confirmed via `node tools/refoundation/validate-refoundation-layers.mjs`'s own \"NEW FORBIDDEN EDGE\" output before adding it here. chronica-adapter-api-server's own Cargo.toml dependency was repointed to the new adapter crate too, but that caller is real (its own tests/esign_audit_route.rs and tests/esign_certificate_route.rs call record_event/list_events directly) and the edge is adapter->adapter, not forbidden -- no second new edge.",
  "_note_2026_09_07m": "One new edge added this pass, disclosed rather than silently absorbed: `chronica-cap-helpdesk-conversations (core) -> chronica-adapter-helpdesk-read-model (adapter)` (exact string taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller change, not hand-guessed). It is the direct, unavoidable consequence of the entangled helpdesk dispatch/event-store/read-model outbox cluster's TOGETHER responsibility-tier decomposition (see docs/_machine/graph-refoundation/subsystem-convergence.jsonl and tools/ci/responsibility-baseline.json's `_forbidden_core_io_count_2026_09_07u` note for the full three-crate split record): `chronica-cap-helpdesk-read-model`'s real `list_inbox`/`project_event`/`append_and_project`/`project_pending` mechanics -- including the one-transaction drain -> project crash-safety sequence -- moved out to the new `chronica-adapter-helpdesk-read-model` (`crates/adapter/`), leaving only the pure `InboxRow` type behind. The real (non-dev) caller of `append_and_project` (7 call sites, corrected 2026-09-07 after an adversarial review of commit 707a3d7a caught this note's original `6` as a stale, unverified figure inherited from the pre-split donor's own doc comment -- see the disclosure_correction ledger entry), `chronica-cap-helpdesk-conversations`, is a `chronica-cap-*`-named capability crate PHYSICALLY misplaced under `crates/core/` from an earlier extraction (confirmed by directory: `crates/core/chronica-cap-helpdesk-conversations/`) and was NOT itself split by this pass (own future responsibility-tier decomposition candidate, out of this pass's declared 3-crate scope) -- it needs the real projection function and has nowhere else to reach it. Re-verified before citing as precedent (still accurate in the current tree, grepped both source trees for real, non-doc-comment usage): `chronica-cap-social-notes (core) -> chronica-adapter-postgres-social-handle (adapter)` and `chronica-cap-media-code-artifacts (core) -> chronica-adapter-postgres-artifact-registry (adapter)` (both this same `forbidden_edges` list) -- the exact same shape: a capability crate PHYSICALLY misplaced under `crates/core/` needing to reach a genuinely-decomposed-out adapter it cannot avoid, an already-established, tolerated debt class in this program, not a novel one. This cluster's two OTHER real callers needed no new edge at all: `chronica-adapter-helpdesk-contacts` (already adapter-tier) repointed its `chronica-cap-helpdesk-event-store` dependency to the new `chronica-adapter-helpdesk-event-store` -- adapter -> adapter, not forbidden under `FORBIDDEN_TARGETS.adapter` (= {infra}); and `chronica-cap-helpdesk-dispatch`'s own real caller was `chronica-cap-helpdesk-read-model` (this same crate, itself split in this pass), so once both live in `crates/adapter/` that internal edge is adapter -> adapter too, not disclosed here at all.",
  "_note_2026_09_07w": "Three new edges added this pass, disclosed rather than silently absorbed: `chronica-adapter-social-postgres-batch3 (core) -> chronica-adapter-social-clips (adapter)`, `chronica-cap-social-routes (cap) -> chronica-adapter-social-clips (adapter)`, and `chronica-cap-social-scheduled-posts (core) -> chronica-adapter-social-clips (adapter)` (exact strings taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller changes, not hand-guessed). All three are the direct, unavoidable consequence of `chronica-cap-social-clips`'s responsibility-tier decomposition (see docs/_machine/graph-refoundation/subsystem-convergence.jsonl): the real `create_clip`/`get_clip`/`list_clips_by_owner`/`add_note_to_clip`/`remove_note_from_clip`/`list_clip_notes`/`favorite_clip`/`unfavorite_clip`/`is_favorited`/`favorited_count` Postgres mechanics (formerly a `#[cfg(feature = \"db\")] mod imp` split across `src/imp/{row,create,favorite,note}.rs` directly under `crates/core/`) moved out of that crate entirely, into the new `chronica-adapter-social-clips` (`crates/adapter/`), flattened into one `src/lib.rs mod imp { ... }` (matching the established convention of every other multi-function split in this program, e.g. `chronica-adapter-social-user-lists`); the pure `ClipRow`/`ClipNoteRow`/`ClipFavoriteRow` types (+ `to_json`) and the pure `CreateClipOutcome`/`FavoriteOutcome`/`AddNoteOutcome` outcome enums stayed in the shrunk, now-always-pure core crate. `ClipTimelineEntry` (`list_clip_notes`'s result item, wrapping `chronica-cap-social-notes::NoteRow`) did NOT move back to the donor -- it stays in the new adapter crate, since `NoteRow` is itself only defined behind `chronica-cap-social-notes`'s own `db` feature and so cannot live in an always-pure, `db`-feature-free donor. These three edges are of TWO DIFFERENT SHAPES, each independently precedented: (1) `chronica-cap-social-routes (cap) -> chronica-adapter-social-clips (adapter)` (the `social_clips.rs` route module, create/list/get/favorite/unfavorite/notes-membership routes) is a `crates/cap/`-tier HTTP route crate reaching the relocated functions -- re-verified as the SAME shape already established by `chronica-cap-social-routes -> chronica-adapter-social-user-lists` (this same forbidden_edges list, `_note_2026_09_07v`) and every sibling `chronica-cap-*-routes -> chronica-adapter-*` edge already in this file. (2) `chronica-adapter-social-postgres-batch3 (core) -> chronica-adapter-social-clips (adapter)` and `chronica-cap-social-scheduled-posts (core) -> chronica-adapter-social-clips (adapter)` are DIFFERENT: both callers are PHYSICALLY under `crates/core/` (confirmed by directory: `crates/core/chronica-adapter-social-postgres-batch3/` and `crates/core/chronica-cap-social-scheduled-posts/` -- the first carries an `-adapter-`-shaped name despite its placement, an unrelated, earlier placement decision from a prior extraction, independent of this pass). `chronica-adapter-social-postgres-batch3`'s `publish_targets::record_target` reuses the relocated `get_clip` for a target-scope check (a real, load-bearing production dependency unchanged by this pass, only its `use chronica_cap_social_clips as clip_store;` alias repointed to `use chronica_adapter_social_clips as clip_store;`). `chronica-cap-social-scheduled-posts`'s scheduled-post target validation reuses the SAME relocated `get_clip` the SAME way (also unchanged by this pass, same alias repoint). Both are the SAME shape as the already-established `chronica-adapter-social-postgres-batch3 (core) -> chronica-adapter-postgres-social-profile-privacy (adapter)` and `chronica-cap-social-notes (core) -> chronica-adapter-postgres-social-handle (adapter)` edges (this same forbidden_edges list, `_note_2026_09_06e`/`_note_2026_09_06c`) -- re-verified before citing as precedent (still accurate in the current tree, grepped both source trees for real, non-doc-comment usage) -- an already-established, tolerated debt class in this program, not a novel one. The pre-existing `chronica-adapter-api-server` Cargo.toml dependency on the pre-split crate was confirmed dead in its own `src/`/`tests/` (zero real references, confirmed via grep; reached the real ops only transitively, via `chronica-cap-social-routes`) and removed outright rather than repointed, per this program's standing LOOP-21 precedent.",
  "_note_2026_09_08_loop58": "Two new edges added this pass, disclosed rather than silently absorbed: `chronica-cap-crm-support-contact-csv-import (core) -> chronica-adapter-crm-support-contacts (adapter)` and `chronica-cap-platform-misc-routes (cap) -> chronica-adapter-crm-support-contacts (adapter)` (exact strings taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller repoints, not hand-guessed). Both are the direct, unavoidable consequence of `chronica-cap-crm-support-contacts`'s responsibility-tier decomposition (LOOP 58, see docs/_machine/graph-refoundation/subsystem-convergence.jsonl): the real `create_contact`/`list_contacts`/`get_contact`/`update_contact` Postgres mechanics over `crm_contacts` (migration 0058), formerly a `#[cfg(feature = \"db\")]`-gated `mod imp` directly under `crates/core/`, moved BYTE-IDENTICAL into the new `crates/adapter/chronica-adapter-crm-support-contacts`; the donor's OWN pure surface (the `CrmContactRow` projection + its `to_json` view, the `CreateContactOutcome`/`UpdateContactOutcome` verdicts, the sparse `ContactPatch` and the `kind_ok` closed-set predicate) stayed behind in a now genuinely pure, feature-free, serde_json-only donor. THE SPLIT'S OTHER TWO REAL CALLERS ADD NO EDGE AT ALL and are therefore absent from this list: `chronica-adapter-crm-http` is already adapter-tier (adapter -> adapter), and `chronica-runtime-crm-contact-execution` is runtime-tier, which this gate deliberately does not order (a runtime crate is a process composition root -- see the module doc of validate-refoundation-layers.mjs). A fifth declared dependent, `chronica-adapter-api-server`, was CONFIRMED DEAD before removal (repo-wide grep of its `src/` and `tests/` for `crm_support_contacts` returned nothing) and its manifest dep + `db` feature-array entry were deleted rather than repointed; it still transitively enables the adapter's `db` feature through `chronica-adapter-crm-http/db`, `chronica-cap-platform-misc-routes/db` and `chronica-cap-crm-support-contact-csv-import/db`. THE TWO EDGES ARE OF TWO DIFFERENT SHAPES, each independently precedented in this same program: (1) the `cap -> adapter` edge from `chronica-cap-platform-misc-routes` is EXACTLY the shape that crate already carries three times over for this same domain -- `chronica-adapter-crm-support-ad-spend`, `chronica-adapter-crm-support-event-definitions` (LOOP 54) and `chronica-adapter-crm-support-money-event-projector` -- all verified still present in this list before being cited; that crate is a `crates/cap/`-resident HTTP route aggregator that needs the real `list_contacts` for the store-platform CRM read surface and has nowhere else to reach it. (2) the `core -> adapter` edge from `chronica-cap-crm-support-contact-csv-import` is the shape already recorded for e.g. `chronica-cap-social-notes (core) -> chronica-adapter-postgres-social-handle (adapter)` and the LOOP 51/57-era `chronica-adapter-social-postgres-batch*` entries. That crate is a `chronica-cap-*`-named capability crate PHYSICALLY misplaced under `crates/core/` from an earlier extraction (confirmed by directory: `crates/core/chronica-cap-crm-support-contact-csv-import/`); it consumes the real `create_contact` for the actual per-row insert and deliberately never re-derives the `person|company` kind rule, so it cannot be served by the pure donor alone. It was NOT itself split by this pass (out of this loop's declared single-crate scope). This is honest, SELF-LIQUIDATING debt: that crate is ITSELF an open M7 `forbidden_core_io_count` hit (its own `#[cfg(feature = \"db\")] mod imp` needs `PgPersistence`/`sqlx`) and is queued for the same responsibility-tier treatment, at which point this edge becomes `adapter -> adapter` and disappears from this baseline rather than needing a waiver. Written by hand, NOT via `--write-baseline`, so the two additions above are the only change to `forbidden_edges` in this pass (115 -> 117).",
  "_note_2026_09_08_loop60": "One new edge added this pass, disclosed rather than silently absorbed: `chronica-cap-platform-misc-routes (cap) -> chronica-adapter-crm-support-analytics-action (adapter)` (exact string taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller repoint, not hand-guessed). It is the direct, unavoidable consequence of `chronica-cap-crm-support-analytics-action`'s responsibility-tier decomposition (LOOP 60, see docs/_machine/graph-refoundation/subsystem-convergence.jsonl): the real `create_action`/`list_actions`/`get_action`/`update_action`/`soft_delete_action`/`match_event` Postgres mechanics over `analytics_action` (migration 0069), formerly a `#[cfg(feature = \\\"db\\\")]`-gated `mod imp` directly under `crates/core/`, moved BYTE-IDENTICAL into the new `crates/adapter/chronica-adapter-crm-support-analytics-action`; the donor's OWN surface (the `AnalyticsActionRow` projection with its `to_action` domain rehydration and its camelCase `to_json` view, the `NewAction` request shape, the `CreateActionOutcome`/`UpdateActionOutcome` verdicts, and the `parse_steps`/`steps_json` serde operators) stayed behind in a now genuinely pure, feature-free donor. SPLIT, NOT A RELOCATION -- and the distinction was checked from the actual current source, not assumed: unlike LOOP 52/53/54/56, whose db-gated code was real I/O over FOREIGN types only, this donor declares four `pub` types and two pure serde operators of its own, so it was kept and shrunk rather than renamed away. FACADE-PURITY JUDGMENT CALL, DISCLOSED (the LOOP 55/57 shape, weaker than LOOP 58's donor which retained no foreign type at all): the retained surface DOES name a foreign type -- `AnalyticsActionRow` holds a `Vec<ActionStep>` and `to_action` returns an `Action`, both owned by `chronica-core-analytics-action`. That crate was verified pure by reading its own Cargo.toml in the current tree (not assumed): its only dependencies are `serde` and `chronica-core`, and it declares NO `[features]` section at all, so it takes `chronica-core` at that facade's DEFAULT feature set -- which `crates/core/chronica-core/Cargo.toml` shows is `default = []`, its `db` feature being the only one it has and opt-in. No sqlx and no db feature reaches the donor through that edge, so the donor's former `db` feature plus its direct `sqlx` and `chronica-core` dependencies were deleted OUTRIGHT rather than narrowed, and the donor now has no cargo features at all. THIS SPLIT HAS EXACTLY ONE REAL (non-dev) PRODUCTION CALLER, found by a fully-qualified-path grep for `chronica_cap_crm_support_analytics_action::` across the repo rather than a bare function-name grep: `chronica-cap-platform-misc-routes`'s `src/analytics/action.rs` (the six axum handlers behind `POST|GET /api/companies/:company_id/analytics/actions`, `GET|PUT|DELETE .../actions/:id` and `POST .../actions/match`), which is PHYSICALLY under `crates/cap/` -- hence this one edge. A second declared dependent, `chronica-adapter-api-server`, was CONFIRMED DEAD before removal (grep of its own `src/` and `tests/` for `chronica_cap_crm_support_analytics_action` returned nothing at all; its `tests/analytics_action_route.rs` exercises the six routes over HTTP only and never imports the crate by name) and its manifest dep + `db` feature-array entry were deleted rather than repointed, per this program's standing rule; it still transitively enables the new adapter's `db` feature through `chronica-cap-platform-misc-routes/db`. THE EDGE IS NOT A NOVEL DEBT CLASS: it is EXACTLY the shape `chronica-cap-platform-misc-routes` already carries four times over for this same CRM domain -- `chronica-adapter-crm-support-ad-spend`, `chronica-adapter-crm-support-contacts` (LOOP 58), `chronica-adapter-crm-support-event-definitions` (LOOP 54) and `chronica-adapter-crm-support-money-event-projector` -- all four independently re-verified as still present in this same `forbidden_edges` array before being cited as precedent. That crate is a `crates/cap/`-resident HTTP route aggregator which cannot be served by the pure donor alone (it needs the real reads and writes) and has nowhere else to reach them without a much larger, out-of-scope route-crate decomposition. Written by hand, NOT via `--write-baseline`, so the single addition above is the only change to `forbidden_edges` in this pass (117 -> 118).",
  "_note_2026_09_08_loop61": "One new edge added this pass, disclosed rather than silently absorbed: `chronica-cap-platform-admin-routes (cap) -> chronica-adapter-crm-support-metadata-view (adapter)` (exact string taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` line after the caller change, not hand-guessed). layer_debt_edge_count: 118 -> 119 (1 new, 0 repaired). LOOP 61 decomposed crates/core/chronica-cap-crm-support-metadata-view: its real Postgres mechanics (create_view/list_views/get_view/soft_delete_view over view_metadata, migration 0068, plus the private ViewMetadataRow::map row mapper and the VIEW_COLS column-list constant) moved BYTE-IDENTICAL into the new crates/adapter/chronica-adapter-crm-support-metadata-view, closing an M7 forbidden_core_io_count hit (120 -> 119, baseline updated in this same change). SPLIT, not relocated: the donor declares its OWN pub types (ViewMetadataRow with its to_view_metadata domain rehydration and its camelCase to_json operator view, NewView, CreateViewOutcome) plus three pure operators (parse_visibility, visibility_str, to_json_array), none of which touches a PgPersistence -- so the donor was kept and shrunk rather than renamed away, exactly as in LOOP 50/55/57/58/60. The one real (non-dev, non-test) production caller, `chronica-cap-platform-admin-routes`'s src/metadata.rs (the create_view/list_views/get_view/delete_view axum handlers behind POST|GET /api/companies/:company_id/metadata/objects/:object_id/views and GET|DELETE .../views/:view_id, plus the view_belongs_to_object route-scope helper and its two regression tests -- found by a fully-qualified-path grep for both the underscore and hyphen crate spellings, 9 references, NOT by a bare function-name grep), is PHYSICALLY under crates/cap/, so repointing its `use`/Cargo.toml/db-feature-array dependency onto the now-crates/adapter/-tier crate creates this edge -- a cap-tier route crate reaching a genuinely-decomposed-out adapter crate it cannot avoid without a much larger, out-of-scope route-crate decomposition. `chronica-cap-platform-admin-routes` already carries FOUR edges of this exact shape in this same forbidden_edges array (`-> chronica-adapter-outbound-fetch`, `-> chronica-adapter-postgres-social-profile-privacy`, `-> chronica-adapter-postgres-social-user-profile`, `-> chronica-adapter-social-worker-directory`, all re-verified by enumerating the array itself before being cited as precedent), so this is its FIFTH, an already-established debt class for this caller rather than a novel one. Note that this caller's other adapter-NAMED dependencies (e.g. chronica-adapter-social-postgres-batch4) are NOT edges here because those crates are still physically under crates/core/ -- the validator keys on the physical tier, not the name, and that was checked rather than assumed. `chronica-adapter-api-server` carried an unused-in-src, non-optional Cargo.toml dependency + db-feature-array entry on the donor crate (confirmed dead via grep across its own src/ and tests/ for `chronica_cap_crm_support_metadata_view` -- zero hits of any kind; its tests/metadata_views_route.rs exercises the four routes over HTTP and never imports the crate by name) -- REMOVED entirely (not repointed), per this program's standing rule; it still reaches the new adapter's db feature transitively through chronica-cap-platform-admin-routes/db. Recorded BY HAND, NOT via --write-baseline (which would destroy the notes above). See tools/ci/responsibility-baseline.json's _metadata_view_split_2026_09_08l for the paired M2/M7 metric disclosure, and docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full ledger entry.",
  "_note_2026_09_08_loop62": "One new edge added this pass, disclosed rather than silently absorbed: `chronica-cap-platform-misc-routes (cap) -> chronica-adapter-crm-support-analytics-alert-rule (adapter)` (exact string taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` line after the caller repoint, not hand-guessed). layer_debt_edge_count: 119 -> 120 (1 new, 0 repaired). LOOP 62 decomposed crates/core/chronica-cap-crm-support-analytics-alert-rule: its real Postgres mechanics (create_rule/list_rules/get_rule/update_rule/delete_rule/evaluate_rule over analytics_alert_rule, migration 0070, plus the private AlertRuleRow::map row mapper and the RULE_COLS column-list constant) moved BYTE-IDENTICAL into the new crates/adapter/chronica-adapter-crm-support-analytics-alert-rule, closing an M7 forbidden_core_io_count hit (119 -> 118, baseline updated in this same change). SPLIT, not relocated -- and the distinction was re-derived from the donor's actual current source rather than inherited from the donor's own module-doc claim that it \"Mirrors chronica-cap-crm-support-analytics-action\" (LOOP 60's crate): the donor declares its OWN pub types (AlertRuleRow with its to_rule domain rehydration and its camelCase to_json operator view, NewRule, CreateRuleOutcome, UpdateRuleOutcome) plus three pure comparison operators (parse_comparison, comparison_str, validate), none of which touches a PgPersistence -- so the donor was kept and shrunk rather than renamed away, exactly as in LOOP 50/55/57/58/60/61. FACADE-PURITY JUDGMENT CALL, DISCLOSED (the LOOP 55/57/60 shape, weaker than LOOP 58's donor which retained no foreign type at all): the retained surface DOES name a foreign type -- to_rule returns an AlertRule, and parse_comparison/comparison_str/validate take or return a ComparisonOp, both owned by chronica-core-analytics-alerts. That crate was verified pure by reading its own Cargo.toml in the current tree (not assumed): its only dependencies are chronica-core and serde, and it declares NO [features] section at all, so it takes chronica-core at that facade's DEFAULT feature set -- which crates/core/chronica-core/Cargo.toml shows is `default = []`, its `db` feature being the only one it has, opt-in, and forwarding only chronica-core-persistence/db + chronica-core-version/db. No sqlx and no db feature reaches the donor through that edge, so the donor's former `db` feature plus its direct `sqlx` and `chronica-core` dependencies were deleted OUTRIGHT rather than narrowed, and the donor now has no cargo features at all. THIS SPLIT HAS EXACTLY ONE REAL (non-dev, non-test) PRODUCTION CALLER, found by a fully-qualified-path grep for both the underscore and hyphen crate spellings across the whole repo rather than a bare function-name grep: `chronica-cap-platform-misc-routes`'s src/analytics/alert_rule.rs (the six axum handlers behind POST|GET /api/companies/:company_id/analytics/alert-rules, GET|PUT|DELETE .../alert-rules/:id and POST .../alert-rules/:id/evaluate), which is PHYSICALLY under crates/cap/ -- hence this one edge. A second declared dependent, `chronica-adapter-api-server`, was CONFIRMED DEAD before removal (grep of its own src/ and tests/ for `analytics_alert_rule`/`alert_rule` returned exactly one hit, a `cargo test -p chronica-api ...` line inside a doc comment; its tests/analytics_alert_rule_route.rs exercises the six routes over HTTP only and never imports the crate by name) and its non-optional manifest dep + db feature-array entry were deleted rather than repointed, per this program's standing rule; it still reaches the new adapter's db feature transitively through chronica-cap-platform-misc-routes/db. THE EDGE IS NOT A NOVEL DEBT CLASS: `chronica-cap-platform-misc-routes` already carries EIGHT cap -> adapter edges in this same forbidden_edges array (-> chronica-adapter-crm-support-analytics-action (LOOP 60), -> chronica-adapter-crm-support-ad-spend, -> chronica-adapter-crm-support-contacts (LOOP 58), -> chronica-adapter-crm-support-event-definitions (LOOP 54), -> chronica-adapter-crm-support-money-event-projector, -> chronica-adapter-osint-http-core, -> chronica-adapter-outbound-fetch, -> chronica-adapter-postgres-artifact-registry -- all eight independently re-verified as still present in this array by enumerating it before being cited as precedent), so this is its NINTH of exactly that shape and its tenth forbidden edge overall (it also carries one cap -> runtime edge, -> chronica-runtime-event-dispatch). That crate is a crates/cap/-resident HTTP route aggregator which cannot be served by the pure donor alone (it needs the real reads and writes) and has nowhere else to reach them without a much larger, out-of-scope route-crate decomposition. Recorded BY HAND, NOT via --write-baseline (which would destroy the notes above), so the single addition above is the only change to forbidden_edges in this pass. See tools/ci/responsibility-baseline.json's _analytics_alert_rule_split_2026_09_08m for the paired M2/M7 metric disclosure, and docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full ledger entry.",
  "_note_2026_09_08_loop63": "One new edge added this pass, disclosed rather than silently absorbed: `chronica-cap-platform-misc-routes (cap) -> chronica-adapter-crm-support-analytics-insight (adapter)` (exact string taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` line after the caller repoint, not hand-guessed). layer_debt_edge_count: 120 -> 121 (1 new, 0 repaired). LOOP 63 decomposed crates/core/chronica-cap-crm-support-analytics-insight: its real Postgres mechanics (create_insight/list_insights/get_insight/rename/set_favorited/share/unshare/soft_delete over analytics_insight, migration 0075, plus the private InsightRow::map row decoder, the INSIGHT_COLS column-list constant and the is_unique_violation duplicate detector) moved BYTE-IDENTICAL into the new crates/adapter/chronica-adapter-crm-support-analytics-insight, closing an M7 forbidden_core_io_count hit (118 -> 117, baseline updated in this same change). SPLIT, not relocated -- the distinction was re-derived by reading the donor's actual current 329-line src/lib.rs in full rather than inherited from a scouting note or from the shape of its LOOP 60/62 siblings: the donor declares its OWN pub types (InsightRow with its to_insight domain rehydration and its camelCase to_json operator view, NewInsight, CreateInsightOutcome), none of which touches a PgPersistence -- so the donor was kept and shrunk rather than renamed away, exactly as in LOOP 50/55/57/58/60/61/62. FACADE-PURITY JUDGMENT CALL, DISCLOSED (the LOOP 55/57/60/61/62 shape, weaker than LOOP 58's donor which retained no foreign type at all): the retained surface DOES name foreign types -- to_insight returns an Insight (owned by chronica-core-analytics-insight) and rebuilds it through the InsightQuery serde shape, and CreateInsightOutcome::InvalidQuery carries an InsightError (both owned by chronica-core-analytics-insight-query). BOTH crates were verified pure by reading their own Cargo.toml in the current tree, and one level deeper: chronica-core-analytics-insight-query deps only chronica-core-business-events + serde and declares NO [features] section at all and no sqlx; chronica-core-analytics-insight deps only that query crate, chronica-core-business-events, chronica-core and serde, again with NO [features] section and no sqlx. One level deeper still, chronica-core-business-events DOES carry an optional sqlx behind a db feature and chronica-core has a db feature -- but both are default = [] and opt-in, and neither insight crate requests them, so each takes those edges at their DEFAULT (empty) feature set. No sqlx and no db feature reaches the donor through either edge, so its db feature, its sqlx dependency and its direct chronica-core dependency were deleted OUTRIGHT rather than narrowed; the donor now has 3 dependencies (chronica-core-analytics-insight, chronica-core-analytics-insight-query, serde_json) and ZERO cargo features. THE CALLER WAS FOUND BY EXACT FULLY-QUALIFIED-PATH GREP, not by bare function name: a repo-wide grep for both the underscore and hyphen crate spellings found exactly ONE real (non-test) production caller, crates/cap/chronica-cap-platform-misc-routes/src/analytics/insight.rs (10 fully-qualified references -- the use line plus 9 call sites across the axum handlers behind POST|GET /api/companies/:company_id/analytics/insights, GET|PUT|DELETE .../insights/:short_id, GET .../insights/:short_id/run and POST .../insights/:short_id/share|unshare), which is cap-tier -- hence the one new edge. It is that route crate's TENTH edge of exactly this already-precedented shape (LOOP 54/60/62 and earlier), not a novel debt class. A second declared dependent, chronica-adapter-api-server, was CONFIRMED DEAD by grepping its own src/ and tests/ for chronica_cap_crm_support_analytics_insight, InsightRow, NewInsight and CreateInsightOutcome: zero hits of any kind, so its manifest dep + db feature-array entry were removed outright rather than repointed, per the standing LOOP-21 precedent -- it still enables the new adapter's db feature transitively through chronica-cap-platform-misc-routes/db, and its analytics_insight_route.rs tests exercise the routes over HTTP, not the crate directly. Recorded BY HAND, NOT via --write-baseline (which would destroy the notes above), so the single addition above is the only change to forbidden_edges in this pass. See tools/ci/responsibility-baseline.json's _analytics_insight_split_2026_09_08n for the paired M2/M7 metric disclosure, and docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full ledger entry.",
  "_note_2026_09_08_loop64": "One new edge added this pass, disclosed rather than silently absorbed: `chronica-cap-platform-misc-routes (cap) -> chronica-adapter-crm-support-feature-flag (adapter)` (exact string taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` line after the caller repoint, not hand-guessed). layer_debt_edge_count: 121 -> 122 (1 new, 0 repaired). LOOP 64 decomposed crates/core/chronica-cap-crm-support-feature-flag: its real Postgres mechanics (create_flag/list_flags/get_flag/update_flag/soft_delete_flag/evaluate_flag over feature_flag, migration 0073, plus the private FeatureFlagRow::map row decoder, the FF_COLS column-list constant, the is_unique_violation duplicate detector and the parse_conditions/parse_variants serde helpers) moved BYTE-IDENTICAL into the new crates/adapter/chronica-adapter-crm-support-feature-flag, closing an M7 forbidden_core_io_count hit (117 -> 116, baseline updated in this same change). SPLIT, not relocated -- the distinction was re-derived by reading the donor's actual current 395-line src/lib.rs in full rather than inherited from a scouting note or from the shape of its LOOP 60/62/63 siblings: the donor declares its OWN pub types (FeatureFlagRow with its to_flag domain rehydration and its camelCase to_json operator view, NewFlag, FlagEvalResult, CreateFlagOutcome, UpdateFlagOutcome, DeleteFlagOutcome), none of which touches a PgPersistence -- so the donor was kept and shrunk rather than renamed away, exactly as in LOOP 50/55/57/58/60/61/62/63. FACADE-PURITY JUDGMENT CALL, DISCLOSED (the LOOP 55/57/60/61/62/63 shape, weaker than LOOP 58's donor which retained no foreign type at all): the retained surface DOES name foreign types -- FeatureFlagRow::to_flag returns a FeatureFlag and rebuilds it through the ReleaseCondition and FlagVariant serde shapes, all three owned by chronica-core-analytics-feature-flag. That crate was verified pure by reading its own Cargo.toml in the current tree, and one level deeper: chronica-core-analytics-feature-flag deps ONLY chronica-core + serde and declares NO [features] section at all and no sqlx, so it takes every edge at its DEFAULT feature set; one level deeper, chronica-core is default = [] and its db feature (the only one it has) is opt-in, forwarding chronica-core-persistence/db + chronica-core-version/db, neither of which the feature-flag domain crate requests. No sqlx and no db feature reaches the donor through that edge, so its db feature, its sqlx dependency and its direct chronica-core dependency were deleted OUTRIGHT rather than narrowed; the donor now has 2 dependencies (chronica-core-analytics-feature-flag, serde_json) and ZERO cargo features. The domain's private can_edit RBAC gate and variants_valid check stay REUSED, never re-implemented -- they are enforced by the ported FeatureFlagStore, which the adapter (not the donor) calls. THE CALLER WAS FOUND BY EXACT FULLY-QUALIFIED-PATH GREP, not by bare function name: a repo-wide grep for both the underscore and hyphen crate spellings found exactly ONE real (non-test) production caller, crates/cap/chronica-cap-platform-misc-routes/src/analytics/feature_flag.rs (7 fully-qualified references -- the use line plus 6 call sites across the axum handlers behind POST|GET /api/companies/:company_id/analytics/feature-flags, GET|PUT|DELETE .../feature-flags/:key and POST .../feature-flags/:key/evaluate), which is cap-tier -- hence the one new edge. It is that route crate's ELEVENTH edge of exactly this already-precedented cap -> adapter shape (LOOP 54/58/60/62/63 and earlier, all re-verified as still present in this same forbidden_edges array before being cited), not a novel debt class. A second declared dependent, chronica-adapter-api-server, was CONFIRMED DEAD by grepping its own src/ and tests/ for chronica_cap_crm_support_feature_flag, FeatureFlagRow, NewFlag and the three outcome enums: zero hits of any kind, so its manifest dep + db feature-array entry were removed outright rather than repointed, per the standing LOOP-21 precedent -- it still enables the new adapter's db feature transitively through chronica-cap-platform-misc-routes/db, and its feature_flag_route.rs tests exercise the routes over HTTP, not the crate directly. Recorded BY HAND, NOT via --write-baseline (which would destroy the notes above), so the single addition above is the only change to forbidden_edges in this pass. See tools/ci/responsibility-baseline.json's _feature_flag_split_2026_09_08_loop64 for the paired M2/M7 metric disclosure, and docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full ledger entry.",
  "_note_2026_09_08_loop67": "One new edge added this pass, disclosed rather than silently absorbed: `chronica-cap-deals-federation-routes (cap) -> chronica-adapter-crm-support-contract-lifecycle (adapter)` (exact string taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` line after the caller repoint, not hand-guessed). layer_debt_edge_count: 123 -> 124 (1 new, 0 repaired). LOOP 67 decomposed crates/core/chronica-cap-crm-support-contract-lifecycle: its real Postgres mechanics -- the seven company-scoped async entry points create_contract/list_contracts/get_contract/update_contract/sign_contract/cancel_contract/update_fulfilment_terms over crm_contracts (migration 0061 plus migration 0411's additive fulfilment columns), the CON_COLS projection constant, the `view` row-to-ContractView builder and the private CrmContractRow::map PgRow decoder -- moved BYTE-IDENTICAL into the new crates/adapter/chronica-adapter-crm-support-contract-lifecycle, closing an M7 forbidden_core_io_count hit (114 -> 113, baseline updated in this same change). SPLIT, not relocated -- re-derived by reading the donor's actual current 445-line src/lib.rs in full: although its WHOLE body sat inside a single #[cfg(feature = \"db\")] mod imp, it declares its OWN pub types (CrmContractRow with to_domain/fulfilment_status/to_json, ContractView, CreateContractOutcome, UpdateContractOutcome, ContractPatch) and its own pure date operators (parse_opt_date, date_ok), none of which touches a PgPersistence -- so the donor was kept and shrunk rather than renamed away, exactly as in LOOP 50/55/57/58/59/60/61/62/63/64/65/66. WHY THIS EDGE IS UNAVOIDABLE, and why -- unlike its immediate LOOP 66 sibling, which added none -- this split could not avoid one: the callers were found by exact fully-qualified-path grep for both the underscore and hyphen crate spellings, not by bare function name, and there are exactly two real production callers. crates/adapter/chronica-adapter-crm-http/src/crm/contract.rs (8 references: the use line plus one call site per moved op, behind POST|GET /api/companies/:company_id/crm/contracts, GET|PUT .../contracts/:id and the /sign, /cancel and /fulfilment sub-routes) is already adapter-tier, so its repoint is adapter -> adapter and free. But crates/cap/chronica-cap-deals-federation-routes is cap-tier and calls the genuinely-I/O list_contracts from BOTH src/search.rs (the CRM content read feeding the search index) and src/legal.rs (read_active_contracts, the legal entity-index active-contract projection), so there is no version of this split in which that crate does not gain an adapter edge. It is that route crate's EIGHTH edge of exactly this already-precedented cap -> adapter shape (the seven pre-existing ones -- to chronica-adapter-esign-document-store, chronica-adapter-esign-envelope-audit-log, chronica-adapter-openbao-vault, chronica-adapter-outbound-fetch, chronica-adapter-postgres-artifact-registry, chronica-adapter-social-kyc-trusted-issuer, and a cap -> runtime edge to chronica-runtime-gdpr-erasure -- were enumerated from this same forbidden_edges array and re-verified as still present before being cited), not a novel debt class. HOW search.rs's DIRECT CONSTRUCTION OF CrmContractRow WAS HANDLED, disclosed because it was the split's main compile risk: search.rs does not only call functions, it builds a CrmContractRow struct literal (in its #[cfg(test)] cross_subsystem_doc_tests module, at src/search.rs:550) and names ContractView in its use line. CrmContractRow and ContractView are PURE and deliberately STAYED in the shrunk donor -- they were not dragged into the adapter to make the caller simpler. The caller still compiles because the adapter re-exports the donor's entire pure surface UNCONDITIONALLY (pub use chronica_cap_crm_support_contract_lifecycle::*, outside the db gate, the LOOP 66 pattern), so a single repoint of the crate path covers the type literal, the type name and the real function alike; the caller therefore needs ONE dependency, not two, and its now-redundant direct donor dependency was dropped rather than kept alongside. A third declared dependent, chronica-adapter-api-server, was CONFIRMED DEAD by grepping its own src/ and tests/ for chronica_cap_crm_support_contract_lifecycle and contract_lifecycle: zero hits of any kind, so its manifest dep + db feature-array entry were removed outright rather than repointed, per the standing LOOP-21 precedent; `cargo check -p chronica-adapter-api-server --features db` was re-run after the removal and compiles clean, and it still enables the new adapter's db feature transitively through chronica-adapter-crm-http/db and chronica-cap-deals-federation-routes/db. Recorded BY HAND, NOT via --write-baseline (which would destroy the notes in this file), so the single addition above is the only change to forbidden_edges in this pass. See tools/ci/responsibility-baseline.json's _crm_contract_lifecycle_split_2026_09_08_loop67 for the paired M2/M7 metric disclosure, and docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full ledger entry.",
  "_note_2026_09_08_loop65": "One new edge added this pass, disclosed rather than silently absorbed: `chronica-cap-platform-misc-routes (cap) -> chronica-adapter-crm-support-analytics-dashboard (adapter)` (exact string taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` line after the caller repoint, not hand-guessed). layer_debt_edge_count: 122 -> 123 (1 new, 0 repaired). LOOP 65 decomposed crates/core/chronica-cap-crm-support-analytics-dashboard: its real Postgres mechanics -- the ELEVEN company-scoped async entry points create_dashboard/list_dashboards/get_dashboard/set_pinned/add_tag/remove_tag/add_annotation/remove_annotation/enable_sharing/disable_sharing/delete_dashboard over analytics_dashboard (migration 0076), the largest real-I/O surface of this split batch so far, plus the private mutate load-apply-persist helper, the private DashboardRow::map row decoder, the DASH_COLS column-list constant and the is_unique_violation duplicate detector -- moved BYTE-IDENTICAL into the new crates/adapter/chronica-adapter-crm-support-analytics-dashboard, closing an M7 forbidden_core_io_count hit (116 -> 115, baseline updated in this same change). SPLIT, not relocated -- the distinction was re-derived by reading the donor's actual current 411-line src/lib.rs in full rather than inherited from a scouting note or from the shape of its LOOP 60/62/63/64 siblings: although the donor's WHOLE body sat inside a single #[cfg(feature = \"db\")] mod imp, it declares its OWN pub types (DashboardRow with its to_dashboard domain rehydration and its camelCase to_json operator view, NewDashboard, CreateDashboardOutcome, AnnotationAdded), none of which touches a PgPersistence -- so the donor was kept and shrunk rather than renamed away, exactly as in LOOP 50/55/57/58/60/61/62/63/64. FACADE-PURITY JUDGMENT CALL, DISCLOSED (the LOOP 55/57/60/61/62/63/64 shape, weaker than LOOP 58's donor which retained no foreign type at all): the retained surface DOES name foreign types -- DashboardRow::to_dashboard returns a Dashboard and rebuilds it through the Annotation serde shape, both owned by chronica-core-analytics-dashboard. That crate was verified pure by reading its own Cargo.toml in the current tree, and one level deeper: chronica-core-analytics-dashboard deps chronica-core-analytics-metric-query, chronica-core-analytics-realtime-panel, chronica-core-analytics-insight-query, chronica-core-business-events, chronica-core and serde, and declares NO [features] section at all and no sqlx, so it takes every one of those edges at its DEFAULT feature set; one level deeper, none of them defaults on a db feature -- the three analytics siblings have no [features] section at all and no sqlx; chronica-core-business-events DOES have a db = [\"dep:sqlx\", \"chronica-core/db\"] feature but is default = [], so its optional sqlx stays off through this edge; and chronica-core is likewise default = [] with db opt-in, forwarding chronica-core-persistence/db + chronica-core-version/db, which the dashboard domain crate never requests. No sqlx and no db feature reaches the donor through that edge, so its db feature, its sqlx dependency and its direct chronica-core dependency were deleted OUTRIGHT rather than narrowed; the donor now has 2 dependencies (chronica-core-analytics-dashboard, serde_json) and ZERO cargo features. The ported DashboardRegistry mutations (the tag de-dup, the annotation retain, the AnnotationNotFound verdict) stay REUSED, never re-implemented -- the adapter (not the donor) loads the row, rebuilds the registry from it and applies the domain's own set_pinned/add_tag/remove_tag/add_annotation/remove_annotation. THE CALLER WAS FOUND BY EXACT FULLY-QUALIFIED-PATH GREP, not by bare function name: a repo-wide grep for both the underscore and hyphen crate spellings found exactly ONE real (non-test) production caller, crates/cap/chronica-cap-platform-misc-routes/src/analytics/dashboard.rs (12 fully-qualified references -- the use line plus 11 call sites, one per moved public op, across the axum handlers behind POST|GET /api/companies/:company_id/analytics/dashboards, GET|DELETE .../dashboards/:dashboard_id and the /pin, /tags, /tags/:tag, /annotations, /annotations/:annotation_id and /share sub-routes), which is cap-tier -- hence the one new edge. It is that route crate's TWELFTH edge of exactly this already-precedented cap -> adapter shape (the eleven pre-existing ones were enumerated from this same forbidden_edges array and re-verified as still present before being cited), not a novel debt class. A second declared dependent, chronica-adapter-api-server, was CONFIRMED DEAD by grepping its own src/ and tests/ for chronica_cap_crm_support_analytics_dashboard and analytics_dashboard: the only hit was a single doc-comment line in tests/analytics_dashboard_route.rs showing the cargo test invocation, with zero code references, so its manifest dep + db feature-array entry were removed outright rather than repointed, per the standing LOOP-21 precedent -- it still enables the new adapter's db feature transitively through chronica-cap-platform-misc-routes/db, and its analytics_dashboard_route.rs tests exercise the routes over HTTP, not the crate directly (all 12 of them re-run green against real Postgres after this change). Recorded BY HAND, NOT via --write-baseline (which would destroy the notes above), so the single addition above is the only change to forbidden_edges in this pass. See tools/ci/responsibility-baseline.json's _analytics_dashboard_split_2026_09_08_loop65 for the paired M2/M7 metric disclosure, and docs/_machine/graph-refoundation/subsystem-convergence.jsonl for the full ledger entry.",
  "_note_2026_09_08_loop68": "TWO new edges added this pass, disclosed rather than silently absorbed: `chronica-cap-crm-support-role-assignment-authorized (core) -> chronica-adapter-authorization-roles (adapter)` and `chronica-cap-helpdesk-inboxes (core) -> chronica-adapter-authorization-roles (adapter)` (both exact strings taken VERBATIM from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller repoints, not hand-guessed). They are the direct, unavoidable consequence of `chronica-cap-authorization-roles`'s responsibility-tier decomposition (LOOP 68, see docs/_machine/graph-refoundation/subsystem-convergence.jsonl): the real Postgres mechanics over `authz_role`/`authz_role_assignment` (migration 0302) -- create_role/get_role/list_roles/assign_role/revoke_role/list_roles_for_principal and the SECURITY-CRITICAL shared `authorize()` decision function, formerly a `#[cfg(feature = \"db\")]`-gated `mod imp` directly under `crates/core/` -- moved BYTE-IDENTICAL into the new `crates/adapter/chronica-adapter-authorization-roles`, while the donor's own genuinely pure surface (RoleRow, RoleAssignmentRow, CreateRoleOutcome, AssignRoleOutcome and their to_json/grants projections) stayed behind in a now feature-free, sqlx-free donor (M7 forbidden_core_io_count 113 -> 112, baseline updated in this same change). UNAVOIDABLE for exactly these two callers and no others: both are core-tier and both call the genuinely-I/O functions -- chronica-cap-helpdesk-inboxes' `authorize_inbox_action` gates every helpdesk inbox command on `authorize()`, and chronica-cap-crm-support-role-assignment-authorized exists SOLELY to wrap `assign_role` behind an `authorize()` check (removing the edge would mean deleting the authorization gate it was created to add). No arrangement of this split leaves them edge-free, and neither crate had ANY prior forbidden edge -- these are each crate's first. The other four callers cost nothing, verified individually: chronica-runtime-crm-support-party-authorized is runtime-tier, which this gate deliberately does not order (same precedent as LOOPs 50/55); chronica-adapter-authorization-role-lifecycle and chronica-adapter-crm-support-lifecycle-events are already adapter-tier; and chronica-cap-authorization-role-lifecycle names only the PURE RoleRow, so it keeps a core -> core edge to the donor rather than gaining an adapter edge. Both new edges are of the already-precedented core/cap -> adapter shape this program has recorded since LOOP 21 (50 such core -> adapter edges now stand), not a novel debt class. layer_debt_edge_count 124 -> 126. Recorded BY HAND, NOT via --write-baseline (which would destroy every note in this file), and the forbidden_edges array's pre-existing order was left untouched -- the two additions above are the only change to it in this pass. Verified after the edit: the gate reports 'no new edges (126 known debt edge(s) remain)'.",
  "_note_2026_09_08_loop69": "One new edge added this pass, disclosed rather than silently absorbed: `chronica-cap-social-routes (cap) -> chronica-adapter-social-data-export (adapter)` (exact string taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller change, not hand-guessed). It is the direct, unavoidable consequence of `chronica-cap-social-data-export`'s responsibility-tier decomposition (LOOP 69; see docs/_machine/graph-refoundation/subsystem-convergence.jsonl): the real `export_account_data` aggregator and the raw `own_notes_any_visibility` SELECT over `social_notes` moved out of that core-tier crate entirely, into the new `chronica-adapter-social-data-export` (crates/adapter/); only the pure `AccountExport` bundle type and its camelCase `to_json` projection stayed in the shrunk, now-always-pure core crate (one dependency, serde_json; no sqlx; no cargo features). The ONE real (non-dev) caller of the relocated function -- found by exact fully-qualified-path grep for `chronica_cap_social_data_export::`, never a bare function name -- is an HTTP route crate under crates/cap/ (`chronica-cap-social-routes`'s `social_data_export.rs`), which cannot depend on crates/adapter/ under FORBIDDEN_TARGETS.cap -- yet a route handler forwarding a read to real persistence has to reach it somehow, and a route crate cannot itself live below crates/cap/ without a much larger, out-of-scope route-crate decomposition. This is the SIXTH edge of this exact, already-precedented class for this same route crate: it already carries `-> chronica-adapter-postgres-social-handle`, `-> chronica-adapter-postgres-social-profile-privacy`, `-> chronica-adapter-postgres-social-user-profile`, `-> chronica-adapter-social-clips` and `-> chronica-adapter-social-user-lists` in this same forbidden_edges list (independently re-verified as still present in the current tree before citing them). No SECOND edge was created: the adapter re-exports the donor's pure AccountExport UNCONDITIONALLY, so the route crate's old `chronica-cap-social-data-export` dependency was DROPPED rather than kept alongside; and the adapter's own five store dependencies (`chronica-core`, `chronica-core-social-relationships`, `chronica-cap-social-notes`, `chronica-adapter-social-postgres-batch2`, `chronica-adapter-social-postgres-batch4`) all resolve to packages physically under crates/core/, so every one of them is a permitted adapter -> core edge. In the other direction this split REMOVED a latent hazard rather than adding one: `chronica-adapter-social-postgres-batch2` and `chronica-adapter-social-postgres-batch4` both documented `chronica-cap-social-data-export` (crates/core/) as a core-tier caller that would forbid THEM from ever relocating to crates/adapter/; that caller is now adapter-tier, so that specific hazard is gone for both (their stale prose was corrected in this same change), leaving only their cap-tier route callers still forcing their placement. Recorded BY HAND, never via --write-baseline. layer_debt_edge_count 126 -> 127.",
  "_note_2026_09_08_loop70": "One new edge added this pass, disclosed rather than silently absorbed: `chronica-cap-platform-admin-routes (cap) -> chronica-adapter-policy-post-comment (adapter)` (exact string taken verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller change, not hand-guessed). It is the direct, unavoidable consequence of `chronica-cap-policy-post-comment`'s responsibility-tier decomposition (LOOP 70; see docs/_machine/graph-refoundation/subsystem-convergence.jsonl): the real `PostCommentRepo` store -- its `list_for_subject`/`get`/`add` statements over the `post_comments` table (migration 0017_post_comments.sql), its `SELECT` column-list const and its private `map_row` PgRow decoder -- moved out of that core-tier crate entirely, into the new `chronica-adapter-policy-post-comment` (crates/adapter/); only the pure vocabulary (`SubjectKind`, `AuthorType`, `PostCommentRow` + its redacting `to_redacted_json` egress projection, `COMMENT_PAGE_MAX`, `clamp_comment_limit`) and the four pure unit tests that exercise it stayed in the shrunk, now-always-pure core crate (no sqlx, no [features] table at all). NOTE ON SHAPE: unlike every LOOP 59-69 sibling, this donor had no `#[cfg(feature = \"db\")] mod imp` block -- it used a WHOLE-FILE `#![cfg(feature = \"db\")]` inner attribute on src/post_comment.rs, so the moved span sat at indentation 0 and was reproduced with zero edits (SHA-256 b6611bc9adfbc4fed112a16c1f4b5560db7f8d216f0f5071bfa6f4fe633fbebd before and after). The ONE real (non-dev) caller of the relocated store -- found by exact fully-qualified-path grep for `chronica_cap_policy_post_comment::` across the whole repo, never a bare function or type name (2 hits, both in the same file) -- is an HTTP route crate under crates/cap/ (`chronica-cap-platform-admin-routes`'s `post_comments.rs`, the GET/POST /api/companies/:company_id/posts/:subject_kind/:subject_id/comments handlers), which cannot depend on crates/adapter/ under FORBIDDEN_TARGETS.cap -- yet a route handler forwarding an append/read to real persistence has to reach it somehow, and a route crate cannot itself live below crates/cap/ without a much larger, out-of-scope route-crate decomposition. This is the SIXTH edge of this exact, already-precedented class for this same route crate: it already carries `-> chronica-adapter-outbound-fetch`, `-> chronica-adapter-postgres-social-profile-privacy`, `-> chronica-adapter-postgres-social-user-profile`, `-> chronica-adapter-social-worker-directory` and `-> chronica-adapter-crm-support-metadata-view` in this same forbidden_edges list (independently re-verified as still present in the current tree, by enumerating the array itself, before being cited as precedent). No SECOND edge was created: the adapter re-exports the donor's pure vocabulary UNCONDITIONALLY (a base, non-optional dependency), so the route crate's old `chronica-cap-policy-post-comment` dependency was DROPPED rather than kept alongside, and the route file still reaches both halves through ONE crate path exactly as before. The adapter's only other dependencies (`chronica-core`, `sqlx`) are a permitted adapter -> core edge and an external crate respectively. `chronica-adapter-api-server` carried an unused-in-src, non-optional Cargo.toml dependency + db-feature-array entry on the donor crate (confirmed dead via grep across its own src/ and tests/ for `chronica_cap_policy_post_comment` and `policy_post_comment` -- zero hits of any kind) -- REMOVED entirely, not repointed; it still reaches the new adapter's db feature transitively through chronica-cap-platform-admin-routes/db. Recorded BY HAND, NOT via --write-baseline (which would destroy the notes above). layer_debt_edge_count 127 -> 128. See tools/ci/responsibility-baseline.json's _post_comment_split_2026_09_08_loop70 for the paired M2/M7 metric disclosure.",
  "_note_2026_09_08_loop71": "THREE new edges added this pass -- exactly three, no more -- disclosed rather than silently absorbed: `chronica-cap-deals-federation-routes (cap) -> chronica-adapter-social-pages (adapter)`, `chronica-cap-platform-admin-routes (cap) -> chronica-adapter-social-pages (adapter)` and `chronica-cap-social-routes (cap) -> chronica-adapter-social-pages (adapter)` (all three strings taken VERBATIM from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller change -- the gate printed exactly these three lines and nothing else -- never hand-guessed). They are the direct, unavoidable consequence of `chronica-cap-social-pages`'s responsibility-tier decomposition (LOOP 71; see docs/_machine/graph-refoundation/subsystem-convergence.jsonl): the real `social_pages` store -- `create_page`/`list_pages`/`get_page`/`update_page`/`delete_page` over the `social_pages` table (migration 0082_social_pages.sql), the `PAGE_COLS` projection const, the `PAGE_LIST_LIMIT` bound and the private `PageRow::map` PgRow decoder (now the free `map_page_row` there, Rust's orphan rule E0116) -- moved out of that core-tier crate entirely, into the new `chronica-adapter-social-pages` (crates/adapter/); only the pure page vocabulary (`PageRow` + its camelCase `to_json` egress projection, `NewPage`, `UpdatePage`, `CreatePageOutcome`, `UpdatePageOutcome`) and the four pure operators (`page_error_reason`, `validate_blocks`, `looks_like_uuid`, `visibility_token`) plus the two pure unit tests that exercise `validate_blocks` stayed in the shrunk, now-always-pure core crate (no sqlx, no chronica-core, no [features] table at all, no dev-dependencies). NOTE ON SHAPE: this donor used the LOOP 59-69 shape, NOT LOOP 70's -- a single `#[cfg(feature = \"db\")] mod imp { ... }` wrapping EVERYTHING, pure and impure alike (`grep -n '^#\\[cfg(feature = \"db\")\\]'` on the pre-split file returns exactly lines 18 and 322, the two real attribute sites), so the adapter reproduces that same `mod imp` nesting and the two large moved spans kept their original 4-space indentation, while the RETAINED pure spans were dedented by 4 as they came out to top level in the donor. THREE callers, not one, and all three are HTTP route crates under crates/cap/: found by exact fully-qualified-path grep for `chronica_cap_social_pages::` across the whole repo (never a bare function or type name), which returned 10 source hits in exactly 3 files -- `chronica-cap-social-routes/src/social_pages.rs` (the five POST/GET/GET/PUT/DELETE /api/companies/:company_id/social/pages handlers, 6 hits), `chronica-cap-platform-admin-routes/src/search_suggest.rs` (`list_pages` for the suggest index, 2 hits) and `chronica-cap-deals-federation-routes/src/search.rs` (`list_pages` plus a direct `PageRow` name, 2 hits). None of the three can depend on crates/adapter/ under FORBIDDEN_TARGETS.cap -- yet a route handler forwarding a read/write to real persistence has to reach it somehow, and a route crate cannot itself live below crates/cap/ without a much larger, out-of-scope route-crate decomposition. Every one of the three is an already-precedented class for that same crate, re-verified as still present by enumerating this forbidden_edges array itself before being cited: `chronica-cap-social-routes` already carries `-> chronica-adapter-postgres-social-handle`, `-> chronica-adapter-postgres-social-profile-privacy`, `-> chronica-adapter-postgres-social-user-profile`, `-> chronica-adapter-social-clips`, `-> chronica-adapter-social-user-lists` and `-> chronica-adapter-social-data-export` (this is its seventh); `chronica-cap-platform-admin-routes` already carries `-> chronica-adapter-outbound-fetch`, `-> chronica-adapter-postgres-social-profile-privacy`, `-> chronica-adapter-postgres-social-user-profile`, `-> chronica-adapter-social-worker-directory`, `-> chronica-adapter-crm-support-metadata-view` and `-> chronica-adapter-policy-post-comment` (this is its seventh); `chronica-cap-deals-federation-routes` already carries `-> chronica-adapter-crm-support-contract-lifecycle`, `-> chronica-adapter-esign-document-store`, `-> chronica-adapter-esign-envelope-audit-log`, `-> chronica-adapter-openbao-vault`, `-> chronica-adapter-outbound-fetch`, `-> chronica-adapter-postgres-artifact-registry` and `-> chronica-adapter-social-kyc-trusted-issuer` (this is its eighth). NO FOURTH-PLUS EDGE was created: the adapter re-exports the donor's pure vocabulary UNCONDITIONALLY (a base, non-optional dependency, declared OUTSIDE its own db gate), so each route crate's old `chronica-cap-social-pages` dependency was DROPPED rather than kept alongside, and each route file still reaches both halves through ONE crate path exactly as before -- which specifically matters for chronica-cap-deals-federation-routes, whose search.rs names `PageRow` directly. The adapter's only other dependencies (`chronica-core`, `chronica-core-social-domain`, `serde_json`, `sqlx`) are permitted adapter -> core edges and external crates. `chronica-adapter-api-server` carried an unused-in-src, non-optional Cargo.toml dependency + a \"chronica-cap-social-pages/db\" db-feature-array entry on the donor crate (confirmed dead via grep across its own src/ and tests/ for `chronica_cap_social_pages` -- zero hits; the only `social_pages` hits there are `chronica_cap_social_routes::social_pages::social_page_routes` in routes/reexports.rs and four test-file doc-comment mentions) -- REMOVED entirely, not repointed; it still reaches the new adapter's db feature transitively through chronica-cap-social-routes/db, which is already in that same array. Recorded BY HAND, NOT via --write-baseline (which would destroy the notes above); the three edges were appended at the end of forbidden_edges, matching this array's existing append-order convention (the array is not, and has never been, globally sorted -- verified programmatically before appending). layer_debt_edge_count 128 -> 131. See tools/ci/responsibility-baseline.json's _social_pages_split_2026_09_08_loop71 for the paired M2/M7 metric disclosure.",
  "_note_2026_09_08_loop73": "EXACTLY ONE new edge added this pass -- one, not two, and the exact string was copied verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller repoint, never hand-guessed: `chronica-cap-social-routes (cap) -> chronica-adapter-social-author-timeline (adapter)`. It is the direct, unavoidable consequence of LOOP 73's responsibility-tier decomposition of `chronica-cap-social-author-timeline` (see docs/_machine/graph-refoundation/subsystem-convergence.jsonl). That decomposition was a whole-crate RELOCATION, not a split, and the reason is structural rather than stylistic: the crate declared NO type, enum, trait, const or type alias of its own -- verified by reading all 335 lines of its src/lib.rs and by a `^\\s*(pub )?(struct|enum|trait|type|static|const) ` grep returning ZERO matches -- and its single production item, `list_author_timeline`, is pure sqlx I/O returning a FOREIGN row type (`chronica_cap_social_notes::NoteRow`). With the `db` feature off it compiles to a library exporting literally nothing, so a split would have left an empty stub under crates/core/. The whole crate therefore moved to crates/adapter/chronica-adapter-social-author-timeline and was renamed to match its tier; no donor remains under crates/core/ and no re-export shim exists. WHY THE EDGE IS UNAVOIDABLE: all three real callers of `list_author_timeline` (`social_author_timeline.rs` -- the profile page's Notes tab; `social_agent_profile.rs` -- the agent recent-activity rail; `social_rss.rs` -- the RSS feed event source) live in the single HTTP route crate `chronica-cap-social-routes`, under crates/cap/, and `FORBIDDEN_TARGETS.cap` bars a cap-tier crate from depending on runtime- or adapter-tier crates. A route handler forwarding a tenant-scoped read to real persistence has to reach that persistence somehow, and moving the route crate itself below crates/cap/ is a much larger, out-of-scope route-crate decomposition. Precedent is already dense and was re-verified in the current tree rather than cited from memory: THIS SAME route crate already carries EIGHT edges of exactly this class in this list -- to chronica-adapter-social-clips, chronica-adapter-social-user-lists, chronica-adapter-social-data-export (LOOP 69), chronica-adapter-social-pages (LOOP 71), chronica-adapter-social-conversations (LOOP 72), chronica-adapter-postgres-social-handle, chronica-adapter-postgres-social-profile-privacy and chronica-adapter-postgres-social-user-profile -- so this is the ninth, structurally identical to the eight before it. NOT ADDED, and worth stating because a reader may expect it: chronica-adapter-api-server carried a direct dependency on the old crate, but it was DEAD (a grep for the exact fully-qualified path `chronica_cap_social_author_timeline` across its src/ AND tests/ returned ZERO hits) and was REMOVED OUTRIGHT rather than repointed, per the standing LOOP-21 precedent -- so it produces no edge at all, and it is adapter-tier anyway, where adapter -> adapter is allowed. PAIRED METRIC DISCLOSURE: unlike the LOOP 69/70/71/72 splits, this relocation moves BOTH M7 (forbidden_core_io_count 108 -> 107: no real sqlx I/O sits under crates/core/ at this address any more) AND M2 (misplaced_cap_package_count 222 -> 221, by_root.core 222 -> 221: the package both stopped being chronica-cap-*-named AND left crates/core/), both recorded in tools/ci/responsibility-baseline.json's _social_author_timeline_relocation_2026_09_08_loop73 in this SAME change. Hand-added at its ALPHABETICAL position within the sorted region of this array (between `... -> chronica-adapter-postgres-social-user-profile (adapter)` and `... -> chronica-adapter-social-clips (adapter)`); the array's final three entries, appended out of order by LOOP 69/71/72, were deliberately left exactly where they are rather than resorted, so this diff shows one inserted line and nothing else. Every existing edge and every existing prose note is preserved verbatim; `--write-baseline` was NEVER run (it would destroy these notes). Edge count 133 -> 134.",
  "_note_2026_09_08_loop72": "TWO new edges added this pass -- exactly two, no more -- disclosed rather than silently absorbed: `chronica-cap-social-notes (core) -> chronica-adapter-social-conversations (adapter)` and `chronica-cap-social-routes (cap) -> chronica-adapter-social-conversations (adapter)` (both strings taken VERBATIM from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller change -- the gate printed exactly these two lines and nothing else -- never hand-guessed). They are the direct, unavoidable consequence of `chronica-cap-social-conversations`'s responsibility-tier decomposition (LOOP 72; see docs/_machine/graph-refoundation/subsystem-convergence.jsonl): the real `social_conversations` store -- `record_note_in_conversations` (the extract-on-note-create hook), `list_conversations`, `mark_conversation_read` and `unread_conversation_count` over the `social_conversations` table (migration 0401_social_conversations.sql), the `CONVERSATION_COLS` projection const and the private `ConversationRow::map` PgRow decoder (now the free `map_conversation_row` there, Rust's orphan rule E0116), together with all five `#[ignore]`d live-DB tests -- moved out of that core-tier crate entirely, into the new `chronica-adapter-social-conversations` (crates/adapter/); only the pure conversation vocabulary (`ConversationRow` plus its camelCase `to_json` egress projection) stayed in the shrunk, now-always-pure core crate (no sqlx, no chronica-core, no [features] table at all, no dev-dependencies -- exactly ONE dependency, serde_json). NOTE ON SHAPE: this donor used the LOOP 59-69/71 shape, NOT LOOP 70's -- a single `#[cfg(feature = \"db\")] mod imp { ... }` wrapping EVERYTHING, pure and impure alike (`grep -n '^#\\[cfg(feature = \"db\")\\]'` on the pre-split file returns exactly lines 28 and 322, the two real attribute sites), so the adapter reproduces that same `mod imp` wrapper and the moved spans keep their original 4-space indentation byte-for-byte. EDGE 1 (`chronica-cap-social-notes`): this crate is a `chronica-cap-*`-named capability crate PHYSICALLY misplaced under `crates/core/` by an earlier extraction, and its `src/imp/create.rs` post-commit hook calls `record_note_in_conversations` best-effort after a `Specified`-visibility note commits (the sole call site, line 212) -- so repointing that one call onto the now-crates/adapter/-tier crate creates a core -> adapter edge it cannot avoid without a much larger, out-of-scope decomposition of the note substrate itself. This is the SAME, already- established debt class as `chronica-cap-social-notes (core) -> chronica-adapter-postgres-social-handle (adapter)` (this same `forbidden_edges` list, re-verified as still present in the current tree before citing, and disclosed in this file's own `_note_2026_09_06c`), which exists for the identical reason on the identical crate. EDGE 2 (`chronica-cap-social-routes`): cap-tier, and its `src/social_conversations.rs` route module is the HTTP surface for this capability -- `list_route` calls `list_conversations` and `mark_read_route` calls `mark_conversation_read` -- so it must follow the mechanics into crates/adapter/ exactly as it already did for `chronica-adapter-social-data-export` (LOOP 69) and `chronica-adapter-social-pages` (LOOP 71), both already in this list. NEITHER caller now carries two crate paths: `chronica-adapter-social-conversations` re-exports the donor's pure `ConversationRow` UNCONDITIONALLY (outside its own `db` gate, via a BASE non-optional dependency on the donor), so each caller's former direct `chronica-cap-social-conversations` dependency was DROPPED rather than kept alongside -- the cap/core -> core edge is retired in the same edit that creates the cap/core -> adapter one, and the net dependency count per caller is unchanged. A THIRD manifest, `crates/adapter/chronica-adapter-api-server/Cargo.toml`, also declared `chronica-cap-social-conversations` (plus `chronica-cap-social-conversations/db` in its `db` feature array); an exact fully-qualified-path grep for `chronica_cap_social_conversations` across that crate's own `src/` and `tests/` returned ZERO hits (its only social-conversation reference is `pub(crate) use chronica_cap_social_routes::social_conversations::social_conversation_routes;` in src/routes/reexports.rs, which goes through the ROUTES crate, not this one), so that dependency was genuinely DEAD and was removed OUTRIGHT rather than repointed -- the standing LOOP-21 precedent -- adding no third edge. M2 misplaced_cap_package_count is deliberately UNCHANGED at 222 by this loop: the donor legitimately stays under crates/core/ holding only pure vocabulary, so this is a SPLIT, not a relocation. Hand-added, alphabetically within the appended pair, with the file's existing content and every existing prose note preserved verbatim; `--write-baseline` was NEVER run (it would destroy these notes). Edge count 131 -> 133.",
  "_note_2026_09_08_loop74": "EXACTLY ONE new edge added this pass -- one, not two, and the exact string was copied verbatim from `node tools/refoundation/validate-refoundation-layers.mjs`'s own `NEW FORBIDDEN EDGE:` output after the caller repoint, never hand-guessed: `chronica-cap-platform-misc-routes (cap) -> chronica-adapter-crm-support-analytics-cohort (adapter)`. The gate printed that one line and nothing else. It is the direct, unavoidable consequence of LOOP 74's responsibility-tier decomposition of `chronica-cap-crm-support-analytics-cohort` (see docs/_machine/graph-refoundation/subsystem-convergence.jsonl). That decomposition was a SPLIT, not a relocation, and the reason is structural rather than stylistic: the donor declares FOUR genuinely pure types of its OWN -- `CohortRow` (nine pub fields) with its `to_cohort` rehydration into the ported `ManagedCohort` domain and its camelCase `to_json` egress view, `NewCohort` (the validated request body), and the two route-verdict enums `CreateCohortOutcome` (Created/Invalid -> 201/422) and `UpdateCohortOutcome` (Updated/NotFound/Invalid -> 200/404/422) -- none of which names a `PgPersistence`, awaits anything or issues a statement. `grep -nE '^\\\\s*(pub )?(struct|enum|trait|type|static|const) '` over `git show HEAD:crates/core/chronica-cap-crm-support-analytics-cohort/src/lib.rs` returns six matches (two consts, two structs, two enums), so the LOOP 73 RELOCATION shape (zero local declarations) was ruled out on evidence and the donor was SHRUNK rather than renamed away. WHY THE EDGE IS UNAVOIDABLE: the ONLY real (non-dev, non-test) caller of the six moved store ops is `crates/cap/chronica-cap-platform-misc-routes/src/analytics/cohort.rs` -- the six HTTP handlers behind POST/GET `/api/companies/:company_id/analytics/cohorts`, GET/PUT/DELETE `.../cohorts/:id` and GET `.../cohorts/:id/members` -- found by EXACT fully-qualified-path grep for `chronica_cap_crm_support_analytics_cohort::` across the whole repo (seven hits, all in that one file; the only other hit anywhere is a prose mention inside a doc comment in crates/core/chronica-cap-crm-support-commerce-kpi/src/lib.rs, which declares no dependency on this crate at all and is therefore not a caller). That route crate lives under crates/cap/, and `FORBIDDEN_TARGETS.cap` bars a cap-tier crate from depending on runtime- or adapter-tier crates. A route handler forwarding a tenant-scoped read/write to real persistence has to reach that persistence somehow, and moving the route crate itself below crates/cap/ is a much larger, out-of-scope route-crate decomposition. Because the new adapter re-exports the donor's whole pure surface UNCONDITIONALLY (outside its own `db` gate), the caller's now-redundant DIRECT dependency on the donor was DROPPED in the same change -- one crate path, not two -- so this split adds exactly one edge and removes none. PRECEDENT, re-verified in the current tree rather than cited from memory: THIS SAME route crate already carries EIGHT edges of exactly this class in this list -- to chronica-adapter-crm-support-analytics-action, chronica-adapter-crm-support-ad-spend, chronica-adapter-crm-support-contacts (LOOP 58), chronica-adapter-crm-support-event-definitions (LOOP 54), chronica-adapter-crm-support-money-event-projector, chronica-adapter-osint-http-core, chronica-adapter-outbound-fetch and chronica-adapter-postgres-artifact-registry -- plus four more appended out of order lower down (analytics-alert-rule, analytics-insight, feature-flag, analytics-dashboard). This is therefore its thirteenth edge of this exact shape, not a novel debt class. NOT ADDED, and worth stating because a reader may expect it: chronica-adapter-api-server carried a direct Cargo.toml dependency on the old crate plus a `chronica-cap-crm-support-analytics-cohort/db` entry in its `db` feature array, but the dependency was DEAD -- an exact-path grep across its src/ AND tests/ for `chronica_cap_crm_support_analytics_cohort`, and for the capability's own type/function names (CohortRow, NewCohort, CreateCohortOutcome, UpdateCohortOutcome, compute_members), returned ZERO hits; its tests/analytics_cohort_route.rs drives the capability purely over HTTP through a local `create_cohort` helper that posts to the route, and the routes themselves live in chronica-cap-platform-misc-routes, whose own `db` feature already forwards to the adapter. Both entries were REMOVED OUTRIGHT rather than repointed, per the standing LOOP-21 precedent -- so they produce no edge at all, and chronica-adapter-api-server is adapter-tier anyway, where adapter -> adapter is allowed. METRIC DISCLOSURE, and note the asymmetry with LOOP 73: this is a SPLIT, so it moves M7 ONLY -- forbidden_core_io_count 107 -> 106 (no real sqlx I/O sits under crates/core/ at this address any more; the donor's Cargo.lock package entry lost its `sqlx` line and now lists exactly two dependencies, chronica-core-analytics-cohort and serde_json) -- while M2 misplaced_cap_package_count stays EXACTLY where it was at 221 (by_root.core 221), because the donor package keeps both its chronica-cap-* name and its crates/core/ home. That is the LOOP 69/70/71/72 shape, not the LOOP 73 relocation shape which moved both. The M7 change is recorded in tools/ci/responsibility-baseline.json's _crm_support_analytics_cohort_split_2026_09_08_loop74 in this SAME change. POSITION: hand-added at its alphabetical position within the sorted region of this array, immediately after `chronica-cap-platform-misc-routes (cap) -> chronica-adapter-crm-support-ad-spend (adapter)` and immediately before `... -> chronica-adapter-crm-support-contacts (adapter)` (`chronica-adapter-crm-support-ad-spend` < `chronica-adapter-crm-support-analytics-cohort` < `chronica-adapter-crm-support-contacts`), which introduces NO new sort breakpoint: the array's pre-existing out-of-order runs -- the single analytics-action/ad-spend transposition at indices 40/41 and the fourteen entries appended out of alphabetical order at the tail by LOOP 60/62/63/65/67/68/69/71/72 -- were deliberately left EXACTLY where they are rather than resorted, so this diff shows one inserted line and nothing else. Every existing edge and every existing note is preserved verbatim; `--write-baseline` was NEVER run (it would destroy these notes). Edge count 134 -> 135.",
  "_note_2026_09_08_loop75": "LOOP 75 (2026-09-08) responsibility-tier decomposition of crates/core/chronica-cap-crm-support-analytics-funnel (the analytics FUNNEL capability, migration 0074_analytics_funnels.sql, at 456 lines the largest crate this program has split so far) added EXACTLY ONE forbidden edge, 134 -> 135 -> 136 total: 'chronica-cap-platform-misc-routes (cap) -> chronica-adapter-crm-support-analytics-funnel (adapter)'. That string was COPIED VERBATIM from validate-refoundation-layers.mjs's own 'NEW FORBIDDEN EDGE:' output line and never hand-guessed; the gate printed that one line and nothing else. The brief warned this crate probably had MULTIPLE real callers, so the callers were found by EXACT fully-qualified-path grep for 'chronica_cap_crm_support_analytics_funnel::' rather than by a bare function or type name, and there are indeed TWO -- but only ONE of them scores. Caller 1: crates/cap/chronica-cap-platform-misc-routes/src/analytics/funnel.rs, seven hits (the use line plus the six op call sites behind POST/GET /api/companies/:company_id/analytics/funnels, GET/PUT/DELETE .../funnels/:id and GET .../funnels/:id/compute); it is cap-tier, FORBIDDEN_TARGETS.cap bars cap -> adapter, hence this edge. Caller 2: crates/adapter/chronica-adapter-crm-support-event-bridge/tests/crm_event_bridge_live.rs, five hits, which is (a) a DEV-dependency, and the gate skips dev/build dependencies outright ('if (dependency.kind) continue' -- they never reach a production binary), and (b) adapter -> adapter regardless, which FORBIDDEN_TARGETS.adapter (= {infra}) permits. So it adds no edge, and the gate's single-line output confirmed that rather than it being assumed. WHY THE ONE EDGE IS UNAVOIDABLE: the scoring caller is a cap-tier HTTP route crate whose handlers must reach real Postgres mechanics, and the mechanics MUST live at adapter tier for M7 to fall; moving the route crate itself out from under crates/cap/ is a much larger, out-of-scope decomposition. chronica-cap-platform-misc-routes already carries THIRTEEN edges of exactly this class (LOOP 54/58/60/62/63/69/71/72/74 and their predecessors), so this is its fourteenth and introduces no new KIND of debt. The entry was hand-added at its alphabetical position in the sorted region -- index 43, immediately after the chronica-adapter-crm-support-analytics-cohort entry LOOP 74 added and immediately before the chronica-adapter-crm-support-contacts entry -- which introduces NO new sort breakpoint: the array's 7 pre-existing breakpoints (the transposition at indices 40/41 and the out-of-order tail deliberately appended by LOOP 60/62/63/65/67/68/69/71/72) were left exactly where they are, and the editing script asserted the breakpoint count was 7 before AND after. --write-baseline was NEVER run on this file.",
  "_note_2026_09_08_loop76": "LOOP 76 (2026-09-08): EXACTLY ONE new forbidden edge, `chronica-cap-agent-session-routes (cap) -> chronica-adapter-agent-edit-history (adapter)`, copied VERBATIM from validate-refoundation-layers.mjs's own NEW FORBIDDEN EDGE line and never hand-guessed; the gate printed that one line and nothing else. It is the unavoidable cost of splitting crates/core/chronica-cap-agent-edit-history: its real Postgres store (AgentEditHistoryStore over agent_edit_history) moved to crates/adapter/chronica-adapter-agent-edit-history, and the SOLE real caller -- confirmed by an EXACT fully-qualified-path grep for `chronica_cap_agent_edit_history::` across the whole tree, which returned exactly one non-donor hit -- is chronica-cap-agent-session-routes/src/agent_edit_history_routes/handlers.rs, a cap-tier HTTP route crate. FORBIDDEN_TARGETS.cap bars cap -> adapter, and moving that route crate below crates/cap/ is a much larger, out-of-scope decomposition. That route crate already carries an edge of exactly this class (`chronica-cap-agent-session-routes (cap) -> chronica-adapter-postgres-analytics-metric-lineage (adapter)`), so this is its SECOND and introduces no new KIND of debt. The trade is M7 forbidden_core_io_count 105 -> 104 against layer_debt_edge_count 136 -> 137. A SECOND caller was checked and produced NO edge: chronica-adapter-api-server declared `chronica-cap-agent-edit-history = { workspace = true, optional = true }` plus a `dep:chronica-cap-agent-edit-history` entry inside its `db` feature array, but that dependency was DEAD -- a grep of its src/ and tests/ for the crate-underscore path and for every item of the capability's vocabulary (AgentEditHistoryStore, AgentEditHistoryView, StoreError, EditHistory, validate_file, record_edit, predict_next_location) found only doc-comment prose, no `use` and no call, and no `package = ` alias of the crate exists anywhere under crates/. Both entries were REMOVED OUTRIGHT per the standing LOOP-21 precedent rather than repointed, so no edge arises from that side (and chronica-adapter-api-server is adapter-tier anyway, where adapter -> adapter is allowed). The edge was hand-added to forbidden_edges (136 -> 137) at its alphabetical position in the sorted region, and the editing script asserted that the array's pre-existing sort-breakpoint count was UNCHANGED by the insertion (so the deliberately out-of-order tail appended by earlier loops was left exactly where it is) and that every pre-existing edge string and every pre-existing key is byte-unchanged. --write-baseline was NEVER run.",
  "_note_2026_09_08_loop79": "LOOP 79 (2026-09-08) added exactly ONE forbidden edge, and it was EXPECTED rather than a surprise: 'chronica-cap-erp-commerce-ops-routes (cap) -> chronica-adapter-commerce-order-intake (adapter)'. The string was COPIED VERBATIM from the gate's own `NEW FORBIDDEN EDGE:` line, never hand-typed or reconstructed from the crate names. WHY IT APPEARED: LOOP 79 relocated the whole crate chronica-cap-commerce-order-intake from crates/core/ to crates/adapter/chronica-adapter-commerce-order-intake and renamed it to its tier. Its sole real caller, chronica-cap-erp-commerce-ops-routes, is a CAP-TIER route crate, so the dependency that previously read cap -> core (permitted) now reads cap -> adapter (forbidden by physical directory). This is the OPPOSITE outcome from LOOP 77 and LOOP 78, which added zero edges only because both of their callers happened to already be adapter-tier; it repeats instead the shape LOOP 73's relocation produced. layer_debt_edge_count 137 -> 138. WHAT THE EDGE DOES AND DOES NOT MEAN: it does NOT represent any new coupling. Not one call site, import, type or SQL statement changed direction, and no dependency was added anywhere -- chronica-cap-erp-commerce-ops-routes called get_decision/record_decision through exactly the same API before this loop as after, at the same five source positions. What changed is that the Postgres I/O it depends on is now HONESTLY LABELLED as adapter-tier instead of being mislabelled as core-tier. The edge was therefore ALREADY REAL and was simply INVISIBLE to the gate while the adapter code sat under crates/core/ wearing a chronica-cap-* name; recording it here converts concealed debt into disclosed debt. That is a net accuracy gain even though the counter goes up, and it is why M7 forbidden_core_io_count fell 102 -> 101 and M2 misplaced_cap_package_count fell 220 -> 219 in the same change: the same relocation that revealed this edge removed a real instance of core-tier I/O and a real misplaced cap package. THE REAL REMAINING DEBT this edge names is that chronica-cap-erp-commerce-ops-routes is itself still cap-tier; when that route crate eventually moves to crates/adapter/ (it is an HTTP route surface, i.e. mechanical I/O), this edge becomes adapter -> adapter and can be retired from this array. Nothing about the order-intake capability needs to change for that to happen. PROCESS: --write-baseline was NEVER run. This single string was hand-added to forbidden_edges, inserted at its correct alphabetical position inside the array's sorted prefix (between the chronica-cap-deals-federation-routes block and the chronica-cap-platform-misc-routes block) so that the prefix remains sorted and no pre-existing entry was reordered, moved or removed.",
  "_note_2026_09_15_esign_contract_signature_store_relocation": "One new edge added this pass, disclosed rather than silently absorbed (exact string taken verbatim from node tools/refoundation/validate-refoundation-layers.mjs's own NEW FORBIDDEN EDGE: output): `chronica-cap-deals-federation-routes (cap) -> chronica-adapter-esign-contract-signature-store (adapter)`. This is a straight RESPONSIBILITY-TIER RELOCATION, not a Class-B split: the former crates/core/chronica-cap-esign-contract-signature-store was confirmed 100% real Postgres persistence mechanics with NO pure vocabulary of its own (with the db feature off it compiled to an empty crate before this move too) -- every pure type it touches (ContractSignature/EsignError/VerificationReport) already lived in chronica-core-esign-jades-signature. Renamed + physically moved crates/core/ -> crates/adapter/, package chronica-cap-esign-contract-signature-store -> chronica-adapter-esign-contract-signature-store, zero logic change (byte-identical src/lib.rs). The one real cap-tier caller, chronica-cap-deals-federation-routes (src/esign.rs), already carried other real adapter-tier dependencies in its own Cargo.toml before this change (chronica-adapter-esign-document-store, chronica-adapter-esign-envelope-audit-log), establishing the same precedent as every sibling cap-routes -> adapter edge in this ledger. chronica-adapter-api-server (adapter-tier, already an existing real caller via its esign_certificate_route.rs test) needed only a name repoint, no new edge (adapter -> adapter is allowed). layer_debt_edge_count 143 -> 144. See tools/ci/responsibility-baseline.json's paired note.",
  "_note_2026_09_15_thread_read_relocation": "One new edge added this pass, disclosed rather than silently absorbed (exact string taken verbatim from node tools/refoundation/validate-refoundation-layers.mjs's own NEW FORBIDDEN EDGE: output): `chronica-cap-platform-misc-routes (cap) -> chronica-adapter-helpdesk-thread-read (adapter)`. Another straight RESPONSIBILITY-TIER RELOCATION (not a Class-B split, same shape as the same-day esign-contract-signature-store relocation): the former crates/core/chronica-cap-helpdesk-thread-read was confirmed 100% real Postgres query mechanics with NO pure vocabulary of its own -- its own module doc already stated every item is gated behind the db feature since every item needs sqlx + a live PgPersistence connection. Renamed + physically moved crates/core/ -> crates/adapter/, package chronica-cap-helpdesk-thread-read -> chronica-adapter-helpdesk-thread-read, zero logic change (byte-identical src/lib.rs). Three real callers: chronica-runtime-helpdesk-automation (runtime-tier, runtime -> adapter already allowed, no disclosure needed), chronica-adapter-helpdesk-http (adapter-tier, adapter -> adapter already allowed), and chronica-cap-platform-misc-routes (cap-tier, src/store_platform/helpdesk.rs -- this crate already carried 14 other real adapter-tier dependencies in its own Cargo.toml before this change, establishing the same precedent as every sibling cap-routes -> adapter edge in this ledger). chronica-adapter-api-server carried a confirmed-unused optional dependency (zero real src/ or tests/ usage) -- removed outright, same as prior cohorts. layer_debt_edge_count 144 -> 145. See tools/ci/responsibility-baseline.json's paired note.",
  "_note_2026_09_15_inboxes_relocation": "RESPONSIBILITY-TIER RELOCATION (not a new edge -- an edge REMOVED): crates/core/chronica-cap-helpdesk-inboxes relocated + renamed to crates/adapter/chronica-adapter-helpdesk-inboxes. Confirmed 100% real Postgres mechanics (the crate's own module doc already said \"Gated behind the db feature\" for every item), no pure vocabulary to extract -- zero logic change (git mv, byte-identical src/lib.rs). This resolves the previously-disclosed chronica-cap-helpdesk-inboxes (core) -> chronica-adapter-authorization-roles (adapter) edge from _note_2026_09_08_loop68: that dependency is now adapter -> adapter, which FORBIDDEN_TARGETS.adapter (={infra}) does not forbid at all, so the entry was removed rather than kept as stale debt. The one real external caller, chronica-adapter-helpdesk-http (already adapter-tier), needed only a use-path repoint -- adapter -> adapter, no disclosure. chronica-adapter-api-server carried a confirmed-unused optional dependency (zero real src/ or tests/ usage) -- removed outright. layer_debt_edge_count 145 -> 144. See tools/ci/responsibility-baseline.json's paired note.",
  "_note_2026_09_15_conversations_relocation": "RESPONSIBILITY-TIER RELOCATION with a net edge swap: crates/core/chronica-cap-helpdesk-conversations relocated + renamed to crates/adapter/chronica-adapter-helpdesk-conversations, in the SAME commit as the sibling chronica-cap-helpdesk-inboxes relocation (see _note_2026_09_15_inboxes_relocation) -- conversations depends on inboxes, so both had to move together to avoid a transient core -> adapter edge. Confirmed 100% real Postgres mechanics (mod imp gated #[cfg(feature = \"db\")], no pure vocabulary outside it, byte-identical src/lib.rs). RESOLVES the previously-disclosed chronica-cap-helpdesk-conversations (core) -> chronica-adapter-helpdesk-read-model (adapter) edge (now adapter -> adapter, no disclosure) -- removed from forbidden_edges. ADDS one new edge: chronica-cap-platform-misc-routes (cap) -> this crate (adapter) -- its own Cargo.toml already carried 14+ other real adapter-tier dependencies before this change (established in the thread-read relocation two commits ago), so this follows the same precedented cap-routes -> adapter shape, not a new debt category. Other real callers repointed with no new edge: chronica-runtime-helpdesk-automation (runtime -> adapter), chronica-adapter-helpdesk-http and chronica-adapter-postgres-helpdesk-automation (both already adapter -> adapter). chronica-adapter-api-server carried a confirmed-unused optional dependency (zero real src/ or tests/ usage) -- removed outright. This crate's own edge swap is net zero (one removed, one added). Combined with the sibling inboxes relocation's -1 in the same commit, layer_debt_edge_count goes 145 -> 144 overall for this commit. See tools/ci/responsibility-baseline.json's paired note.",
  "_note_2026_09_16_osint_investigation_split": "CLASS-B SPLIT (not a relocation): crates/core/chronica-cap-osint-investigation-repo mixed a pure Investigation/Finding/ConfidenceTier/InvestigationStatus vocabulary (unconditional, no db feature) with real Postgres repo mechanics (OsintRepo, over osint_investigations/osint_findings, idempotent upserts + bounded retry on transient conflicts) behind a #[cfg(feature = \"db\")] pub mod repo; gate -- confirmed via full source read (both files together only 999 lines). Split into crates/core/chronica-core-osint-investigation (the pure half, zero deps beyond serde + chronica-core) and crates/adapter/chronica-adapter-osint-investigation-repo (the impure half, re-gated the same #[cfg(feature = \"db\")] mod imp {...} discipline used throughout this session's relocations). Zero logic change to either half's own content. The donor Cargo.toml also carried seven genuinely unused dependencies (rusqlite, quick-xml, serde_yaml, url, sha1, hmac, chrono -- confirmed via a full-crate grep before the split: none appeared anywhere in either module) inherited from the historical \"0-change move\" out of the old flat chronica-osint crate and never cleaned up; dropped rather than carried into either new crate. ONE new edge: chronica-cap-platform-misc-routes (cap) -> the new adapter crate -- its own Cargo.toml already carried 14+ other real adapter-tier dependencies before this change (established precedent from the thread-read relocation earlier this session), same cap-routes -> adapter shape, not a new debt category. All 5 of that crate's real callers (osint/{company,fetch,read,social,web}.rs) use ONLY the impure OsintRepo symbol -- none needed a dependency on the new pure crate at all. chronica-adapter-api-server carried a confirmed-unused NON-optional dependency (zero real src/ or tests/ usage) -- removed outright. layer_debt_edge_count 144 -> 145. See tools/ci/responsibility-baseline.json's paired note.",
  "_note_2026_09_16_control_room_read_model_relocation": "RESPONSIBILITY-TIER RELOCATION: crates/core/chronica-cap-policy-control-room-read-model relocated + renamed to crates/adapter/chronica-adapter-policy-control-room-read-model. Confirmed 100% real Postgres mechanics via the crate's own module doc (\"Reads only; NEVER money, NEVER mutation\" over a single whole-module #[cfg(feature = \"db\")] gate), no pure vocabulary to extract, zero logic change (git mv, byte-identical src file). Its two chronica-cap-policy-* dependencies (approval-repo, decision-ledger) stay core-tier for now -- adapter -> core is always allowed, so the relocation needed no change to either. TWO new disclosed edges (this crate had 2 real cap-tier callers, unlike the single-caller relocations earlier this session): chronica-cap-deals-federation-routes (cap) -> the new adapter crate (that caller already carried 12 other real adapter-tier deps -- established precedent from the esign-contract-signature-store relocation earlier this session) and chronica-cap-org-workflow-routes (cap) -> the new adapter crate (1 prior adapter-tier dep, smaller but non-zero precedent). chronica-adapter-api-server carried a confirmed-unused NON-optional dependency (zero real src/ or tests/ usage) -- removed outright. A stale tenancy_filter_lint allow-list path (5 entries, wrong crates/cap/ prefix predating this move) corrected to the real physical path. layer_debt_edge_count 145 -> 147. See tools/ci/responsibility-baseline.json's paired note.",
  "_note_2026_09_16_decision_ledger_relocation": "RESPONSIBILITY-TIER RELOCATION: crates/core/chronica-cap-policy-decision-ledger relocated + renamed to crates/adapter/chronica-adapter-policy-decision-ledger. Confirmed 100% real Postgres mechanics via the crate's own doc (\"ROUTES AND RECORDS ONLY\" over a single whole-module db-gate), no pure vocabulary to extract, zero logic change. Its chronica-cap-policy-approval-repo dependency stays core-tier -- adapter -> core is always allowed, no change needed there. ONE new disclosed edge: chronica-cap-org-workflow-routes (cap) -> the new adapter crate (already had 1 prior adapter-tier dep from the control-room-read-model relocation earlier today). No new edge from chronica-cap-policy-posture-evidence (core-tier): its own use is a [dev-dependencies]-only, test-fixture-seeding edge (already called out in that crate's own Cargo.toml comment as \"a sanctioned test-only edge\" before this relocation), confirmed compiling clean via cargo check --workspace --features db --tests. Also real, already-adapter-tier: chronica-adapter-policy-control-room-read-model (its own dependency repointed, adapter -> adapter, no new edge). chronica-adapter-api-server carried a confirmed-unused NON-optional dependency -- removed outright. A stale tenancy_filter_lint allow-list path (3 entries, wrong crates/cap/ prefix) corrected. layer_debt_edge_count 147 -> 148. See tools/ci/responsibility-baseline.json's paired note.",
  "_note_2026_09_16_posture_evidence_split": "CLASS-B SPLIT with a DISCLOSED core -> adapter edge (unusual for this session -- every prior relocation/split's real callers were cap-tier; this is the first core-tier one). crates/core/chronica-cap-policy-posture-evidence mixed a pure PostureEvidenceCounts vocabulary (deliberately unconditional, no db feature -- F8) with a real Postgres repo (PostureEvidenceRepo, one read-only aggregate SELECT over decision_ledger) behind a #[cfg(feature = \"db\")] gate -- confirmed via full source read. Split into crates/core/chronica-core-policy-posture-evidence (the pure struct, no .chronica descriptor of its own, matching the chronica-core-helpdesk-kb precedent) and crates/adapter/chronica-adapter-policy-posture-evidence (the real repo, re-gated the same #[cfg(feature = \"db\")] mod imp discipline used throughout this session). Zero logic change to either half. THE EDGE: chronica-core-api-state's own posture_floor.rs module already constructed PostureEvidenceRepo directly and called .counts() -- a REAL, PRE-EXISTING Postgres dependency, itself already gated behind chronica-core-api-state's OWN #[cfg(feature = \"db\")] (confirmed by source read: that crate stays pure by default; this I/O only compiles when its own db feature is active). Before this split the edge was core -> core (both crates lived under crates/core/) and invisible to the layer gate; splitting posture-evidence makes an ALREADY-REAL dependency visible for the first time -- it does not create a new one. This is consistent with the ALREADY-disclosed precedent: chronica-core-api-state already carries 4 other real core -> adapter edges in this baseline (chronica-adapter-api-gateway-server, chronica-adapter-outbound-fetch, chronica-adapter-projection-read-model, chronica-adapter-projection-replay-state) predating this session entirely -- this is the established, accepted shape for that specific crate, not a new architectural category. chronica-core-strategy-posture-gate (core-tier) needed only the pure sibling crate -- it uses PostureEvidenceCounts alone, never the repo, confirmed via source read; no edge from it. chronica-adapter-api-server carried a confirmed-unused NON-optional dependency -- removed outright. A stale tenancy_filter_lint allow-list path (3 entries, wrong crates/cap/ prefix) corrected. NOT addressed by this commit (flagged for a future pass): chronica-core-api-state's own posture_floor.rs doing real, optional/db-gated Postgres I/O is itself a Class-B-shaped crate -- its own future decomposition is out of scope here given the crate's much larger size and blast radius. layer_debt_edge_count 148 -> 149. See tools/ci/responsibility-baseline.json's paired note.",
  "_note_2026_09_16_analytics_session_recording_split": "CLASS-B SPLIT of chronica-cap-analytics-session-recording (misplaced-cap-package cleanup): the pure rrweb core stayed in place, renamed to chronica-core-analytics-session-recording-rrweb (core tier). The impure store/http halves moved to a new chronica-adapter-analytics-session-recording crate (adapter tier). The real caller chronica-cap-platform-admin-routes's session_recording.rs already depended on the old crate for both halves; it now depends on the adapter crate directly for store/http, which is a cap->adapter edge FORBIDDEN_TARGETS disallows (cap tier may not depend on adapter tier). This is the same pattern as every other same-day Class-B split this session (e.g. agent-skills-package): the pre-existing caller dependency on the old flat crate becomes, post-split, a caller dependency on the new adapter crate, since the caller genuinely needs the impure halves (store/http), not just the pure core. No behavior change -- the caller's own request-orchestration logic is unchanged, only the crate boundary moved. M7 (forbidden_core_io_count) drops by one since the real Postgres I/O is no longer misclassified as core-tier; this edge is the disclosed, accepted cost of that improvement.",
  "_note_2026_09_16_browser_commerce_relocation": "MISPLACED-CAP-PACKAGE CLEANUP: chronica-cap-browser-commerce (BrowserCommerceRun jsonb persistence store -- money-adjacent, not a money primitive; 100% impure, no db-free code anywhere in the module) relocated wholesale from crates/core/ to crates/adapter/, renamed to chronica-adapter-browser-commerce. Its real caller, chronica-cap-company-money-actions (companies::browser_commerce_run's create/approve/arm lifecycle routes and companies::browser_commerce_exec's Sub-2a money-commit executor), already depended on the old flat crate for real; that pre-existing dependency becomes a cap->adapter edge FORBIDDEN_TARGETS disallows (cap tier may not depend on adapter tier), same pattern as every other same-day relocation/split this session. No behavior change -- only the crate boundary moved. M2 (misplaced_cap_package_count) drops by one since the crate is no longer named chronica-cap-*; this edge is the disclosed, accepted cost of that improvement.",
  "_note_2026_09_16_browser_webapp_relocation": "MISPLACED-CAP-PACKAGE CLEANUP: chronica-cap-browser-webapp (WebAppDefinition + invocation-receipt jsonb persistence store -- money-free; 100% impure, no db-free code anywhere in the module) relocated wholesale from crates/core/ to crates/adapter/, renamed to chronica-adapter-browser-webapp. Its real caller, chronica-cap-company-platform-ops's webapp_routes, already depended on the old flat crate for real; that pre-existing dependency becomes a cap->adapter edge FORBIDDEN_TARGETS disallows (cap tier may not depend on adapter tier), same pattern as every other same-day relocation/split this session (see the matching chronica-cap-company-money-actions -> chronica-adapter-browser-commerce note directly above). No behavior change -- only the crate boundary moved. M2 (misplaced_cap_package_count) drops by one since the crate is no longer named chronica-cap-*; this edge is the disclosed, accepted cost of that improvement.",
  "_note_2026_09_16_commerce_amazon_observation_relocation": "MISPLACED-CAP-PACKAGE CLEANUP: chronica-cap-commerce-amazon-observation (append-only Amazon marketplace observation-history persistence store; money-free) relocated wholesale from crates/core/ to crates/adapter/, renamed to chronica-adapter-commerce-amazon-observation. Its real caller, chronica-cap-company-commerce-storefront's commerce_services_routes, already depended on the old flat crate for real; that pre-existing dependency becomes a cap->adapter edge FORBIDDEN_TARGETS disallows (cap tier may not depend on adapter tier), same pattern as every other same-day relocation/split this session. No behavior change -- only the crate boundary moved. M2 (misplaced_cap_package_count) drops by one since the crate is no longer named chronica-cap-*; this edge is the disclosed, accepted cost of that improvement.",
  "_note_2026_09_16_commerce_cart_relocation": "MISPLACED-CAP-PACKAGE CLEANUP: chronica-cap-commerce-cart (cart CRUD + compare-and-swap concurrency persistence store; money-free) relocated wholesale from crates/core/ to crates/adapter/, renamed to chronica-adapter-commerce-cart. Its real callers, chronica-cap-company-commerce-storefront's commerce_cart_routes and commerce_store_routes, already depended on the old flat crate for real; that pre-existing dependency becomes a cap->adapter edge FORBIDDEN_TARGETS disallows (cap tier may not depend on adapter tier), same pattern as every other same-day relocation/split this session (see the three matching notes directly above for chronica-adapter-browser-commerce/-browser-webapp/-commerce-amazon-observation, all same real caller family). No behavior change -- only the crate boundary moved. M2 (misplaced_cap_package_count) drops by one since the crate is no longer named chronica-cap-*; this edge is the disclosed, accepted cost of that improvement.",
  "_note_2026_09_16_commerce_kiosk_relocation": "MISPLACED-CAP-PACKAGE CLEANUP: chronica-cap-commerce-kiosk (kiosk session issuance + atomic claim-before-commit order lifecycle persistence store; money-free) relocated wholesale from crates/core/ to crates/adapter/, renamed to chronica-adapter-commerce-kiosk. Its real caller, chronica-cap-company-commerce-storefront's commerce_kiosk_routes, already depended on the old flat crate for real; that pre-existing dependency becomes a cap->adapter edge FORBIDDEN_TARGETS disallows (cap tier may not depend on adapter tier), same pattern as every other same-day relocation/split this session (fourth relocation this cycle onto the same real caller family -- see the three matching notes directly above for chronica-adapter-browser-commerce/-browser-webapp/-commerce-amazon-observation/-commerce-cart). No behavior change -- only the crate boundary moved. M2 (misplaced_cap_package_count) drops by one since the crate is no longer named chronica-cap-*; this edge is the disclosed, accepted cost of that improvement.",
  "_note_2026_09_16_company_business_map_relocation": "MISPLACED-CAP-PACKAGE CLEANUP: chronica-cap-company-business-map (COMPANY-OS business-map persistence adapter; money-free; every item lives inside a single #[cfg(feature = \"db\")] mod imp { ... } wrapper, re-exported only under the same db gate -- public surface literally empty with db off) relocated wholesale from crates/core/ to crates/adapter/, renamed to chronica-adapter-company-business-map. Its real caller, chronica-cap-org-workflow-routes's business_map routes, already depended on the old flat crate for real; that pre-existing dependency becomes a cap->adapter edge FORBIDDEN_TARGETS disallows (cap tier may not depend on adapter tier), same pattern as every other same-day relocation/split this session. No behavior change -- only the crate boundary moved. M2 (misplaced_cap_package_count) drops by one since the crate is no longer named chronica-cap-*; this edge is the disclosed, accepted cost of that improvement.",
  "_note_2026_09_16_company_invitations_split": "CLASS-B SPLIT of chronica-cap-company-invitations (misplaced-cap-package cleanup): the pure in-memory invitations lifecycle (InvitationLedger: invite/resend/revoke/accept, plus identifier/email validation) stayed in place, renamed to chronica-core-company-invitations (core tier). The impure halves -- store (tenant-scoped Postgres persistence over company_invitations), membership (the shared_record_membership many-to-many table), and db_accept (DB-backed acceptance wiring store+membership together) -- moved to a new chronica-adapter-company-invitations crate (adapter tier). The real caller chronica-cap-org-workflow-routes's organization_invitations.rs already depended on the old flat crate for ALL of these (both pure error/bound types AND the impure store/membership/db_accept modules); post-split it depends on BOTH new crates -- the cap->core edge onto chronica-core-company-invitations is always allowed, but the cap->adapter edge onto chronica-adapter-company-invitations is FORBIDDEN_TARGETS-disallowed (cap tier may not depend on adapter tier), same pattern as every other same-day Class-B split this session (e.g. the analytics-session-recording split earlier in this cycle). No behavior change -- only the crate boundary moved. M7 (forbidden_core_io_count) drops by one since the real Postgres I/O is no longer misclassified as core-tier; this edge is the disclosed, accepted cost of that improvement.",
  "_note_2026_09_16_commerce_site_intelligence_relocation": "MISPLACED-CAP-PACKAGE CLEANUP: chronica-cap-crm-support-commerce-site-intelligence (site-intelligence persistence adapter, read-only over business_event + commerce_amazon_observation_history; money-free; every item lives inside a single #[cfg(feature = \"db\")] mod imp { ... } wrapper -- public surface literally empty with db off) relocated wholesale from crates/core/ to crates/adapter/, renamed to chronica-adapter-crm-support-commerce-site-intelligence. Its real caller, chronica-cap-platform-misc-routes's analytics/commerce.rs, already depended on the old flat crate for real; that pre-existing dependency becomes a cap->adapter edge FORBIDDEN_TARGETS disallows (cap tier may not depend on adapter tier), same pattern as every other same-day relocation/split this session. No behavior change -- only the crate boundary moved. M2 (misplaced_cap_package_count) drops by one since the crate is no longer named chronica-cap-*; this edge is the disclosed, accepted cost of that improvement.",
  "_note_2026_09_16_contact_csv_import_relocation": "STALE DEBT EDGE REMOVED (self-liquidated): chronica-cap-crm-support-contact-csv-import (core) -> chronica-adapter-crm-support-contacts (adapter), disclosed 2026-09-08 (LOOP 58, key _note_2026_09_08_loop58) as honest, self-liquidating debt -- that note explicitly named this crate as 'itself an open M7 forbidden_core_io_count hit... queued for the same responsibility-tier treatment, at which point the edge becomes adapter -> adapter and disappears'. This change relocates chronica-cap-crm-support-contact-csv-import wholesale from crates/core/ to crates/adapter/ (renamed chronica-adapter-crm-support-contact-csv-import), fulfilling that queued treatment: the edge onto chronica-adapter-crm-support-contacts is now adapter -> adapter, no longer forbidden, and is removed here per the established STALE DEBT EDGE protocol (repaired edges are removed, not renamed).",
  "_note_2026_09_16_cross_holding_store_relocation": "MISPLACED-CAP-PACKAGE CLEANUP: wholesale relocation of chronica-cap-cross-holding-store (100% impure DB-backed persistence adapter, migration 0165, over chronica-core-cross-holding-domain's pure SharedProjectAgreement/SharedRoomBridge types -- no #[cfg(feature=\"db\")] gating anywhere and no [features] section at all, sqlx is a plain unconditional dependency, so this is a plain wholesale relocation, not a Class-B split) from crates/core/ to crates/adapter/chronica-adapter-cross-holding-store. The real (only) caller is chronica-cap-deals-federation-routes's src/cross_holding.rs (cap-tier), so repointing its use/Cargo.toml/db-feature-array dependency onto the now-adapter-tier crate creates exactly the one new edge captured verbatim above from validate-refoundation-layers.mjs's own NEW FORBIDDEN EDGE output. Same established shape as every other wholesale relocation this window (chronica-adapter-commerce-cart, chronica-adapter-crm-support-commerce-site-intelligence, etc).",
  "_note_2026_09_16_erp_fulfillment_relocation": "MISPLACED-CAP-PACKAGE CLEANUP: wholesale relocation of chronica-cap-erp-fulfillment (100% impure ERP fulfillment lifecycle persistence adapter over erp_fulfillments / erp_fulfillment_items, migration 0316 -- no #[cfg(feature=\"db\")] gating anywhere and no [features] section at all, sqlx is a plain unconditional dependency, so this is a plain wholesale relocation, not a Class-B split) from crates/core/ to crates/adapter/chronica-adapter-erp-fulfillment. The real (only) caller is chronica-cap-erp-commerce-ops-routes's src/erp_fulfillment.rs (cap-tier), so repointing its use/Cargo.toml dependency onto the now-adapter-tier crate creates exactly the one new edge captured verbatim above from validate-refoundation-layers.mjs's own NEW FORBIDDEN EDGE output. Same established shape as every other wholesale relocation this window.",
  "_note_2026_09_16_inventory_ledger_relocation": "MISPLACED-CAP-PACKAGE CLEANUP: wholesale relocation of chronica-cap-inventory-ledger (100% impure inventory stock-ledger persistence adapter -- no #[cfg(feature=\"db\")] gating anywhere and no [features] section at all, sqlx is a plain unconditional dependency, so this is a plain wholesale relocation, not a Class-B split) from crates/core/ to crates/adapter/chronica-adapter-inventory-ledger. The real (only) caller is chronica-cap-erp-commerce-ops-routes's src/inventory.rs (cap-tier), so repointing its use/Cargo.toml dependency onto the now-adapter-tier crate creates exactly the one new edge captured verbatim above from validate-refoundation-layers.mjs's own NEW FORBIDDEN EDGE output. The crate's pre-existing one-way dependency onto chronica-cap-inventory-item (still core-tier) is adapter->core, permitted by FORBIDDEN_TARGETS -- no additional edge. Same established shape as every other wholesale relocation this window.",
  "_note_2026_09_16_runtime_sandbox_relocation": "CRATES/CAP EXTINCTION: wholesale relocation of chronica-cap-runtime-sandbox (self-described RUNTIME_BOUND maturity; 0-change move lifted verbatim from the former flat chronica-runtime crate -- worker-owned file sandbox primitives, real std::fs I/O, no DB, no network) from crates/cap/ to crates/runtime/chronica-runtime-sandbox. This is the first of the ~11 self-described RUNTIME_BOUND crates/cap/* packages being relocated as part of the crates/cap extinction wave. Four real callers repointed: three cap-tier route crates (chronica-cap-platform-misc-routes, chronica-cap-runtime-ops-routes, chronica-cap-runtime-engine-preview -- each captured verbatim above from validate-refoundation-layers.mjs's own NEW FORBIDDEN EDGE output) and one core-tier crate (chronica-core-api-state), whose PRE-EXISTING disclosed core->cap edge onto this crate was renamed in place to core->runtime (same underlying dependency, new target tier) rather than left as a stale duplicate -- see the forbidden_edges entry 'chronica-core-api-state (core) -> chronica-runtime-sandbox (runtime)', which replaces the former '... -> chronica-cap-runtime-sandbox (cap)' entry.",
  "_note_2026_09_16_runtime_host_io_relocation": "CRATES/CAP EXTINCTION: wholesale relocation of chronica-cap-runtime-host-io (self-described RUNTIME_BOUND maturity; 0-change move lifted verbatim from the former flat chronica-runtime crate -- ProcessRegistry/PidFile/StorageProvisioner/exporter_queue, real OS process/filesystem I/O, no DB, no network) from crates/cap/ to crates/runtime/chronica-runtime-host-io. The real (only) caller is chronica-cap-runtime-ops-routes's runtime_setup.rs/runtime_exporter_queue.rs (cap-tier), so repointing its use/Cargo.toml dependency onto the now-runtime-tier crate creates exactly the one new edge captured verbatim above from validate-refoundation-layers.mjs's own NEW FORBIDDEN EDGE output. Second of the ~11 self-described RUNTIME_BOUND crates/cap/* packages being relocated as part of the crates/cap extinction wave.",
  "_note_2026_09_16_network_privacy_scheduler_relocation": "CRATES/CAP EXTINCTION: wholesale relocation of chronica-cap-network-privacy-scheduler (self-described RUNTIME_BOUND maturity; 0-change move -- hour-aligned rollup dispatcher over network_knowledge_audit/network_knowledge_aggregate, real Postgres advisory-lock coordination, no money primitive per its own money_guard.rs) from crates/cap/ to crates/runtime/chronica-runtime-network-privacy-scheduler. ZERO new disclosed edges: both real production callers (chronica-runtime-api-background-dispatch's background.rs, the spawn site; a dev-dependency in chronica-adapter-api-server's tests/network_rollup_integration.rs) were already runtime-tier or a dev-only edge before this move, confirmed via `node tools/refoundation/validate-refoundation-layers.mjs` reporting no new edges after the relocation. Third of the ~11 self-described RUNTIME_BOUND crates/cap/* packages being relocated as part of the crates/cap extinction wave.",
  "_note_2026_09_16_workforce_dag_runtime_relocation": "CRATES/CAP EXTINCTION: wholesale relocation of chronica-cap-workforce-dag-runtime (self-described RUNTIME_BOUND maturity; 0-change move -- async DAG executor plus its two live-IO NodeRunner edges, ComfyUI image generation over a real SSRF-guarded reqwest sink and provider-gateway-admitted LLM chat completion; money-free per its own tests/money_guard.rs) from crates/cap/ to crates/runtime/chronica-runtime-workforce-dag-runtime. Fourth of the ~11 self-described RUNTIME_BOUND crates/cap/* packages being relocated as part of the crates/cap extinction wave. Net +2 edges (168 -> 170): the crate's pre-existing PRE-EXISTING disclosed edges were repaired/renamed rather than left stale -- 'chronica-cap-workforce-dag-runtime (cap) -> chronica-adapter-workforce-llm-chat (adapter)' is now runtime -> adapter (not a FORBIDDEN_TARGETS pair; removed per the established STALE DEBT EDGE protocol) and 'chronica-core-cli-misc (core) -> chronica-cap-workforce-dag-runtime (cap)' was renamed in place to '... -> chronica-runtime-workforce-dag-runtime (runtime)' (core -> runtime remains forbidden, same shape as the chronica-core-api-state -> chronica-runtime-sandbox rename earlier in this wave). THREE new disclosed edges for the crate's real cap-tier callers, none of which moved: chronica-cap-company-platform-ops (workforce.rs, delegation/mod.rs), chronica-cap-workforce-orchestration (routing_tree.rs/capability_registry.rs/evaluation.rs/assignment.rs/worker_memory.rs plus tests), and chronica-cap-workforce-delegation (delegation.rs) each now hold a cap -> runtime edge to the renamed crate, captured verbatim above from validate-refoundation-layers.mjs's own NEW FORBIDDEN EDGE output. chronica-adapter-api-server carried a confirmed-unused optional dependency (zero real src/ usage; only reached indirectly via chronica-cap-workforce-orchestration's re-export) -- removed outright, along with its dead db-feature-array entry. The crate's own CAPABILITY.md/.chronica descriptor claimed a nonexistent 'crates/chronica-cli' caller -- corrected to the real caller, crates/core/chronica-core-cli-misc/src/comfyui_dispatch_eval.rs (confirmed via `find crates -maxdepth 2 -iname \"*cli*\"` returning no plain chronica-cli crate).",
  "_note_2026_09_16_workforce_delegation_relocation": "CRATES/CAP EXTINCTION: wholesale relocation of chronica-cap-workforce-delegation (self-described RUNTIME_BOUND maturity; 0-change move -- bounded subagent-delegation DAG-node model, depth/fan-out/deadline checked, optional Postgres persistence adapter under its own db feature via chronica-core's PgPersistence; money-free by construction, a closed DelegatedCapability enum with no money-authorizing variant, per its own tests/money_guard.rs) from crates/cap/ to crates/runtime/chronica-runtime-workforce-delegation. Fifth of the ~11 self-described RUNTIME_BOUND crates/cap/* packages being relocated as part of the crates/cap extinction wave, and the second of two in the same dependency cluster (this crate's own dependency on chronica-runtime-workforce-dag-runtime was relocated in the immediately preceding commit). Net edge count UNCHANGED (170 -> 170): this relocation REPAIRED the PRE-EXISTING disclosed edge 'chronica-cap-workforce-delegation (cap) -> chronica-runtime-workforce-dag-runtime (runtime)' by removing it entirely per the established STALE DEBT EDGE protocol -- both endpoints are now runtime-tier, no longer a FORBIDDEN_TARGETS pair -- while adding exactly ONE new disclosed edge for the crate's real (only) cap-tier caller, chronica-cap-company-platform-ops (src/delegation/mod.rs and src/delegation/execute.rs), which now holds 'chronica-cap-company-platform-ops (cap) -> chronica-runtime-workforce-delegation (runtime)', captured verbatim from validate-refoundation-layers.mjs's own NEW FORBIDDEN EDGE output. chronica-adapter-api-server carried a confirmed-unused optional dependency (zero real src/ or tests/ usage, confirmed via grep across both) -- removed outright, along with its dead db-feature-array entry. The crate's own CAPABILITY.md/.chronica descriptor claimed a nonexistent 'crates/chronica-api' caller and stale exposes_to route paths (missing the /workforce/ segment and the /review endpoint) -- corrected to the real caller (crates/cap/chronica-cap-company-platform-ops/src/delegation/{mod,execute}.rs) and the real route table in crates/adapter/chronica-adapter-api-server/src/router/company_execution_routes.rs. One stale tenancy_filter_lint allow-list path (crates/core/chronica-core-persistence/tests/tenancy_filter_lint/allow_list_chunk_01.rs, wrong crates/cap/ prefix predating this move) corrected to the real physical path.",
  "_note_2026_09_16_workforce_orchestration_relocation": "CRATES/CAP EXTINCTION: wholesale relocation of chronica-cap-workforce-orchestration (self-described RUNTIME_BOUND maturity; 0-change move -- the AI-workforce capability registry, routing tree, assignment engine, and evaluation engine, matching a task's RequirementsVector to workers by skill-vector cosine similarity and learning live via EMA; optional Postgres persistence adapter for the registry/worker-memory under its own db feature via chronica-core's PgPersistence; money-free by construction, no chronica-policy dependency and no money primitive, per its own tests/money_guard.rs) from crates/cap/ to crates/runtime/chronica-runtime-workforce-orchestration. Sixth of the ~11 self-described RUNTIME_BOUND crates/cap/* packages being relocated as part of the crates/cap extinction wave, and the third of three in the same dependency cluster (its own dependency on chronica-runtime-workforce-dag-runtime was relocated two commits prior; the sibling chronica-runtime-workforce-delegation crate references this crate only in doc comments, never a real Cargo dependency -- confirmed via source read, no cycle). Net edge count UNCHANGED (170 -> 170): this relocation REPAIRED the PRE-EXISTING disclosed edge 'chronica-cap-workforce-orchestration (cap) -> chronica-runtime-workforce-dag-runtime (runtime)' by removing it entirely per the established STALE DEBT EDGE protocol -- both endpoints are now runtime-tier, no longer a FORBIDDEN_TARGETS pair -- while adding exactly ONE new disclosed edge for the crate's real (only) cap-tier caller, chronica-cap-company-platform-ops (src/workforce.rs, the read-only assignment-recommendation route), which now holds 'chronica-cap-company-platform-ops (cap) -> chronica-runtime-workforce-orchestration (runtime)', captured verbatim from validate-refoundation-layers.mjs's own NEW FORBIDDEN EDGE output. chronica-adapter-api-server carried a confirmed-unused optional dependency (zero real src/ or tests/ usage, confirmed via grep across both) -- removed outright, along with its dead db-feature-array entry. The crate's own CAPABILITY.md/.chronica descriptor claimed a nonexistent 'crates/chronica-api' caller -- corrected to the real caller, crates/cap/chronica-cap-company-platform-ops/src/workforce.rs (the real route path POST /api/companies/:company_id/workforce/assign was independently re-verified against crates/adapter/chronica-adapter-api-server/src/router/company_execution_routes.rs and found accurate, no change needed there). GENUINE BUG FOUND AND FIXED (not caused by this session's edits): the crate's own tests/money_guard.rs used naive prefix/substring matching to forbid a dependency on the legacy pre-refoundation monolith crate literally named `chronica-runtime` (confirmed gone from the repo entirely, via `find crates -maxdepth 2 -type d -name chronica-runtime` returning nothing) -- once this crate and its real dependency chronica-runtime-workforce-dag-runtime were renamed under this session's unrelated tier-naming convention, both false-positive-panicked (`l.trim_start().starts_with(\"chronica-runtime\")` matched the manifest's own package name and its dag-runtime dependency line; `content.contains(\"chronica_runtime\")` matched a `use chronica_runtime_workforce_dag_runtime::` import). Fixed by adding a word-boundary-aware `contains_as_word` matcher (byte immediately before/after a match must not be an identifier-continuation character) to both checks -- an exact `chronica-runtime = {...}` dependency or bare `chronica_runtime::` import would still be caught; the new tier-prefixed sibling crates no longer spuriously trip it. Re-ran the crate's full test suite after the fix: 44/44 pass (17 unit + 2 money_guard + 15 workforce + 1 comfyui_routing + 6 workforce_dag + 3 provider_gateway, run single-threaded with --include-ignored against the local Postgres instance).",
  "_note_2026_09_16_hard_refoundation_backend_reset": "forbidden_edges cleared 170 -> 0: the HARD REFOUNDATION backend reset deleted the entire crates/ tree (crates/cap, crates/core, crates/runtime, crates/adapter, crates/infra -- 1,315 crates total) and replaced it with a minimal core/runtime/adapter workspace at the repository root. Every one of the 170 disclosed edges above named at least one crate under the retired crates/ layout -- validate-refoundation-layers.mjs's own STALE DEBT EDGE output confirmed all 170 as stale, not a handful. This is a wholesale repair via backend deletion, not 170 individual crate-by-crate relocations; the historical entries above remain as the record of how each edge was originally created and (in the many cases already repaired pre-reset) resolved. See docs/decisions/0016-legacy-backend-retired-to-git-history.md for recovery instructions and the full pre-reset inventory. forbidden_edges = [] is the enforced floor going forward: any new core/runtime/adapter/organism code that reintroduces a FORBIDDEN_TARGETS violation must disclose it here again, from zero, not by resurrecting any of the entries above."
}
