[package]
name = "ops_production"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

# Atlas production support kernels. This is not a Chronica organism layer; it supports operating,
# verifying and evolving the Atlas system itself.

[dependencies]
serde.workspace = true
