{
  "description": "IRIS stage0 bootstrap seed — frozen Rust binary that loads JSON-compiled IRIS programs",
  "binary": "bootstrap/iris-stage0",
  "build": "cargo build --release --features syntax --bin iris-stage0 && cp target/release/iris-stage0 bootstrap/",
  "build_date": "2026-03-30",
  "target_triple": "x86_64-unknown-linux-gnu",
  "artifacts": [
    "bootstrap/tokenizer.json",
    "bootstrap/parser.json",
    "bootstrap/lowerer.json",
    "bootstrap/interpreter.json"
  ]
}
