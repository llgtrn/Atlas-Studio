-- Greatest Common Divisor (GCD)
-- Euclid's algorithm: gcd(a, b) = gcd(b, a mod b), stopping when b = 0.
-- O(log min(a,b)) steps. [cost: Unknown] because step count depends on inputs.
let rec gcd a b : Int -> Int -> Int [cost: Unknown] =
  if b == 0 then a
  else gcd b (a % b)
