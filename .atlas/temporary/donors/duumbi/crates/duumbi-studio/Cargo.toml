[package]
name = "duumbi-studio"
version = "0.4.1-preview"
edition = "2024"
license = "MPL-2.0"

[dependencies]
duumbi = { path = "../.." }
leptos = "0.8.20"
leptos_axum = { version = "0.8.10", optional = true }
leptos_router = "0.8.15"
leptos_meta = "0.8.6"
leptos_config = "0.8.10"
axum = { version = "0.8.9", optional = true }
tokio = { version = "1.53.1", features = ["full"], optional = true }
tower-http = { version = "0.7.1", features = ["cors", "fs"], optional = true }
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
serde_yaml = { version = "0.9.34", optional = true }
toml = { version = "1.1.6", optional = true }
petgraph = { version = "0.8.3", optional = true }
chrono = { version = "0.4.45", optional = true }
anyhow = "1.0.104"
tracing = "0.1.44"
tracing-subscriber = { version = "0.3.23", features = ["env-filter"], optional = true }
web-sys = { version = "0.3.105", features = ["WheelEvent", "MouseEvent", "HtmlInputElement", "EventTarget"] }
wasm-bindgen = "0.2.128"
console_error_panic_hook = "0.1.7"

[[bin]]
name = "studio"
path = "src/bin/studio.rs"
required-features = ["ssr"]

[features]
default = []
ssr = ["leptos/ssr", "leptos_axum", "axum", "tokio", "tower-http", "serde_yaml", "toml", "petgraph", "tracing-subscriber", "chrono"]
hydrate = ["leptos/hydrate"]
