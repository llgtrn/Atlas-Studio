name: Stage 3b - Inbox Enrichment

on:
  schedule:
    - cron: '17 * * * *'
  repository_dispatch:
    types: [duumbi-inbox-captured]
  workflow_dispatch:
    inputs:
      target_path:
        description: Optional Inbox note path; empty means bounded scheduled sweep
        required: false
        type: string
        default: ''
      scan_limit:
        description: Maximum Inbox notes to inspect while looking for one candidate
        required: false
        type: number
        default: 200

permissions:
  contents: read

concurrency:
  group: duumbi-vault-intake
  cancel-in-progress: false

jobs:
  dispatch:
    name: Enrich up to five Inbox notes
    runs-on: ubuntu-latest
    steps:
      - name: Check out source repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false

      - name: Checkout DUUMBI vault
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        continue-on-error: true
        with:
          repository: ${{ github.repository_owner }}/duumbi-vault
          path: duumbi-vault
          token: ${{ secrets.GH_PROJECT_PAT || github.token }}
          persist-credentials: true

      - name: Enrich Inbox note
        id: enrich
        uses: actions/github-script@v9
        env:
          GH_PROJECT_PAT: ${{ secrets.GH_PROJECT_PAT }}
          DUUMBI_INBOX_OWNER: ${{ vars.DUUMBI_INBOX_OWNER || github.repository_owner }}
          DUUMBI_INBOX_OWNER_SLACK_ID: ${{ vars.DUUMBI_INBOX_OWNER_SLACK_ID }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          DEEPSEEK_MODEL: ${{ vars.DEEPSEEK_MODEL || 'deepseek-v4-pro' }}
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          SLACK_REVIEW_CHANNEL_ID: ${{ secrets.SLACK_REVIEW_CHANNEL_ID }}
          DUUMBI_AGENT_DISPATCH_CHANNEL_ID: ${{ secrets.DUUMBI_AGENT_DISPATCH_CHANNEL_ID }}
          DUUMBI_METRICS_PATH: duumbi-workflow-metrics.json
        with:
          script: |
            const enrichment = await import(`${process.env.GITHUB_WORKSPACE}/scripts/github-actions/inbox-enrichment-dispatch.mjs`);
            await enrichment.runInboxEnrichmentBatch({
              env: process.env,
              context,
              core,
              summary: core.summary,
              fetchImpl: fetch,
              workspace: process.env.GITHUB_WORKSPACE,
            });

      - name: Upload DUUMBI workflow metrics
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          name: duumbi-workflow-metrics-inbox-enrichment-${{ github.run_id }}-${{ github.run_attempt }}
          path: duumbi-workflow-metrics.json
          if-no-files-found: warn
