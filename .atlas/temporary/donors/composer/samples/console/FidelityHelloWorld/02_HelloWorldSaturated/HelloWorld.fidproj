# HelloWorldSaturated - FidelityHelloWorld Sample 02
# Pattern: Saturated function calls (all args provided at once)

[package]
name = "HelloWorldSaturated"
version = "0.1.0"
description = "HelloWorld with saturated function calls"
authors = ["SpeakEZ Technologies, Inc."]
license = "MIT"

[compilation]
target = "cpu"

[dependencies]
# Types and operations are CCS intrinsics - no external library needed
# Arena<'lifetime> is now an CCS intrinsic
platform = { path = "/home/hhh/repos/Fidelity.Platform/Profiles/Linux_x86_64_Default/Fidelity.Platform.fidproj" }

[build]
sources = [
    "HelloWorld.fs"
]
output = "helloworld"
output_kind = "console"  # Requires libc for string operations (memcpy)
