# Sequence application forms

This source oracle covers direct and pipeline application, stored partial
operations, bare operation aliases and callback aliases retaining an immutable
capture. It preserves both `fold` partial frontiers, independent accumulator and
payload dimensions, formation effects, fresh enumeration and predicate stopping.
The eleven operations in the C-07 contract appear without extrema or collection
materialization extensions.

Native exits 211–217 locate the seven semantic groups. Fresh compilation, stock
MLIR verification, native exit zero and exact `ExpectedOutput.txt` are required.
The fixture is an acceptance requirement; registration alone records no pass.
