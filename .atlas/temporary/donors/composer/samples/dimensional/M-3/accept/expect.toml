# Expected outcome for M-3/accept. Read by vet.sh; keys are one-line strings.
rule = "access is part of identity, no subtyping: a ReadWrite handle where ReadOnly is required passes only through Ptr.asReadOnly"
verdict = "accept"
code = ""
pending = "step 5"  # the hardening step (plan §5) that gates this row; judged only when vet.sh --through >= 5
