# Expected outcome for W-2/accept. Read by vet.sh; keys are one-line strings.
rule = "operands of -, *, /, % are numeric; operands of + are both numeric (D5), never bool; unary - and + on int and float"
verdict = "accept"
code = ""
pending = "step 3"  # the hardening step (plan §5) that gates this row; judged only when vet.sh --through >= 3
