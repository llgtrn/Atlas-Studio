# Expected outcome for UoM-1/reject. Read by vet.sh; keys are one-line strings.
rule = "addition and subtraction require equal dimensions"
verdict = "reject"
code = "CCS8040"
pending = "step 2"  # the hardening step (plan §5) that gates this row; judged only when vet.sh --through >= 2
