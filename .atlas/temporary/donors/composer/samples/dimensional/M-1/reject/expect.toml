# Expected outcome for M-1/reject. Read by vet.sh; keys are one-line strings.
rule = "no write through a ReadOnly handle"
verdict = "reject"
code = "CCS8020"
pending = "step 5"  # the hardening step (plan §5) that gates this row; judged only when vet.sh --through >= 5
