# Expected outcome for W-4/reject. Read by vet.sh; keys are one-line strings.
rule = "no silent default: an integer whose range is unobservable and that carries no seal from any source is a diagnostic"
verdict = "reject"
code = "CCS8011"
pending = "step 7"  # the hardening step (plan §5) that gates this row; judged only when vet.sh --through >= 7
