module Vet

[<Measure>] type m
[<Measure>] type s
[<Measure>] type kg
[<Measure>] type N = kg m / s^2

// A bounded range: the comparison bounds n on its branch, so the result is [0, 1000].
let f (n: int) = if n < 1000 then n + 1 else 0

[<EntryPoint>]
let main _ =
    let _ = f 5
    0
