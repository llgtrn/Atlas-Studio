# Expected outcome for M-5/accept. Read by vet.sh; keys are one-line strings.
rule = "every dimensional component is part of identity (D2): unit, region, access"
verdict = "accept"
code = ""
pending = "step 5"  # the hardening step (plan §5) that gates this row; judged only when vet.sh --through >= 5
