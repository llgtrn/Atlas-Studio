# Expected outcome for UoM-7/accept. Read by vet.sh; keys are one-line strings.
rule = "annotation and inference agree at calls"
verdict = "accept"
code = ""
pending = "step 2"  # the hardening step (plan §5) that gates this row; judged only when vet.sh --through >= 2
