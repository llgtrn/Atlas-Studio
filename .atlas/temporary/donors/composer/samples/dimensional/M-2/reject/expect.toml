# Expected outcome for M-2/reject. Read by vet.sh; keys are one-line strings.
rule = "no read through a WriteOnly handle"
verdict = "reject"
code = "CCS8021"
pending = "step 5"  # the hardening step (plan §5) that gates this row; judged only when vet.sh --through >= 5
