# Expected outcome for UoM-6/accept. Read by vet.sh; keys are one-line strings.
rule = "comparison requires equal dimensions"
verdict = "accept"
code = ""
pending = "step 2"  # the hardening step (plan §5) that gates this row; judged only when vet.sh --through >= 2
