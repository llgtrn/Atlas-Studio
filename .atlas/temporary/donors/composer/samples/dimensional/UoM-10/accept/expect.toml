# Expected outcome for UoM-10/accept. Read by vet.sh; keys are one-line strings.
rule = "a measure-polymorphic function is generalised and instantiated at two dimensions"
verdict = "accept"
code = ""
pending = "step 2"  # the hardening step (plan §5) that gates this row; judged only when vet.sh --through >= 2
scheme = "float<'u> -> float<'u>"  # the inferred scheme, one body (design note b.4)
