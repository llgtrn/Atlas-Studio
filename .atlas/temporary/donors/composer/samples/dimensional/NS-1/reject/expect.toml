# Expected outcome for NS-1/reject. Read by vet.sh; keys are one-line strings.
rule = "dimensioning seam (reals): an unbounded bare real flowing into a dimensioned context fires at the dimensioning boundary and names the bare source"
verdict = "reject"
code = ""
pending = "step 8"  # the hardening step (plan §5) that gates this row; judged only when vet.sh --through >= 8
family = "numeric-selection"  # the code is allocated with the D3 mapping table (plan NS rows); until then an empty code means: any error CCS code
