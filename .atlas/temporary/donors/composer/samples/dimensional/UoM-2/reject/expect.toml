# Expected outcome for UoM-2/reject. Read by vet.sh; keys are one-line strings.
rule = "multiplication adds exponent vectors"
verdict = "reject"
code = "CCS8040"
pending = "step 3"  # the hardening step (plan §5) that gates this row; judged only when vet.sh --through >= 3
