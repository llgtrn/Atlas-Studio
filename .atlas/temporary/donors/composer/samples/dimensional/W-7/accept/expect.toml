# Expected outcome for W-7/accept. Read by vet.sh; keys are one-line strings.
rule = "+ on strings concatenates, D5"
verdict = "accept"
code = ""
pending = "step 3"  # the hardening step (plan §5) that gates this row; judged only when vet.sh --through >= 3
