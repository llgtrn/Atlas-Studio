# Expected outcome for UoM-5/accept. Read by vet.sh; keys are one-line strings.
rule = "measures normalise as an abelian group"
verdict = "accept"
code = ""
pending = "step 2"  # the hardening step (plan §5) that gates this row; judged only when vet.sh --through >= 2
