{
  "objective": "P3 first-50 #21 rkyv: no internal pointers in the .atlas wire; the real extraction cache loads its JSON in ~0.35 s of a 7.3 s run, so the conditional rkyv dependency lapses; REFERENCE_ONLY; checkout physically extinct; census unchanged except the pinned deleted-donor list",
  "changed_paths": [
    "runtime/src/donor_storage.rs"
  ],
  "total_changes": [
    "+graph_edges",
    "+graph_nodes"
  ],
  "entity_changes": [
    "CHANGED runtime donor_storage/tests/repository_donor_storage_has_no_unrecorded_violation()."
  ]
}
