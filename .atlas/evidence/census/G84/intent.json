{
  "objective": "P3 first-50 #20 capnproto: the canonicalization checklist compared against the executing .atlas reader/writer; the one unstated item (unknown optional fields under a different root) becomes a SEALED-publication contract clause; REFERENCE_ONLY; checkout physically extinct; census unchanged except the pinned deleted-donor list",
  "changed_paths": [
    "runtime/src/donor_storage.rs"
  ],
  "total_changes": [
    "+graph_edges",
    "+graph_nodes"
  ],
  "entity_changes": [
    "CHANGED runtime donor_storage/tests/repository_donor_storage_has_no_unrecorded_violation()."
  ]
}
