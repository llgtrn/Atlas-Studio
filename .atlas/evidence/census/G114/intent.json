{
  "objective": "P3 first-50 #50 c2rust: bounded census; no C semantic frontend or migration pipeline exists, so translation and pointer-permission analysis have no consumer; REFERENCE_ONLY; checkout physically extinct (DC1 tests pass without it); census unchanged except the pinned deleted-donor list",
  "changed_paths": [
    "runtime/src/donor_storage.rs"
  ],
  "total_changes": [
    "+graph_edges",
    "+graph_nodes"
  ],
  "entity_changes": [
    "CHANGED runtime donor_storage/tests/repository_donor_storage_has_no_unrecorded_violation()."
  ]
}
