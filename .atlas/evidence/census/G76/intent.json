{
  "objective": "P3 first-50 #15 miri: 0 unsafe constructs in Atlas (the UB interpreter has no target) and nightly-only; shims model effects at libc level while the EFFECT gap is 504 effectful std calls (P0 input for G77); REFERENCE_ONLY; checkout physically extinct; census unchanged except the pinned deleted-donor list",
  "changed_paths": [
    "runtime/src/donor_storage.rs"
  ],
  "total_changes": [
    "+graph_edges",
    "+graph_nodes"
  ],
  "entity_changes": [
    "CHANGED runtime donor_storage/tests/repository_donor_storage_has_no_unrecorded_violation()."
  ]
}
