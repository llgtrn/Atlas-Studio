{
  "schema": "atlas.self-build.generation-evidence.v1",
  "wave": "CORRECTNESS -- close the open follow-up question from the STATE nested-field-write fabrication fix: do OWNERSHIP or DATA_FLOW share the same fallback-fabrication defect class?",
  "wave_title": "`state-multi-level-field-access-fabrication-fixed.json` explicitly recorded as not-yet-undertaken: 'whether OWNERSHIP (R4.9) or DATA_FLOW (R4.7) have any structurally similar fallback re-enters a sibling arm and fabricates a partial [fact]' the way STATE's own `self.a.b = x` handling once did (fabricating a spurious Read of `a` alone while never recording the real write). This generation answers that question directly by reading both walkers' own `Expr::Field` handling.",
  "priority_classification": "correctness / architectural integrity (priority 1) -- directly resolves a named open question about whether a real, previously-fixed defect class recurs elsewhere in the R4 extractor, rather than leaving it open indefinitely.",
  "method": "Read `adapter/src/semantic/rust/ownership.rs` and `adapter/src/semantic/rust/dataflow.rs`'s own `walk_expr` match arms for `syn::Expr::Field` directly, and DATA_FLOW's compound-assignment (`syn::Expr::Binary` with `is_compound_assign_op`) arm, which is the one place DATA_FLOW attempts place-specific identity extraction analogous to STATE's own field-write handling.",
  "key_finding": "Neither walker shares STATE's specific failure shape, for a structural reason: STATE's bug existed because STATE *attempts to extract a specific field's own name and read/write kind* for `self.field` expressions (`self_field_path`), and its fallback path for an unresolved nested chain re-entered that same field-identity logic, fabricating a partial claim. OWNERSHIP's and DATA_FLOW's `Expr::Field` arms (`adapter/src/semantic/rust/ownership.rs:218`, `adapter/src/semantic/rust/dataflow.rs:342`) are IDENTICAL, minimal, purely structural: `self.walk_expr(&field.base, false)` -- neither extracts a field name, neither classifies a field access as a read/write/move/borrow of anything in particular, both simply recurse into the base expression treating it as 'not a value position'. There is no field-identity-extraction logic here at all for either dimension to have a broken fallback IN. DATA_FLOW's compound-assignment arm (`dataflow.rs:320-333`) only recognizes a *simple path identifier* (`spelling::simple_path_ident`) as its Use+Store target; `self.counter += 1`'s left-hand side is `Expr::Field`, not a simple path, so it falls through to `None => self.walk_expr(binary.left.as_ref(), false)` -- the exact same safe, structural-only recursion, never attempting (and therefore never fabricating) a field-specific Use/Store fact.",
  "verification_before_acting": [
    "Read the full `walk_expr` match arm lists in both files, not just the `Expr::Field` line in isolation, to confirm no OTHER arm (e.g. a special-cased compound-assignment or index-expression path) re-enters field-identity logic the way STATE's own fallback did.",
    "Confirmed DATA_FLOW's compound-assignment special case exists specifically for BARE identifiers (`x += 1`), gated by `spelling::simple_path_ident` returning `Some`, and that this guard correctly excludes `Expr::Field`-shaped left-hand sides (a compile-time-verifiable fact about `simple_path_ident`'s own signature: it only ever matches `syn::Expr::Path` with a single unqualified segment, never `syn::Expr::Field`).",
    "Confirmed this is a genuine architectural difference from STATE, not an oversight: STATE's entire purpose is per-field read/write identity (`StateAccessIdentity.name`), so it MUST attempt field-name extraction and therefore has a fallback path that could go wrong; OWNERSHIP's purpose (borrow/move site detection) and DATA_FLOW's purpose (value definition/use tracking for resolvable places) both only ever attempt identity extraction for BARE, unambiguous identifiers or explicit borrow/move expressions -- neither ever tries to name a specific struct field at all, so there is no field-identity code path to have a broken fallback in."
  ],
  "stable_validator_law_compliance": {
    "claim": "OWNERSHIP and DATA_FLOW do not share STATE's fixed fabrication defect class, because neither attempts field-specific identity extraction for `Expr::Field` at all -- confirmed by direct code reading of both walkers' complete `Expr::Field`-relevant match arms, not merely a surface pattern match on the presence of `Expr::Field` in each file.",
    "self_written_evidence": "N/A this generation -- a definitive negative result from direct code reading, following the same precedent as the CFG wildcard-fallthrough sweep and the recursion-DoS parser sweep earlier this session (an investigation whose own deliverable is the closed, evidenced answer, not a code change). No synthetic test was added: a test asserting 'no fabricated fact appears for `self.a.b = 5`' in OWNERSHIP/DATA_FLOW would trivially pass for the same reason a naive placeholder would -- neither walker emits ANY field-specific fact for that expression shape today, correct or otherwise, so there is no fabrication behavior to lock in a regression guard against.",
    "independent_evidence_channel": "No code changed this generation; the existing full workspace suite (534 tests) is unaffected and was not required to change to support this conclusion.",
    "no_circular_self_certification": true
  },
  "implementation": {
    "note": "NO CODE CHANGED. This is an investigation-and-closure record answering a prior generation's own explicitly recorded open question with a definitive negative result."
  },
  "adversarial_and_falsification_coverage": [
    "did not stop at 'the Expr::Field arm looks the same' -- explicitly checked DATA_FLOW's ADDITIONAL compound-assignment special case, the one place in either file that attempts any place-specific identity extraction at all, and confirmed its own guard (`simple_path_ident`) structurally excludes field expressions rather than merely happening not to trigger on the test fixtures examined",
    "articulated WHY the STATE bug's specific shape cannot recur here (STATE attempts field-name extraction with a risky fallback; OWNERSHIP/DATA_FLOW never attempt field-name extraction at all), rather than asserting a bare 'looks fine' conclusion"
  ],
  "validation_this_generation": {
    "commands": {
      "note": "No commands run -- no code changed. Prior generation's full validation (534 tests, gate_ready true) remains the standing baseline."
    }
  },
  "recensus_scope_after_promotion": [
    "no prior CLOSED/EXTINCT conclusion is affected",
    "the open question from `state-multi-level-field-access-fabrication-fixed.json` is now definitively answered: no, neither OWNERSHIP nor DATA_FLOW share the fixed STATE fabrication defect class, for an architectural reason specific to what each dimension attempts to extract",
    "R4.6-R4.11's own closure status remains unchanged -- this generation strengthens confidence in what is already correctly scoped without changing what remains explicitly open (expression-nested control flow, closures, resolution-context modeling, etc.)"
  ],
  "overall_verdict": "INVESTIGATED AND CLOSED WITH AN EVIDENCED NEGATIVE RESULT. Neither OWNERSHIP nor DATA_FLOW shares the STATE dimension's fixed nested-field-write fabrication defect, because neither attempts field-specific identity extraction for `Expr::Field` at all -- a structural, architectural difference confirmed by direct reading of both walkers' complete relevant match arms, including DATA_FLOW's one additional place-identity-extraction special case (compound assignment), whose own guard correctly excludes field expressions. No code changed; the prior generation's own explicitly recorded open question is now closed rather than left open indefinitely.",
  "historical_evidence_retained": true
}
